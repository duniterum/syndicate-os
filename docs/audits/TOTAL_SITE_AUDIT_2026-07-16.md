# TOTAL SITE AUDIT — THE CLEAN BASE (2026-07-16)

**Method.** 31-agent ultracode fleet: 1 surface-inventory agent + 15 founder lenses + 15 adversarial verifiers. Every finding was re-verified against the tree by an independent verifier agent; REFUTED findings are dropped from this canon, CORRECTED findings carry their correction inline (marked "[corrected]"). Findings a verifier never ruled on are marked "[unverified]".

**Verification rule.** Every claim was checked against TODAY'S code and canon (the current tree, 2026-07-16, plus the served api-server truth sources) — never page-vs-page. A page contradicting another page is only a finding when one of them contradicts the tree's ground truth (checkoutGate, referralProgram lifecycle, backbone/served sourceStatus, truthStatus).

## 0. Surface inventory

SURFACE INVENTORY — syndicate-os, read from the current tree (2026-07-16). All paths repo-relative.

### 0.1 Studio routes (artifacts/studio/src/App.tsx — 24 public mounts + 15 operator mounts + catch-all; postures from artifacts/studio/src/lib/seo-route-registry.ts)

PUBLIC (wrapped `PublicRoute` = PublicLayout + AccessGate, App.tsx:104-181):

| Route | File | Posture |
|---|---|---|
| `/` | src/pages/PublicHome.tsx | INDEX (sitemap, priority 1.0) |
| `/proof` | src/pages/ProofDashboard.tsx | INDEX |
| `/status` | src/pages/SystemStatus.tsx | INDEX |
| `/learning` | src/pages/Learning.tsx | INDEX (content: src/config/learningModules.ts) |
| `/whitepaper` | src/pages/Whitepaper.tsx | INDEX |
| `/tokenomics` | src/pages/Tokenomics.tsx | INDEX |
| `/faq` | src/pages/Faq.tsx | INDEX (FAQPage JSON-LD baked into shell via scripts/prerender-routes.ts:90 from src/lib/seo-faq-jsonld.ts) |
| `/docs` | src/pages/Docs.tsx | INDEX |
| `/contracts` | src/pages/ContractMemory.tsx | INDEX (posture-only, no live reads) |
| `/map` | src/pages/ProtocolMap.tsx | INDEX (reads GET /api/protocol/reality) |
| `/referral` | src/pages/SourceAttribution.tsx | INDEX (canonical human URL) |
| `/source-attribution` | src/pages/SourceAttribution.tsx | REDIRECT posture (200 alias, noindex,follow, canonical → /referral; registry:391-410) |
| `/support` | src/pages/Support.tsx | INDEX (intake preview, no backend write) |
| `/archive` | src/pages/Archive.tsx | PENDING (noindex,follow; not in sitemap; registry:429-442) |
| `/activity` | src/pages/ActivityTeaser.tsx | INDEX, LIVE (LiveActivityFeed, complete served history) |
| `/chronicle` | src/pages/ChronicleTeaser.tsx | INDEX, LIVE (committed register src/config/chronicleRegister.ts; TeaserSurface only as empty-register fallback, ChronicleTeaser.tsx:36) |
| `/fire-ledger` | src/pages/FireLedgerTeaser.tsx | INDEX, LIVE (numbered Proof of Burn record) |
| `/recognition` | src/pages/Recognition.tsx | PENDING (noindex,follow; registry:320-333) |
| `/member` | src/pages/MemberAccess.tsx | INDEX (door for visitors / full dashboard for signed members; MemberShell) |
| `/wallet` | src/pages/MemberWallet.tsx | INDEX (flat member door) |
| `/liquidity` | src/pages/Liquidity.tsx | INDEX |
| `/toolkit` | src/pages/MemberToolkit.tsx | INDEX |
| `/join` | src/pages/JoinProtocol.tsx | INDEX (priority 0.8) |
| `/source` | src/pages/SourceLinkBuilder.tsx | INDEX (public link builder; old operator console moved to /os-source) |

OPERATOR/INTERNAL (bare `Route` → OperatorRoute, App.tsx:184-228; all INTERNAL/noindex,nofollow/robots-disallowed):
- `/studio` → operator/OperatorConsole.tsx page "studio" → layout/Shell.tsx + src/pages/Home.tsx (Studio OS overview)
- `/proof-studio` → Shell + src/pages/ProofStudio.tsx
- `/founder` → Shell + src/pages/OperatorPreview.tsx (moduleId "founder")
- `/os-source` → Shell + src/pages/OperatorPreview.tsx (moduleId "source")
- `/os-map` → Shell + src/pages/OsMap.tsx (+ operator/LiveEvidencePanel.tsx; data src/config/protocolOsMap.ts)
- `/admin` + 9 sections (`/admin/members|sources|operators|content|modules|broadcast|audit|support|settings`) → components/admin/AdminShell.tsx (own chrome, ADMIN_SECTIONS at AdminShell.tsx:99-109) rendering src/pages/admin/sections.tsx + AdminHome.tsx + panels.tsx; unknown /admin/* fails closed to "/admin" classification (OperatorConsole.tsx:43-49)
- catch-all `*` → src/pages/not-found.tsx under PublicLayout — UTILITY, noindex,nofollow

PRERENDERED SHELLS (scripts/prerender-routes.ts, wired into `build`, package.json:8): flat `dist/public/<route>.html` for every PUBLIC + PENDING registry route = 24 shells (21 INDEX + noindex shells for /archive, /recognition, /source-attribution) + real 404.html; INTERNAL + catch-all excluded (prerender-routes.ts:174-176). CSP + referrer meta baked (lines 119-131). Clean-URL rewrites generated into .replit-artifact/artifact.toml from the same registry (scripts/generate-serving-rewrites.ts, --check wired as seo:rewrites:check).

### 0.2 API-server routes (artifacts/api-server)

Mounting (src/app.ts): CSP `default-src 'none'` JSON-only headers (17-23); CORS GET/HEAD/OPTIONS allowlist (95-110); no app-wide body parser; trust proxy deliberately unset (25-31).

/api/auth (src/auth/router.ts, mounted app.ts:121 behind authExposureGate — src/auth/authExposure.ts: dark 404 in prod unless `SYNDICATE_AUTH_ENABLED === "true"` exact; dev always on). Zone-scoped JSON parser + cookieParser (75-76) + rejectCrossOrigin (120) + throttle:
- POST /api/auth/challenge (:128) · POST /verify (:163) · GET /session (:266) · GET /operator-context (:286) · GET /member-self (:318) · GET /member-standing (:379) · GET /source-standing (:546) · GET /member-purchases (:613) · POST /logout (:690) · scoped error handler → uniform 400 (:702)
- support modules: authConfig.ts, clientIdentity.ts, engineReadback.ts, memberRoster.ts, nonceStore.ts, operatorContext.ts, sessionStore.ts, throttle.ts

/api/operator (src/operator/router.ts, mounted app.ts:123 behind the SAME exposure gate — WRITE zone; shape: throttle → wallet session → lookupActiveOperator role → WRITE_ROLES `{founder_root, protocol_admin}` (:31) → zod validate → fail-closed service):
- POST /api/operator/referral-terms (:55, referralTermsService.ts) · POST /operators invite (:102) · POST /operators/suspend (:146) · GET /operators (:188) (operatorRegistryService.ts)

/api read-only spine (src/routes/index.ts mounts, app.ts:125):
- GET /api/healthz (health.ts:6) · GET /api/source-status (sourceStatus.ts:7) · GET /api/protocol/reality (protocolReality.ts:16) · GET /api/source/validate (sourceValidate.ts:33, publicReadThrottle) · GET /api/join/quote (joinQuote.ts:42, publicReadThrottle) · GET /api/holder-index (holderIndex.ts:29) · GET /api/protocol/verify-links (verifyLinks.ts:101) · GET /api/backbone/status (backboneStatus.ts:21) · GET /api/backbone/feed (backboneFeed.ts:17) · GET /api/backbone/capital-standing (capitalStanding.ts:39)
- JSON 404 catch-all for unknown /api/* (app.ts:130-132)
- src/index.ts: PORT required; startBackbone() dark unless `SYNDICATE_BACKBONE_ENABLED === "true"` (:28-29); backbone code in src/backbone/

### 0.3 Shared chrome
- src/components/layout/PublicLayout.tsx — public header + footer on every public route AND the not-found and OperatorPreviewUnavailable renders. Header: Wordmark + chapter badge; primary nav ×3 (Protocol→/proof, Economy→/contracts, Membership→/member) + "More" dropdown ×5 (Chronicle→/archive, Recognition, Join, Docs, Status) from src/config/navigation.ts:22-31; ChainPill/LiveChip derived from live protocol-reality read (:86-91, fail-closed); ThemeToggle; /proof shield icon; lazy auth-gated MemberHeaderAffordance (:26-36) + session-aware SeatCtaSlot (:42-72); mobile Sheet. Footer: 4 groups (navigation.ts:60-65) — Protocol [/proof,/map,/status,/contracts,/referral], Learn [/whitepaper,/tokenomics,/faq,/docs,/learning,/recognition,/archive], Membership [/member,/join,/source,/support], Console [/studio]. Floating SyndicateGuide on every public surface (:415).
- src/components/layout/Shell.tsx — operator console chrome (sidebar = modules nav.sidebar: studio, os-map, admin, proof-studio, os-source, founder; renders WalletAuthComingSoon at :74).
- src/components/member/MemberShell.tsx — member dashboard shell; doors from src/config/memberDoors.ts (MEMBER_DOOR_GROUPS: Member Home, Referral dashboard, Recognition, Wallet, Toolkit, Activity, Fire Ledger, Chronicle, Archive, Protocol graph, Settings + Growth group).
- Access shell: components/access/AccessGate.tsx + AccessStateProvider.tsx (fail-closed visibility, wraps every route).
- Head/scroll: components/SeoHeadManager.tsx, RouteScrollManager.tsx, RouteContextBar.tsx; page chassis components/PublicPage.tsx.

### 0.4 Admin/operator surfaces + gating
- Gate: src/config/operatorPreviewGate.ts — `OPERATOR_PREVIEW_ENABLED = import.meta.env.DEV || __OPERATOR_PREVIEW__` (:26-27); prod default folds false → Rollup dead-code-eliminates the whole console; INTERNAL routes render src/pages/OperatorPreviewUnavailable.tsx inside PublicLayout (App.tsx:66-79). Explicitly visibility, not auth (gate file :17-20).
- Single entry: src/operator/OperatorConsole.tsx (only conditional dynamic import, App.tsx:53-55); enforced by scripts/guard-operator-gate.ts.
- Surfaces: Studio home (pages/Home.tsx), ProofStudio, OperatorPreview (founder + os-source), OsMap (+LiveEvidencePanel, protocolRealityEvidence.ts), AdminShell 10 sections (+ components/admin/ChroniclePrepare.tsx), operator/AccessStateSimulator.tsx (rendered on Studio home).
- Defense-in-depth: robots.txt Disallow ×15 (public/robots.txt) + INTERNAL noindex,nofollow via registry; no prerendered shells for INTERNAL. Server-side writes separately gated by session + role + authExposureGate (§0.2).

### 0.5 Teaser / duplicate / orphan candidates
- Home.tsx vs PublicHome.tsx: pages/Home.tsx is the OPERATOR Studio OS overview served at /studio (OperatorConsole.tsx:13,57); PublicHome.tsx serves "/". No route conflict; pure filename drift (module id "home" = "/" while file Home.tsx = internal) — confusion hazard only, P3.
- ProtocolMap vs OsMap: /map (public proof organism over GET /api/protocol/reality) vs /os-map (internal founder org map from config, DESIGN_PREVIEW). Distinct; not duplicates.
- Docs vs Learning class: /docs = registry-driven index of surfaces w/ status pills; /learning = plain-language lessons; /whitepaper = anchor prose; /faq = number-free Q&A. Four distinct education surfaces; Docs is the hub that links the others.
- "Teaser" filenames are stale: ActivityTeaser/ChronicleTeaser/FireLedgerTeaser all serve LIVE INDEX surfaces; components/TeaserSurface.tsx now used only by ChronicleTeaser's empty-register fallback. P3 naming drift.
- SourceAttribution dual-mount (/referral + /source-attribution) — intentional alias, documented.
- WalletAuthComingSoon.tsx — not an orphan: honest dark-auth fallback (MemberAccess.tsx:261, Shell.tsx:74); should never render in prod (auth is ON).
- Orphans: none — all 28 files in src/pages + pages/admin/* + not-found.tsx are mounted.
- Nav crossovers worth other lenses: (a) header "More" label "Chronicle" points to /archive (navigation.ts:26, module id archive) while a real live /chronicle page exists with NO header/footer entry — wayfinding truth candidate, P2; (b) public footer "Console" group links /studio (navigation.ts:64; modules.ts:88 nav.footer:true) — a public footer door to an internal route (renders OperatorPreviewUnavailable in prod) — public-sees-admin-never candidate, P2; (c) modules.ts has NO entries for /wallet, /toolkit, /liquidity, /activity, /chronicle, /fire-ledger — six live public surfaces absent from the module registry that drives header/footer nav (reachable via member doors/in-page links only).

### 0.6 SEO registry vs real routes delta
- Zero structural delta: all 39 App.tsx mounts + "*" have exact registry entries; parity guarded by scripts/check-seo-registry.ts; sitemap.xml (21 URLs) == the 21 INDEX entries; robots.txt Disallow ×15 == the 15 INTERNAL entries.
- Stale text inside the registry/App (entries themselves correct): (a) seo-route-registry.ts:82 claims "14 explicit routes + the catch-all" — there are 39 entries, P3; (b) registry comment :443-445 and App.tsx:150 still call /activity//chronicle//fire-ledger "§11 designed teasers — honest PENDING/noindex promises" while all three entries are INDEX/live, P3; (c) registry "*" note :734-735 says soft-404 "deferred to Slice 2.18E" while prerender-routes.ts:196-200 already writes a real 404.html served with HTTP 404 per its own comment — one of the two notes is stale, P3.

## Lens 1 — truth-liveness

**Coverage.** Swept all 24 public studio pages (PublicHome full incl. hero sections, ProofDashboard, SystemStatus full, Docs, ContractMemory, Archive, ChronicleTeaser, ActivityTeaser, FireLedgerTeaser, Learning, Support, Recognition, Tokenomics head+data-source audit) + targeted greps for future-tense/"not wired"/"coming" claims and hardcoded figures; all copy-bearing config (syndicateFacts, truthStatus + every TruthLabel/LifecycleBadge consumer, modules, surfaceClassification, supportIntake, learningModules, contractMemory, referralProgram, sharedCopy, chronicleRegister, liquidityPool, joinAmounts, memberDoors, moduleRegistry); content/ (docs-content, faq-content, guide-content); seo-route-registry entries for the truth-bearing routes; served api notes (artifacts/api-server/src/data/sourceStatus.ts full); go-live ground truth from checkoutGate.ts, referralProgram lifecycle, api joinQuote/sourceValidate/backbone. Verified-clean: FAQ content, Activity/FireLedger page copy, Recognition (honest FUTURE), homepage awaitingWiring card, holder-index/introduction snapshots, MemberReferralDashboard per-receipt PENDING card.

| Sev | Finding | Where | Detail + verifier note |
|---|---|---|---|
| P0 | /proof (primary-nav public page) declares the whole proof layer unwired and the site "view-only: no payments, no live chain reads" — every clause false today | artifacts/studio/src/pages/ProofDashboard.tsx:73-77, 80, 99 | Lead: "This page is an honest account of what proof will cover — and why none of it is wired in this foundation yet"; page badge PENDING_ADAPTER ("Not live yet"); line 77 renders safetyCopy.readOnly = "Right now this is view-only: no payments, no live chain reads, and nothing is changed behind the scenes" (artifacts/studio/src/config/sharedCopy.ts:34-35); headings future tense ("What proof will cover", "How verification will work"). Today's truth: CHECKOUT_ENABLED=true since C5 (config/checkoutGate.ts:11-15) — seats are bought with real money; served source-status canon says "Live event/proof data is served" for /proof (artifacts/api-server/src/data/sourceStatus.ts:135-139); modules.ts:135-138 killed /proof's not-live badge; seo-route-registry.ts:127-137 describes /proof as "read live from the chain". Google and /status say live, the page says nothing is wired and no payments exist — a public lie about the money path on the header's primary "Protocol" destination (navigation.ts:23). C5 MUST-CHANGE read-only-claim class; sharedCopy.ts:34-35 dies with the page rewrite. Verifier: CONFIRMED — every clause re-proven at :73/:74/:77/:80/:99. |
| P1 | /proof facet cards: "registry is paused by precaution", receipts/burn/archive "Not live yet" — all four organs are live | artifacts/studio/src/pages/ProofDashboard.tsx:23-48 | "Membership receipts" PENDING_ADAPTER (:28) — Holder Index + standing self-readback live since 2026-07-11 (config/truthStatus.ts:44-51), /api/auth/member-purchases serves own receipt rows (api-server/src/auth/router.ts:613); "Source attribution — The registry is paused by precaution" NOT_ACTIVE (:33-34) — program ACTIVE with real commissions paid inside the buyer's tx (config/referralProgram.ts:170-179 activeCopy, programLifecycle='LIVE_ACTION' at :35); "Proof of Fire … once the event read-model is wired" PENDING_ADAPTER (:39-40) — numbered Proof of Burn record served (/fire-ledger LIVE, pages/FireLedgerTeaser.tsx:1-9); "Archive memory" body says "counts and prices read live" under a "Not live yet" badge (:45-46) — self-contradiction in one card. "Paused by precaution" is P0-adjacent: tells a prospective referrer the money system is paused while it pays today. Verifier: CONFIRMED — all four re-proven. |
| P1 | /contracts card claims Source Registry V1 is "intentionally inactive — no attribution is read or written here" while it is the ACTIVE registry paying commissions | artifacts/studio/src/config/contractMemory.ts:104-112 | Entry "source-registry-v1" renders PAUSED_BY_PRECAUTION ("Paused for safety") + note "A deployed source-policy registry, intentionally inactive. No attribution is read or written here." (renders on public /contracts via ContractMemory.tsx:50-59). Today: SourceRegistryV1 (0x780013bB…) is what the live join quote validates against (artifacts/api-server/src/routes/joinQuote.ts:37-38), /api/source/validate reads it on demand (routes/sourceValidate.ts:25-31), the backbone indexes its lifecycle + attribution activity (backbone/introductionRefresh.ts:73, data/protocolTargets.ts:81,409,518), reality spine reconciles "Active engine linked to canon Source Registry" (lib/protocol/realityService.ts:979); program ACTIVE since 2026-07-13 (referralProgram.ts:16-23,:35,:170-175). Flat truth bug on the money-routing contract. Verifier: CONFIRMED. |
| P1 | /contracts Proof of Fire entry: "require event read-model wiring before any event can be shown" — the numbered burn record is served | artifacts/studio/src/config/contractMemory.ts:208-216 | Entry "proof-of-fire" renders PENDING_ADAPTER ("Not live yet") + note "Burn-based proof identifiers exist in canon but require event read-model wiring before any event can be shown." The event backbone serves the complete numbered Proof of Burn record on /fire-ledger (FireLedgerTeaser.tsx:1-9; served canon sourceStatus.ts:137/:287). Same file: entry "liquidity-pair" note :195 "No live price, reserve, or quote is read in this foundation" — false at foundation scope: /liquidity shows live pair reserves (config/surfaceClassification.ts:203) and /tokenomics reads the live Trader Joe pool price (pages/Tokenomics.tsx:108-113) + live LP reserve cards (:111-129). Verifier: CONFIRMED incl. sibling. |
| P1 | /contracts page banner: "this app only reads" — the app has written real-money joins since C5 | artifacts/studio/src/pages/ContractMemory.tsx:73-77 | "Live read-only reality" section states "No addresses, no wallet, and no purchase, transaction, or referral surface — this app only reads" (:75-76). Since C5 GO-LIVE (checkoutGate.ts:11-15) the app builds and submits the two-signature approve→buy on /join (visitor-signed); /wallet offers revoke as a member-signed act (surfaceClassification.ts:213); truthStatus.ts:109-113 records "the foundation is no longer all-read-only." Can be honestly scoped ("this page reads only; the one write is /join, signed from your wallet") but as written it is the read-only-era claim class. Verifier: CONFIRMED. |
| P1 | Public /status Surface map renders "Contracts — Not live yet" and modules.ts says "/contracts … no live reads" while the page renders live holdings | artifacts/studio/src/config/modules.ts:349-363 | Module "contracts" carries truthStatus "LIVE_SOURCE_MISSING" (:358 → TruthLabel "Not live yet", rendered on public /status Surface map at pages/SystemStatus.tsx:313, summary :310) + description "Read-only contract & economy memory — roles and structure, no live reads" (:359). But ContractMemory.tsx renders ProtocolAssetsCard (live protocol holdings, :26), a live ProtocolRealityPanel (:79), and its own title says "live holdings" (:21). A live surface must render no not-live badge (truthStatus.ts:48-51, the codebase's own S7 rule). Sibling truth bug in the served head: seo-route-registry.ts:340-349 /contracts description "no addresses, balances, or live chain reads" (:341) + note "nothing read live" (:349) — the SEO-rides-the-slice era-drift class. Verifier: CONFIRMED incl. SEO sibling. |
| P1 | Public /status Surface map renders "Source Attribution — Not live yet" via the retired-from-render reason code the S7 sweep claimed had no consumers | artifacts/studio/src/config/modules.ts:209-226 | Module "source" (label "Source Attribution", path /os-source) binds truthStatus: surfaceStatus.sourceAttribution (:219) = SOURCE_INDEXER_NOT_WIRED → "Not live yet" (truthStatus.ts:23,59). truthStatus.ts:43-51 states every consumer rendering those four as Not-live-yet was retired in the sweep and forbids new ones — this consumer was missed (grep: modules.ts:219 is the sole surfaceStatus.sourceAttribution consumer). Renders publicly: /status Surface map (SystemStatus.tsx:290 includes OPERATOR_PREVIEW, :300, :313) plus operator Home.tsx:57 and OperatorPreview.tsx:44. Same /status page's served registry row says "Introduction/source registry is live" (api-server data/sourceStatus.ts:161-168) — one page, two opposite answers. Fix is an honest FOUNDER_GATED/DESIGN_PREVIEW-class label, not the retired indexer-not-wired reason. Verifier: CONFIRMED. |
| P1 | Public /status Surface map summaries still promise /activity and /fire-ledger "arrive with the event backbone" — both are live served surfaces | artifacts/studio/src/config/surfaceClassification.ts:226-253 | Summaries rendered verbatim on public /status (SystemStatus.tsx:310): "/activity" = "Activity teaser — the public heartbeat; arrives with the event backbone" (:232); "/fire-ledger" = "Fire Ledger teaser — total burn live; per-event ledger arrives with the backbone" (:250). Backbone is LIVE (startBackbone in prod; served sourceStatus.ts:137,287); both pages serve the complete record (ActivityTeaser.tsx:1-20 "LIVE V3 … complete heartbeat"; FireLedgerTeaser.tsx:1-6). "/chronicle" summary (:241) still says "teaser" with 4 promoted entries published; section header comment :226 ("§11 designed teasers — honest PENDING promises") same fossil. Exact S7 dead-band siblings on the page the whole site names "the authoritative ledger". Verifier: CONFIRMED. |
| P1 | /archive tells the public "no chronicle entries are stored or published in this foundation" — /chronicle publishes four founder-promoted entries | artifacts/studio/src/pages/Archive.tsx:38-50 | Chronicle card renders LifecycleBadge DESIGN_CONCEPT ("Concept") with body "A narrative record of protocol milestones and decisions. A design concept today — no chronicle entries are stored or published in this foundation." CHRONICLE_REGISTER carries 4 committed entries (config/chronicleRegister.ts:40-119: the duplicate seat, the first real-money seat, the first referral source, the ladder decision — entries at :43,:67,:91,:115), served as the solemn record by ChronicleTeaser.tsx:35-48; served canon "The Chronicle is live" (sourceStatus.ts:251-258). The header "More" menu labels its /archive link "Chronicle" (navigation.ts:26), so this false sentence is what a visitor who clicks "Chronicle" actually reads. Verifier: CONFIRMED. |
| P1 | /archive "What unlocks it": "Archive reads … wiring them is its own slice / When the reads are wired" — contradicting the same page's lead that reads are live | artifacts/studio/src/pages/Archive.tsx:65-75 | Lines 66-68: "Archive reads against the deployed Archive 1155 contract — wiring them is its own slice. Nothing renders here before it can be read and verified." Lines 72-74: "When the reads are wired, this page becomes the shelf your seat's history sits on." The page's own lead (:17) says "counts and prices are read live from the contract"; modules.ts:421-424 records "17 minted, counts and prices read live"; served canon confirms live archive reads (sourceStatus.ts:247). Same-page self-contradiction; the honest remaining gap is the per-wallet holdings/museum view, which the copy should name instead. Related P2: seo-route-registry.ts:429-442 keeps /archive PENDING/noindex with note "ARCHIVE_READS_NOT_WIRED. NOINDEX until archive reads are wired" (:441) while its own description (:435) says reads are live — noindex may remain a founder choice, but its recorded reason is dead. Verifier: CONFIRMED incl. the registry sibling. |
| P1 | /learning lessons: "This foundation does not read the chain yet" and "a seat is … not live yet" — contradicted by the page's own intro two scrolls up | artifacts/studio/src/config/learningModules.ts:59, 67 | Rendered as lesson topics on public /learning (Learning.tsx:22-32). Line 59: "This foundation does not read the chain yet, so proof surfaces stay labelled as pending." Line 67: "A seat is a verifiable membership record, founder-gated and not live yet." Same file's intro (:19): "seats sell on-chain today, and the Status ledger always shows exactly what is live"; topic :29: "every figure on this site is read from the chain." Checkout live (checkoutGate.ts:15), chain reads live everywhere. Pre-C5 fossils. Verifier: CONFIRMED. |
| P1 | /support membership channel: "Questions about seats and membership — founder-gated and not live yet" | artifacts/studio/src/config/supportIntake.ts:50-54 | Rendered on public /support (Support.tsx:24 maps supportIntake.channels). Seats sold on-chain to the public since C5 2026-07-13 (checkoutGate.ts:11-15); membership self-readback and Member Home are live production surfaces (modules.ts:169-173). "Not live yet" about membership is the dead-band pattern; the honest unwired thing is the SUPPORT INTAKE (said correctly at supportIntake.ts:35), not membership. Verifier: CONFIRMED. |
| P1 | /docs referral card: "attribution of a growth contribution, never a commission" — the program's own live copy and served meta say a bounded commission IS paid | artifacts/studio/src/content/docs-content.ts:126-131 | The /referral card on public /docs says the verified introduction is "never a commission." Today's canon: activeCopy (referralProgram.ts:171-175, selected by programLifecycle=LIVE_ACTION at :35,:178-179) — "generates a bounded commission, paid directly to the introducer's wallet inside the buyer's own purchase transaction"; modules.ts:391-392 and the /referral meta description (seo-route-registry.ts:380-381) say the same. The card states the retired paused-era doctrine as present truth — a money-fact contradiction between two public surfaces. Same file, same class: :151-156 /proof card "what public, auditable proof WILL mean … an honest account of what isn't wired yet" and :177-182 /archive card "Concept memory today; reads not yet wired" — both false today. Verifier: CONFIRMED incl. siblings. |
| P1 | Served /api/source-status token note: "No price, supply, or balances are wired or exposed" — /tokenomics reads all three live; the registry's own treasury row contradicts it | artifacts/api-server/src/data/sourceStatus.ts:200-209 | The "token" entry note ends "No price, supply, or balances are wired or exposed" (:207); rendered verbatim in the public /status registry table (SystemStatus.tsx:258). Today: /tokenomics reads live supply (Tokenomics.tsx:80-81), live per-wallet distribution (:92-99), both prices (:106-109), burn (:160-161); doc comment :16-20 "zero hardcoded figures". The SAME registry's "treasury" entry (:217) says "Live treasury balances are served … the seven allocation wallets." The file's own S4 same-slice law (:89-94) was missed for this entry. Sibling: the entry's surface field is "/token" — a route that does not exist (grep: zero matches; the page is /tokenomics) (:208). Verifier: CONFIRMED incl. /token sibling. |
| P2 | Served /api/source-status operator note: "authentication deferred" — SIWE auth and the role-gated operator write zone are live | artifacts/api-server/src/data/sourceStatus.ts:290-299 | The "operator" entry note reads "Operator/founder controls reserved; authentication deferred" (:297), served onto public /status. The auth zone is live in prod (founder switched auth ON 2026-07-11; same registry's walletSession entry :310-318 describes the live SIWE session) and the operator WRITE zone exists behind wallet session + WRITE_ROLES (artifacts/api-server/src/operator/router.ts:31, :55-77, plus invite/suspend). FUTURE posture for the build-gated console remains defensible; "authentication deferred" is a dead clause. Verifier: CONFIRMED. |
| P2 | Homepage hero footer renders a "Preview" pill directly beside the disclaimer "Every figure on this page is read live … anything marked Preview is a demonstration, not live data" | artifacts/studio/src/pages/PublicHome.tsx:344-347 | `<SampleTag kind="illustrative" />` renders the text "Preview" (components/SampleTag.tsx:10-14) at the whole cockpit-card footer, next to heroSystem.disclaimer (config/syndicateFacts.ts:612-613) whose final sentence defines Preview as "a demonstration, not live data." The hero is fully live-bound (HeroStat docs :318-323: "There is NO static value here — no simulated figures exist"); the only preview element is the Entry Preview widget, which carries its own note (:599-602). By the disclaimer's own definition the pill brands the live hero as a demonstration — the inverse-direction truth bug. Move the tag to the Entry Preview widget or delete it; also retire the stale hero doctrine comment (:307-316 "renders ILLUSTRATIVE protocol figures … Nothing here is a live read"). Verifier: CONFIRMED. |
| P2 | Dead config still photographs the read-only era: heroContent binds "Not live yet" homepage badges and studioPreviewPanel claims adapters unwired, with a comment asserting PublicHome renders it | artifacts/studio/src/config/syndicateFacts.ts:47-57, 152-212 | heroContent (:47-57) binds homepageStatus.heroBadge=NOT_LIVE and heroCore=AWAITING_CHAIN_INDEX (truthStatus.ts:72-75); studioPreviewPanel (:160-212) carries rows "Proof of Fire … once the event adapter is wired" (:167), "Source attribution … once the introduction indexer is wired" (:173), "Activity chronicle … a separate founder-gated slice" (:179), "Membership receipts await the membership indexer" (:185) — all live today — and its doc comment (:152-158) falsely claims "Rendered by PublicHome.tsx" (:157). Grep: heroContent/studioPreviewPanel/studioPreview have ZERO consumers. The file's own S7 doctrine (:13-17) deleted protocolSurfaces for exactly this reason: "dead config carrying stale claims is a lie waiting for an import." Same treatment due here (homepageStatus in truthStatus.ts:69-75 dies with it). Verifier: CONFIRMED. |
| P3 | Stale truth-comments contradicting today's code (comment-only, no render path) | artifacts/studio/src/config/referralProgram.ts:181-184 | (a) referralProgram.ts:181-184 comment says "the introduction read-model / indexer is SPEC slice R5 and is not wired yet" while the adjacent card :192 serves counts "from the R5 introduction read-model" and truthStatus.ts:59 records R5 LIVE — the one honest PENDING card (:193 per-receipt history) risks being "fixed" wrong from this comment. (b) surfaceClassification.ts:11-17 comment "no auth system exists" — auth live since 2026-07-11. (c) ChronicleTeaser's TeaserSurface fallback path is honest (renders only when the register is empty; it never is today). (d) contractMemory.ts:120 "No commission or financial benefit is implied or paid" on the FUTURE Attribution Router card — literally scoped to that undeployed router but misreadable program-wide while commissions are paid via direct payment — worth a scoped rewrite when /contracts is touched. Verifier: CONFIRMED all four sub-items. |

Refuted at verification: 0 of 18 findings.

## Lens 2 — cross-page-era-drift

**Coverage.** Swept all 24 public studio pages via full reads or targeted era-vocabulary greps; every rendered content/config source (faq-content, docs-content, guide-content, learningModules, syndicateFacts, truthStatus, sharedCopy, contractMemory, referralProgram, sourceAttributionTerminology, connectorLadder, joinAmounts, chronicleRegister, modules, navigation, surfaceClassification, memberDoors, checkoutGate, protocolOsMap); the SEO route registry's served titles/descriptions/postures/notes; the api-server truth spine (data/sourceStatus.ts as today's-truth reference, capitalAxisReadmodel ladder canon, syndicate-config tier names). Stale claims cross-checked against canon decisions (C5 go-live, referral ACTIVE 2026-07-13, S7 truth sweep, D-TRUTH, capital-axis H2-⑰, joinAmounts naming law) — never page-against-page alone; consumer/dead-config status confirmed by grep before grading. Biggest systemic pattern: the S7 truth sweep updated modules.ts/registry descriptions but left the PAGES (/proof entirely, /archive tail, docs cards, learning modules, whitepaper §8/§10, surfaceClassification summaries) in the pre-backbone era — the head/registry tells today's truth while the rendered body speaks the dead one.

| Sev | Finding | Where | Detail + verifier note |
|---|---|---|---|
| P0 | /proof — the flagship verify page still speaks the dead pre-backbone era: "none of it is wired in this foundation yet" | artifacts/studio/src/pages/ProofDashboard.tsx:23-99 | Page body: title "Proof, when there is something real to prove.", lead "why none of it is wired in this foundation yet" (:72-74), page badge PENDING_ADAPTER (:74), facets "Membership receipts" PENDING_ADAPTER (:28), "Source attribution … The registry is paused by precaution" NOT_ACTIVE (:33-34), "Proof of Fire … once the event read-model is wired" (:39), section heads "What proof WILL cover" / "How verification WILL work" (:80,:99); archive facet contradicts itself in one card — body "counts and prices read live" under a PENDING_ADAPTER badge (:43-47). Today's truth (canon, not another page): the event backbone is LIVE (api sourceStatus.ts:137 "Live event/proof data is served… complete heartbeat"), the referral registry is ACTIVE and paying (referralProgram.ts:16-23, LIVE_ACTION), numbered burns are served, and the route's OWN served meta head says "membership receipts, treasury routing, numbered burns and referral payments — read live from the chain, each with its own verify link" (seo-route-registry.ts:127-129, prerendered into the shell). The S7 sweep updated modules.ts:135-138 but never touched the page. Whole page needs the S7 live rewrite. Verifier: CONFIRMED — [corrected] the "View public proof" CTA is not the homepage hero's (heroContent has no consumer); it renders site-wide from PublicLayout.tsx:290-291, and /proof is the primary-nav "Protocol" door (navigation.ts:23). Public exposure stands. |
| P1 | /proof safety banner: "no payments, no live chain reads" — the retired read-only-foundation sentence still rendered | artifacts/studio/src/config/sharedCopy.ts:34-35 | safetyCopy.readOnly = "Right now this is view-only: no payments, no live chain reads, and nothing is changed behind the scenes." — rendered on /proof (ProofDashboard.tsx:77). Seats are bought in-page with real money (checkoutGate.ts:15, C5 2026-07-13), referral commissions are paid inside the buyer's transaction, and virtually every public figure is a live chain read. Pre-C5 foundation-era voice verbatim; delete or rewrite; only consumer is /proof. Verifier: CONFIRMED — sole consumer confirmed by grep; checkoutGate's own header says "The same commit rewrites every read-only claim" — this one survived. |
| P1 | /learning tells visitors the foundation "does not read the chain yet" and a seat is "not live yet" — contradicting its own intro two paragraphs above | artifacts/studio/src/config/learningModules.ts:59, 67, 69 | Rendered on /learning (public INDEX): :59 "This foundation does not read the chain yet, so proof surfaces stay labelled as pending." · :67 "A seat is a verifiable membership record, founder-gated and not live yet." · :69 "Source attribution records who introduced a member, as recognition, never as compensation." Same file's intro (:19) says "seats sell on-chain today"; module 1 (:29) "every figure on this site is read from the chain" — two eras in one rendered config. Seats sell live on /join; referral commissions ARE compensation (referralProgram.ts notCharityLine, founder doctrine 2026-07-07) — :69 speaks the repealed pre-commission "recognition-only" law. Header comment :5 ("systems that are mostly not wired yet") is the stale root. Verifier: CONFIRMED — all three verbatim; rendered by Learning.tsx:19-22. |
| P1 | /archive claims "no chronicle entries are stored or published" while /chronicle publishes the live founder-promoted register — and the header nav labels this page "Chronicle" | artifacts/studio/src/pages/Archive.tsx:44-49 | Chronicle card renders DESIGN_CONCEPT ("Concept") with "A design concept today — no chronicle entries are stored or published in this foundation." CHRONICLE_REGISTER (config/chronicleRegister.ts:40+) holds founder-promoted entries; /chronicle serves them live (ChronicleTeaser.tsx renders TeaserSurface only when the register is EMPTY); api canon "The Chronicle is live" (sourceStatus.ts:257). Compounding: the public header "More" item labelled "Chronicle" points to /archive (navigation.ts:26) — the word Chronicle in the nav leads to the one page that denies the Chronicle exists, while the real live /chronicle has no header or footer entry at all. Fix the card and repoint/split the nav item. Verifier: CONFIRMED — modules.ts has no /chronicle module at all (grep), so /chronicle has no header/footer entry. |
| P1 | /archive contradicts itself: lead says counts/prices "read live from the contract", tail says "wiring them is its own slice… When the reads are wired" | artifacts/studio/src/pages/Archive.tsx:65-74 | "What unlocks it — Archive reads against the deployed Archive 1155 contract — wiring them is its own slice. Nothing renders here before it can be read and verified." + "When the reads are wired, this page becomes the shelf…" (:71-74). The page's lead (:17) says "counts and prices are read live from the contract"; the S7 sweep killed the Not-live badge on this very page (:18-21 comment); modules.ts:424 and api sourceStatus.ts:247 confirm live reads. The §11 teaser block survived the page's go-live — a fossil to delete/rewrite. Verifier: CONFIRMED. |
| P1 | /docs card for /referral says an introduction is "never a commission" — the program pays commissions today under founder doctrine | artifacts/studio/src/content/docs-content.ts:127-129 | Rendered on /docs (public INDEX): "/referral — How a verified introduction is recognised — attribution of a growth contribution, never a commission." Founder doctrine 2026-07-07/13 (referralProgram.ts:6-23): "Commission = business word", program ACTIVE, LIVE_ACTION; /referral's own served meta (seo-route-registry.ts:380-381) says "pays a bounded commission to the introducer's wallet inside the buyer's own transaction." The docs card speaks the repealed pre-2026-07-07 charity/attribution-only law — a direct parallel truth one click from the page it describes. Verifier: CONFIRMED. |
| P1 | /docs card for /archive: "Concept memory today; reads not yet wired" | artifacts/studio/src/content/docs-content.ts:177-181 | Rendered on /docs: "/archive — The archive and chronicle — protocol memory and milestones. Concept memory today; reads not yet wired." Archive reads live (api sourceStatus.ts:247; modules.ts:421-424; artifacts minted on-chain today) and the Chronicle register is live and published. Docs' own doctrine header (:14-17) says status pills derive from the registry precisely so cards can't lie — the purpose STRING lies anyway. Verifier: CONFIRMED (verbatim at :178-180). |
| P1 | /contracts tells the public the Source Registry is "Paused for safety… intentionally inactive" while the referral program pays through it live | artifacts/studio/src/config/contractMemory.ts:106-111 | Rendered on /contracts (public INDEX): "Source Registry V1 … PAUSED_BY_PRECAUTION ('Paused for safety') … A deployed source-policy registry, intentionally inactive. No attribution is read or written here." Program ACTIVE since 2026-07-13 (referralProgram.ts:16-23); the registry is live and readable (/source validates against it; sourceAttributionTerminology.ts:76-77 "It is real and readable today"); purchases record attribution on-chain and commissions pay from it (api sourceStatus.ts:167). /proof repeats the same stale "paused by precaution" line (ProofDashboard.tsx:33). Both sites need the ACTIVE-era rewrite. Verifier: CONFIRMED. |
| P1 | /contracts Proof of Fire row still says events "require event read-model wiring before any event can be shown" | artifacts/studio/src/config/contractMemory.ts:210-215 | Rendered on /contracts: "Proof of Fire … PENDING_ADAPTER … Burn-based proof identifiers exist in canon but require event read-model wiring before any event can be shown." The event read-model is wired and /fire-ledger serves the complete numbered Proof of Burn record with per-event verify links (FireLedgerTeaser.tsx:177-189; api sourceStatus.ts:137). Flip to live and name where the record lives. Verifier: CONFIRMED — lead at :177 "the complete numbered record — every burn since the first block", READ_ONLY_PROOF badge :178. |
| P1 | FAQ asserts a rank system "from Citizen to Cornerstone derived from SYN balance" — today's standing is the capital axis (Citizen→Monolith, cumulative USDC), and named join tiers were repealed | artifacts/studio/src/content/faq-content.ts:192, 122 | Rendered on /faq AND baked into the FAQPage JSON-LD served to Google: :192 "Rank is derived from a wallet's on-chain SYN balance, across a series of tiers from Citizen to Cornerstone." · :122 "A featured entry amount on Join is simply a highlighted amount mapped one-to-one to a recognition tier." Today's ladder is CAPITAL_AXIS_LADDER (api-server src/backbone/capitalAxisReadmodel.ts:58-71): 12 rungs Citizen→Monolith derived from CUMULATIVE GROSS USDC, not SYN balance; "Cornerstone" is not a rung; joinAmounts.ts:3-6 records the settled naming law ("No names, no badges, no tiers…", RANKS_V2 poisoned-canon flag). Both answers also surface via the floating Guide's suggested answers on every page. Rewrite both to the capital-axis truth. Verifier: CONFIRMED — exposure amplified: FAQ_ENTRIES_FLAT feeds the FAQPage JSON-LD (seo-faq-jsonld.ts:15,25) and the Guide (SyndicateGuide.tsx:6,133). |
| P1 | Whitepaper §8 brands the Archive "PAUSED" and "read-only today" while artifacts mint openly on-chain | artifacts/studio/src/pages/Whitepaper.tsx:200-205 | Flagship /whitepaper §8: eyebrow status="PAUSED", body "The Archive is read-only today." — while the very same sentence shows a LIVE minted count. FAQ (faq-content.ts:218) says "At least one public artifact mint is open on Avalanche"; contractMemory.ts:130 "some artifact classes are open"; modules.ts:421-424; api sourceStatus.ts:247. An open public mint is neither paused nor read-only. Flip the eyebrow and sentence. Verifier: CONFIRMED (:200, :203-204). |
| P1 | Whitepaper §10 lists "source payment/referral" and "live activity feed" as KNOWN FUTURE MODULES — both are live and paying/serving today | artifacts/studio/src/pages/Whitepaper.tsx:220-227 | "Known future modules: later eras · marketplace · source payment/referral · gamification & seasons · admin console · live activity feed." Source payment/referral is ACTIVE with real commissions inside buyers' transactions (referralProgram.ts:16-23; /referral live copy); the live activity feed is the shipped /activity heartbeat plus the homepage Live-activity panel (seo-route-registry.ts:449-453; syndicateFacts.ts:522-529 "the backbone is live in production"). The same paragraph promises modules "flip from future to active by themselves" — these two never flipped. Prune the list. Verifier: CONFIRMED (verbatim at :225-226, promise at :223-224). |
| P1 | /status surface map still says /activity "arrives with the event backbone" and the /fire-ledger per-event record "arrives with the backbone" — on the honesty hub itself | artifacts/studio/src/config/surfaceClassification.ts:226-253 | Rendered publicly in SystemStatus.tsx:284-328 "Surface map": :232 "/activity — Activity teaser — the public heartbeat; arrives with the event backbone." · :250 "/fire-ledger — Fire Ledger teaser — total burn live; per-event ledger arrives with the backbone." · :241 "/chronicle — Chronicle teaser…". The backbone is live and both surfaces serve the complete records (their own leads; seo registry :449-497; api sourceStatus.ts:137); the same /status page's source-status rows say so twenty lines up — the authoritative ledger contradicts itself within one viewport. Section comment :226 is the stale root. Verifier: CONFIRMED — /chronicle stale only in the word "teaser" (the register is live). |
| P2 | /status labels the live Member Home audience "Member preview" | artifacts/studio/src/config/surfaceClassification.ts:27, 32-36, 264-274 | /member is classified audience MEMBER_PREVIEW and /status renders the group heading "Member preview" (surfaceAudienceText). The member dashboard is a sealed live production surface (S7/S7-e, auth ON since 2026-07-11; modules.ts:169-173 calls the old preview posture "a fossil"). The file's own header (:11-18, "no auth system exists", "PREVIEW_LABELLED = renders exactly as today") photographs the IA-1 era. Rename the audience/enforcement vocabulary or the rendered text. Verifier: CONFIRMED — heading rendered publicly at SystemStatus.tsx:290-297. |
| P2 | /recognition lead claims "no standing figure is computed today" — the capital-axis rung and footprint ARE computed and shown publicly | artifacts/studio/src/pages/Recognition.tsx:44, 27-31 | Lead "…it is a future concept: no standing figure is computed today" (:44); dimension "Archive memory" badged PENDING_ADAPTER "Not live yet" (:27-31). Capital-axis standing (rung, cumulative footprint, next rung) is computed live and rendered on /activity rises and the member dashboard (capitalAxisReadmodel.ts; capital-standing route; truthStatus.ts:52-53 concedes "the capital axis lives on /activity"); archive+chronicle memory reads are live. The full recognition MODEL staying future is honest — the absolute "no standing figure" sentence is not. Whitepaper §9 has the same overreach (Whitepaper.tsx:214-216) while §5 (:163-165) speaks the live Capital axis in present tense. Route is noindex PENDING — exposure lower. Verifier: CONFIRMED — page is half-swept: the Source-attribution dimension :24 was already updated to "live and paying today". |
| P2 | /archive is still NOINDEX/PENDING "until archive reads are wired" — the unlock condition has been met for days | artifacts/studio/src/lib/seo-route-registry.ts:428-442 | routeType PENDING, indexStatus PENDING, sitemap:false, notes "ARCHIVE_READS_NOT_WIRED. NOINDEX until archive reads are wired." — while the SAME entry's description says "Artifacts are live on Avalanche today, mint counts and prices read from the chain." Reads are wired (api sourceStatus.ts:247; the page reads live). The gate's stated condition is satisfied but the posture never flipped — the exact "SEO layer rides the slice" violation. Founder decision needed: flip /archive to INDEX or restate the real remaining gap (museum surface / per-wallet holdings). Verifier: CONFIRMED (description at :434-435, notes :441). |
| P2 | /status token row: "No price, supply, or balances are wired or exposed" — contradicted by the treasury row beside it and by /tokenomics | artifacts/api-server/src/data/sourceStatus.ts:200-209 | Served by GET /api/source-status, rendered in /status's registry table: token note ends "No price, supply, or balances are wired or exposed." The same payload's treasury row (:217) says "Live treasury balances are served by the reality spine (… the seven allocation wallets)", and /tokenomics reads live supply, per-allocation balances, market price and entry rate (registry entry :270-281). True of the posture-only registry era; today reads as a protocol-wide denial. Reword to "this posture registry exposes none of them; the reality spine serves them live". Verifier: CONFIRMED. |
| P2 | Floating Guide on /join still offers "an exact read-only quote" — hiding the live two-signature join it sits on | artifacts/studio/src/content/guide-content.ts:38 | guideGreeting("/join") = "Considering a seat? I can walk you to an exact read-only quote." /join completes the join live (C5; checkoutGate.ts:15) and the same file's quick-route blurb (:25) already speaks it: "Your exact live quote — and the join itself, two signatures from your own wallet." Two eras in one file; the greeting renders on the page where the difference matters most. Align :38 with :25. Verifier: CONFIRMED. |
| P2 | /contracts Liquidity Pair note: "No live price, reserve, or quote is read in this foundation" — a foundation-wide denial contradicted by /liquidity, /tokenomics and /join | artifacts/studio/src/config/contractMemory.ts:190-195 | "A liquidity pair named in canon. No live price, reserve, or quote is read in this foundation." Live pair reserves and market price are read and shown on /liquidity and /tokenomics (registry :200-202, :270-281); /join computes live quotes. Other notes on the page correctly scope with "here"; this one claims the whole foundation. Same class at :119-120 (Attribution Router: "No commission or financial benefit is implied or paid" — commissions are paid today, just not via a router). Rescope both notes. Verifier: CONFIRMED — Whitepaper.tsx:187-197 renders live reserves and market price too. |
| P2 | Internal /os-map tells the founder Member Home has "no authentication or member data wired" | artifacts/studio/src/config/protocolOsMap.ts:207-214 | Rendered on operator /os-map: "Member Home … The future member surface. Labelled preview only; no authentication or member data is wired." SIWE auth is ON in prod (2026-07-11); the member dashboard serves live own-row standing/purchases/referral pay (D-TRUTH sealed). Internal-only (operatorPreviewGate), but it is the founder's own decision map speaking a dead era. Update the node and sweep siblings. Verifier: CONFIRMED — sibling "public-dashboard" node :205 ("Design preview") similarly dated. |
| P3 | Dead config still photographs the read-only era — "a lie waiting for an import" by the repo's own doctrine | artifacts/studio/src/config/syndicateFacts.ts:47-57, 144-212 | heroContent (badge NOT_LIVE / heroCore AWAITING_CHAIN_INDEX via truthStatus.ts:72-75) and studioPreviewPanel (rows: Proof of Fire PENDING_ADAPTER "once the event adapter is wired", Activity chronicle FUTURE, Membership receipts PENDING_ADAPTER) have NO consumer today (grep: PublicHome imports only operationalReality/awaitingWiring), yet studioPreviewPanel's doc comment (:158) claims "Rendered by PublicHome.tsx". Same class: referralProgram.ts memberCards (:190-197, unrendered; "Referral receipts" PENDING_ADAPTER) and its header comment :182-184 saying the R5 indexer "is not wired yet" while :192 says it serves live; truthStatus.ts:56-67 keeps the four retired "…NOT_WIRED" fossil values its own warning says not to consume, but modules.ts:219 still binds surfaceStatus.sourceAttribution into the "source" module. syndicateFacts deleted protocolSurfaces for exactly this reason (:13-17). Delete or re-derive the dead blocks. Verifier: CONFIRMED — memberCards' only reference is self (:414). |
| P3 | Stale era comments and notes in the routing/SEO layer (not rendered, but canon-misleading) | artifacts/studio/src/lib/seo-route-registry.ts:227-228, 443-445, 82 | (a) /join notes field still says "Transaction sending deliberately not enabled" (:227-228) — C5 enabled it 2026-07-13; the notes field is the registry's posture memory and now contradicts the entry's own live description. (b) Comment block :443-445 (and App.tsx:150) still calls /activity //chronicle //fire-ledger "§11 designed teasers — honest what-this-will-be pages, NOINDEX until each goes live" while all three entries are INDEX/live (:448-450, :466-468, :483-485). (c) Header comment claims "14 explicit routes" (:82) — [corrected] the registry holds 40 path entries (grep '^    path: "' = 40), not 39 as the finding stated; the header is stale either way. Same family: surfaceClassification.ts:14 "no auth system exists"; the three live pages keep "Teaser" filenames (App.tsx:151-158); TeaserSurface survives only as the chronicle empty-register fallback (ChronicleTeaser.tsx:35-36). Clean in one sweep. Verifier: CORRECTED (the 40-entry count). |
| P3 | FAQ still calls the seat number a "founder number" and points PENDING at primitives /status doesn't itemize | artifacts/studio/src/content/faq-content.ts:186-188, 54 | (a) "What is a founder number? — A permanent archive ID assigned in join order" (:186-188): the rest of the site speaks "member number" / "seat number" (member dashboard, /activity lines, whitepaper §5 "your number, chapter"); "founder number" is origin-era vocabulary appearing nowhere else in today's system. (b) :54 and :248 tell readers "The Status ledger lists exactly what is which" for the pending primitives (programmatic Vault contract, identity-record contract, governance, AI Layer) — the served registry's EXPECTED_KEYS (api-server src/data/sourceStatus.ts:356-381) has no rows for any of them, so the pointer over-promises. Vocabulary alignment + either add the rows or soften the pointer. Verifier: CONFIRMED. |

Refuted at verification: 0 of 23 findings.

## Lens 3 — human-readable

**Coverage.** Swept all 24 public studio page files; shared chrome (PublicLayout, MemberShell, registryPosture, LifecycleBadge/TruthLabel/PostureBadge/StatusPill mappings, HeroLedger, LiveActivityFeed KIND_LABEL); wallet flow (JoinCheckout, JoinHistoricalGate, MemberWalletPanel, ProposeSourceCreate/Promotion, MemberReferralDashboard); and the api-server failureReason/mode/status vocabulary (realityService, sourceValidate, joinQuote, holderIndex, sourceStatus, auth router) to verify what the client interpolations actually print today. Verified the enum→human mapping layer (truthStatus.ts, ProtocolReality maps) is the established pattern the flagged sites bypass, and that block-number formatting / humanReadFailure / formatRawUnitsDisplay are the sealed S7-e conventions the findings extend. Member Home itself (S7-e sealed) treated as reference canon, not re-audited.

| Sev | Finding | Where | Detail + verifier note |
|---|---|---|---|
| P2 | /status renders raw wire enums as user-facing badges (VERIFIED, READ_ONLY_HOLDER_INDEX_AGGREGATES, POSTURE_ONLY, static-canon) | artifacts/studio/src/pages/SystemStatus.tsx:89, 92, 192, 196 | Public /status (INDEX, sitemap) prints server enum values verbatim: :89 `{data.status}` renders the literal "VERIFIED" badge; :92 `{data.mode}` renders "READ_ONLY_HOLDER_INDEX_AGGREGATES" (pinned at artifacts/api-server/src/routes/holderIndex.ts:13); :192 `{data.mode}` renders "POSTURE_ONLY" and :196 `{data.generatedBy}` renders "static-canon" (artifacts/api-server/src/data/sourceStatus.ts:484-485). The codebase already has the humanizing pattern these bypass — displayLifecycleText maps every lifecycle enum to plain English (truthStatus.ts:129-141, e.g. READ_ONLY_PROOF → "Verified — view only") and ProtocolReality.tsx:42-54 maps sourceType/confidence enums. The un-extended remainder of the S7-e pass. Verifier: CONFIRMED — note truthStatus.ts is src/config/, not src/lib/. |
| P2 | Public proof table renders raw booleans "true"/"false" and the raw mode enum READ_ONLY_PROTOCOL_REALITY | artifacts/studio/src/components/ProtocolReality.tsx:68, 151, 248 | RealityValue (:68) renders boolean chain reads as `String(item.value)` → literal dev tokens "true"/"false" in the Value column (pause flags, isConfigured, chain-id match). :151 and :248 print `{data.mode}` verbatim → "READ_ONLY_PROTOCOL_REALITY" (artifacts/api-server/src/lib/protocol/realityEnvelope.ts:97). The same file proves the intended pattern (sourceType/confidence maps :42-54, applied at :99/:102); booleans and mode were missed. (:99/:102 also fall back to the raw enum on an unknown key — acceptable fail-open only because the maps are exhaustive today.) [corrected] Location: RealityTable/MetaStrip render on public /status (SystemStatus.tsx:181), /map (ProtocolMap.tsx:165), /contracts, /join, /source — NOT on /proof; and :248 (ProtocolRealitySummary) is operator-console only (pages/Home.tsx:34 via OperatorConsole.tsx:57), so :248 is not a public site. Verifier: CORRECTED as noted. |
| P2 | Buyer-facing /join interpolates server diagnostics verbatim into body copy | artifacts/studio/src/pages/JoinProtocol.tsx:174, 186, 470 | :174/:186 (`The referral code could not be validated: ${data.failureReason}` / `The referral state could not be read: ${data.failureReason ?? "unknown"}`) and :469-470 (`The engine could not be read: {data.failureReason}`) splice api-server diagnostics straight into sentences a buyer reads at the join gate. Server vocabulary today: "source id format invalid (expected 0x + 64 hex characters)" (api-server/src/routes/sourceValidate.ts:50), "expected deployed engine code not found on chain; quote skipped" (joinQuote.ts:149), "metadata decode failed", "source existence read failed (reported as unavailable)". The S7-e-sealed pattern exists: MemberReferralDashboard.tsx:71,109-112 humanizes via humanReadFailure() and keeps the exact reason in a title tooltip. /join never got that pass. Same defect on public /source: SourceLinkBuilder.tsx:55 (:54-55) and :108. Verifier: CONFIRMED. |
| P2 | "fail-closed" spoken to buyers/public — the S7-e law not extended beyond /member | artifacts/studio/src/wallet/JoinHistoricalGate.tsx:128 | S7-e engraved "'fail-closed' never spoken to a buyer" but only /member was cured. Still speaking it: JoinHistoricalGate.tsx:128 "Fail-closed: no live verification, no assumption" on the buyer block card inside /join; PublicHome.tsx:141 trust strip "unavailable (fail-closed)"; ProtocolMap.tsx:72 "unreadable (fail-closed)" on public /map; components/registry/registryPosture.tsx:53 "Posture unavailable (fail-closed)" and :107/:110 "Proof unavailable (fail-closed)" (renders on PublicHome via import at PublicHome.tsx:30-34); components/layout/PublicLayout.tsx:102 and :118 bake "fail-closed" into header tooltips on every public page; faq-content.ts:54 uses "fail-closed" in a public FAQ answer with no plain-language gloss. Fix pattern is established: say what the reader needs ("couldn't be read — nothing is assumed") and keep the doctrine word for docs/verifier lines. Verifier: CONFIRMED — every site re-proven, line numbers exact. |
| P2 | camelCase contract identifiers in buyer reading copy on the historical-member block card | artifacts/studio/src/wallet/JoinHistoricalGate.tsx:113, 127 | :113 puts the raw contract function name "(claimHistoricalMembership)" inside the main explanation paragraph a blocked buyer must read. [corrected] It IS parenthesized (the finding called it "unparenthesized") — the actual defect is that the raw function name sits inside the buyer's text-sm prose paragraph (:106-115) rather than in a verifier aside. :125-128 renders "Live read: knownMember = false · membership proof verified against the engine's on-chain historical root" — a code expression in the visible card, styled as a mono verifier line but at font-mono text-[10px], below the engraved 12px floor, compounding the readability defect. Verifier: CORRECTED as noted. |
| P2 | explainError fallback leaks the raw viem/wallet error first line to buyers and members | artifacts/studio/src/wallet/JoinCheckout.tsx:149-150 | explainError humanizes known reverts and user-rejection/chain-mismatch, but any OTHER failure falls through to `The transaction did not go through: ${firstLine.slice(0, 180)}` — the verbatim first line of a viem error (e.g. "ContractFunctionExecutionError: The contract function \"buyMembership\" reverted…") shown mid-checkout to a buyer. Same pattern at MemberWalletPanel.tsx:54 (160 chars). S7-e's rule for exactly this class: human sentence in the body, exact diagnostic in a tooltip. The 180-char raw splice is a diagnostic in the body. Verifier: CONFIRMED — neither routes the diagnostic to a tooltip. |
| P2 | /map shows server failureReason verbatim in the visible body, not a tooltip | artifacts/studio/src/pages/ProtocolMap.tsx:73-74 | When a figure is unreadable, /map (public INDEX route, App.tsx:132) renders the raw server diagnostic as visible mono text: `<span className="font-mono text-[10px] block">{failureReason}</span>` — e.g. "RPC endpoints did not respond", "expected deployed code not found on chain" (api-server/src/lib/protocol/realityService.ts:156,235). Contrast the S7-e-sealed member pattern (human line visible, exact reason in title= tooltip — CapitalAxisCard.tsx:169-171, MemberReferralDashboard.tsx:107-109). Also at text-[10px], below the 12px floor. ProtocolReality.tsx:62-63 does the same (failureReason at text-[10px] in the Value column) — rendered on /status, /map, /contracts, /join, /source (not /proof). Verifier: CONFIRMED. |
| P3 | freezeBlock rendered unformatted while every other block number on the site is grouped | artifacts/studio/src/pages/SystemStatus.tsx:120 | `Freeze block {data.freezeBlock}` prints the raw integer (e.g. 65431234) with no toLocaleString grouping. Convention everywhere else: LiveActivityFeed.tsx:430, pages/FireLedgerTeaser.tsx:151, MilestonesPanel.tsx:71, MemberPulse.tsx:75, MemberReferralDashboard.tsx:127 all use `.toLocaleString("en-US")` (part of the S7-e "block numbers formatted" seal). Same miss operator-side at ProposeSourcePromotion.tsx:237-238 ({INTRODUCTION_INDEX_SNAPSHOT.asOfBlock} raw) — admin-gated, lower stakes. Verifier: CONFIRMED. |
| P3 | Dev vocabulary in public prose: "payload", "null", "endpoint", "read-model" | artifacts/studio/src/pages/SystemStatus.tsx:71, 175, 178, 187, 231 | Public /status body copy speaks implementation language: "the hash-pinned VERIFIED snapshot from the holder-index endpoint" (:70-71), "This payload itself carries no member data" (:175), "any value that cannot be verified renders as null with a reason" (:177-178), DataStatusNote "every value here is null by design" (:187), "endpoint" again (:230-231). Also public /proof: ProofDashboard.tsx:39 "once the event read-model is wired" — "read-model"/"wired" are internal architecture words with no plain line beside them. Each needs the Human-First rewrite ("this page's data", "shows nothing rather than a guess", "once live event history is connected"). Verifier: CONFIRMED at every cited site. |
| P3 | Checkout receipt prints full 18-decimal SYN precision | artifacts/studio/src/wallet/JoinCheckout.tsx:450 | `{formatRawUnits(receipt.synOutRaw, synDecimals)} SYN sent to your wallet.` renders exact un-floored units in the success receipt; lib/rawUnits.ts:52 formatRawUnitsDisplay is the sealed floored alternative. [corrected] (1) Trailing fractional zeros are trimmed by formatRawUnits (rawUnits.ts:40), so long zero-tails cannot occur — only significant digits up to 18 render; (2) the premise that S7-e applied floored display to "checkout" figures is not in the current tree — formatRawUnitsDisplay appears only in MemberWalletPanel.tsx:98-99 and wallet/ownReads.ts:46/74; JoinCheckout uses exact formatRawUnits throughout (:339, :450, :485), so the receipt is consistent with its own page, weakening the defect claim (already P3/hedged). If receipt exactness is deliberate, consider floored-display + exact-raw-alongside. Verifier: CORRECTED as noted. |
| P3 | Raw ISO dates (2026-07-16) as the only human date across all public feeds | artifacts/studio/src/components/activity/LiveActivityFeed.tsx:430 | isoDayUtc rendered verbatim as the event date on every feed surface: LiveActivityFeed.tsx:429-430, pages/FireLedgerTeaser.tsx:151, MilestonesPanel.tsx:71, ProtocolOverviewPanel.tsx:268, MemberPulse.tsx:75, and "As of 2026-07-16" style at SystemStatus.tsx:204. Machine-sortable ISO is a consistent, defensible UTC style for proof surfaces, so polish only — but it is the S7-e "unformatted dates" class; "Jul 16, 2026" (with ISO in a tooltip) would read as human on member/marketing-adjacent surfaces like MemberPulse. Verifier: CONFIRMED. |
| P3 | Admin console speaks raw diagnostics to the non-technical founder | artifacts/studio/src/wallet/ProposeSourceCreate.tsx:650, 328, 545 | Admin-gated (renders only via pages/admin/sections.tsx:121-133), so admin-sees-public-never is not violated — but the console's one real user is the non-technical founder. :650 "Source exists with status code {record.status}" prints a raw numeric enum; :328 "unavailable — fail closed"; :545 "Fail closed — nothing is signed over an unknown state." (verifier: exact wording, not "nothing is proposed" as quoted — same line, same class). AdminOperatorsCrud.tsx:118/146 append `(reason: ${reason})` verbatim from the server. Lower severity by gating, but the S7-e founder-readability rationale applies here too. Verifier: CONFIRMED. |

Refuted at verification: 0 of 12 findings.

## Lens 4 — public-private

**Coverage.** Swept: studio route table + gating (App.tsx, operatorPreviewGate.ts, vite.config.ts define, OperatorConsole entry), public chrome (PublicLayout header/footer/mobile sheet, navigation.ts, modules.ts, brand/syndicateFacts/sharedCopy), registry-consuming public pages (SystemStatus full read, Docs, guide components), member boundary (memberDoors.ts, MemberShell/MemberQuickActions, MemberHeaderAffordance, MemberAccess), wallet layer (walletSession.ts operator-context probe, OperatorBadge/OperatorSignInAction importers — AdminShell-only), guards (guard-operator-gate.ts all 9 rule groups), serving layer (.replit-artifact/artifact.toml rewrites, generate-serving-rewrites.ts, public/robots.txt), and a dist-level grep of the local production build. API side: app.ts (CSP/CORS/mounting), authExposure.ts dark-404 gate, auth/router.ts (session-bound own-row-only reads, rejectCrossOrigin), operator/router.ts (role-gated writes), capitalStanding.ts, backboneStatus.ts + feedProjection output gates. Admin-sees-public-never verified: no public endpoint serves roster/directory data; operator reads role-gated with masked wallets.

| Sev | Finding | Where | Detail + verifier note |
|---|---|---|---|
| P1 | Public /status page renders the complete operator/admin surface map — every internal route linked and described to any visitor (public-sees-admin-never, Ruling ②) | artifacts/studio/src/pages/SystemStatus.tsx:290-327 | The "Surface map" section iterates surfaceClassification for audience OPERATOR_PREVIEW and renders, on the fully public INDEX route /status, all 15 internal surfaces with labels, live `<Link href>` anchors, and operational summaries: /studio, /proof-studio, /founder, /os-source, /os-map, /admin and all 9 admin sections (src/config/surfaceClassification.ts:287-441 — e.g. "/admin/operators — Admin section: operator roles and registry previews", "/admin/broadcast", "/admin/audit", "/admin/settings — build flags and system health"). A hand-delivered inventory of the admin console to every visitor and crawler (/status is in the sitemap), directly violating founder Ruling ② 2026-07-16 (zero admin trace on public+member surfaces). Client-side click renders OperatorPreviewUnavailable inside public chrome; a hard load 404s at the serving layer (artifact.toml has no rewrites for internal paths) — so the section is also a wall of dead doors. Because /status is indexable while robots.txt Disallows the internal paths, Google can index the internal URLs reference-only from these anchors (Disallow prevents ever fetching their noindex meta). Verifier: CONFIRMED — SystemStatus.tsx:290 iterates ["PUBLIC","MEMBER_PREVIEW","OPERATOR_PREVIEW"], :314-319 render the links; exactly 15 OPERATOR_PREVIEW entries at surfaceClassification.ts:286-441; /status INDEX/sitemap priority 0.8 (seo-route-registry.ts:106-115); artifact.toml :44-228 has no internal rewrite; dist/public/404.html exists. |
| P1 | Public footer "Console → Studio OS" link to /studio on every public and member page — the exact class the founder already retired from the home page, and a dead door in prod | artifacts/studio/src/config/navigation.ts:64 | footerGroupSpec includes { heading: "Console", itemIds: ["studio"] }; modules.ts:88 sets studio nav.footer:true, so PublicLayout.tsx:370-392 renders a "Console" footer group with a "Studio OS" link on every public route AND every member-shell page. The founder ruled this class out 2026-07-13 — syndicateFacts.ts:139-143 records the /studio home teaser "RETIRED FROM THE PUBLIC HOME: the teaser promoted an operator category on a public view"; the footer door is the same violation still live, now also violating Ruling ② 2026-07-16. In prod it is additionally broken: client-side click renders "Internal preview is not enabled on this deployment" (OperatorPreviewUnavailable); a full-page load of /studio returns the static 404.html (no rewrite in .replit-artifact/artifact.toml:44-228). guard-operator-gate.ts:15-17 still calls a config-driven /studio teaser "the intended, documented exception" — stale against the retirement and Ruling ②. Verifier: CONFIRMED — visible:true at modules.ts:82 keeps it past the navigation.ts:69 filter; App.tsx:163-175 routes /member,/wallet,/liquidity,/toolkit through PublicRoute → PublicLayout so member pages carry it too. |
| P2 | Admin/operator strings ship in the public production bundle — dist-verified — because modules.ts, surfaceClassification.ts and the SEO registry are statically imported by public chrome | artifacts/studio/src/config/modules.ts:77-125, 144-159, 209-242 | The operatorPreviewGate correctly dead-code-eliminates the console PAGES (verified: "INTERNAL FOUNDER PREVIEW" probe absent from dist/public/assets), but the operator module registry entries ride into the public chunk via PublicLayout → navigation.ts → modules.ts, SystemStatus → surfaceClassification.ts, and SeoHeadManager → seo-route-registry.ts (INTERNAL entries at :499-711). Dist evidence: artifacts/studio/dist/public/assets/index-DSnsVSxD.js contains "Admin section: operator roles and registry previews", "Operator console overview", "Founder/operator controls preview" (×2). Any visitor viewing source learns the admin console's full section map, module descriptions, and internal vocabulary (S11, OPERATOR_PREVIEW, DESIGN_PREVIEW). Under Ruling ② these are admin traces on a public surface even where no pixel renders them. (Local dist hash differs from the recorded prod bundle index-CJU-JUzs.js, but the static import graph guarantees the same inclusion in any build.) Verifier: CONFIRMED — bundle referenced by current dist/public/index.html:24 re-grepped; import graph re-verified (SystemStatus.tsx:23-28, navigation.ts:1, seo-route-registry.ts:499-711 "Studio OS — Operator Console" at :503). |
| P2 | guard-operator-gate rule 6 has a designed blind spot: it greps only 3 files for LITERAL internal path strings, so config-driven internal links in public chrome pass the guard | artifacts/studio/scripts/guard-operator-gate.ts:240-255 | Rule 6 ("Public chrome renders no literal INTERNAL links") scans only PublicLayout.tsx, PublicHome.tsx and OperatorPreviewUnavailable.tsx for literal internal path strings. Both live violations bypass it: the footer /studio link arrives through footerGroups (navigation.ts:64 → PublicLayout.tsx:373-386, href={item.path}), and the /status surface-map links arrive through surfaceClassification mapped into `<Link href={s.routePath}>` (SystemStatus.tsx:314-319) — neither scanned file contains a literal internal path. The regression net reports green while public chrome renders 16 internal links. Fix: rendered-output/DOM-level check, or a rule that no PUBLIC-audience component may pass an INTERNAL routePath into a Link, plus removal of the stale "documented exception" clause (:15-17). Verifier: CONFIRMED — scan is code.includes(`"${p}"`), a quoted literal path. |
| P2 | GET /api/backbone/capital-standing is an unauthenticated per-seat lookup: anyone can enumerate seat=1..N and rebuild the full member wealth/rung table the public feed's no-amount voice refuses to show | artifacts/api-server/src/routes/capitalStanding.ts:39-86 | The route takes any seat ordinal as a query param (SEAT_SHAPE_RE /^[1-9][0-9]{0,6}$/, 1-9999999) with no session binding and returns that seat's rung title and cumulativeUsdcRaw (:76-77). Contrast: every other own-standing read is session-bound own-row-only by explicit doctrine (auth/router.ts:366-378 member-standing — "no query/params lookup surface of any kind"); the OWN-ACCOUNT DISPLAY RULE (S7-b, quoted in this file's own header) covers the member's OWN dashboard. A trivial loop over seat numbers reconstructs a ranked wealth table of the entire membership — exactly the aggregate the feed's no-amount pin exists to prevent (:18-19 "a feed line never carries the amount"). Payload is address-free and derivable from public chain data, and the header claims CANON_VISIBILITY_LAW cover, so this may be founder-accepted — but the acceptance on file covers own-dashboard display, not an enumerable public lookup API. Needs an explicit founder decision or a session-bound move into the /api/auth zone. Verifier: CONFIRMED — mounted on the public read zone with zero auth middleware (routes/index.ts:24). |
| P3 | Public wallet layer probes /api/auth/operator-context on every session resolution — an admin-authority call visible in every member's network tab, useless in default prod builds | artifacts/studio/src/wallet/walletSession.ts:157-170, 233-244 | resolveAccessState fetches operator-context for every signed-in (S4) user to detect S11. The endpoint is fail-closed and coarse (auth/router.ts:286-308, account never echoed), so no data leaks — but in a default production build the operator console is dead-code-eliminated, so the S11 answer can never change what renders; the call only advertises operator machinery to any member who opens devtools. Under Ruling ②'s zero-trace standard, gate the probe behind OPERATOR_PREVIEW_ENABLED (or an equivalent build fold) so public builds never emit it. Verifier: CONFIRMED — AccessGate is pass-through for PREVIEW_LABELLED surfaces (AccessGate.tsx:9-11, 68-77); function body 163-181, fetch at 233-246. |
| P3 | Operator WRITE zone lacks the rejectCrossOrigin middleware the auth zone applies — weaker CSRF defense-in-depth on the highest-privilege router | artifacts/api-server/src/operator/router.ts:24-31 | auth/router.ts:96-120 rejects browser-declared cross-site POSTs (Origin allowlist + Sec-Fetch-Site) as an explicit second layer alongside SameSite=Strict cookies. The operator router — the only zone with authorized writes (referral-terms, operator invite/suspend) — mounts throttle/session/role checks but not this middleware. Practical exploitability ~nil today (cookie SameSite=Strict Path=/api, global CORS allows only GET/HEAD/OPTIONS, zone parses only application/json so a non-preflighted simple request yields an empty body → zod 400), but the write zone should not hold a strictly weaker origin posture than the read/auth zone it depends on. One router.use(rejectCrossOrigin) closes the asymmetry. Verifier: CONFIRMED — grep for rejectCrossOrigin/sec-fetch/origin over operator/router.ts returns nothing; write routes at :55/:102/suspend. |
| P3 | OperatorPreviewUnavailable names the internal surface inventory inside public chrome | artifacts/studio/src/pages/OperatorPreviewUnavailable.tsx:19-25 | The public-layout fallback rendered at internal routes tells any visitor the deployment contains a "Studio OS console, Proof Studio, founder and source postures, and the protocol OS map". Honest, but under Ruling ② it is an admin trace served to the public; a zero-trace version says only that nothing public lives at this address. Related structure disclosure: public/robots.txt:7-21 enumerates all 15 internal paths by name (including /admin/broadcast and /admin/audit) — standard tradeoff, but combined with the /status links it makes the admin map fully self-documenting to outsiders; a wildcard Disallow (/admin/ + the 5 console slugs) would disclose less. Verifier: CONFIRMED. |

Refuted at verification: 0 of 8 findings.

## Lens 5 — design-harmonization

**Coverage.** Static sweep of all of artifacts/studio/src for arbitrary sub-12px classes (text-[8..11px], counted 6/27/117/71 = 221 occurrences, 215 lines, 49 files) plus CSS font-size declarations (index.css tokens all ≥12px now); src/components/member confirmed 0 sub-12 (S7-b claim verifies); ADR-001 readability-floor and fluid-surface text re-read (ADR-001:110-155) so every judgment binds to today's canon. Fluid surface: index.html viewport meta verified (no maximum-scale, viewport-fit=cover), body safe-areas (inline axis only), all max-w caps enumerated, bare-vh grep (1 site). Header/footer chrome: single PublicLayout on all public mounts confirmed structurally; its own type sizes audited. Dark/light parity and empty states: spot-checked only. Own-account display: structural check only.

| Sev | Finding | Where | Detail + verifier note |
|---|---|---|---|
| P2 | Readability floor: 221 sub-12px occurrences (215 lines) across 49 files remain site-wide — recount of the mapped ~219 | artifacts/studio/src (aggregate) | ADR-001 amendment 2026-07-16 (docs/adr/ADR-001-design-system-et-methodologie.md:117-124) engraves: reading copy ≥14px, labels ≥12px, NOTHING user-visible under 12px, arbitrary text-[9px]/[10px]/[11px] forbidden. Today: text-[8px] ×6 (2 files), text-[9px] ×27 (10 files), text-[10px] ×117 (42 files), text-[11px] ×71 (27 files) = 221 occurrences across 49 files. Split: ~49 on internal/operator surfaces (pages/admin/panels.tsx ×20, AdminShell ×4, AdminHome ×3, referral/Admin* ×8, OsMap ×5, Home ×1, OperatorPreview ×1, AccessStateSimulator ×3, LiveEvidencePanel ×3, ChroniclePrepare ×1); ~172 on PUBLIC and member surfaces. /member composition itself is clean (0 matches in src/components/member) and the token layer was raised (index.css:231-232 syn-label/syn-caption 12px, --text-caption 0.75rem). ADR-001:128-132 acknowledges the general sweep as REMAINING work — a tracked gap, not a new lie; the count is the audit deliverable. Verifier: CONFIRMED — recount exact (221/215/49, per-size splits match); internal/public split approximate (~44-49 internal), headline count exact. |
| P2 | 8px text on the public homepage hero — the deepest floor violations sit on "/" | artifacts/studio/src/components/hero/SeatFlowDiagram.tsx:273, 298, 319, 341, 377 | The seat-flow diagram on PublicHome renders node subtitles and the routing line at font-mono text-[8px] (:273 "text-[8px] uppercase tracking-[0.12em]", :298/:319/:341/:377 node sub-labels). 8px is two-thirds of the engraved 12px absolute floor, on the single most-visited public surface, carrying live protocol truth (deriveNodeSub reads reality). Same file: text-[9px] ×5 (:249,:297,:317,:340,:376). Companion: PublicLayout.tsx:172 renders the header chapter badge at text-[8px] (sm:text-[9px]) — the 8px class ships in the site-wide chrome on mobile. Verifier: CONFIRMED — mount chain re-proven (PublicHome.tsx:24, App.tsx:104-105). |
| P2 | Site-wide header primary nav labels are 11px below 1536px viewport; dropdown path hints 10px | artifacts/studio/src/components/layout/PublicLayout.tsx:221, 243, 273 | Primary nav links (Protocol / Economy / Membership) and the "More" trigger are text-[11px] font-semibold, only rising to text-[12px] at 2xl (:221, :243) — so on every viewport under 1536px the main navigation labels of EVERY public page sit below the engraved 12px label floor. The "More" dropdown item path hint is text-[10px] (:273). Same chrome: ChainPill/LiveChip at text-[9px] (:103,:119), SYNDICATE badge at text-[9px] (:180). One fix repairs all 24 public mounts; today all 24 inherit the violation. Note: S7-e's "header dropdown raised to the 12px floor" covered the MEMBER header dropdown; the public-layout More dropdown and nav labels were not raised. Verifier: CONFIRMED. |
| P2 | Multi-sentence READING copy rendered at 10-11px (14px-class content, below even the 12px absolute floor) | artifacts/studio/src/components/activity/LiveActivityFeed.tsx:319, 391 | The activity health banner — a full paragraph of coverage truth ("Complete history, served by the event indexer... never evidence of absence") — is text-[11px] (:319-345); the summary row text-[11px] (:391). Same pattern: pages/FireLedgerTeaser.tsx:95 and :112 (burn-record banners, 11px), components/activity/MilestonesPanel.tsx:54 (11px) and :131 (notes 10px), JoinProtocol.tsx:397 (11px explanation), Liquidity.tsx:91 and :122 (11px entitlement/legal-adjacent lines), components/guide/SyndicateGuide.tsx:390 (11px), TeaserSurface.tsx:64 (11px). Under ADR-001:117-118 these are ≥14px-class sentences rendering at 69-79% of that — and they carry the honesty/coverage language the protocol most wants read. Verifier: CONFIRMED at every line. |
| P2 | Shared design-system atoms bake sub-12px into every consumer: Amount, StatusPill, Tag, DataTable, StatCard | artifacts/studio/src/components/amount/Amount.tsx:10, 13, 16, 32, 42 | Amount.tsx hardcodes font-mono text-[9px] for compact amounts and unit suffixes (:10,:13,:16,:42) and text-[11px] for the grouped label (:32) — every money display using the Amount atom inherits 9px units. components/status-pill/StatusPill.tsx:28-29 (sm=text-[10px], xs=text-[9px]) and components/tag/Tag.tsx:19-20 (identical scale) put 9-10px in every pill/tag site. components/data-table/DataTable.tsx:132 cell meta at text-[9px]. components/stat-card/StatCard.tsx:68/:76 delta+meta at text-[11px]. Page-by-page sweeps cannot close the floor while the atoms violate it; these five files are the highest-leverage fix in the whole 221-site count. Verifier: CONFIRMED — lines exact, directories kebab-case as noted. |
| P2 | The promised no-sub-12px-text guard does not exist — the readability floor is unenforced | docs/adr/ADR-001-design-system-et-methodologie.md:131 | ADR-001:131 commits to "un guard no-sub-12px-text qui rendra le plancher structurel". artifacts/studio/scripts contains 13 guards (no-fake-live, operator-gate, route-nav-drift, surface-coverage, freshness, forbidden-copy, posture-map, safe-source, surface-naming, lifecycle-labels, nav-link-display, era-drift, access-state) and zero mention font sizes (grep sub-12|12px|font-size across scripts/: no files). Until it exists, every new slice can silently add text-[10px] and the 221-site debt grows; the repo's own doctrine is that rules without guards drift. Verifier: CONFIRMED — exactly 13 guard-*.ts, grep returns nothing. |
| P2 | Member money surfaces still below the floor: /wallet panel note and checkout labels at 10-11px | artifacts/studio/src/wallet/JoinCheckout.tsx:458, 505, 526 | JoinCheckout renders the verify link and the two amount-row labels at font-mono text-[10px] (:458,:505,:526); JoinHistoricalGate.tsx:125 a 10px note — this is the PAYMENT flow, where misread labels have real cost; ADR-001:130 names "checkout" as remaining sweep scope. MemberWalletPanel.tsx:265 renders an 11px explanatory note on /wallet, a signed-member own-account surface the S7 member pass did not cover because the panel lives in src/wallet/, not src/components/member/. JoinProtocol.tsx has 7 more sub-12 sites (:205,:262,:327,:397,:410,:529,:532) including the routing-detail disclosure. Verifier: CONFIRMED at every line. |
| P2 | Fluid-surface inconsistency inside the member experience: /member is fluid, /wallet and /toolkit are capped at 1024px | artifacts/studio/src/pages/MemberWallet.tsx:22-35 | ADR-001bis rule 1 (ADR-001:140-142): app surfaces (dashboards, consoles) are FLUID full width, never capped. The signed-in /member dashboard complies (MemberAccess.tsx:127 and :145 "w-full mx-auto px-4 sm:px-6 lg:px-8"). But its sibling doors in the SAME MemberShell sidebar — /wallet (MemberWallet.tsx:22 wraps MemberShell in PublicPage) and /toolkit (MemberToolkit.tsx:15 same) — inherit PublicPage's max-w-5xl 1024px cap. A member clicking Wallet from the fluid dashboard watches the same shell snap from edge-to-edge to a 1024px column: a container-consistency break inside one product surface, narrower than the 1200-1440px prose law's own floor. Either these member app doors go fluid like /member, or the composition decision is recorded; today it is silent drift. Verifier: CONFIRMED — [corrected] PublicPage lives at src/components/PublicPage.tsx (max-w-5xl at :18 and :34). |
| P3 | Admin console main column capped at max-w-6xl — an app console under a width cap (fluid rule, internal surface) | artifacts/studio/src/components/admin/AdminShell.tsx:380 | AdminShell's main content area is "p-6 lg:p-8 max-w-6xl w-full mx-auto" (:380). ADR-001bis rule 1 says consoles are fluid; its scope line reads "générale, toutes surfaces" (:138-141). Internal-only (operatorPreviewGate keeps it out of prod bundles), founder/operator eyes only, so P3 — but wide admin tables (members registry, audit) at 1152px on a 2560px screen is exactly the pattern the rule was engraved against. Same class: SystemStatus.tsx:157 max-w-6xl (public, arguably prose-lawful), Home.tsx:20. [corrected] OsMap.tsx:46 is max-w-5xl and ProofStudio.tsx:13 / OperatorPreview.tsx:34 are max-w-4xl — still capped internal consoles, just tighter caps than claimed. Verifier: CONFIRMED with that correction. |
| P3 | Naked vh unit survives on the prod-facing fallback page (fluid rule 3: svh/dvh, never bare vh) | artifacts/studio/src/pages/OperatorPreviewUnavailable.tsx:10 | min-h-[60vh] on OperatorPreviewUnavailable — the ONLY bare-vh site left in studio/src (everything else uses svh, e.g. MemberAccess.tsx:237 min-h-[65svh]). ADR-001bis:147 engraves "jamais vh nu". This page is what internal routes render publicly in prod, so it does ship. One-token fix to min-h-[60svh]. Verifier: CONFIRMED — tree-wide grep for bare [Nvh] returns only this site. |
| P3 | Homepage hero lead paragraphs use off-scale arbitrary sizes text-[0.8rem] (12.8px) for reading copy | artifacts/studio/src/pages/PublicHome.tsx:316 | The hero lead block is "text-[0.8rem] leading-5 ... xl:text-[0.88rem]" — 12.8px reading copy (14.1px only at xl), below the 14px reading floor at all widths under 1280px, expressed as arbitrary rem values that bypass both the Tailwind scale and the type-* tokens (index.css:305-311 define type-body etc. for exactly this). It escapes the sub-12 grep family, so a future no-sub-12px guard keyed on text-[N px] would miss it — the guard should catch arbitrary rem values too. Verifier: CONFIRMED — wraps the multi-sentence heroSystem.explainer paragraphs. |
| P3 | Safe-area padding covers only the inline axis — no env(safe-area-inset-top/bottom) anywhere | artifacts/studio/src/index.css:313-318 | The FLUID SURFACE safe-area block sets padding-left/right: env(safe-area-inset-left/right) on body only; grep finds no other safe-area usage in studio/src. With viewport-fit=cover in index.html:5, the top and bottom insets are uncompensated: the toast viewport is "fixed top-0" (components/ui/toast.tsx:17) and would sit under an iOS notch in landscape/PWA contexts; bottom-anchored UI (SyndicateGuide floating button) can collide with the home indicator. ADR-001bis:144-146 says safe-areas "sur le body" so the rule's letter is met for left/right; top/bottom are the gap — low cost, matches the rule's own "coût zéro hors encoches" framing. Verifier: CONFIRMED. |
| P3 | ADR-001 token note is stale: says --text-caption is 11px pending re-evaluation, code already raised it to 12px | docs/adr/ADR-001-design-system-et-methodologie.md:123 | ADR-001:123 reads "--text-caption 11px → à réévaluer au sweep" and :131 lists "syn-label/syn-caption 9-10px" as remaining — but index.css:122 already sets --text-caption: 0.75rem (12px, with the READABILITY FLOOR comment) and index.css:231-232 set syn-label/syn-caption to 12px. The canon doc lags the code by one iteration (the S7-b token raise); a reader of the ADR alone would re-do or mis-scope the sweep. Doc-only truth drift. Verifier: CONFIRMED. |

Refuted at verification: 0 of 13 findings.

## Lens 6 — url-routing

**Coverage.** Swept: all 39 App.tsx mounts + catch-all against seo-route-registry.ts (parity clean; guard-route-nav-drift.ts covers router↔registry both ways), sitemap.xml (21 URLs == 21 INDEX entries), robots.txt (15 Disallows == 15 INTERNAL), prerender-routes.ts shell set (PUBLIC+PENDING flat files + real 404.html), artifact.toml generated rewrites (both slash variants for every PUBLIC/PENDING non-home route; INTERNAL correctly excluded so they 404 dark). Grepped every internal link source: JSX href/to/navigate/setLocation, template hrefs, and config path/href fields (navigation.ts, modules.ts, memberDoors.ts, sharedCopy.ts, syndicateFacts.ts, moduleRegistry.ts, learningModules.ts, memberActions.ts, guide-content.ts, not-found.tsx, routeMemory.ts) — every target verified against mounted routes; anchors verified (/member#settings and #referral-dashboard exist in MemberAccess.tsx:159/213/310; /chronicle#<id> matches ChronicleTeaser.tsx:52 article ids); /referral-program-terms-v1.txt exists in public/. Canonical layer verified: /referral canonical + /source-attribution 200-alias with cross-canonical (documented no-301-until-domain-transfer).

| Sev | Finding | Where | Detail + verifier note |
|---|---|---|---|
| P1 | /archive page claims live reads it does not perform — and contradicts itself on the same page | artifacts/studio/src/pages/Archive.tsx:17 | The lead (:17) says "Artifacts mint on-chain today — counts and prices are read live from the contract" but the component has ZERO live reads: no useQuery/fetch/chainReads anywhere in the file; it renders only static config via getContractsByCategory('archive') (:11, contractMemory.ts). Lines 66-68 of the SAME page then say "Archive reads against the deployed Archive 1155 contract — wiring them is its own slice. Nothing renders here before it can be read and verified" — the page asserts live reads and denies them in one breath. The SEO registry posture matches the code (seo-route-registry.ts:429-441, PENDING/noindex, note "ARCHIVE_READS_NOT_WIRED"), so the S7 truth-sweep COPY is the lie, and it propagated: modules.ts:424 ("counts and prices read live") and memberDoors.ts:52 ("counts read live"). LIVE-PRODUCTION-RULE class bug in reverse: a dash-era page claiming live figures it never renders. Verifier: CONFIRMED — full-file read; getContractsByCategory is a static filter over config (contractMemory.ts:219-221; zero fetch/useQuery/readContract in that file). |
| P1 | /archive tells the public "no chronicle entries are stored or published" while /chronicle serves three founder-promoted entries live | artifacts/studio/src/pages/Archive.tsx:44-49 | The Chronicle card carries LifecycleBadge DESIGN_CONCEPT (:44) and "A design concept today — no chronicle entries are stored or published in this foundation" (:47-48). src/config/chronicleRegister.ts holds three founder-promoted entries (ids at :44, :68, :92, all promotedUtc 2026-07-14) served on /chronicle, a live INDEX+sitemap route (seo-route-registry.ts:464-479, App.tsx:154-156, sitemap.xml:94). The card also has NO link to /chronicle — the one page named "Archive & chronicle" is the one place the real Chronicle is denied and unreachable. Compounded by the header: the "More" item labeled "Chronicle" routes here, so a user clicking "Chronicle" lands on a page declaring the Chronicle doesn't exist. Verifier: CONFIRMED — Archive.tsx contains no link to /chronicle anywhere (full-file read). |
| P2 | Public footer "Console" group links /studio — an internal, build-gated route that hard-404s on reload in prod | artifacts/studio/src/config/navigation.ts:64 | footerGroupSpec includes { heading: "Console", itemIds: ["studio"] }; modules.ts:88 sets studio nav.footer:true; PublicLayout.tsx:373-388 renders it for every visitor. /studio is INTERNAL (seo-route-registry.ts:498-510), robots-disallowed (public/robots.txt:7), excluded from prerender shells (scripts/prerender-routes.ts:174-176) and has NO rewrite in .replit-artifact/artifact.toml — in prod a click renders OperatorPreviewUnavailable and a reload/direct hit returns the real 404.html. A permanent public footer door into the operator zone violates public-sees-admin-never (founder Ruling ② 2026-07-16) and ships a link whose only outcomes are "unavailable" or 404. Verifier: CONFIRMED — toml :31-36 confirm unmatched paths get 404.html at real HTTP 404; SPA click path App.tsx:66-73, 184-186. |
| P2 | Header "More" item labeled "Chronicle" points to /archive; the real live /chronicle has no nav entry anywhere | artifacts/studio/src/config/navigation.ts:26 | headerSpec: { id: "archive", label: "Chronicle", zone: "more" } — the word "Chronicle" navigates to /archive, whose own Chronicle card denies entries exist (Archive.tsx:47-48). Meanwhile /chronicle is a live INDEX sitemap surface (App.tsx:154-156, seo-route-registry.ts:464-479) reachable only via member doors (memberDoors.ts:47), the /activity feed, and ChronicleLatest — no header or footer entry (modules.ts has no chronicle module; footer groups navigation.ts:60-65 omit it). Wayfinding truth bug: the label promises a page the link does not deliver, and the promised page is unreachable from public chrome. Verifier: CONFIRMED — PublicLayout.tsx grep: zero matches for /activity|/chronicle|/fire-ledger|/liquidity. |
| P2 | Six live public routes missing from modules.ts, the registry navigation.ts declares "the single route source of truth" | artifacts/studio/src/config/modules.ts:60-429 | App.tsx mounts /wallet (:167), /liquidity (:170), /toolkit (:173), /activity (:151), /chronicle (:154), /fire-ledger (:157) — all PUBLIC/INDEX in the SEO registry and sitemap — but modules.ts has no entry for any of them, so none can ever surface in the header or footer (navigation.ts builds only from module ids, :3-8, :60-65) and getModuleByPath() returns undefined for six live surfaces. navigation.ts:11-12 claims "The single route source of truth stays in modules.ts" — false today. guard-route-nav-drift.ts only checks module→registry, never registry→module, so this gap is unguarded. Result: /liquidity, /activity, /fire-ledger, /chronicle are invisible in all public chrome. Verifier: CONFIRMED — registry entries wallet:160-163, toolkit:178-181, liquidity:196-199, activity:446-450, chronicle:464-468, fire-ledger:481-485; getModuleByPath at modules.ts:441-442. |
| P3 | modules.ts nav.header flags are dead — the header is curated by a separate hand-list that contradicts them | artifacts/studio/src/config/navigation.ts:22-31 | headerSpec renders contracts, member, archive, recognition — whose modules.ts nav.header is FALSE (modules.ts:361, :176, :426, :255) — and omits learning whose nav.header is TRUE (modules.ts:330; home:72 also true, served by the wordmark). modules.ts:56-58 comment says "`nav` controls placement: header → public marketing header". Today only nav.footer and nav.sidebar are honored (navigation.ts:49-50, :69); the header flag is ignored entirely. Consistency drift that will mislead the next slice that flips a header flag and sees nothing change. Verifier: CONFIRMED — buildHeaderNav (navigation.ts:33-40) filters only on visibility; grep for "nav.header" finds zero consumers. |
| P3 | Stale registry banner: "14 explicit routes + the catch-all" — there are 39 entries | artifacts/studio/src/lib/seo-route-registry.ts:82 | The registry doc comment still says "Every actual route in `src/App.tsx` (14 explicit routes + the catch-all)". Today: 39 route entries + "*" (verified against App.tsx's 24 public + 15 operator mounts). Comment fossil from the 2.18 era. Verifier: CONFIRMED — grep counts 40 'path:' entries = 39 routes + '*'; App.tsx re-counted 24+15=39, matching. |
| P3 | Stale "designed teasers — honest PENDING/noindex promises" comments over three live INDEX routes | artifacts/studio/src/App.tsx:150 | App.tsx:150 ("§11 slot 2c designed teasers — honest PENDING/noindex promises") and seo-route-registry.ts:443-445 (same wording) still describe /activity, /chronicle, /fire-ledger as PENDING/noindex teasers. All three registry entries are INDEX + sitemap:true (registry:446-497; sitemap.xml:89-102). The route table is correct; the comments announce a dead era. Related filename drift: ActivityTeaser.tsx / ChronicleTeaser.tsx / FireLedgerTeaser.tsx serve live surfaces; TeaserSurface.tsx survives only as ChronicleTeaser's empty-register fallback. Verifier: CONFIRMED — imports at App.tsx:25,29,30; TeaserSurface used only by ChronicleTeaser.tsx:12,36. |
| P3 | Catch-all note claims soft-404 "deferred to Slice 2.18E" while the real 404 already ships | artifacts/studio/src/lib/seo-route-registry.ts:734-735 | The "*" entry note says "Currently returns HTTP 200 (soft-404); true 404 + noindex deferred to Slice 2.18E". Reality: prerender-routes.ts:196-200 writes a real branded 404.html (noindex,nofollow, no canonical) and artifact.toml:31-37 documents the blanket SPA rewrite as REMOVED with 404.html served at real HTTP 404 for unmatched paths. The registry note is the stale one. 404 grade otherwise strong: flat shells per route, both slash variants rewritten, branded client not-found with four recovery links. Verifier: CONFIRMED. |
| P3 | not-found recovery link "Take your seat" targets /member; everywhere else that CTA targets /join | artifacts/studio/src/pages/not-found.tsx:10 | not-found.tsx:10 maps label "Take your seat" to /member. Canon CTA copy binds that label to /join: sharedCopy.ts:13 (requestSeat → /join), syndicateFacts.ts:45 (seatCta → /join), Archive.tsx:80-81 ("Take your seat" → /join). A lost visitor clicking the buy-intent CTA lands on the member door instead of the join surface. One-line fix, but it's the money path from the 404 page. Verifier: CONFIRMED. |
| P3 | Source-family module ids are a three-way naming trap: "source" = /os-source, "source-link" = /source, "source-attribution" = /referral | artifacts/studio/src/config/modules.ts:195-226, 380 | modules.ts id "source" points at /os-source (operator console, :210,:213), id "source-link" at /source (public builder, :195,:197), id "source-attribution" at /referral (:380,:382) — no id matches its path, and the id that IS a path ("source") belongs to a different route. Same class as the Home.tsx (operator /studio overview) vs PublicHome.tsx ("/") filename drift. No functional break (routes verified distinct and mounted), but each is a wrong-file/wrong-id edit hazard. Also reference-only: routeMemory.ts:139 maps the legacy family to the /source-attribution alias rather than canonical /referral (routeMemory is imported nowhere in src — dormant config, not a rendered link; only other mention is a comment in surfaceNaming.ts:47). Verifier: CONFIRMED. |

Refuted at verification: 0 of 11 findings.

## Lens 7 — legal-trust

**Coverage.** Swept read-only: all 29 studio page files (Glob), shared chrome (PublicLayout.tsx footer block :365-418 full read), nav/footer registries (navigation.ts full, modules.ts via footerGroupSpec, brand.ts full), sharedCopy.ts full, seo-route-registry.ts (legal-term grep), the purchase flow (JoinProtocol.tsx disclaimer block + JoinCheckout.tsx full-file grep for terms/risk/acceptance language), risk-prose surfaces (Whitepaper, Tokenomics :78-192, Faq content, Liquidity, Support full read), tracker/analytics grep across the whole studio app, cookie mechanics in api-server/src/auth (router.ts cookie attributes, sessionStore.ts, throttle.ts per-IP keying, memberRoster.ts wallet↔memberNumber DB pairing), and docs/ for any decided legal-page posture (none found — only the internal §11 privacy matrix and auth-design cookie hygiene, engineering docs, not user disclosures). Verified today's truth for every claim: checkout live, chain reads live, auth cookies essential-only, no third-party trackers.

| Sev | Finding | Where | Detail + verifier note |
|---|---|---|---|
| P0 | MISSING: Terms of Use / participation agreement — live seat sales run with no contractual terms anywhere | artifacts/studio/src/wallet/JoinCheckout.tsx:171 | The protocol sells membership seats live in-page (/join lazy-mounts JoinCheckout, JoinProtocol.tsx:60-61,119) yet no Terms of Use, Terms of Service, or participation agreement exists anywhere in the tree: no route in App.tsx, no entry in seo-route-registry.ts (grep for terms/privacy/legal hits only the internal admin note at :587), no page file, no footer link (navigation.ts:60-65 has no Legal group). JoinCheckout.tsx itself contains ZERO terms/risk/acceptance language (grep for terms|agree|accept|disclaim|loss|refund|final returns only two `finally {` blocks at :358/:432) — a buyer signs a real USDC transaction with no acceptance step, no refund/finality contract, no dispute or liability terms. The API's "referral-terms" (operator/router.ts:55) is on-chain source economics, not legal terms. For a public crypto protocol taking money today this is the top-of-checklist item and it is absent, not weak. Checkout IS live (checkoutGate.ts:15; brand.ts:8-9 "the protocol sells seats live, in-page"). Verifier: CONFIRMED — absence re-proven exhaustively (29 page files enumerated, none legal). |
| P0 | PUBLIC LIE on /proof: page still claims "view-only: no payments, no live chain reads" while the protocol sells seats live | artifacts/studio/src/pages/ProofDashboard.tsx:73-77 | ProofDashboard (mounted at /proof — the primary header nav "Protocol" destination, navigation.ts:23) renders safetyCopy.readOnly at :77: "Right now this is view-only: no payments, no live chain reads, and nothing is changed behind the scenes" (sharedCopy.ts:34-35), plus a foundation-era lead at :73 ("why none of it is wired in this foundation yet") and a PENDING_ADAPTER badge at :74. Payments ARE live (/join checkout), chain reads ARE live (reality spine, backbone, heartbeat), and brand.ts:8-9 itself records that the read-only claim "DIED" in the M1-c truth sweep — but this page kept it. As a trust/honesty statement it is a public misstatement of the site's legal posture (a site that takes money while declaring it takes none). safetyCopy.readOnly is era-dead vocabulary; ProofDashboard is its only remaining render site. Verifier: CONFIRMED — site-wide grep confirms :77 is the ONLY render site; sharedCopy.ts lives at src/config/ (lines exact). |
| P1 | MISSING: Privacy Policy — personal data is processed (wallet-linked member records, session cookies, per-IP throttling) with no privacy disclosure anywhere | artifacts/studio/src/config/navigation.ts:60-65 | No privacy policy page, route, registry entry, or footer link exists (footerGroupSpec = Protocol/Learn/Membership/Console only; site-wide grep for "privacy" hits no legal surface). Yet the system processes personal data today: HttpOnly/Secure/SameSite=Strict session cookies bound to wallet addresses (api-server/src/auth/router.ts:79-81), a DB member-continuity registry pairing wallet↔memberNumber (memberRoster.ts:62-80), per-member purchase history at /api/auth/member-purchases (router.ts:613), per-IP request throttling (throttle.ts:1). Wallet + IP addresses are personal data under GDPR; Art. 13 notice obligations attach regardless of how privacy-preserving the architecture is (and it genuinely is — no trackers found). The internal privacy matrix (docs/architecture/WALLET_IDENTITY_AND_HOLDER_INDEX_DESIGN.md §11) governs exposure but is not a user-facing disclosure. Verifier: CONFIRMED — COOKIE_OPTIONS at router.ts:78-83. |
| P1 | WEAK: no affirmative risk/terms acknowledgment at the point of purchase — disclaimer lives in a separate card, checkout has none | artifacts/studio/src/pages/JoinProtocol.tsx:613-618 | The permanent money-surface disclaimer ("not an investment… Joining can result in total loss", :616-618, rendered unconditionally per the AUDIT FIX 8.1 comment) plus the honest economics lines (:608-612 "The market is free… Not equity. Not yield.") live in the EconomicsCard — but the actual transaction component JoinCheckout.tsx contains no risk language and no acknowledgment step. World-class checklist expects the buyer to affirmatively acknowledge risk/terms in the signing flow itself, not merely have prose elsewhere on the page. Present-but-weak: disclosure exists on the page; acceptance does not exist in the flow. Verifier: CONFIRMED — grep re-run on JoinCheckout: only two `finally {` blocks. |
| P2 | MISSING: working contact channel / imprint — Support is a NOT_ACTIVE preview and no legal entity, email, or address exists anywhere | artifacts/studio/src/pages/Support.tsx:13 | The only support surface (/support) is an intake PREVIEW badged NOT_ACTIVE (:13) with "what you'll be able to raise" future-tense copy — no working submission and no email address. The only reachable contact channels site-wide are the footer social links: X + two Telegram channels (brand.ts:51-70). No operator identity exists anywhere: brand.ts:3-12 gives only the name "The Syndicate" — no legal entity, jurisdiction of organization, registered address, or imprint/legal-notice page. For EU visitors an imprint-class disclosure is a statutory expectation for a commercial site; for everyone it is the trust baseline for a site taking money. Contact WEAK (social-only), imprint MISSING. Verifier: CONFIRMED — supportIntake.ts grep for @|email|contact: zero matches. |
| P2 | MISSING: footer legal-links block — the footer carries zero legal wayfinding on every public page | artifacts/studio/src/components/layout/PublicLayout.tsx:407-410 | The footer bottom line renders only "© 2026 The Syndicate. All rights reserved." + the verify note (:408-409, from brand.ts:10-11); footerGroupSpec (navigation.ts:60-65) defines Protocol/Learn/Membership/Console with no Legal group. The world-class pattern — Privacy · Terms · Risk Disclosure · Contact on every page footer — has no anchor because the target pages do not exist. This is the structural carrier for the P0/P1 gaps above: once legal pages exist, modules.ts + footerGroupSpec is the single registry-driven place to mount them. Sub-note (P3): the copyright year at :408 is hardcoded "© 2026" literal text — fine today, a staleness landmine each January. Verifier: CONFIRMED — literal confirmed. |
| P2 | MISSING: eligibility / jurisdiction / sanctions / age statement despite live public sales | artifacts/studio/src/pages/JoinProtocol.tsx:608-618 | No surface states who may participate: greps across studio/src, Whitepaper.tsx, Tokenomics.tsx, and faq-content.ts find no jurisdiction, sanctions, restricted-persons, or minimum-age language anywhere. A live crypto purchase flow open to the world with zero geographic/eligibility posture (not even "participation may be restricted in your jurisdiction; you are responsible for your local law") is a standard gap. Would normally live inside the missing Terms of Use; flagged separately so the inventory is complete. Verifier: CONFIRMED — only unrelated grep hits (referral-eligibility copy, access-state labels, code comments). |
| P3 | Cookie posture is compliant-by-architecture (essential-only, no trackers) but declared nowhere | artifacts/api-server/src/auth/router.ts:79-81 | Present/strong on substance: the only cookie is the strictly-necessary auth session cookie (httpOnly:true, sameSite:'strict', secure:true; path:'/api') and no analytics/tracking scripts exist in the studio (grep for gtag/plausible/posthog/hotjar/mixpanel/pixel matches only social share-URL builders in lib/shareTargets.ts:21-23 and a comment in MemberSigil.tsx). Under ePrivacy the strictly-necessary exemption means NO consent banner is required — the absence of a cookie banner is correct, not a bug. Weak side: this posture is documented in zero user-facing words; the missing Privacy Policy is where "we set one essential session cookie and run no trackers" belongs. Practice PRESENT/strong, disclosure MISSING. Verifier: CONFIRMED, slightly stronger — secure:true is a LITERAL at :80; index.html has no external script/analytics tags. |
| P3 | Risk disclosure is PRESENT and strong in prose but scattered — no canonical risk/legal surface a footer link could target | artifacts/studio/src/pages/Whitepaper.tsx:115-123 | Positive inventory: substantive risk/legal callouts at Whitepaper.tsx:115-123 ("Risk & legal" — experimental utility token, not a security, total loss, not financial advice), Tokenomics.tsx:84-89, :131-136, :183-191 (three "Legal"/"Read this" callouts), JoinProtocol.tsx:608-618 (unconditional money-surface disclaimer), faq-content.ts:235-244 (dedicated "Risk & Legal" FAQ section, id "risk") and :170-174 (LP risks), Liquidity.tsx:161-166 ("The LP Risk Notice"), plus reusable safetyCopy.notInvestment (sharedCopy.ts:28-29). The gap is structural, not substantive: per-page copy with no single consolidated Risk Disclosure page, so nothing legal is linkable from the footer and wording drifts per surface (Whitepaper and Tokenomics :184-191 carry near-duplicate hand-maintained paragraphs). Verifier: CONFIRMED at every cited site — the Tokenomics "Read this" callout near-duplicates the Whitepaper paragraph word-for-word (drift risk confirmed); faq-content.ts path is src/content/. |

Refuted at verification: 0 of 9 findings.

## Lens 8 — a11y

**Coverage.** Code-level sweep: shared chrome (PublicLayout header/footer/mobile sheet, MemberShell both renders, PublicPage chassis, index.html template + prerender-routes.ts shells); all 24 public page mounts for one-h1-per-page (confirmed exactly one each); design tokens in index.css both themes with computed contrast ratios for gold/cyan/muted-foreground/destructive; full-repo greps for sub-12px arbitrary type (215 hits/49 files), outline-none vs replacement rings (all hits reviewed incl. every ui/* primitive — shadcn primitives clean), aria-live/role=status/alert inventory, aria-current inventory, all `<img>` sites (2 files, both have alt), and every raw input site (30 hits) for label association; interactive deep-reads: DataTable (sort a11y confirmed fixed and correct), FaqAccordion, SyndicateGuide, LiveActivityFeed, JoinProtocol quote + JoinCheckout two-step money path, ProposeSourceCreate/Promotion, MemberReferralDashboard, ChroniclePrepare, ThemeToggle, ui/button. ADR-001 readability-floor amendment read in full. API server has no HTML surface — out of a11y scope.

| Sev | Finding | Where | Detail + verifier note |
|---|---|---|---|
| P1 | Light-theme accent tokens fail text contrast (gold ~2.96:1, cyan ~2.85:1) and are used as small-text colors site-wide | artifacts/studio/src/index.css:83-99 | Token-level, today's values: light --gold-500 = hsl(38 74% 44%) (~#C3861D) on --background hsl(210 40% 98%) ≈ 2.96:1; --cyan-500 = hsl(190 90% 40%) (~#0AA3C2), aliased to --primary/--proof/--live, ≈ 2.85:1. WCAG 1.4.3 AA requires 4.5:1 for normal text and these tokens are used as NORMAL-SIZE (and frequently sub-12px) text everywhere in light theme: text-gold at 8-9px in the header wordmark descriptor and chapter badge (PublicLayout.tsx:172,180), mobile-menu route paths text-gold (:333), text-primary 10px links in the Guide (SyndicateGuide.tsx:191,314), verify links text-proof/80 at 10px (LiveActivityFeed.tsx:435,444; JoinCheckout.tsx:458 — 80% opacity drops it to roughly 2.3:1), FaqAccordion selected chips text-identity at 10px (:111). --destructive hsl(0 84% 60%) error text also fails for small error copy (JoinProtocol.tsx:719, JoinCheckout.tsx:484). Dark theme values pass comfortably — a LIGHT-theme-only token defect. EAA in force makes this legal-exposure adjacent, on public + money-path surfaces. Verifier: CONFIRMED — token lines re-proven (gold :94, cyan :83, aliases :84/:98/:99, destructive :92); [corrected] destructive computes ~3.5:1 rather than ~3.3:1 — still a clear AA failure. |
| P1 | ADR-001 readability floor (nothing user-visible under 12px) violated by 215 arbitrary sub-12px classes across 49 files, incl. 8px text; the named guard does not exist | artifacts/studio/src/components/hero/SeatFlowDiagram.tsx:273, 298, 319, 341, 377 | ADR-001 amendment 2026-07-16 (docs/adr/ADR-001-design-system-et-methodologie.md:117-132) rules: reading copy ≥14px, labels ≥12px, "RIEN d'utilisateur-visible sous 12px", text-[9px]/[10px]/[11px] forbidden. Today's tree: 215 occurrences of text-[8|9|10|11]px across 49 files (rg count). Worst: text-[8px] on the PUBLIC homepage hero SeatFlowDiagram (:273,:298,:319,:341,:377) and the header brand descriptor text-[8px] (PublicLayout.tsx:172); 9px on ChainPill/LiveChip/chapter badge (:103,:119,:180), HeroLedger (7 sites), HeroStatusChips, StatusPill.tsx:29, Amount.tsx:10-42 (money display sub-line at 9px), SystemStatus (12 sites), admin panels.tsx (20 sites). The ADR lists this as "RESTE (tranche sweep générale)" — but the promised guard "no-sub-12px-text" (ADR-001:131) is NOT in artifacts/studio/scripts/ (13 guards, none for type size), so the floor is doctrine without enforcement and new sub-12 sites keep landing (S7-e raised only the header dropdown; the raised dropdown still contains a 10px path hint at PublicLayout.tsx:273). Verifier: CONFIRMED — rg returns exactly 215/49 today; every cited line re-verified. |
| P2 | No skip link / bypass block on any surface (WCAG 2.4.1) | artifacts/studio/src/components/layout/PublicLayout.tsx:196-368 | The sticky header (wordmark + 3 primary nav links + More dropdown + social row + chips + theme toggle + shield + member affordance + CTA — 12+ tab stops) repeats on all 24 public routes; rg for "skip" across src finds no skip-to-content link anywhere; `<main>` (:368) has no id/tabindex target. Keyboard and screen-reader users must traverse the full header on every page. Same gap in Shell.tsx (operator) and AdminShell.tsx. Fix: one visually-hidden-until-focused anchor before the header + id on `<main>`. Verifier: CONFIRMED — only grep hits are unrelated comments. |
| P2 | Money-path amount input has no label — placeholder only (WCAG 3.3.2 / 4.1.2), and its error is not programmatically associated | artifacts/studio/src/pages/JoinProtocol.tsx:703-712 | The custom join amount `<Input>` on /join carries only placeholder="Or enter your own (USDC)" — no label, no aria-label; the placeholder disappears on typing and screen readers announce an unnamed textbox on the purchase flow. The validation error at :718-722 is a plain `<p>` with no aria-describedby link and no role="alert"/aria-live, so a screen-reader user typing an invalid amount hears nothing. The project's own Field atom (components/field/Field.tsx:71) already implements label + role=alert error wiring — it just isn't used here. Verifier: CONFIRMED. |
| P2 | FAQ accordion answer region labels itself — broken aria-labelledby reference | artifacts/studio/src/components/faq/FaqAccordion.tsx:170-174 | The open-answer container has id={`${key}-panel`} AND aria-labelledby={`${key}-panel`} — the region points at its own id, not at the question button (which has no id at all). The accessible name of the role="region" collapses to its entire content / nothing useful. Should reference an id placed on the question. Everything else in this accordion (aria-expanded, aria-controls, h2/h3 hierarchy, aria-pressed chips) is correct — this one reference is the defect. Verifier: CONFIRMED — panelId at :148, identical string at :171/:173; button (:152-167) has no id. |
| P2 | Activity feed filter chips expose selected state by color only — no aria-pressed (WCAG 4.1.2 / 1.4.1) | artifacts/studio/src/components/activity/LiveActivityFeed.tsx:357-371 | The /activity (and every embedded feed) filter buttons style the active filter with gold border/background only and set no aria-pressed / aria-current / role=tab — a screen-reader user cannot tell which lane filter is active. The sibling pattern in FaqAccordion.tsx:107 sets aria-pressed correctly, so this is drift, not a missing pattern. The chips are also text-xs with muted-foreground vs foreground as the only redundant cue. Verifier: CONFIRMED — buttons carry type/onClick/className/data-testid only. |
| P2 | Guide greeting bubble removes the keyboard focus indicator (focus-visible:outline-none with no replacement ring) | artifacts/studio/src/components/guide/SyndicateGuide.tsx:185-189 | The "Open the Guide" greeting button (rendered once per browser on every public page) has className="block pr-4 text-left focus-visible:outline-none" — outline suppressed and NO focus-visible:ring added, unlike every other control in this file (:181,:204,:248,:256 all pair outline-none with ring-2). A keyboard user tabbing to it gets zero visible focus — WCAG 2.4.7 fail on a global element. Verifier: CONFIRMED. |
| P2 | muted-foreground at /60-/70 opacity used for real (and tiny) text — contrast ~2.2:1 in light theme | artifacts/studio/src/components/layout/PublicLayout.tsx:273 | Light --muted-foreground hsl(215 16% 47%) is itself borderline (~4.5-4.7:1); at 60% alpha over white it blends to roughly 2.2:1. Concrete sites: the "More" dropdown route-path hints — font-mono text-[10px] text-muted-foreground/60 (:273, inside the very dropdown S7-e "raised to the 12px floor"); search placeholders placeholder:text-muted-foreground/60 (FaqAccordion.tsx:85, SyndicateGuide.tsx:279 — placeholders are text under 1.4.3); inert member door chips text-muted-foreground/70 (MemberShell.tsx:84, plus /70 group titles at :105); DataTable idle sort glyph text-muted-foreground/50 at 9px (DataTable.tsx:132-133, aria-hidden so lower stakes). Pattern-level: any /50-/70 alpha on muted-foreground fails AA in light theme. Verifier: CONFIRMED — token at index.css:89. |
| P2 | Founder-facing propose/sign forms use placeholder-only inputs (no label) with border-color-only focus | artifacts/studio/src/wallet/ProposeSourcePromotion.tsx:283-290 | The "member wallet 0x…" input that binds a promotion signature has no label/aria-label (placeholder only) — the explanatory `<p>` at :275 is not associated. The class string ("outline-none focus:border-primary/60") gives only a 1px 60%-alpha border-color shift as the focus indicator — far below the WCAG 2.2 focus-appearance advisory, hard to see in light theme. Contrast ProposeSourceCreate.tsx:396-408, which labels its identical input correctly via htmlFor — the pattern exists one file over. Also: admin ChroniclePrepare.tsx:72-76 (5 placeholder-only fields, build-gated) and AdminOperatorSurfaces.tsx:41-46. Verifier: CONFIRMED. |
| P3 | Dynamic surfaces have no live-region announcements (loading, results, feed refresh, checkout progress) | artifacts/studio/src/components/activity/LiveActivityFeed.tsx:398-399 | rg for aria-live/role=status/role=alert across src hits only Field errors, ui/alert, ui/field and ui/spinner — zero aria-live anywhere. The activity feed's "Reading the chain…" → results transition, the Re-read refresh (:372-385), the Guide's search result count changes (SyndicateGuide.tsx:284-336), and the FAQ "N questions" count (FaqAccordion.tsx:122-127) all update silently for screen readers. A polite aria-live on the count/status lines would cover it. Checkout is partially OK because button text itself changes ("Waiting for your wallet…"). Verifier: CONFIRMED. |
| P3 | No aria-current="page" on active nav states (header, mobile sheet, member doors, footer) | artifacts/studio/src/components/layout/PublicLayout.tsx:219-232 | Active route is conveyed by color + a decorative aria-hidden underline bar only; rg shows aria-current used solely in SectionIndex.tsx and two unused ui primitives (breadcrumb, pagination). Header primary nav (:219-232), More dropdown items (:264-276), mobile sheet links (:323-335), and MemberShell door chips/rows (MemberShell.tsx:69-80, 110-117) should set aria-current="page" on the matching Link. WCAG 1.3.1/advisory; cheap, mechanical fix. Verifier: CONFIRMED. |
| P3 | ChainPill/LiveChip explanations live in title attributes only — unreachable by keyboard and touch | artifacts/studio/src/components/layout/PublicLayout.tsx:102, 118, 179 | The "every public figure is a live chain read, fail-closed" explanations on the header chips and the chapter badge exist only as title= on non-focusable `<span>`s — mouse-hover-only. The visible text (chain name + state) stands alone, so this is supplementary-info loss, not a truth gap. Same pattern on the "memory" anchor tag in LiveActivityFeed.tsx:418-421. A shared tooltip primitive (ui/tooltip exists in the tree) or visually-hidden text would fix the class. Verifier: CONFIRMED — exact title strings re-proven. |
| P3 | Guide panel: role=dialog without aria-modal, focus trapped while background stays interactive | artifacts/studio/src/components/guide/SyndicateGuide.tsx:217-222, 139-160 | The open Guide sets role="dialog" and hard-traps Tab inside the panel (onPanelKeyDown wraps first/last), but omits aria-modal and does not inert the page — a hybrid: keyboard users cannot Tab out to the page (only Esc exits, which works and returns focus to the launcher, so no 2.1.2 keyboard-trap fail), while the page behind remains fully interactive to pointer/screen-reader browse. Either drop the Tab wrap (non-modal chat widget) or go fully modal. Minimized state (:229 h-14) keeps the trap with only 2 buttons — functional but odd. Verifier: CONFIRMED. |
| P3 | Read-only referral link input has no accessible name | artifacts/studio/src/components/referral/MemberReferralDashboard.tsx:253 | `<Input readOnly value={link}>` holds the member's referral URL with no label/aria-label; the visible "Your referral link" text at :245 is a sibling span, not associated. Screen reader announces an unnamed readonly textbox containing a long URL. aria-label="Your referral link" is the one-line fix. (The Copy button next to it is properly labeled by its text.) Verifier: CONFIRMED. |
| P3 | Prerendered shells ship an empty div#root — no landmark, heading, or noscript content in the served HTML | artifacts/studio/scripts/prerender-routes.ts:133-152 | renderRoute() bakes head-only (title/meta/JSON-LD/CSP) into the built index.html whose body is a bare empty #root (index.html:25-28) — acknowledged head-only SSG by the script's own comment (:15-18). Pre-hydration and JS-off users get a titled blank page: no h1, no landmarks, no noscript notice. Post-hydration every route mounts exactly one h1 (verified across all 24 public pages) and index.html has lang="en" (:2), so the hydrated app is sound; a static-shell-only gap. A `<noscript>` line in the template would be honest and cheap. Verifier: CONFIRMED. |
| P3 | Footer h3 group headings can skip a level on pages whose main content lacks h2s | artifacts/studio/src/components/layout/PublicLayout.tsx:375-377 | Footer group headings are hardcoded `<h3>` on every public route. Pages that render only the PublicPage h1 plus card-title divs produce h1 → h3 document outlines. Non-blocking (heading-skip is best-practice, not a hard AA failure), trivially fixed by h2 with the visual style kept. [corrected] MemberToolkit.tsx is a valid example (zero h1/h2 in its body; relies on the PublicPage h1, so its outline is h1 → footer h3); the Support.tsx example is wrong — Support.tsx renders h2 at :22 and :35, so its outline reaches the footer h3 through h2s. Keep the finding with MemberToolkit (and similar h2-less short pages); drop Support.tsx. Verifier: CORRECTED as noted. |

Refuted at verification: 0 of 16 findings.

## Lens 9 — seo-machine

**Coverage.** Swept: seo-route-registry.ts in full (all 39 entries + catch-all + every helper: getRobotsDirective, getCanonicalUrl, resolveRouteHead, breadcrumb projections); scripts/prerender-routes.ts (per-route head baking, CSP/referrer meta, JSON-LD injection, 404.html); scripts/check-seo-registry.ts (all 15+ check groups — router↔registry↔sitemap↔robots parity enforced, guard runs in release:gate alongside seo:rewrites:check); scripts/generate-sitemap.ts; public/sitemap.xml (21 locs == 21 INDEX entries, no leaks, no fabricated lastmod); public/robots.txt (15 disallows == 15 INTERNAL, Sitemap line present); index.html base template; SeoHeadManager.tsx + FaqJsonLd.tsx runtime injectors (mounted: App.tsx:255, Faq.tsx:75) and single-source builders seo-jsonld.ts / seo-faq-jsonld.ts; brand.ts (sameAs); artifact.toml clean-URL rewrites; public/ asset inventory; opengraph.jpg measured on disk (1280×720 — matches declared og:image dimensions, no lie); API headers for machine-visibility posture. No P0/P1 found in this lens — the SEO spine (registry-as-single-source, prerendered per-route heads, guarded parity, honest sitemap/robots) is genuinely grade-A; findings are one implementation-vs-intent gap (alias canonical), one platform-icon gap, and polish.

| Sev | Finding | Where | Detail + verifier note |
|---|---|---|---|
| P2 | /source-attribution alias emits NO rel=canonical to /referral — the consolidation the registry itself promises is not implemented | artifacts/studio/src/lib/seo-route-registry.ts:391-410, 791-794 | The alias entry's own note (:392-394) says: "noindex,follow + cross-canonical → Google consolidates". But getCanonicalUrl (:791-794) returns null for any non-INDEX entry, and /source-attribution is indexStatus REDIRECT — so resolveRouteHead yields canonical=null, prerender-routes.ts setCanonical (:73-77) STRIPS the canonical from the source-attribution.html shell, and SeoHeadManager removes it at runtime. check-seo-registry.ts:317-323 actively enforces canonical===null for non-INDEX routes, locking the gap in. Today the alias serves 200 + noindex,follow + og:url=/referral (og:url is not a canonicalization signal) — Google gets no consolidation hint, and per Google guidance a long-noindexed page's links may eventually stop being followed. Either the REDIRECT class should emit a cross-canonical (the documented intent) or the registry note is a false claim; one of the two must change. Verifier: CONFIRMED — resolveRouteHead :853-866, ogUrl falls back to canonicalPath "/referral". |
| P2 | Icon set is a single SVG favicon — no apple-touch-icon, no PNG/ICO fallback, no web manifest, no theme-color | artifacts/studio/index.html:20 | index.html declares only `<link rel="icon" type="image/svg+xml" href="/favicon.svg">` and public/ contains only favicon.svg (no apple-touch-icon.png, no favicon.ico/png, no site.webmanifest — verified by glob). Safari (macOS and iOS) does not use SVG favicons and iOS home-screen/bookmark icons require apple-touch-icon, so Apple users get a blank/generic icon for a brand that trades on polish; grep confirms zero occurrences of apple-touch/manifest/theme-color anywhere in the studio tree. Standard fix: apple-touch-icon 180×180 PNG + 32/16 PNG or ICO fallback + manifest + `<meta name="theme-color">`. Verifier: CONFIRMED. |
| P3 | Registry and SeoHeadManager still claim prerender "is not added yet" — stale by five slices | artifacts/studio/src/lib/seo-route-registry.ts:11-13 | :11-13 ("static social/AI preview bots that do not run JS may still see the base index.html until SSR/prerender is added later") and SeoHeadManager.tsx:7-10 (same claim) describe a pre-Slice-2.0 world. Today scripts/prerender-routes.ts bakes per-route head + JSON-LD into flat static shells on every build (package.json:8), so non-JS crawlers DO get per-route metadata. Internal-comment truth drift on the SEO single-source-of-truth file. Verifier: CONFIRMED. |
| P3 | Registry header says "14 explicit routes + the catch-all" — there are 39 entries | artifacts/studio/src/lib/seo-route-registry.ts:82 | The doc comment above seoRouteRegistry still states the Slice-2.18B count. Current tree: 39 entries + catch-all (24 public/pending + 15 internal), parity with App.tsx enforced by check-seo-registry.ts. Stale count on the canonical SEO file. Verifier: CONFIRMED — 40 `path:` entries = 39 explicit + "*" at :725; stale by 25 routes. |
| P3 | Stale "§11 designed teasers — NOINDEX until live" comment sits above three INDEX/live entries | artifacts/studio/src/lib/seo-route-registry.ts:443-445 | The comment block introducing /activity, /chronicle, /fire-ledger still calls them "honest what-this-will-be pages, NOINDEX until each goes live", while all three entries beneath are indexStatus INDEX, sitemap true, describing live served surfaces. Entries correct; comment describes a dead era. Verifier: CONFIRMED (/activity :447-450, /chronicle :465-468, /fire-ledger :482-485). |
| P3 | Catch-all note claims soft-404 "deferred to Slice 2.18E" while the real 404 is already shipped | artifacts/studio/src/lib/seo-route-registry.ts:734-735 | The "*" entry note says "Currently returns HTTP 200 (soft-404); true 404 + noindex deferred to Slice 2.18E." Today prerender-routes.ts:196-200 writes a real branded 404.html (noindex,nofollow, no canonical) and .replit-artifact/artifact.toml:31-37 documents the blanket SPA rewrite REMOVED with unmatched paths falling through to 404.html at real HTTP 404. One of the two notes is stale — it's this one. Verifier: CONFIRMED (artifact.toml :25-37). |
| P3 | No og:site_name / og:locale on any page; no og:image:alt / twitter:image:alt; no twitter:site despite an official X handle | artifacts/studio/scripts/prerender-routes.ts:134-152 | renderRoute sets title/description/robots/canonical/og:title/og:description/og:url/og:image/twitter:card+title+description+image and nothing more; grep confirms zero occurrences of og:site_name, og:locale, og:image:alt, or twitter:site anywhere (index.html base template included). The brand HAS an official X handle (@TheSyndicateOne, brand.ts:55) that twitter:site should carry; og:site_name governs the attribution line on link previews; image alt is the accessibility of every social card. All one-line additions to the template + renderRoute. Verifier: CONFIRMED. |
| P3 | Several meta descriptions far exceed the ~160-char SERP display budget | artifacts/studio/src/lib/seo-route-registry.ts:452-453 | [corrected] Measured from the live registry via Node: /activity = 273 chars (not ~350 — that figure belongs to the /activity NOTES field at :461-462, not the description), /docs = 251, /faq = 241, /tokenomics = 209. All still far exceed ~160; Google truncates at roughly 155-160 chars, so the crafted closing clauses — including the protective "Not a security; no promise of gain" on /faq (:290), /docs (:308), /tokenomics (:272) — sit past char 160 and are cut from the snippet, the exact truncation hazard the registry's own SEO-metadata law comment (:377-379) warns about for negations. check-seo-registry.ts:89-95 validates presence only (trim().length > 0), no length ceiling — a length check would hold the line. Verifier: CORRECTED as noted. |
| P3 | One shared opengraph.jpg for all 24 shells — no per-route OG images | artifacts/studio/src/lib/seo-route-registry.ts:76 | Every entry sets ogImage: DEFAULT_OG_IMAGE (/opengraph.jpg, verified 1280×720 on disk matching the baked og:image:width/height in index.html:14-15 — dimensions honest). The registry field already supports per-route images; distinct cards for the flagship surfaces (/join, /tokenomics, /activity, /whitepaper) would differentiate shares. Pure opportunity, wiring already exists. Verifier: CONFIRMED — distinct set of ogImage values across all 40 entries is ['/opengraph.jpg', undefined]; getOgImageUrl (:797-799) falls back to DEFAULT_OG_IMAGE. |
| P3 | Structured data stops at Organization + FAQPage — WebSite and BreadcrumbList are missing, and the protocol's verifiable facts carry no schema | artifacts/studio/scripts/prerender-routes.ts:87-92 | setRouteJsonLd emits Organization JSON-LD on "/" and FAQPage on /faq only (both correctly single-sourced and baked server-side — good). Missing standard blocks: (a) WebSite JSON-LD with name/url on the homepage — the signal Google uses for the SERP site-name; (b) BreadcrumbList — RouteContextBar already renders visible breadcrumbs from getRouteBreadcrumb (seo-route-registry.ts:955-970) so the data exists, but no matching JSON-LD is emitted. The larger opportunity — verifiable facts (contract addresses, chain, fixed supply) as schema — remains untouched; Organization deliberately excludes chain figures (seo-jsonld.ts:7-9), a defensible honesty stance, but static facts like canon contract addresses are stable and bakeable. [corrected] "grep: BreadcrumbList absent from the tree" was wrong — the string appears in RouteContextBar.tsx:4,45,65 and ui/breadcrumb.tsx:15,28,109, but those are shadcn React UI components, not schema.org markup; zero BreadcrumbList JSON-LD is emitted anywhere, so the conclusion stands. Verifier: CORRECTED as noted. |
| P3 | Dev base index.html description drifts from the registry "/" entry and speaks "fail-closed" jargon | artifacts/studio/index.html:7 | The base template description ("...read live from the blockchain — read-only, fail-closed, never invented", repeated at og:description :10 and twitter:description :18) differs from the registry "/" description (seo-route-registry.ts:93-94, the buyer-voiced "A members club that lives on-chain...") and uses "fail-closed" — the exact term the S7-e pass ruled is never spoken to a buyer. Prod is safe (prerender overwrites description on every shell including index.html, prerender-routes.ts:139), so this only serves in dev and to anything reading the raw template; the base should mirror the registry so there is one homepage description, not two. check-seo-registry.ts check 15 (:279-299) validates the template's origin/og:url but not title/description parity — a parity check would close this class. Verifier: CONFIRMED. |
| P3 | /api/* JSON endpoints carry no X-Robots-Tag and robots.txt does not disallow /api — public JSON can enter the index | artifacts/api-server/src/app.ts:17-23 | The API security header block sets CSP/XFO/nosniff/Referrer-Policy but no "X-Robots-Tag: noindex", and robots.txt (:7-21) disallows only the 15 operator routes — /api is crawlable. Same-origin GET endpoints like /api/backbone/feed or /api/protocol/reality return 200 JSON that Google can index as raw-JSON results, surfacing as ugly SERP entries competing with the real pages. One header line (X-Robots-Tag: noindex) on the API middleware is the clean fix; a robots Disallow: /api is the weaker alternative (blocks crawl, not indexing of already-known URLs). Verifier: CONFIRMED — grep for X-Robots-Tag across all of artifacts/: zero hits; API same-origin at /api per api-server/.replit-artifact/artifact.toml:10. |

Refuted at verification: 0 of 12 findings.

## Lens 10 — perf-security

**Coverage.** Swept from the current tree: full dist build weight — artifacts/studio/dist/public = 7.5 MB total, assets = 6.4 MB/181 files; raw+gzip measured for the 7 largest chunks (entry 1,588,286/482,203; index-oYUtB-Rh 595 KB; metamask-sdk 555 KB; core 470 KB; index.es 407 KB; CSS 211,481/31,335); entry-chunk composition probed by string search (wagmi/viem/walletconnect present); no .map files ship in dist. Images: brand/ (284 KB logo @28px, 195 KB AVAX pill), hero/ (throne png/webp pair), opengraph.jpg 126 KB. Chunking config: vite.config.ts (no manualChunks/target/sourcemap), App.tsx import graph (24 eager public pages, 2 lazy gates). Security headers: api-server app.ts (CSP default-src 'none', XFO DENY, nosniff, Referrer-Policy no-referrer, x-powered-by off — solid), CORS allowlist GET/HEAD/OPTIONS credential-free, trust-proxy deliberately unset with documented rationale, auth cookie flags exemplary, rejectCrossOrigin + sec-fetch-site defense, prerendered meta CSP + referrer meta in all 24 shells, both artifact.toml files, .replit, robots/sitemap. Throttle/DoS: publicReadThrottle covers the two per-request-RPC routes (joinQuote, sourceValidate); protocol/reality has 30s cache + SWR + request coalescing (realityService.ts:1788-1796); holder-index and verify-links static module-load payloads with fail-closed address gates; backbone feed/capital-standing in-memory snapshots, no RPC. Secrets scan: clean; 64-hex constants classified as library constants.

| Sev | Finding | Where | Detail + verifier note |
|---|---|---|---|
| P2 | Monolithic 1.59 MB entry bundle (482 KB gzip) loads on every public page — no route-level code splitting, wallet stack eager at root | artifacts/studio/src/App.tsx:8-41 | Measured from the current dist (built 2026-07-16 11:18): dist/public/assets/index-DSnsVSxD.js = 1,588,286 bytes raw / 482,203 bytes gzip, the ONLY `<script>` in every prerendered shell (dist/public/index.html:24) — the homepage and every SEO landing page pays ~513 KB gzip JS+CSS before first render. Two causes: (1) App.tsx:8-31 statically imports all 24 public pages — only OperatorConsole (:54) and WalletSessionBoot (:63) are lazy; (2) App.tsx:41 statically imports WalletWagmiProvider from @/wallet/RainbowKitRoot at the app root, pulling wagmi/viem/walletconnect core into the entry (grep confirms the strings inside the bundle). The root wallet provider is a pinned architectural decision (guard-access-state rule 15, comment App.tsx:38-40), but the 24 eagerly-imported page modules are not pinned by anything — React.lazy per public route would cut the entry substantially. vite.config.ts:68-71 has no manualChunks/target/chunk tuning. Total dist assets 6.4 MB/181 files. Real-user LCP/TTI impact needs prod measurement; the byte counts are repo facts today. Verifier: CONFIRMED byte-for-byte (gzip -c | wc -c = 482,203; only 3 lazy() sites in App.tsx; assets = 181 files / 6,195,468 bytes). |
| P2 | The known header-image weight class is still live: 284 KB logo rendered at 28×28 px + 195 KB Avalanche PNG in the ChainPill, on every public page | artifacts/studio/src/components/layout/PublicLayout.tsx:106,166 | dist/public/brand/syn-mark-gold.png = 284,591 bytes, rendered at h-7 w-7 (28 px) in the header wordmark (:166 via brand.ts:19); dist/public/brand/avalanche-avax-token.png = 195,169 bytes, rendered inside the small ChainPill (:106). Both ship as raw `<img>` with no `<picture>`/webp/resized variant — ~480 KB of image weight on EVERY public route, for two icons whose display size justifies <10 KB each. The repo already demonstrates the fix pattern: hero/throne has a webp variant used via `<picture>` (SeatThroneMark.tsx:4-6, throne.webp = 68 KB vs throne.png = 341 KB). Resize+webp (or inline SVG for the mark) is a pure build-side fix, no prod dependency. Verifier: CONFIRMED — ChainPill is h-5 w-5. |
| P2 | Public pages have NO verifiable clickjacking protection — frame-ancestors cannot ride the meta CSP and the serving layer's headers are not in the repo | artifacts/studio/scripts/prerender-routes.ts:116-118 | The prerender script itself documents it: "frame-ancestors is IGNORED in a meta CSP by spec — anti-framing for PAGES must be set at the Replit serving layer (handoff note)". The studio artifact.toml contains only rewrites — no header mechanism — so whether thesyndicate.money pages send X-Frame-Options/frame-ancestors today is unknowable from the repo. The API side is covered (X-Frame-Options DENY, app.ts:19, plus frame-ancestors 'none' in the API CSP :18), but a framable public site with a wallet-connect money path is a phishing/overlay vector. NEEDS PROD MEASUREMENT: curl -sI https://thesyndicate.money/ and check for X-Frame-Options or a header CSP with frame-ancestors; if absent, this escalates to a real gap to hand Replit. Verifier: CONFIRMED. |
| P3 | No HSTS anywhere (API headers or static layer) — documented deferral, still a today-gap | artifacts/studio/.replit-artifact/artifact.toml:37 | artifact.toml:36-37 says "HSTS/preload = Phase 6" and the API security-header block (api-server/src/app.ts:17-23) sets CSP/XFO/nosniff/Referrer-Policy but no Strict-Transport-Security. TLS is platform-terminated (Replit), auth cookies are secure:true (auth/router.ts:80), so exposure is first-visit downgrade only. Tracked deferral — flagged so the Phase 6 promise doesn't silently rot. Prod check when measured: whether Replit's edge already injects HSTS. Verifier: CONFIRMED — no Strict-Transport-Security string anywhere in src. |
| P3 | Auth-zone GET responses carry no Cache-Control: no-store — personal standing/purchase JSON is cacheable by default heuristics | artifacts/api-server/src/auth/router.ts:266-688 | GET /api/auth/session (:266), /member-self (:318), /member-standing (:379), /source-standing (:546), /member-purchases (:613) return per-member data gated by an HttpOnly cookie, but no route or middleware sets Cache-Control/Pragma (grep across src/auth/* returns nothing; the global header block app.ts:17-23 doesn't either). Without an explicit no-store, browsers may heuristically cache and a misconfigured intermediary could too. Cookie flags themselves are exemplary (HttpOnly, Secure, SameSite=Strict, path=/api — router.ts:78-83). Standard fix: res.setHeader('Cache-Control','no-store') in the auth zone middleware. Verifier: CONFIRMED — grep across the whole api-server src = zero hits. |
| P3 | Render-blocking third-party Google Fonts stylesheet on the critical path of every prerendered shell | artifacts/studio/index.html:21-23 | Every shell (source :21-23, baked into all dist shells) loads https://fonts.googleapis.com/css2 (3 families, 9 weight/style cuts) as a blocking `<link rel=stylesheet>`. Preconnects present (good) and display=swap set, but first render still waits on a third-party CSS round-trip, and the CSP had to open style-src/font-src to Google for it (prerender-routes.ts:120-121). Self-hosting the three families (e.g. fontsource) would remove the third-party critical-path dependency, tighten CSP, and drop the fonts.google.com privacy surface. Measurable in prod as LCP delta only. Verifier: CONFIRMED — 25/25 dist shells carry it (24 routes + 404.html); families: Instrument Serif ital 0;1, Work Sans 400-700, IBM Plex Mono 400-600. |
| P3 | Page CSP breadth: style-src 'unsafe-inline', img-src https:, connect-src https: wss: — deliberate but wide | artifacts/studio/scripts/prerender-routes.ts:119-122 | The baked meta CSP keeps script-src 'self' as the strict XSS boundary (correct; the comment block :99-115 argues the trade-off honestly: over-narrow connect-src could break wallet connect). Residual truth: any same-origin script compromise can exfiltrate to ANY https host (connect-src https:), and inline style injection is unrestricted. Grade: strong for a static SPA (A-), not maximal. When the wallet-provider endpoint set stabilizes, narrowing connect-src to an explicit allowlist and adding a Permissions-Policy header at the serving layer are the two upgrades; both need the Replit header layer, not the repo. Verifier: CONFIRMED — SECURITY_META at :120-123 (finding's range one line high, within the block); verified live in dist/public/index.html:26. |
| P3 | WalletConnect project ID committed in .replit for dev AND production | .replit:23-27 | [userenv.development] and [userenv.production] both hard-code VITE_WALLETCONNECT_PROJECT_ID = "227f2e3b4e4c1880a5385c9bc007b796". Not a secret in the credential sense — VITE_ vars are embedded in the client bundle by design and WalletConnect project IDs are public — so informational: rotating it requires a commit, and WalletConnect Cloud domain-allowlisting (dashboard-side) is the actual abuse control. Verified no real secrets in the tree: git grep for sk_live/PRIVATE_KEY/BEGIN PRIVATE/AKIA patterns = zero hits; the 64-hex constants found (protocolEventScan.ts:56 etc.) are the ERC-20 Transfer topic, empty-merkle roots, and pinned tx hashes — library constants, matching the prior classification. Verifier: CONFIRMED — exact lines .replit:25 and :28 (finding's range one line short of the production entry, substance identical). |
| P3 | throne.png 341 KB webp-fallback and 181-file asset sprawl (sub-1KB icon chunks) — minor weight polish | artifacts/studio/public/hero/throne.png | (1) hero/throne.png = 341,720 bytes serves only as the `<picture>` fallback for non-webp browsers (components/hero/SeatThroneMark.tsx:4-6; throne.webp = 68 KB does the real work) — the fallback could be recompressed to ~100 KB without visible loss; (2) dist/public/assets holds 181 files, dozens of 231-550 byte icon/connector chunks emitted by the wallet stack's dynamic imports — per-request overhead that HTTP/2 mostly absorbs; only worth touching if prod is ever measured serving HTTP/1.1. The ~20 RainbowKit locale chunks (54-110 KB each, ~1.3 MB total) are lazy per-locale and correctly NOT in the entry. Verifier: CONFIRMED — 31 chunks under 600 B; 19 locale chunks measured 54,617-110,283 B. |

Refuted at verification: 0 of 9 findings.

## Lens 11 — modular-organism

**Coverage.** Swept: studio route spine (App.tsx all 39 mounts + gates), every config registry — modules.ts, moduleRegistry.ts, navigation.ts, surfaceClassification.ts, memberDoors.ts, memberActions.ts, featureFlags.ts, truthStatus.ts, routeMemory.ts, deploymentRegistry.ts, docs-content.ts — plus seo-route-registry.ts, all 18 studio guard scripts (guard-route-nav-drift and guard-surface-coverage read in full; verified what each parses and what it does NOT), consumers of each registry field via exhaustive grep, the public renderers of registry data (SystemStatus surface map, Docs, PublicHome promoted strip, admin panels/AdminModulesConsole), and the api-server module reality: routes/index.ts flat composition, auth/operator zone routers, backbone read-models + feedProjection line shapes + protocolEventScan pins + protocolTargets, the vendored protocol-event-registry canon, api guard suite (17 guards), and the client feed-kind map in LiveActivityFeed. Architectural read — clean modules: the SEO registry + surfaceClassification + guard net is a real, working module-contract core; api-server is cleanly modular; committed-canon registers (chronicle, deployments) and memberActions/memberDoors are good door/data modules. Entangled/debt: modules.ts+moduleRegistry are the decaying optional layer; prose truth lives in THREE parallel per-surface description fields (modules.description, classification.summary, registry.description) of which slices reliably update only one; feed classes have no single registration point. A module contract here = one manifest per module pinning {id, route(s), zone+audience, nav placement, SEO entry, classification row, posture source, doors/actions, feed kinds introduced, governance row, guard hooks} with BIDIRECTIONAL guards — ~80% of the pieces already exist; missing are the reverse-direction guards, deletion of the dead fields, and adoption of the vendored event-classification-table pattern. Plug-in readiness: Referrer Kit mostly unblocked; Swap/Bridge and Seasons blocked mainly by the N-file feed-class ritual and the optional module layer.

| Sev | Finding | Where | Detail + verifier note |
|---|---|---|---|
| P1 | /docs public cards contradict today's live reality — "never a commission", /proof "isn't wired yet", /archive "reads not yet wired" | artifacts/studio/src/content/docs-content.ts:129, 152-155, 178-181 | The /docs hub (PUBLIC, INDEX, in sitemap) renders curated card copy, and three cards state the dead era as present truth. (a) :127-129 /referral card: "attribution of a growth contribution, never a commission" — the referral program is ACTIVE and pays a bounded COMMISSION per eligible completed introduction inside the buyer's own transaction (modules.ts:388-393, S7 truth sweep). A public page denying a real financial mechanism is a truth bug with legal texture. (b) :152-155 /proof card: "What public, auditable proof WILL mean … an honest account of what isn't wired yet" — the heartbeat is LIVE and /proof reads live. (c) :178-181 /archive card: "Concept memory today; reads not yet wired" — archive reads live (17 minted; modules.ts:420-424). Modular root cause: docs-content.ts is a curated shadow registry that only derives the status PILL from the SEO registry (Docs.tsx:21-27) while the card PROSE is hand-frozen and covered by no guard — no docs-rides-the-slice equivalent exists, so every era transition since slice 2.4 left this file behind. Verifier: CONFIRMED — rendered publicly at Docs.tsx:97 ({card.purpose}); no script under artifacts/studio references docs-content. |
| P1 | Public /status surface map serves "arrives with the event backbone" summaries for surfaces that are live today | artifacts/studio/src/config/surfaceClassification.ts:228-250 | SystemStatus.tsx:283-329 renders the full surfaceClassification list on public /status, printing each entry's summary (:310). Three summaries still speak the pre-backbone era: /activity:232 "Activity teaser — the public heartbeat; ARRIVES WITH the event backbone", /fire-ledger:250 "per-event ledger ARRIVES WITH the backbone", /chronicle:241 "Chronicle teaser". The backbone is LIVE and all three surfaces serve complete history (INDEX/live per the SEO registry and the S7 record). The inverse LIVE-PRODUCTION-RULE bug: a live thing publicly described as pending, on the page whose whole job is the honest live-vs-pending ledger. guard-era-drift did not catch this vocabulary class. Modular root cause: classification summaries are a second hand-written description field parallel to modules.ts descriptions and seo-route-registry descriptions — three prose fields per surface, only some updated per slice. Verifier: CONFIRMED — registry :447-497 says "the complete indexed history", "ACT-1 LIVE". |
| P2 | Module layer under-coverage: six live public surfaces have no modules.ts entry and no moduleRegistry governance row; the drift guard only checks the module→registry direction | artifacts/studio/src/config/modules.ts:60-429 | modules.ts declares "One module list, two audiences" (:56) but holds 23 modules while the app mounts 29 non-admin surfaces: /wallet, /toolkit, /liquidity, /activity, /chronicle, /fire-ledger have no module. Consequences: (1) the public /status surface map falls back to raw route paths as display labels (SystemStatus.tsx:308 `module?.label ?? s.routePath` — a visitor sees "/fire-ledger" where every older surface gets a human label); (2) the Admin "Modules" console and governance panels render moduleRegistry (AdminModulesConsole.tsx:62, admin/panels.tsx:364,579) — the six newest live modules are invisible to the operator's module management view, and none has a riskClass/forbiddenClaims/adminManaged governance row (moduleRegistry.ts:115-318 covers 12 entries); (3) getModuleByPath (modules.ts:441) silently returns undefined. The asymmetry is guard-shaped: guard-route-nav-drift.ts:66-75 asserts every MODULE path is a registered route but never the reverse — the module layer decays silently every time a slice adds a page the fast way (App.tsx + SEO registry + classification, all guard-forced) and skips the optional layers. The single biggest structural block to a plug-in module contract: the layer that would BE the manifest is the only optional one. Verifier: CONFIRMED — exactly 23 modules; 29 non-admin routes counted (24 public + 5 operator); moduleRegistry = 12 specs. |
| P2 | Dead module-contract fields carrying false values: featureFlags says membership/chain-reads/referral are OFF; SyndicateModule.live/enabled/phase/dependencies/flag and nav.header are consumed by nothing | artifacts/studio/src/config/featureFlags.ts:8-13 | featureFlags.ts declares realChainReads:false, membershipLive:false, sourceRegistryLive:false, recognitionLive:false — three FALSE-for-live (chain reads, membership, referral registry live in prod since 2026-07-11/13). Verified by grep: isEnabled and featureFlags have ZERO runtime consumers (only featureFlags.ts itself and a type import modules.ts:27); the flag field on modules is never read. Every module carries live:false (including proof/join/member — all live), and live, enabled, phase, dependencies have no consumer anywhere in src. NavPlacement.header is also dead: navigation.ts reads only nav.sidebar (:50) and nav.footer (:69); the real header is the separate headerSpec (:22-31), so modules home/learning nav.header:true never render and contracts/member header:false DO render. Stale doc comment modules.ts:56-59 still describes a "Home / Proof / Learn / Status" header that does not exist. Hazard: loaded ammunition — the first future consumer trusting the documented contract (isEnabled("membershipLive"), m.live, m.nav.header) renders live systems as off. Enforce with a guard or delete; today they are negative documentation. Verifier: CONFIRMED — all greps re-run (plus the unrelated featureFlagsSample in referralProgram.ts). |
| P2 | Public /status renders the OPERATOR_PREVIEW surface group — names, summaries and links for /studio, /admin, /os-map, /founder — on a public page | artifacts/studio/src/pages/SystemStatus.tsx:290-327 | The surface map iterates all three audiences including OPERATOR_PREVIEW (:290) and renders each operator surface's label, summary, and a live `<Link>` to its route (:314-319) on the public, indexed /status page. Combined with the public footer "Console" group linking /studio (navigation.ts:64 + modules.ts:88 nav.footer:true), the public site actively advertises the operator console's shape and routes, against founder Ruling ② 2026-07-16. In prod these routes render OperatorPreviewUnavailable, so wayfinding leakage, not a data leak — but the modular defect is structural: SurfaceAudience exists on every classification entry, yet no consumer-side rule (and no guard) says a PUBLIC-layout page must filter to public audiences before rendering registry rows. The registry-driven pattern is right; the missing audience filter is the module-contract hole. Verifier: CONFIRMED — operator rows at surfaceClassification.ts:287-351 + :361-435. |
| P2 | Two event-kind canons: the origin's single classification table is vendored as dead code while the live backbone's feed classes are hand-duplicated across ~6 files — the N-file ritual that will tax Swap/Bridge and Seasons | artifacts/api-server/src/canon/the-syndicate/proof/protocol-event-registry.ts:5-8, 35-49 | The vendored registry (excluded from the TS program, "not yet imported by active app code" per its own header) is precisely the modular pattern the organism needs: ONE table declaring each event kind's category, metric effects, chronicle-candidacy, and surfacing — and it already names the kinds the roadmap modules will need (swap-buy, swap-sell, vault-in, vault-out, rank-reached :35-49). The LIVE backbone instead spreads each feed class across parallel hand-kept sites: event pins in protocolEventScan.ts (:54-89), targets in data/protocolTargets.ts, per-kind read-models (activityHeartbeat/protocolEvent/milestone/era/capitalAxis Readmodels), per-kind line shapes in feedProjection.ts (:65-127), the client kind map SERVED_KIND_TO_WINDOW_KIND (studio LiveActivityFeed.tsx:47-67), plus KIND_LABEL/sentences/filters. Typed Records make omissions fail tsc (good) and protocolTargets has a reconcile guard — but there is no single registration point, so the Heartbeat Completeness Invariant is enforced only by discipline across two packages. A Swap/Bridge module's two kinds would today touch ≥6 files in a fixed ritual nobody has written down as a checklist. Either adopt the vendored table's pattern as the live single classification point, or delete/annotate the vendored file so it stops advertising a canon the runtime does not use. Verifier: CONFIRMED — tsconfig.json:11 excludes src/canon; one nuance: sale-event-semantics-reconcile.guard.ts:27 does import the vendored table, so it is reconciled-against but still unused by runtime — the no-single-registration-point claim stands. |
| P3 | memberDoors.ts and docs-content.ts route targets sit outside the drift-guard net — a door or docs card can silently point at a retired route | artifacts/studio/src/config/memberDoors.ts:34-70 | guard-route-nav-drift.ts textually parses only App.tsx, seo-route-registry, modules.ts, navigation.ts and AdminShell (:16-22, :101). No script under artifacts/studio/scripts references memberDoors.ts or content/docs-content.ts. Both files are the exact class the guard exists for — hand-curated href/routePath lists ("every live door targets an EXISTING surface", memberDoors.ts:11; "REAL ROUTES ONLY", docs-content.ts:9) — yet the invariant each file states about itself is enforced by nothing. All targets are valid today; the debt is that the member's primary navigation surface and the public knowledge hub are the two remaining unguarded link registries in an otherwise lockstep-guarded nav system. A ~10-line extension of guard-route-nav-drift closes it. Verifier: CONFIRMED — one consumer detail incomplete: MemberAccess.tsx:24 also imports memberDoors (MEMBER_HOME_RESERVED_SLOTS) alongside MemberShell.tsx:14 and Docs.tsx:10 — does not change the finding. |
| P3 | truthStatus.ts declares sourceAttribution "RETIRED FROM RENDER" while modules.ts still wires it and /status still renders it | artifacts/studio/src/config/truthStatus.ts:43-60 | The S7 sweep comment (:43-55) says four surfaceStatus keys are "RETIRED FROM RENDER", kept "for type totality only" — but modules.ts:219 still assigns truthStatus: surfaceStatus.sourceAttribution (SOURCE_INDEXER_NOT_WIRED) to the os-source module, and public /status renders module.truthStatus badges (SystemStatus.tsx:313), so the key is NOT retired from render. The rendered "Not live yet" happens to be honest for the operator source console surface itself, but the reason code (source indexer not wired) is false today (R5 live), and the file's own retirement claim is contradicted by a live consumer two files away. Either repoint the os-source module to an honest code (e.g. DESIGN_PREVIEW, matching /admin and /os-map) or amend the retirement comment. Verifier: CONFIRMED — surfaceClassification.ts:328-330 gives /os-source moduleId "source". |
| P3 | Module-spine doc rot: registry header claims 14 routes (39 real), App.tsx and registry notes still call live surfaces "designed teasers", filenames say Teaser on INDEX pages | artifacts/studio/src/lib/seo-route-registry.ts:82 | Contract-hygiene debt on the files a new module author reads first: seo-route-registry.ts:82 "14 explicit routes + the catch-all" — there are 39 entries; App.tsx:150 "§11 slot 2c designed teasers — honest PENDING/noindex promises" sits above /activity //chronicle //fire-ledger which are INDEX and live; the page files remain ActivityTeaser.tsx/ChronicleTeaser.tsx/FireLedgerTeaser.tsx. None of this renders publicly (the registry ENTRIES are correct), but the module contract's inline documentation is where the ritual gets copied from — stale comments in the single-source-of-truth files teach the next slice the dead era. (Registry notes :443-445 and the "*" soft-404 note :734-735 carry the same class.) Verifier: CONFIRMED — 40 path entries counted; imports at App.tsx:25,29,30. |

Refuted at verification: 0 of 9 findings.

## Lens 12 — archive-nft

**Coverage.** Swept every studio src file matching archive/artifact/mint (40+ files read or grepped) — Archive.tsx in full, Whitepaper §8, docs-content, faq-content (archive section verified honest), navigation/modules/moduleRegistry/memberDoors/routeMemory/surfaceClassification/truthStatus/sharedCopy/contractMemory/deploymentRegistry/syndicateFacts, seo-route-registry /archive+/wallet entries, hero (useHeroReality/HeroLedger/SeatFlowDiagram/ProtocolOverviewPanel), LiveActivityFeed + backboneFeedClient archive lanes, wallet ownReads/MemberWalletPanel/chainReads D5 holdings, admin panels/AdminHome, ProtocolMap/ContractMemory/ProofDashboard/Recognition/SystemStatus/ProofStudio. API-server: vendored canon (archive-nft-abi.ts full, archive-id-registry.ts full, syndicate-config, PROVENANCE), archiveDecoders.ts full, realityService buildArchiveGroup, protocolTargets, backbone protocolEventScan/protocolEventReadmodel/feedProjection archive paths. Origin quarries (read-only): TheSyndicate — ARCHIVE_ENGINE_V1.md, ARCHIVE1155_CANONICAL_ARCHITECTURE.md (full), NFT_ARCHIVE_SOLIDITY_SPEC_V1.md, archive-id-registry.ts, archive-preview-catalog.ts (9-id names/categories/statuses), full routes list; Supa-Exchange — shared/schema.ts NFT tables, rewards marketplace 'nft' reward type, Marketplace.tsx. Chronicle register content verified in-repo, not against the served page.

| Sev | Finding | Where | Detail + verifier note |
|---|---|---|---|
| P1 | /archive publicly declares the Chronicle "a design concept — no chronicle entries are stored or published" while /chronicle is LIVE with promoted entries | artifacts/studio/src/pages/Archive.tsx:44-49 | The Chronicle card on public /archive carries LifecycleBadge DESIGN_CONCEPT and "A design concept today — no chronicle entries are stored or published in this foundation." Today's truth: config/chronicleRegister.ts:40+ holds a committed register with real promoted entries ("The duplicate seat", "The first real-money seat", "The first referral source", "The ladder decision") served live at /chronicle (INDEX per seo-route-registry). Checked against the register config itself, never another page. A public lie understating the protocol (anti-scarcity/pride law also violated: the OS hides a live organ). Verifier: CONFIRMED — rendered live by ChronicleTeaser.tsx:43-83 at /chronicle (App.tsx:154-155; registry :465-479). |
| P1 | /archive contradicts itself: lead says "counts and prices are read live", the "What unlocks it" section says wiring is pending — and the page renders ZERO live figures either way | artifacts/studio/src/pages/Archive.tsx:17, 65-69 | :17 (lead): "Artifacts mint on-chain today — counts and prices are read live from the contract." :65-69: "Archive reads against the deployed Archive 1155 contract — wiring them is its own slice. Nothing renders here before it can be read and verified." Both on the same public page. Today's truth: archive reads ARE wired — api-server src/lib/protocol/realityService.ts:434-660 (buildArchiveGroup) serves archive.paused + per-id configured/minted/price live, and /contracts already renders the archive group (ContractMemory.tsx:79). Yet /archive itself renders only static contractMemory cards — no minted count, no price, no artifact grid anywhere. LIVE-PRODUCTION RULE violation: readable figures (minted counts and USDC prices, live in GET /api/protocol/reality) are displayable today and shown nowhere on the archive surface, while the homepage hero (syndicateFacts.ts:481, HeroLedger nftRevenueUsdc) leads visitors to /archive via an "NFT Artifacts" revenue door that opens on a page with no NFTs, no numbers, no mint. Verifier: CONFIRMED — the page imports only static contractMemory (Archive.tsx:7,11); nftRevenueUsdc live in HeroLedger.tsx:80-98. |
| P1 | Whitepaper §8 stamps the Archive "PAUSED" and "read-only today" while public mint is OPEN on-chain and the same paragraph counts live mints | artifacts/studio/src/pages/Whitepaper.tsx:200-204 | Eyebrow n=8 status="PAUSED" renders a caution StatusPill literally reading PAUSED, and the prose says "The Archive is read-only today." Today's truth: ids 1 (The First Signal, 0.50 USDC) and 3 (Patron Seal, 5.00 USDC) are LIVE_PUBLIC_MINT per the vendored canon registry (api-server src/canon/the-syndicate/archive/archive-id-registry.ts:61-68, 87-94), contractMemory.ts:130 says "some artifact classes are open", the heartbeat announces archive-mints publicly, and the paragraph itself renders r.nftMintedTotal — a live, non-zero minted count. "PAUSED" also collides with the contract's real Pausable.paused() flag, which the reality spine reads live (realityService.ts:455-506) — the whitepaper asserts as static text what the API reads as live truth. If the intended meaning was "the SITE offers no mint surface", the words must say that; today they misstate the contract. Verifier: CONFIRMED — Eyebrow renders the literal pill (Whitepaper.tsx:56-64); on-chain state itself not checkable from this audit, but the static claim contradicts the repo's own canon and live-read spine. |
| P1 | /docs card for the archive says "reads not yet wired" — stale public claim | artifacts/studio/src/content/docs-content.ts:168-180 | The public /docs hub describes /archive as "The archive and chronicle — protocol memory and milestones. Concept memory today; reads not yet wired." Both halves are false today: archive reads are wired (buildArchiveGroup; modules.ts:424 "artifacts minted on-chain, counts and prices read live") and the chronicle is live at /chronicle with promoted entries. The SEO-rides-the-slice pattern in reverse: the docs surface stood still while the archive reality moved. Verifier: CONFIRMED — exact purpose string at :178-180. |
| P2 | Header "More" item labeled "Chronicle" navigates to /archive while a live /chronicle route exists with no header/footer entry | artifacts/studio/src/config/navigation.ts:26, 62 | navItems has { id: "archive", label: "Chronicle", zone: "more" } — the word "Chronicle" in primary navigation opens /archive, the one page that claims the Chronicle is a dead design concept (Archive.tsx:44-49), while the real live /chronicle (committed register, INDEX, sitemap) is reachable only via member doors and in-page links. Footer "Learn" group (:62) likewise lists "archive" but has no chronicle module at all — modules.ts contains NO entry for /chronicle (also none for /activity, /fire-ledger, /wallet, /toolkit, /liquidity). Wayfinding actively routes readers away from the live surface toward the stale one. Verifier: CONFIRMED — /chronicle reachable only via in-page doors (e.g. hero flow source syndicateFacts.ts:480). |
| P2 | /archive PENDING/noindex posture's stated unlock condition is already met — posture decision due, registry note stale | artifacts/studio/src/lib/seo-route-registry.ts:429-441 | The /archive entry stays PENDING with robots noindex,follow and notes "ARCHIVE_READS_NOT_WIRED. NOINDEX until archive reads are wired." The named condition is satisfied: the reality spine serves archive paused/configured/minted/price live and truthStatus.ts:60 itself marks ARCHIVE_READS_NOT_WIRED as "RETIRED FROM RENDER — reads live". The description in the same entry (:435) already says "Artifacts are live on Avalanche today, mint counts and prices read from the chain" — an INDEX-voiced description under a NOINDEX posture. Whether /archive graduates to INDEX is a founder call (the page needs the P1 fixes first), but the registry cannot keep citing a condition that no longer exists. Verifier: CONFIRMED — PENDING maps to "noindex, follow" (:761,776); truthStatus.ts is in src/config. |
| P2 | Admin content panel claims "activity read-model script-only and unwired; archive reads are not wired" — both false today | artifacts/studio/src/pages/admin/panels.tsx:622-628 | The admin Chronicle/Archive posture panel's footnote says "The activity read-model remains script-only and unwired; archive reads are not wired. This panel becomes a real queue only after a founder-approved chronicle slice." Today: the event backbone + heartbeat are LIVE (activity read-model served at /api/backbone/feed), archive reads are wired in the reality spine, and the chronicle slice has happened (live register + founder promotion flow, ChroniclePrepare.tsx exists). The operator console lying to the operator is a truth bug even behind the gate. Verifier: CONFIRMED — protocolEventReadmodel.ts serves archive-mint items (:354-375). |
| P2 | NAMING DECISION FOR THE FOUNDER — Archive vs NFT: the deployed contract is a 255-slot commerce-capable NFT engine; today's OS surfaces only its memory-reading face. Three naming options. | artifacts/api-server/src/canon/the-syndicate/contracts/abi/archive-nft-abi.ts:22-130 | Complete capability map of deployed SyndicateArchive1155 (0xB2AE1eb7aAf7577182e616DA497E0BC822E7D54d, chain 43114; vendored ABI + origin specs NFT_ARCHIVE_SOLIDITY_SPEC_V1.md + ARCHIVE1155_CANONICAL_ARCHITECTURE.md): (a) PUBLIC PAID MINT — mint(id,qty) pulling USDC; ids 1 (First Signal 0.50) and 3 (Patron Seal 5.00) OPEN today = a live revenue stream; (b) OWNER adminMint — ceremonial/grant minting for ids 4-8 (origin names: Heart Signal, Genesis Sealed, First Liquidity Event, First Routing Signal, Legacy Era I) — could equally serve recognition grants/airdrops; (c) 255 token-id slots with per-id price/walletLimit/maxSupply/ownerOnly/freeze = a general digital-product shelf; (d) on-chain SVG renderer (templateId/iconId/visualFamily/chapterLabel) = certificates and badges without any external host; (e) ERC-2981 royalties (setDefaultRoyalty) = secondary-market economics, pure NFT-collection capability; (f) full ERC-1155 transferability + marketplace identity name()='Syndicate Archive', symbol()='SYN-ARC' — IMMUTABLE on-chain; (g) dedicated treasury + withdrawUSDC, deliberately separate from the 70/20/10 membership routing; (h) pause/unpause as founder-signed public acts (already a heartbeat lane); (i) ID 2 reserved pointer to a future SeatRecord721 identity contract; (j) ID 9 Protocol Chronicle slot (not configured). What today's OS rebuilt: reads only — reality-spine archive group (ids 1+3), heartbeat archive-mint/pause lanes, /wallet own-holdings (D5), hero NFT-revenue aggregate. Unharvested from the quarries: the origin's entire mint engine (MintFirstSignal/MintPatronSeal, mint-phase.ts CTA state machine, error classification, success/ownership panel), the artifact gallery (/archive 9-card grid, /nft, /nfts, /my-syndicate "My Archive"), and Supa-Exchange's non-archival NFT patterns (rewards marketplace with 'nft' reward type, nftCollections/userNfts portfolio tables, NFT bridging — Supa-Exchange/shared/schema.ts:314-380,536-580). THE THREE OPTIONS: [A] PURE ARCHIVE (status quo) — honest to doctrine (Whitepaper §8 "SYN is the seat, NFTs are the memory", banned-vocabulary rule) but hides the live commerce lane and undersells the 255-slot shelf. [B] PURE NFT ("Syndicate NFTs", collection framing, royalties, marketplace presence) — legible to crypto natives but collides head-on with locked doctrine (origin canon §8 bans speculative-collection framing; the Patron Seal was explicitly REFRAMED away from wealth tiers; forbidden-vocabulary law applies); highest legal/positioning risk. [C] HYBRID — NFT WITH ARCHIVE FUNCTION: keep "The Archive" as the canopy name and doctrine, and inside it name the lanes the contract already has — (1) the open mint room (First Signal + Patron Seal, the commerce lane, harvest the origin mint engine), (2) sealed protocol memory (owner-only 4-8, ceremonial, feed-announced), (3) the reserved identity pointer (ID 2 → SeatRecord721, future), (4) the chronicle slot (ID 9, roadmap). Lens read: Option C — the on-chain identity is immutable (marketplaces will always display it as an archive), doctrine is locked against B, and A leaves a live revenue capability and the 255-slot shelf invisible (the named gold-mine failure); C is also the origin's own trajectory (ARCHIVE1155_CANONICAL_ARCHITECTURE §7 sketches source-aware V2 / ProductSaleRouter as SEPARATE future architecture). The founder decides; whichever option wins, the P1 truth fixes above are prerequisites, and any mint surface is its own wireframed slice under the VISUAL CHANGE LAW. Verifier: CONFIRMED — every checkable anchor re-proven (ABI :65-74 mint, :85-91 paused, :105-111 treasury, :113-129 getArtifactCore, :238-242 ONCHAIN_SVG; registry ids 1/3 LIVE_PUBLIC_MINT, id 2 :71-82, id 9 :162-165; origin spec ERC-2981 :15,383, MAX_TOKEN_ID=255 :94, adminMint :310, name/symbol :156; architecture doc :116-117; Supa-Exchange ranges hold). The option choice itself is a founder decision, correctly framed as such. |
| P3 | Reality spine tracks only artifact ids 1 and 3 of the 8 configured on-chain ids | artifacts/api-server/src/data/protocolTargets.ts:192-195 | ARCHIVE_ARTIFACTS = ids 1 and 3 only. The vendored canon registry records ids 2 and 4-8 as configuredOnChain:true (owner-only / reserved). The heartbeat scan is complete (protocolEventScan.ts decodes TransferSingle mints for ANY id) and the feed falls back to "Artifact #N" for unmapped ids (protocolEventReadmodel.ts:365 — label map holds only 1 and 3), so an adminMint of id 4-8 would appear in /activity as an unnamed artifact and NEVER appear in the /contracts / /map archive group. Per the heartbeat-completeness spirit, the reality-spine inventory and the label map should carry all configured ids in the slice that first surfaces them. Verifier: CONFIRMED — label map at protocolEventReadmodel.ts:212-215; scan id-agnostic at protocolEventScan.ts:74-77 (src/backbone/). |
| P3 | Internal maps still say "no archive reads are wired" / chronicle "not live data" | artifacts/studio/src/config/protocolOsMap.ts:184-189 | protocolOsMap.ts:189 (os-map, operator surface): "Archive1155 ABI and ID registry are vendored; no archive reads are wired" — stale, reads are wired. Companions: routeMemory.ts:117-121 maps the legacy Chronicle surface to /archive with "narrative memory, not live data" (the chronicle now lives at /chronicle with live promoted entries); protocolRealityEvidence.ts:45-48,76 keeps the payload archive group as BLOCKED_FUTURE with a deferral comment. All internal-only; fix opportunistically in the same slice that repairs the /archive P1s. Verifier: CONFIRMED — protocolRealityEvidence actual path: src/operator/. |

Refuted at verification: 0 of 10 findings.

## Lens 13 — banned-vocabulary

**Coverage.** Swept every studio public-string source — all 28 src/pages + member surfaces (MemberAccess, MemberWallet, MemberShell/memberDoors, CapitalAxisCard, MemberSettings, MemberHeaderAffordance); all string configs (syndicateFacts, sharedCopy, referralProgram, sourceAttributionTerminology, liquidityPool, learningModules, chronicleRegister incl. full Entry-2/3 bodies, contractMemory, memberDoors, modules); content (faq-content, docs-content); SEO layer (all 39 registry titles+descriptions, seo-faq-jsonld, index.html base meta :7-19, prerender templates — both clean); feed sentences (studio backboneFeedClient.ts :580-687 — "routed never raised" honored; api feedProjection/milestoneReadmodel labels); checkout edge translation (checkoutVocabulary.ts — zero banned tokens); public/ static text (referral-program-terms-v1.txt read in full); api-server served strings (realityService notes, canon syndicate-config disclaimers, protocol-event-registry ids). Banned list swept with word-boundary regexes: invest*, raised/fundraise, donation/charity/contribution, profit, return (financial sense), yield, APY/APR/ROI, dividend, passive income, guaranteed*, 100x, moon (only the lucide icon — canon-documented exemption), pump, package/tier-package, buy-NFT-to-join shape (none), governance weight, equity, trust-us shape, plus promise-shapes (upside, appreciation, gain, wealth, bonus, discount, earn/reward/payout/downline/airdrop/farming/jackpot/FDV/market-cap). guard-forbidden-copy.ts re-run live: PASS over 237 files. Judgment calibrated to today's canon (CANON_PROTOCOL_LANGUAGE.md §5 read in full). RESULT: zero P0/P1 — no positive-sense financial-upside claim on any public surface; findings are metadata-truncation, guard-coverage, interrogative-shape, or canon-flagged-divergence cases.

| Sev | Finding | Where | Detail + verifier note |
|---|---|---|---|
| P2 | Served /api/protocol/reality note contains "passive income" + "yield" (negated) while the dormant payload-discipline net has NO negation-awareness — planned re-enable will fail-close the core public API on its own honest disclaimer | artifacts/api-server/src/lib/protocol/realityService.ts:1683 | The referral-cumulative note served in the public protocol-reality payload ends "…transparent payment for eligible completed member introductions, never passive income or yield." Honest negation — compliant TODAY (enforcement lifted, DISCIPLINE_ENFORCED=false at payloadDiscipline.ts:32, founder decision 2026-07-11). But payloadDiscipline.ts has zero negation-awareness: FORBIDDEN_SUBSTRINGS includes "passive income" (:47) and FORBIDDEN_PATTERNS includes /\byield/i (:66). The file's own header says the net "is expected back at the Phase-5 crypto-lawyer pass" — the day that one-line flip happens, assertProtocolRealityDiscipline throws on this exact sentence and /api/protocol/reality 500s fail-closed, taking down the homepage hero, /map, ChainPill/LiveChip. No build guard covers this collision (studio guard-forbidden-copy scans studio src only). Fix shape: port the studio guard's isNegated window into payloadDiscipline, or rewrite the note without the banned tokens, BEFORE the flip. Verifier: CONFIRMED — the net is wired on the live route (routes/protocolReality.ts:19 calls it on the envelope before return); assertProtocolRealityDiscipline (:68-89) is a raw lowercase substring/regex scan; the studio isNegated window (guard-forbidden-copy.ts:97-101) has no counterpart. |
| P3 | FAQ question "Are liquidity providers guaranteed rewards?" — banned-shape phrase in positive interrogative form, baked into the /faq shell as FAQPage JSON-LD | artifacts/studio/src/content/faq-content.ts:169 | The question text carries "guaranteed rewards" with no negation inside the sentence (the "No." lives in the answer, :170). Via seo-faq-jsonld.ts the Q&A is prerendered into the /faq shell (prerender-routes.ts:90); in Google FAQ rich results the QUESTION renders standalone with the answer collapsed — a searcher can see "guaranteed rewards" next to the brand with the negating answer hidden. Also a guard blind spot: guard-forbidden-copy.ts PHRASES bans "guaranteed return"/"guaranteed benefit" (:31-32) but not "guaranteed rewards", and bare "reward" is allowed — the same shape in a future POSITIVE sentence would pass silently. Suggest rewording ("Are liquidity providers promised rewards?" — "promised" already the house verb per liquidityPool.ts:58) and adding "guaranteed reward" to PHRASES. Verifier: CONFIRMED — seo-faq-jsonld.ts:25-31 maps e.q verbatim into Question.name. |
| P3 | /referral + /source-attribution meta/OG description: the "Membership is not an investment." disclaimer sits ~197 chars deep — SERP truncation (~160 chars) always drops it, leaving a commission-payment snippet with no counterweight | artifacts/studio/src/lib/seo-route-registry.ts:381 | Both entries (:381 and the /source-attribution twin :401) use the same ~230-char description: "How The Syndicate's referral program works: an eligible completed introduction pays a bounded commission to the introducer's wallet inside the buyer's own transaction — on-chain, shown by receipt. Membership is not an investment." The banned word "investment" appears only inside the trailing negation, so truncation can never surface a POSITIVE claim — but it silently deletes the whole disclaimer on every Google result and length-limited social card, leaving "pays a bounded commission…" as the entire public promise. Move the disclaimer into the first ~150 chars or shorten the mechanics clause. Same string doubles as og:description/twitter:description via SeoHeadManager and the prerendered shells. Verifier: CONFIRMED — measured exactly: 229 chars, disclaimer starts at char index 197; the finding is actually UNDERSTATED: the registry's own comment directly above (:376-379) states the SEO-metadata law that banned financial words never appear in a description "NOT EVEN NEGATED" because truncation can strip negation — this description violates its own adjacent law. |
| P3 | Member-dashboard Quests door note "Learn & Earn earns XP" — double positive-sense "earn" on a member-facing surface, nearest neighbor of the banned "earn rewards" class | artifacts/studio/src/config/memberDoors.ts:76 | Rendered via MemberShell's door groups on the signed-in member dashboard as a FUTURE-lifecycle note: "Daily and weekly recognition quests — Learn & Earn earns XP." XP is non-financial recognition (gamification doctrine), so not a financial promise, and the guard passes it (bare "earn" deliberately allowed; only "earn rewards" is banned, guard-forbidden-copy.ts:29). But "Earn" as a module NAME plus "earns" as the verb is the banned shape minus one word — MEMBER_HOME_TOTAL_AUDIT_2026-07-16.md E56 already singled this "earn" out as the sole non-disclaimer hit. When the Quests slot becomes real copy, rename the hook ("Learn & Level", "quests grant XP") or explicitly settle "Learn & Earn (XP)" as canon so it stops resurfacing in audits. Verifier: CONFIRMED — audit doc E56 at :299, also C20 at :214. |
| P3 | Doctrine-listed word "contribution/contributed" live in positive sense on public surfaces (home hero KPI unit, whitepaper, FAQ, Recognition) — canon-accepted divergence, recorded as the watch item it is | artifacts/studio/src/config/syndicateFacts.ts:549 | Positive-sense sites: syndicateFacts.ts:549 hero KPI unit "contributed" (NFT Artifacts card on "/"), :426 "Every net contribution routes 70/20/10", :52/:79 "routed contribution"; Whitepaper.tsx:214 "contribution leaderboard"; faq-content.ts:102/110/192 "contribution depth"; Recognition.tsx:24/34/44 "growth contribution". NOT a violation of current canon — CANON_PROTOCOL_LANGUAGE.md §5 flagged-divergences (:154-157) records "contribution" as doctrine-listed but DELIBERATELY not guard-banned; buyer-facing checkout writes "net" via the edge translation (checkoutVocabulary.ts contains zero banned tokens). Flagged per the lens list with the canon verdict attached; the canon's own condition ("revisit when those surfaces migrate") means each NEW public money surface must keep choosing "net"/"routed" — the hero "contributed" unit label is the one site where the word faces a cold visitor next to a USDC figure. Verifier: CONFIRMED — protocolContribution survives only as the legacy field name renamed to netProtocolRaw (:43). |
| P3 | On-chain-committed terms document says "The rate may be raised over time" — doctrine-banned token "raised" in positive sense, though in the rate-increase sense, not the fundraise sense; unfixable without terms v2 | artifacts/studio/public/referral-program-terms-v1.txt:52 | The routed-never-raised vocabulary law (CANON_PROTOCOL_LANGUAGE.md:291, backbone.guard.ts:725) targets the FUNDRAISE sense of "raised". Here the sense is a commission-rate increase under the Connector ladder — no financial-upside framing, and the doc's §4 negation block (:61-65) is exemplary. Flagged because (a) the lens lists "Raised" flatly, (b) this file is served verbatim at thesyndicate.money/referral-program-terms-v1.txt and is OUTSIDE every vocabulary guard (guard-forbidden-copy walks src/**/*.ts(x) only), and (c) its keccak256 is the on-chain metadataHash fingerprint — the wording is immutable for v1; any change is a v9-section new-version event. Verdict: sense-safe, no action for v1; when terms v2 is drafted, prefer "increased". Note :97 "payoutWallet field" is the canon-allowed contract-field context. Verifier: CONFIRMED — terms :52-54; negation block :59-65; backbone.guard.ts:722-725 is milestone-label-only. |
| P3 | Vocabulary-guard coverage gap: public/ static text and index.html metadata are outside every banned-vocabulary guard; api-server served strings have only the DISABLED discipline net + the milestone-label check | artifacts/studio/scripts/guard-forbidden-copy.ts:108 | guard-forbidden-copy.ts walks only src/**/*.{ts,tsx} (:103-111). Not scanned by ANY vocabulary guard today: (1) artifacts/studio/public/*.txt — referral-program-terms-v1.txt is a public, hash-committed legal surface; (2) artifacts/studio/index.html base meta/OG/Twitter descriptions (clean today, verified :7-19); (3) api-server served copy — realityService notes, syndicate-config disclaimer, feedProjection sentences — covered only by the founder-lifted payloadDiscipline (DISCIPLINE_ENFORCED=false) and the narrow backbone.guard.ts:725 routed-never-raised milestone check. The studio feed SENTENCES (backboneFeedClient.ts:580-687) are in src and clean ("routed", "SEATS", "brought by"). A future copy edit to the terms file or an api note has no automated net. Suggested slice: extend the guard's walk to studio public/*.txt + index.html, and add a build-time vocabulary scan over api-server served-string modules with the same isNegated logic. [corrected] The cited api disclaimer is LEGAL_DISCLAIMER, not SALE_DISCLAIMER, at api-server/src/canon/the-syndicate/contracts/syndicate-config.ts:164-165 (line right, constant name wrong). Verifier: CORRECTED as noted — everything else stands. |
| P3 | INTERNAL /admin/content registry description carries doctrine-listed "packages" and is served as a meta description when the path is hit in prod | artifacts/studio/src/lib/seo-route-registry.ts:615 | "Internal admin section: homepage/content governance, packages and address-label reservations. Not a public destination." SeoHeadManager emits registry descriptions for INTERNAL routes too (deliberate, registry:51), so a prod visitor who types /admin/content gets this string in the served head while the body renders OperatorPreviewUnavailable. Mitigations in place: noindex,nofollow + robots Disallow + no prerendered shell + console dead-code-eliminated. "package" in admin copy is a canon-accepted divergence (CANON_PROTOCOL_LANGUAGE.md:154-157) — flagged only because this one string leaks ADMIN business vocabulary onto a publicly reachable response head (public-sees-admin-never adjacency). Cheapest fix: generic descriptions ("Internal operator section. Not a public destination.") for all INTERNAL entries. Verifier: CONFIRMED — resolveRouteHead (:853-866) returns entry.description unconditionally; SeoHeadManager.tsx:94/99/105 emits description + og: + twitter:. |

Refuted at verification: 0 of 8 findings.

## Lens 14 — functional-bugs

**Coverage.** Static behavior trace of every public interactive surface in artifacts/studio: join/checkout money path (JoinProtocol, JoinCheckout, JoinHistoricalGate + historicalMembers gate, checkoutVocabulary, rawUnits, chainReads incl. all revert-translation tables and web3 edge states), auth/session stack (walletSession, rainbowAuthAdapter, RainbowKitRoot, WalletSessionBoot, AccessGate/AccessStateProvider, api-server auth/router verify binding cross-checked), member dashboard (MemberAccess both compositions, MemberYourSeat, MemberKpiRow, CapitalAxisCard, MemberSettings, MemberPulse, ProtocolSnapshot, ChronicleLatest, MemberShell + memberDoors, MemberQuickActions, MemberHeaderAffordance incl. reSign/copy/share/disconnect), wallet door (/wallet MemberWalletPanel + revoke write), referral share surfaces (MemberReferralDashboard, ShareCard, ShareMenu, QrCodeBlock, SourceLinkBuilder, sourceIdentity/payingSourceId), feeds (LiveActivityFeed filters/re-read/merge/sort, activityFeed chunked scanner, backboneFeedClient validation + lexicon, MemberPulse), chrome (PublicLayout header/dropdown/mobile Sheet, ThemeToggle, SyndicateGuide, FaqAccordion, RouteScrollManager), Liquidity rail + useTokenomics + JoinEconomics derivation, Support, operator writes (operatorClient, ProposeSourceCreate, AdminReferralCrud, ChroniclePrepare, OperatorSignInAction, AdminOperatorSurfaces), gates (checkoutGate=true, walletSessionGate=true, operatorPreviewGate, featureFlags). REPO↔PROD PARITY: HEAD a947c82 is docs-only on top of a8a43c6 = the prod-sealed bundle index-DSnsVSxD.js (SHA-256-verified per seal record), deploy backlog empty — no committed-but-undeployed code.

| Sev | Finding | Where | Detail + verifier note |
|---|---|---|---|
| P1 | Post-sign-in redirect rips a buyer out of the /join checkout (and every non-/member flow) | artifacts/studio/src/wallet/rainbowAuthAdapter.ts:72 | verify() runs `if (window.location.pathname !== "/member") navigate("/member")` after EVERY successful SIWE verify (S7-e founder order, applied globally). RainbowKitAuthenticationProvider is mounted app-wide whenever the auth zone is live (RainbowKitRoot.tsx:75), so any wallet connect chains into the SIWE sign step. Failure scenario: a first-time buyer on /join enters an amount, reads the quote, clicks the checkout's own "Connect wallet" (JoinCheckout.tsx:257) — after connecting, RainbowKit prompts "Verify your account"; the natural act is to sign — and the adapter navigates them to /member. JoinProtocol unmounts, the quote state (submittedRaw, JoinProtocol.tsx:633) is lost, and the buyer must find /join again and re-enter the amount mid-purchase. The same redirect fires for the /wallet panel's connect (MemberWalletPanel.tsx:148) and for the admin console's OperatorSignInAction, whose stated design is "resolves the role in place" (OperatorSignInAction.tsx:12-13) — an operator signing in from /admin is dumped on the member dashboard. Checkout is LIVE in prod (checkoutGate.ts:15) and this bundle (a8a43c6) is the deployed one, so the live money path carries this trap. The redirect should be scoped to sign-ins initiated from the header/door, not to every verify. Verifier: CONFIRMED — every hop re-proven (OperatorSignInAction.tsx:69 opens the same modal). |
| P2 | The Settings and Referral-dashboard member doors are dead clicks — /member#hash navigation never scrolls | artifacts/studio/src/components/RouteScrollManager.tsx:53-93 | Two failure modes cover both directions. (a) Already on /member (where the MemberShell sidebar lives): wouter Link → history.pushState; pushState fires NO hashchange event, and wouter's location is pathname-only (node_modules/wouter/src/use-browser-location.js:42), so /member → /member#settings changes nothing RouteScrollManager can see — no scroll, no visual response at all. (b) From another page (e.g. the header dropdown's Settings item, MemberHeaderAffordance.tsx:448-453): the location effect runs one rAF after mount, but MemberAccess boots signedIn=false (MemberAccess.tsx:66) and renders the visitor DOOR first — the `<section id="settings">` (:213) doesn't exist yet, scrollToHash fails, and the manager falls back to resetToTop; when the session resolves the dashboard appears with no scroll. Affected controls: memberDoors.ts:56 ("Settings" door — added by founder order 2026-07-16 precisely to make Settings reachable), memberDoors.ts:39 ("Referral dashboard" door), MemberHeaderAffordance.tsx:449 (header dropdown Settings). Side symptom: the door "active" highlight compares door.href===location, so hash doors never highlight (MemberShell.tsx:116). Verifier: CONFIRMED — RouteScrollManager is the only hash handler in the app; wouter Link preventDefaults so the native anchor jump is also suppressed. |
| P2 | Mobile nav Sheet stays open covering the page after navigating via the member identity menu | artifacts/studio/src/components/layout/PublicLayout.tsx:354 | Inside SheetContent, the mobile SeatCtaSlot gets onNavigate={() => setMobileOpen(false)} (:358) and the plain nav links close on click (:327), but MemberHeaderSlot variant="mobile" receives no close callback — and MemberHeaderAffordance's menu items ("Open Member Home", "Settings", :441-453) navigate via wouter Links without closing anything. PublicLayout persists across routes, so mobileOpen stays true: a mobile member who opens the hamburger, opens their identity menu, and taps "Open Member Home" lands on /member with the nav Sheet still covering the viewport and must dismiss it manually. The SeatCtaSlot's own onNavigate prop proves the pattern was known — the member slot never got it. Verifier: CONFIRMED — MemberHeaderAffordance accepts only a `variant` prop (:102-106); setMobileOpen called only at :327/:358; no route-change effect closes it. |
| P2 | A mined-but-REVERTED buy is reported to the buyer as "The transaction confirmed" | artifacts/studio/src/wallet/JoinCheckout.tsx:410-423 | handleBuy awaits viem's publicClient.waitForTransactionReceipt(), which returns the receipt without throwing when receipt.status === "reverted" (unlike wagmi's action). The code never checks status: a reverted buy (possible when engine state changes between gas estimation and mining — e.g. EraInventoryInsufficient, SlippageExceeded on an era flip, EnforcedPause) produces zero MembershipPurchasedV3 logs, so the buyer is told "The transaction confirmed (0x…) but the receipt event could not be decoded here" — a wrong diagnosis for a purchase where the USDC did NOT move; the carefully-built KNOWN_REVERTS table (:117-134) is bypassed entirely because no error was thrown. Same unchecked pattern: handleApprove (:353, silent — resolve() re-reads so no lie renders) and MemberWalletPanel's revoke, which shows "Revoked — your own signed transaction" plus the tx hash even if the approve(0) reverted (MemberWalletPanel.tsx:131-133). Fix: check receipt.status before parsing logs and route "reverted" through an honest message. Verifier: CONFIRMED — viem's waitForTransactionReceipt has zero 'reverted' handling (node_modules grep); MemberWalletPanel renders the success line at :253. |
| P2 | Header SIWE flow signs the WALLET's chainId while the server hard-binds 43114 — wrong-network sign-in fails silently | artifacts/studio/src/wallet/rainbowAuthAdapter.ts:45-58 | Code facts: createMessage builds the SIWE message from the chainId RainbowKit passes in (:50) instead of the server challenge's chainId (which the adapter already has); the server rejects any chainId ≠ AUTH_CHAIN_ID=43114 with 401 binding_mismatch (api-server/src/auth/router.ts:193,199-200; authConfig.ts:18); the bespoke path does it correctly (walletSession.ts:104 uses challenge.chainId). [corrected] The failure MECHANISM is different from the finding's claim: wagmiConfig registers ONLY avalanche (wagmiConfig.ts:16), and RainbowKit's SignIn takes chainId from useAccount().chain?.id and early-returns when it's undefined — a wallet on Ethereum/Base has chain === undefined (not in the config), so no message is ever created or signed: the sign step is a silent dead click, and the 401 binding_mismatch path is unreachable through this flow. The wrong-network member still gets no "switch to Avalanche" guidance, but the proposed one-line challenge.chainId fix would NOT cure it — the early return happens before createMessage. Verifier: CORRECTED as noted (RainbowKit dist ~2389-2397). |
| P3 | Signed-in member with a disconnected wallet sees permanent dashes on Your SYN / Your USDC with no reconnect affordance | artifacts/studio/src/wallet/MemberKpiRow.tsx:97-108 | The SYN/USDC tiles read via useAccount().address (client chain reads). A member whose server session survives (cookie) but whose wallet is not connected in this browser gets "—" on both tiles indefinitely — the dashboard offers no "connect your wallet to read balances" affordance anywhere (MemberYourSeat also renders a null-address sigil, MemberYourSeat.tsx:111). Under the LIVE-PRODUCTION rule these figures are only unreadable because the page never invites the reconnection that would make them readable. The footprint and introductions tiles (server-side) still resolve, making the half-dash row look like a bug. Verifier: CONFIRMED — ownReads.ts:42/:69 bail to null when !wallet; KpiTile renders "—" for null (:48); signed-in composition (MemberAccess.tsx:118-227) contains no connect affordance. |
| P3 | ShareMenu popover has no outside-click/Escape close and an unhandled clipboard rejection | artifacts/studio/src/components/referral/ShareMenu.tsx:30-45 | The hand-rolled popover (not the Radix dropdown used elsewhere) only closes via the toggle button or after choosing an item — clicking anywhere else leaves it floating open. copyLink() calls navigator.clipboard?.writeText(url).then(...) with no .catch: a clipboard permission denial rejects unhandled and the menu closes with no feedback either way. Same missing .catch: MemberYourSeat.copyProof (:78), MemberReferralDashboard.copyLink (:235), MemberQuickActions.copy (:67) — the header menu's handleCopy (MemberHeaderAffordance.tsx:248-258) is the correct model (try/catch, no fake confirmation). Verifier: CONFIRMED — popover at :40-68, no outside-click listener, no Escape handler. |
| P3 | Date-less recent-window items sort ABOVE everything when Chronicle lines are present in the feed | artifacts/studio/src/components/activity/LiveActivityFeed.tsx:257-268 | The merge comparator uses `a.dateUtc || "9999-12-31"` when either side is a chronicle-entry. A window item whose block-header read failed carries dateUtc "" (activityFeed.ts:252 — honest absence), so against any chronicle line it compares as year 9999 and floats to the very top of /activity, above today's newest lines. Also makes the comparator non-transitive (chain items compare by block among themselves but by day against chronicle lines), so exact ordering depends on the sort algorithm. Low frequency (needs a failed getBlock during the scan) but the top feed slot is the most visible line on the page. Verifier: CONFIRMED — chronicle lines exist (entry one promoted), so the branch is live. |
| P3 | ShareCard's clickable proof area hardcodes the prod origin — full reload in prod, cross-origin jump on the dev rig | artifacts/studio/src/components/referral/ShareCard.tsx:48 | The card body is `<a href={link}>` where link is the absolute https://thesyndicate.money/join?source=… (built in MemberReferralDashboard.tsx:299-301). Correct for the COPY/share actions (an external link must be absolute), but as the card's own click target it forces a full page load in prod and, on the localhost rig, navigates the founder's preview session to production. An internal wouter Link to `/join?source=…` for the click surface (keeping the absolute string for copy/QR/share) fixes both. Verifier: CONFIRMED. |
| P3 | AccessBlockedPanel copy claims "No real authentication or session system exists yet" — stale against the live auth stack | artifacts/studio/src/components/access/AccessGate.tsx:43-47 | The panel text hardcodes "No real authentication or session system exists yet — every visitor is an anonymous visitor (S1)". Today's truth: SIWE auth is LIVE in prod, S4/S7/S11 wire through WalletSessionBoot, and the member dashboard runs on it. The path is currently dead (no GATED surfaces exist; guard-enforced, AccessGate.tsx:12), so no user sees the lie today — but the first gated surface will render read-only-era copy verbatim. Also useless-but-live: featureFlags.ts:8-13 still declares realChainReads/membershipLive/sourceRegistryLive/recognitionLive all false with zero runtime consumers (only the FeatureFlag TYPE is imported, modules.ts:27) — pure drift bait for a future reader. Verifier: CONFIRMED — isEnabled has zero call sites. |
| P3 | AdminReferralCrud preview panel contradicts today's live program (status "Paused", comment says Save persists nothing while it posts real writes) | artifacts/studio/src/components/referral/AdminReferralCrud.tsx:22-56 | Operator-gated surface (dead-code-eliminated from prod builds), still a functional truth drift on the rig: (a) the Program-status Switch boots active=false showing "Paused" while the real program has been ACTIVE since 2026-07-13 (referralProgram lifecycle LIVE_ACTION); the toggle changes nothing and is not labeled inert; (b) the header comment (:7-10) says "Save persists nothing" while handleSave actually POSTs every term through /api/operator/referral-terms (operatorClient.saveReferralTerm) — a real founder-gated write with real success copy "Terms saved and audit-logged" (:170). One of the two claims is wrong; an operator can't tell whether Save is a mock. Verifier: CONFIRMED — minor softening: the toggled-on label reads "Active (preview)" (:75), a partial inert hint, but the boot state still contradicts reality and the Save comment is flatly wrong. |
| P3 | Missing VITE_WALLETCONNECT_PROJECT_ID would white-screen the entire app at boot — no fail-soft; needs Replit env confirmation on future rebuilds | artifacts/studio/src/wallet/wagmiConfig.ts:11-18 | getDefaultConfig throws at module evaluation when projectId is undefined, and wagmiConfig is statically imported by the app root chain (RainbowKitRoot → App.tsx), so a build/deploy where the env var is absent renders nothing on every route — not a degraded wallet feature but a dead site. Prod today is fine (the sealed bundle works), but nothing guards the next rebuild: worth either a guard (build fails loudly when the var is missing) or a fail-soft (fall back to injected-only connectors). Replit verification item: confirm VITE_WALLETCONNECT_PROJECT_ID (and that VITE_AVALANCHE_RPC_URL overrides are unset or https) in the deploy environment. Verifier: CONFIRMED — RainbowKit throws "No projectId found…" (dist :7124-7126); static import chain RainbowKitRoot.tsx:26 → App.tsx:41. |
| P3 | Signed-in member sees the visitor door flash before the dashboard on every /member load | artifacts/studio/src/pages/MemberAccess.tsx:66-90 | useOwnSession boots signedIn=false and resolves only after a dynamic import + GET /api/auth/session round-trip, so a recognized member loading /member always renders the full-screen visitor DOOR ("connect" CTA) for the first beats, then swaps to the dashboard. Fail-closed is right, but there is no "checking" intermediate — the member briefly sees the wrong composition claiming they are signed out, and the door band's ConnectButton is clickable during the flash. A three-state (checking → door | dashboard) with a neutral loading band would remove the false frame. The correct pattern is in the same tree: MemberHeaderAffordance.tsx:170 returns null while "checking". Verifier: CONFIRMED — memberView (:116) false for the first frames; door at :231-349 with live ConnectButton (:258-259). |

Refuted at verification: 0 of 13 findings.

## Lens 15 — nobody-asked

**Coverage.** Swept: api-server auth zone in full (router.ts, sessionStore, nonceStore, throttle, clientIdentity, authConfig, engineReadback, operatorContext), operator write zone (router + operatorRegistryService + operatorWriteZone.ts schema), app.ts/index.ts wiring (CSP, CORS, trust-proxy posture, 404), backbone (backboneRunner, backboneDb, backboneConfig — cycle state machine, single-flight scheduling, last-good model discipline, full-scan costs), rpcTransport + publicReadThrottle + protocolReality/capitalStanding routes, lib/db (pool construction, drizzle push-only tooling, schema constraints), studio wallet/session state machine (AccessStateProvider, WalletSessionBoot, walletSession, ownReads, sessionEvents, MemberAccess composition — counted the per-page auth-call fan-out), prerender-routes.ts + generated artifact.toml serving config for both artifacts, all package manifests (dependency pinning, scripts, engines), guard/CI enforcement surface (.github, no workflows; no git hooks; release:gate manual-only), test-framework presence (none). State-machine/race review: nonce consume (atomic, sound), session rotate/fixation (sound), suspend FOR-UPDATE guards (sound), invite check-then-insert (benign race, mislabeled error), client access-state resolution (unguarded last-writer-wins, flagged). Docs-drift spot-checked only where it intersected findings; a full docs/ canon-vs-code reconcile was out of budget for this lens.

| Sev | Finding | Where | Detail + verifier note |
|---|---|---|---|
| P1 | pg Pool has no "error" handler — an idle-connection error crashes the whole api-server process | lib/db/src/index.ts:13 | `export const pool = new Pool({ connectionString: process.env.DATABASE_URL });` registers no `pool.on("error", ...)` listener and passes no pool tuning. node-postgres emits "error" on the Pool when an IDLE client's backend connection dies (managed Postgres — the kind Replit provisions — routinely terminates idle connections; network resets too). An unhandled "error" event on a Node EventEmitter is a process crash: the entire api-server (auth zone, operator writes, protocol spine, backbone feed) dies out-of-band of every try/catch in operatorContext.ts / backboneDb.ts, which only cover query-time errors. All in-memory state (sessions, nonces, last-good backbone read-models) is lost with it. Every DB consumer lazily imports this one module (auth/operatorContext.ts:48, backbone/backboneDb.ts:71 etc.), so this single unguarded emitter sits under the whole write zone and the event backbone. One line (`pool.on("error", (err) => logger.warn(...))`) removes the crash class; platform auto-restart softens but does not excuse it — a restart also signs out every member (in-memory sessions). Verifier: CONFIRMED — repo-wide grep for pool.on / on("error") returns zero source hits; consumers also memberRoster.ts:57, operatorRegistryService.ts:57; sessions in-memory at sessionStore.ts:31. |
| P2 | No CI, no git hooks — the entire guard system runs only on operator discipline; nothing structurally stops an unguarded commit reaching prod | .github | .github/ contains only ISSUE_TEMPLATE — no workflows. No .husky/, no lint-staged, no pre-commit (verified: repo root and all package.json files). The guard fleet (17 studio guards, 17 api-server guards, seo:check, surface:audit, release:gate) is invoked ONLY by hand. The deploy model is "Replit pulls main and deploys" (CLAUDE.md), so a commit pushed without running release:gate — or a future contributor/agent who doesn't know the ritual — lands on thesyndicate.money with zero automated verification. The system's named failure mode (drift the guards exist to catch) has exactly one unguarded channel left: the path to main itself. A minimal GitHub Actions workflow running release:gate on push to main closes the loop; even a required status check without branch protection would surface red builds. Verifier: CONFIRMED — release:gate at root package.json:11 (finding cited :10, immaterial). |
| P2 | Member dashboard self-DoS budget: ~11 auth-zone GETs per /member load against a shared 30/min throttle bucket, each member-standing costing 3 uncached public-RPC round trips | artifacts/api-server/src/auth/authConfig.ts:47-49 | THROTTLE_WINDOW_MS=60s / THROTTLE_MAX_PER_WINDOW=30 is ONE fixed-window bucket per hashed client IP shared by ALL /api/auth/* GETs, sign-in POSTs AND the /api/operator zone. A signed member loading /member fires independent, non-deduplicated fetches: member-standing from MemberYourSeat, MemberKpiRow, CapitalAxisCard, MemberSettings, MemberQuickActions, MemberHeaderAffordance (always-mounted), plus WalletSessionBoot's resolveWiredAccessState (session + operator-context + member-standing, walletSession.ts:163-181), useAuthAvailability (session), and source-standing from MemberQuickActions + MemberReferralDashboard — roughly 11-12 bucket hits per page view. Two-three refreshes or a sign-in inside one minute → 429s → tiles fall to their honest "unavailable" fallbacks: a readable figure rendered as pending under NORMAL member behavior, the exact LIVE-PRODUCTION-RULE failure shape, self-inflicted. Compounding upstream: EVERY member-standing/member-self request re-runs probeChain + eth_getCode + eth_call with no per-account cache (engineReadback.ts:46-90) against free public Avalanche endpoints (rpcTransport.ts:27-28) — ~18 RPC calls per member page view. Documented cliff: when XFF derivation fails, ALL clients share ONE fallback bucket (clientIdentity.ts:33). Fixes: client-side shared member-standing read (react-query already present) + short server-side per-account read cache. Verifier: CONFIRMED — 7 independent fetchMemberStanding call sites enumerated (incl. HeroSeatCta.tsx:37); same allowRequest guards all 7 auth + 4 operator endpoints. |
| P2 | Prod schema management is drizzle-kit push (with a push-force escape hatch), no versioned migrations — a destructive push can silently damage the canonical indexed record | lib/db/package.json:10-12 | The only schema tooling is `push` and `push-force` (`drizzle-kit push --force` auto-accepts data-loss statements); drizzle.config.ts defines no migrations directory and none exists in lib/db/. CLAUDE.md's deploy verdict tells Replit to "run migrations", but there are no migrations — there is schema push against the live DB. The tables under it are not scratch state: sale_event_raw / protocol_event_raw / block_timestamp are the event backbone's canonical, cursor-guarded indexed record (rebuildable from chain, but a rebuild is a full founder-gated rescan), and operator/audit_log are the write-zone's append-only audit trail (NOT rebuildable). One ambiguous rename or type change pushed with --force = dropped column, no journal, no rollback, no record of what ran when. Move to generated, committed migration files (drizzle-kit generate + migrate) before the next schema change, and delete the push-force script. Verifier: CONFIRMED — scripts at lib/db/package.json:11-12; no `out`, no migrations folder or journal. |
| P2 | Dev-skeleton auth stores now carry production traffic: 1000-session in-memory cap fails sign-in closed at 503, is attacker-pinnable, and every deploy signs out all members | artifacts/api-server/src/auth/authConfig.ts:29-36 | SESSION_MAX_ENTRIES=1000 / NONCE_MAX_ENTRIES=5000, both in-memory and both fail-closed when full (sessionStore.ts:50-55 returns null → verify answers 503 "unavailable"; nonceStore.ts:33-37 likewise for challenge). The auth zone is LIVE in prod (SYNDICATE_AUTH_ENABLED, header sign-in sealed in S7-e). Consequences: (a) hard ceiling of 1000 concurrent signed-in members — growth silently converts to sign-in 503s; (b) an adversary with any wallet and a modest IP pool (30 verifies/min/IP under the throttle) can create ~1000 valid sessions and PIN the store at cap, denying all member sign-ins — bounded-store fail-closed is a memory guarantee but an availability hole; (c) single-instance only — any horizontal scale or the pg-pool crash logs out the entire membership. [corrected] (a) sessionStore.ts's header does NOT say dev skeleton — re-titled "Public MVP wallet session" with a founder-approved amendment (:1-14); the surviving "IA-2 dev skeleton" framing is in authConfig.ts:1 and throttle.ts:1. (b) Pinning arithmetic: idle TTL is 15 min (authConfig.ts:35), not 60 — a pinner must re-fill every ≤15 min per entry (trivially doable with an IP pool, so the availability hole stands, but "60 minutes rolling" overstated the per-session hold). Verifier: CORRECTED as noted — core mechanics all re-proven (verify 503 at router.ts:249-253; challenge at :134-138). |
| P2 | Studio pages ship with ZERO HTTP security headers — the anti-framing "Replit handoff note" was never landed as config, so clickjacking protection on wallet/member pages is unverified | artifacts/studio/.replit-artifact/artifact.toml:22-25 | The static serving config (serve="static", publicDir, rewrites) declares no response headers of any kind. The CSP ships only as `<meta http-equiv>` baked by prerender-routes.ts, and that script's own comment (:116-118) states frame-ancestors is IGNORED in meta CSP and "anti-framing for PAGES must be set at the Replit serving layer (handoff note)". Nothing in the repo pins, generates, or checks that handoff — unlike every other cross-layer contract here, which has a guard (seo:rewrites:check exists precisely because the serving layer drifts). The pages that host wallet connect, SIWE sign-in and the join checkout are frameable unless an out-of-repo Replit setting says otherwise, and no artifact records that setting. Also absent at the page layer: HSTS (deferred to "Phase 6" in the toml comment), X-Content-Type-Options, Referrer-Policy-as-header (meta only). Minimum fix: record the serving-layer header state in-repo (or add headers support to the artifact config if the platform allows) and add it to the release gate's checklist; the API side already does this correctly (app.ts:17-23). Verifier: CONFIRMED — artifact.toml read in full (233 lines), no headers table; /member, /wallet, /join among the rewritten static pages (toml:60-98). |
| P3 | Access-state and own-reads hooks race: overlapping session-changed fetches are last-writer-wins with no generation counter — a slow stale response can overwrite a fresher state | artifacts/studio/src/wallet/WalletSessionBoot.tsx:18-34 | resolve() fires on mount AND on every syndicate:session-changed event; each call is an independent promise chain writing wire(state) when it lands. Two overlapping resolves (e.g. sign-out immediately after a sign-in) have no ordering: the elevated resolve (session → Promise.all(operator-context, member-standing) — the SLOW path, ~3 network hops incl. eth_call) can land AFTER the fast S1 resolve triggered by logout, wiring a stale S7/S11 back over the fresh S1 until the next event. Same unguarded pattern in ownReads.ts useOwnSourceStanding/useOwnPurchases — read() on every session event, no abort, no sequence token; the `active` flag only guards unmount, not staleness. Standard fix: a monotonically increasing token per effect-scope (or AbortController) so only the newest in-flight resolve may write. Low likelihood per event, but this is the app's ROOT identity state machine — a regression renders a signed-out user as a member (visibility only, server unaffected, but a truth-display bug when it fires). Verifier: CONFIRMED — actual sub-lines :108-125/:131-148 (finding's off by ~2, immaterial). |
| P3 | No test framework exists anywhere in the workspace — the pure read-model builders (milestone/era/capital/own-purchase folds) have zero regression tests | artifacts/api-server/package.json:6-60 | Grep across all package.json files and source trees finds no vitest/jest/node:test anywhere; the only things named "test" are live fixture scripts (auth-skeleton-test.ts, operator-write-test.ts) that exercise a running server. The guard fleet is excellent at DRIFT detection but guards are not unit tests: the money-adjacent pure functions — buildCapitalAxisReadModel's rung folds, buildMilestoneReadModel's cumsum + contradiction-withhold logic, feedProjection's output gate, formatRawUnitsDisplay's floor-not-round rule (the S7-e "money never overstates" law), clientIdentity's XFF parsing — have no executable specification. Every one was hand-verified once at slice time; a future refactor re-verifies by eye or not at all. These are exactly the functions unit tests are cheapest for: pure inputs → outputs, no mocks. Lock at least the money-formatting and read-model fold semantics in tests wired into release:gate. Verifier: CONFIRMED — release:gate runs typecheck + guards + build only; no unit test stage exists. |
| P3 | Toolchain unpinned: no engines field in any package.json while the build depends on Node >=22.6 native TypeScript execution | package.json:1-24 | No package.json in the workspace declares `engines` (verified root, studio, api-server, lib/db). The studio build runs `node scripts/prerender-routes.ts` (package.json:8) — native TS type-stripping, which the script's own comment says needs Node >= 22.6; several guards run the same way. packageManager pins pnpm@10.26.1 (good) but the Node side is left to whatever the local machine and the Replit image ship (local is v24.18.0 today). If the deploy image moves to an older/newer Node line, the failure is a broken or subtly different build discovered at deploy time — the exact environment-drift class the MSYS2 BASE_PATH incident already cost once. One `"engines": { "node": ">=22.12" }` at the root plus an .nvmrc removes the ambiguity. Verifier: CONFIRMED — no .nvmrc outside node_modules; prerender-routes.ts:28 states the Node requirement (finding cited :29, off by one). |
| P3 | Operator registry gaps: concurrent duplicate invite reports "unavailable" instead of "already_exists", and no reactivation path exists for SUSPENDED operators | artifacts/api-server/src/operator/operatorRegistryService.ts:60-88 | (a) inviteOperator's existence check (:61-66) and insert (:67-74) are inside a transaction but the check is not locked; two concurrent invites of the same wallet both pass the check, the second insert trips the operator_wallet_unique index (operatorWriteZone.ts:47) and the blanket catch (:86-87) reports "unavailable" — data stays correct, but the operator sees a false infrastructure error instead of already_exists. (b) The state machine has no un-suspend: suspend flips ACTIVE→SUSPENDED (:141-144), invite refuses existing wallets (:66), and no reactivate/resume endpoint exists anywhere in the router — a suspended operator can only be restored by a manual DB write, which bypasses the audit_log the zone otherwise scrupulously maintains. Fix the suspend/reactivate asymmetry before the first real mistaken suspension, so the correction is itself audited. Verifier: CONFIRMED — the check has no .for("update") while suspend's equivalent at :115 does (proving the team knows the pattern); grep for reactivate|resume|unsuspend in src/operator: zero hits. |
| P3 | Backbone rebuilds every read-model from a FULL table scan of both raw lanes every 5-minute cycle — an O(n)-forever scaling cliff with a redundant second full scan | artifacts/api-server/src/backbone/backboneDb.ts:527-557 | loadActivityHeartbeatInput selects EVERY sale_event_raw row (:533-546), then runs a SECOND full-table select of all chainIds purely for the stray-chain check (:548-552 — could be `select distinct chain_id` or a WHERE-filtered count); loadProtocolEventRows (:353-367) full-scans protocol_event_raw the same way, plus all block_timestamp rows, every cycle (default 300s, backboneConfig.ts:18), and every derived model (heartbeat, protocol, milestone, era, capital, own-purchase) refolds the complete history in memory. Correct-by-construction and fine at today's row counts, but cost grows linearly with protocol history forever — DB egress, cycle duration (single-flight means slow cycles stretch the serving staleness window), and process memory (lastGood* models retain full-history items, backboneRunner.ts:197-207). No action needed now; leave a dated marker on the incremental-fold design (the cursor discipline already exists for scans — the read-model layer is the part that refolds from zero) before the record is 10-100x today's size. Verifier: CONFIRMED — correctly triaged P3. |
| P3 | No graceful shutdown: no SIGTERM/SIGINT handling — every deploy hard-drops in-flight requests and (by design) all member sessions | artifacts/api-server/src/index.ts:19-30 | app.listen with no signal handlers, no server.close() drain, no pool.end(). On every Replit redeploy the process is killed mid-flight: in-progress verifies/operator writes are severed at the socket (the operator writes are transactional so the DB stays consistent — good), and all in-memory sessions vanish, signing out every member (the founder's deploy-cost rule means deploys batch, so this fires on every batched deploy day). A ~15-line shutdown handler (close server, stop accepting, end pool, exit) plus session-store externalization would make deploys invisible to signed-in members. Low urgency; listed because "every deploy logs out everyone" is currently an unstated product behavior nobody decided on-record. Verifier: CONFIRMED — grep for SIGTERM|SIGINT|process.on across api-server src: zero hits. |
| P3 | capital-standing is a sessionless public endpoint serving ANY seat's cumulative USDC by enumerable ordinal — the "own-account" boundary is client-side convention | artifacts/api-server/src/routes/capitalStanding.ts:39-53 | GET /api/backbone/capital-standing?seat=N takes any 1-7-digit seat ordinal with no session, no throttle (cheap in-memory read, so no DoS issue), and returns that seat's rung + cumulativeUsdcRaw (:76-77). The route header honestly grounds this in CANON_VISIBILITY_LAW ("every figure here is public chain data made legible") and the S7-b own-account ruling — the data IS derivable from public chain events, so not a leak. The delta: the feed deliberately never pairs a seat with an amount (:18-19), yet this route lets anyone build the complete seat→cumulative-USDC table in one loop of ~N requests. [corrected] The claim that "the decision exists only implicitly / undecided on-record" is wrong: docs/audits/MEMBER_HOME_TOTAL_AUDIT_2026-07-16.md:298 (E55) — a canon audit record per the sealing commit — already records exactly this as settled ("Per-seat cumulative-spend endpoint is public and unauthenticated — settled in canon, recorded here… the own-account restriction… is a client-side rendering convention, not a server gate"). Any-seat-to-any-caller (whose N-request enumeration is a trivial corollary) is on record; the finding reduces to a duplicate of E55. Verifier: CORRECTED as noted — technical facts all re-proven. |

Refuted at verification: 0 of 13 findings.

## Cross-lens severity ranking

184 findings survived verification (0 refuted, 9 corrected). Totals: **P0 ×4 · P1 ×41 · P2 ×64 · P3 ×75**. Three of the four P0s converge on the same page: /proof.

### P0 — all four

| # | Finding | Where | Lens |
|---|---|---|---|
| 1 | /proof (primary-nav public page) declares the whole proof layer unwired and the site "view-only: no payments, no live chain reads" — every clause false today | artifacts/studio/src/pages/ProofDashboard.tsx:73-77, 80, 99 (+ config/sharedCopy.ts:34-35) | 1 truth-liveness |
| 2 | /proof — the flagship verify page still speaks the dead pre-backbone era: "none of it is wired in this foundation yet" (whole-page rewrite) | artifacts/studio/src/pages/ProofDashboard.tsx:23-99 | 2 cross-page-era-drift |
| 3 | MISSING: Terms of Use / participation agreement — live seat sales run with no contractual terms anywhere in the tree | artifacts/studio/src/wallet/JoinCheckout.tsx:171 (absence: App.tsx, seo-route-registry.ts, navigation.ts:60-65) | 7 legal-trust |
| 4 | PUBLIC LIE on /proof: "view-only: no payments, no live chain reads" while the protocol sells seats live — a public misstatement of the site's legal posture | artifacts/studio/src/pages/ProofDashboard.tsx:73-77 | 7 legal-trust |

### P1 — all forty-one

| # | Finding | Where | Lens |
|---|---|---|---|
| 1 | /proof facet cards: "registry is paused by precaution", receipts/burn/archive "Not live yet" — all four organs live | ProofDashboard.tsx:23-48 | 1 |
| 2 | /contracts card: Source Registry V1 "intentionally inactive — no attribution is read or written here" while it pays commissions | config/contractMemory.ts:104-112 | 1 |
| 3 | /contracts Proof of Fire entry: "require event read-model wiring" — the numbered burn record is served | config/contractMemory.ts:208-216 | 1 |
| 4 | /contracts banner: "this app only reads" — the app has written real-money joins since C5 | pages/ContractMemory.tsx:73-77 | 1 |
| 5 | Public /status renders "Contracts — Not live yet" while the page renders live holdings | config/modules.ts:349-363 | 1 |
| 6 | Public /status renders "Source Attribution — Not live yet" via the retired-from-render reason code | config/modules.ts:209-226 | 1 |
| 7 | Public /status summaries promise /activity and /fire-ledger "arrive with the event backbone" — both live | config/surfaceClassification.ts:226-253 | 1 |
| 8 | /archive: "no chronicle entries are stored or published in this foundation" — /chronicle publishes four | pages/Archive.tsx:38-50 | 1 |
| 9 | /archive "What unlocks it": "wiring them is its own slice / When the reads are wired" vs its own live lead | pages/Archive.tsx:65-75 | 1 |
| 10 | /learning: "This foundation does not read the chain yet" + seat "not live yet" — pre-C5 fossils | config/learningModules.ts:59, 67 | 1 |
| 11 | /support membership channel: "founder-gated and not live yet" | config/supportIntake.ts:50-54 | 1 |
| 12 | /docs referral card: "never a commission" — a bounded commission IS paid | content/docs-content.ts:126-131 | 1 |
| 13 | Served /api/source-status token note: "No price, supply, or balances are wired or exposed" (+ phantom "/token" surface) | api-server/src/data/sourceStatus.ts:200-209 | 1 |
| 14 | /proof safety banner: "no payments, no live chain reads" — retired read-only sentence still rendered | config/sharedCopy.ts:34-35 | 2 |
| 15 | /learning: chain-not-read/seat-not-live/"never as compensation" — three lies self-contradicted in-file | config/learningModules.ts:59, 67, 69 | 2 |
| 16 | /archive denies the Chronicle while nav labels the page "Chronicle" | pages/Archive.tsx:44-49 | 2 |
| 17 | /archive same-page era split (lead live, tail "When the reads are wired") | pages/Archive.tsx:65-74 | 2 |
| 18 | /docs card for /referral: "never a commission" (repealed-law voice) | content/docs-content.ts:127-129 | 2 |
| 19 | /docs card for /archive: "Concept memory today; reads not yet wired" | content/docs-content.ts:177-181 | 2 |
| 20 | /contracts Source Registry "Paused for safety… intentionally inactive" while the program pays through it | config/contractMemory.ts:106-111 | 2 |
| 21 | /contracts Proof of Fire row: "require event read-model wiring before any event can be shown" | config/contractMemory.ts:210-215 | 2 |
| 22 | FAQ asserts the repealed SYN-balance Citizen→Cornerstone rank system — served in FAQPage JSON-LD | content/faq-content.ts:192, 122 | 2 |
| 23 | Whitepaper §8 brands the Archive "PAUSED"/"read-only today" while artifacts mint openly | pages/Whitepaper.tsx:200-205 | 2 |
| 24 | Whitepaper §10 lists "source payment/referral" and "live activity feed" as KNOWN FUTURE MODULES — both live | pages/Whitepaper.tsx:220-227 | 2 |
| 25 | /status surface map: /activity + /fire-ledger "arrive with the backbone" on the honesty hub itself | config/surfaceClassification.ts:226-253 | 2 |
| 26 | Public /status renders the complete operator/admin surface map — every internal route linked and described (Ruling ②) | pages/SystemStatus.tsx:290-327 | 4 |
| 27 | Public footer "Console → Studio OS" link to /studio on every public and member page — retired class, dead door in prod | config/navigation.ts:64 | 4 |
| 28 | /archive claims live reads it does not perform — the component has ZERO live reads (copy is the lie; propagated to modules.ts + memberDoors.ts) | pages/Archive.tsx:17 | 6 |
| 29 | /archive "no chronicle entries" while /chronicle serves three — and the card has no link to /chronicle | pages/Archive.tsx:44-49 | 6 |
| 30 | MISSING: Privacy Policy — wallet-linked member records, session cookies, per-IP throttling with no privacy disclosure | config/navigation.ts:60-65 | 7 |
| 31 | WEAK: no affirmative risk/terms acknowledgment at the point of purchase — checkout flow has none | pages/JoinProtocol.tsx:613-618 | 7 |
| 32 | Light-theme accent tokens fail text contrast (gold ~2.96:1, cyan ~2.85:1) used as small text site-wide | index.css:83-99 | 8 |
| 33 | ADR-001 readability floor violated by 215 sub-12px classes across 49 files incl. 8px text; the promised guard does not exist | components/hero/SeatFlowDiagram.tsx:273-377 | 8 |
| 34 | /docs public cards contradict today's live reality — "never a commission", proof "isn't wired yet", archive "reads not yet wired" (shadow-registry root cause) | content/docs-content.ts:129, 152-155, 178-181 | 11 |
| 35 | Public /status surface map serves "arrives with the event backbone" summaries for live surfaces (three parallel prose fields root cause) | config/surfaceClassification.ts:228-250 | 11 |
| 36 | /archive declares the Chronicle "a design concept — no chronicle entries" while /chronicle is LIVE | pages/Archive.tsx:44-49 | 12 |
| 37 | /archive self-contradiction + renders ZERO live figures while the hero's "NFT Artifacts" revenue door points here | pages/Archive.tsx:17, 65-69 | 12 |
| 38 | Whitepaper §8 stamps the Archive "PAUSED"/"read-only today" while public mint is OPEN and the paragraph counts live mints | pages/Whitepaper.tsx:200-204 | 12 |
| 39 | /docs card for the archive: "reads not yet wired" — stale public claim | content/docs-content.ts:168-180 | 12 |
| 40 | Post-sign-in redirect rips a buyer out of the /join checkout (and every non-/member flow) — live money path | wallet/rainbowAuthAdapter.ts:72 | 14 |
| 41 | pg Pool has no "error" handler — an idle-connection error crashes the whole api-server process | lib/db/src/index.ts:13 | 15 |

(Overlap note, factual: P1s #8/#16/#29/#36 are one defect (the /archive Chronicle card) seen by four lenses; #9/#17/#28/#37 one defect-cluster (the /archive live-reads contradiction); #12/#18/#34 and #19/#39 one each (the /docs cards); #7/#25/#35 one (the /status summaries); #2/#20 and #3/#21 one each (the /contracts cards); #23/#38 one (Whitepaper §8). Deduplicated, the P1 layer is ~24 distinct defects.)

### P2/P3 — counts per lens

| Lens | P2 | P3 |
|---|---|---|
| 1 truth-liveness | 3 | 1 |
| 2 cross-page-era-drift | 7 | 3 |
| 3 human-readable | 7 | 5 |
| 4 public-private | 3 | 3 |
| 5 design-harmonization | 8 | 5 |
| 6 url-routing | 3 | 6 |
| 7 legal-trust | 3 | 2 |
| 8 a11y | 7 | 7 |
| 9 seo-machine | 2 | 10 |
| 10 perf-security | 3 | 6 |
| 11 modular-organism | 4 | 3 |
| 12 archive-nft | 4 | 2 |
| 13 banned-vocabulary | 1 | 7 |
| 14 functional-bugs | 4 | 8 |
| 15 nobody-asked | 5 | 7 |
| **Total** | **64** | **75** |

## Slicing plan (Claude Code, 2026-07-16 — awaiting founder GO; optimizes prod-lies-die-fastest · zero rework · zero double-paid gates)

1. **AUD-P0 — THE /PROOF TRUTH KILL (immediate, first slice, 🚀 its own deploy).**
   P0 #1/#2/#4 are one page speaking a dead era over a live money path — the worst
   public lie in the protocol. Whole-page truth rewrite of ProofDashboard (+ the
   sharedCopy line), lens 1's /proof facet P1s ride the same slice (same file).
2. **AUD-T — TERMS OF USE + THE LEGAL LAYER (founder-gated content).** P0 #3. I draft
   the Terms + Privacy + Risk set from lens 7's world-class checklist; the founder
   reads the FULL text on screen (the standing rule); pairs with S5 (the soul document
   awaits his paste). Content, not code — starts immediately in parallel.
3. **AUD-TRUTH — THE SITE-WIDE ERA-DRIFT SWEEP.** The lens 1+2+3 P1s (~15 distinct
   defects: /contracts, /status, /archive, teasers) — one truth-sweep slice, the S7
   pattern; the 4-lens /archive Chronicle-card defect dies here.
4. **RECEIPT SLICE** (approved wireframe: checkout-success ticket + kind-extensible
   model + guards + REAL-ROW/EXACT rules) — kills the post-purchase dead end.
5. **MENU SLICE** (approved wireframe; a11y P1s living in the shell files ride).
6. **HOME RECOMPOSITION** (approved wireframe; Z3/Z4 on D-TRUTH data; remaining
   member-surface a11y P1s ride).
7. **AUD-ROUTE — ROUTING + SEO RATIONALIZATION** (lens 6+9 P1s: duplicates/orphans/
   canonicals/JSON-LD; the Home-vs-PublicHome class the founder named).
8. **/ADMIN-IN-PROD** (Ruling ②, neutral wall; lens 4's P1s ride) — deliberately
   BEFORE M-INT-1: the member ledger is operator-side and needs its production home.
9. **THE ACTED ORDER RESUMES:** M-INT-1 → A1 (the receipt binder, placements ②+③) →
   B1 → A2 → M-INT-2.
10. **QUEUED BEHIND:** S8 performance (lens 10 feeds it) · the Archive→NFT naming
    decision (lens 12's options — founder decides on screen) · P2/P3 waves batched
    per lens · the no-sub-12px guard (lens 5's recount).

## Coverage honesty

Each lens's coverage and could-not-check notes, compressed; the Replit/prod items are what no repo read can answer.

**Lens 1 (truth-liveness).** Swept all 24 public pages + copy-bearing config + content/ + served api notes (sourceStatus.ts full) + go-live ground truth. Verified-clean: FAQ content, Activity/FireLedger page copy, Recognition, homepage awaitingWiring card, hash-pinned snapshots, MemberReferralDashboard per-receipt PENDING card. NOT CHECKED / NEEDS RUNTIME: the deployed prod bundle and rendered DOM on thesyndicate.money (whether these files are what prod serves — repo says S7-e sealed, but /proof, /contracts and the /status surface map were clearly NOT covered by the S7 sweep); prod env values (SYNDICATE_AUTH_ENABLED / SYNDICATE_BACKBONE_ENABLED inferred from seal records, not read); live API responses; the S7-e member dashboard components only spot-checked (sealed today under their own 170-finding audit); Whitepaper.tsx, Liquidity.tsx, JoinProtocol.tsx, MemberWallet/MemberToolkit passed the stale-claim greps but did not get line-by-line reads.

**Lens 2 (cross-page-era-drift).** All 24 public pages + every rendered content/config source + registry titles/descriptions/postures/notes + api truth spine; stale claims cross-checked against canon decisions, never page-vs-page; dead-config status confirmed by grep. NOT CHECKED (needs Replit/prod): the deployed bundle on thesyndicate.money (whether prod is on the commit carrying these files), live API payloads at runtime, prerendered shell HTML as actually served, admin/operator panels' deep content beyond spot checks. Biggest systemic pattern: the S7 truth sweep updated modules.ts/registry descriptions but left the PAGES in the pre-backbone era.

**Lens 3 (human-readable).** All 24 public page files, shared chrome, wallet flow, and the api-server failureReason/mode/status vocabulary; verified the enum→human mapping layer and S7-e conventions the findings extend. NOT checked: rendered prod output on thesyndicate.money (needs Replit/browser), actual runtime wallet/viem error strings, and the full curated copy of learningModules/faq/docs beyond jargon greps. Member Home (S7-e sealed) treated as reference canon, not re-audited.

**Lens 4 (public-private).** Route table + gating, public chrome, member boundary, wallet layer, guards, serving layer, dist-level grep of the local production build; API zones verified session-bound/role-gated; admin-sees-public-never verified (no public roster endpoint). NOT checkable from here (needs Replit/prod): the actual deployed bundle at thesyndicate.money (local dist hash index-DSnsVSxD.js differs from the recorded prod hash index-CJU-JUzs.js — findings rest on the static import graph, which is build-invariant), live values of VITE_OPERATOR_PREVIEW / SYNDICATE_AUTH_ENABLED / SYNDICATE_BACKBONE_ENABLED / CORS_ALLOWED_ORIGINS in prod, and the serving layer's real HTTP responses (404-vs-rewrite for internal paths) which only a live probe confirms.

**Lens 5 (design-harmonization).** Static sweep of all sub-12px classes (221 occurrences located per file), CSS tokens, ADR text re-read, viewport meta, safe-areas, all max-w caps, bare-vh grep. Dark/light parity spot-checked only (hardcoded bg-white limited to QR blocks; NOT a pixel-level parity pass); empty states spot-checked; own-account display structural check only. NOT checked (needs Replit/prod or a rendered browser pass): computed font sizes after inheritance, real 320-2560px rendering, actual iOS/Android safe-area behavior, prod bundle's dead-code-eliminated admin styles, and contrast ratios in both themes.

**Lens 6 (url-routing).** All 39 mounts + catch-all vs registry/sitemap/robots/shells/rewrites; every internal link source grepped and every target verified; anchors verified; canonical layer verified. NOT checkable here: live prod HTTP behavior (that rewrites/404 actually serve as artifact.toml documents, that the prod bundle really dead-codes the console — needs Replit/prod), and external URL liveness (x.com/TheSyndicateOne, t.me, dexscreener, traderjoexyz — registry notes them verified 2026-07-14; no network access in this audit).

**Lens 7 (legal-trust).** All 29 page files, footer block, nav/footer registries, brand.ts, purchase flow, risk-prose surfaces, tracker/analytics grep, cookie mechanics, docs/ for any decided legal-page posture (none). Could NOT check: the actually-served prod pages at thesyndicate.money (whether Replit serves any legal document outside this repo, actual Set-Cookie headers in prod, any Cloudflare/hosting-injected cookies or banners), the Telegram/X channel content, and any off-repo legal documents the founder may hold — all need prod/Replit access.

**Lens 8 (a11y).** Code-level: shared chrome, all 24 mounts one-h1 check, token contrast computed, greps for sub-12px/outline-none/aria-live/aria-current/img-alt/input labels, interactive deep-reads. NOT checked (needs a running rig or prod): real rendered contrast after opacity/backdrop-blur stacking, 200%-zoom reflow, actual screen-reader/keyboard walkthroughs, focus order under framer-motion mounts, Radix runtime behavior (Sheet/DropdownMenu focus return), the wallet-connect modal (third-party RainbowKit UI), and the operator/admin console at runtime (build-gated; statically reviewed — AdminShell/panels carry the same sub-12px and placeholder-label patterns). API server has no HTML surface — out of scope.

**Lens 9 (seo-machine).** Registry in full, prerender/check/generate scripts, sitemap/robots parity, base template, runtime injectors, artifact.toml rewrites, asset inventory, OG image measured on disk, API headers. NOT checkable from the tree (needs Replit/prod): that the deployed serving layer actually returns HTTP 404 for unmatched paths and 200 flat shells per the artifact.toml handoff notes; live response headers on thesyndicate.money (whether the host injects anything); Google Search Console state (coverage, sitemap acceptance, site-name display); whether the currently DEPLOYED bundle carries the current registry (prod could lag the tree per the deploy-backlog rule); live rendering of the JSON-LD by real crawlers.

**Lens 10 (perf-security).** Full dist build weight measured raw+gzip, entry composition probed, no .map in dist, images measured, chunking config, API security headers/CORS/cookies, throttle/DoS posture, secrets scan (clean). NEEDS REPLIT/PROD MEASUREMENT: (1) actual response headers on thesyndicate.money pages — X-Frame-Options/frame-ancestors (the P2), HSTS, X-Content-Type-Options, Permissions-Policy, Cache-Control/immutable on /assets; (2) whether the static layer serves gzip/brotli (the gzip figures are potentials); (3) real HTTP status of unmatched paths (404.html contract) and rewrites; (4) Lighthouse/CWV (LCP/TTI/CLS) for the 482 KB-gzip entry on real devices; (5) API response compression + live /api/protocol/reality envelope size; (6) whether the deployed bundle still matches — the last prod seal recorded index-CJU-JUzs.js while today's local dist emits index-DSnsVSxD.js (local rebuild newer than the seal; hash parity needs the next Replit report); (7) www→apex redirect (deferred to the ~Sept 2026 domain transfer, artifact.toml:36-37).

**Lens 11 (modular-organism).** Route spine + every config registry + all 18 studio guards (what each parses and does NOT) + exhaustive consumer greps + public renderers + api-server module reality + vendored event canon + client feed-kind map. Architectural read recorded (clean modules vs entangled debt; module-contract manifest shape; plug-in readiness of Referrer Kit / Swap-Bridge / Seasons). COULD NOT CHECK: prod runtime rendering at thesyndicate.money (whether the stale /docs and /status copy is in the deployed bundle — needs Replit/prod verification), DB state, and env-gated behavior (SYNDICATE_AUTH_ENABLED/SYNDICATE_BACKBONE_ENABLED values in prod).

**Lens 12 (archive-nft).** Every archive/artifact/mint-matching studio file (40+), api-server vendored canon + decoders + reality/backbone paths, and the origin quarries read-only (TheSyndicate archive docs/specs/catalogs, Supa-Exchange NFT schema). COULD NOT CHECK (needs Replit/prod/RPC): the live chain values themselves — actual paused() flag, real minted counts, whether ids 4-8 are truly configured on-chain today, treasury/owner addresses; and how prod thesyndicate.money currently renders /archive (bundle-level verification). Chronicle register verified in-repo, not against the served page.

**Lens 13 (banned-vocabulary).** Every studio public-string source + SEO layer + feed sentences + checkout translation + public/ static text + api served strings, swept with word-boundary regexes over the full banned list + promise-shapes; guard-forbidden-copy re-run live (PASS over 237 files); judgment calibrated to CANON_PROTOCOL_LANGUAGE.md §5. Zero P0/P1 — no positive-sense financial-upside claim on any public surface. COULD NOT CHECK (needs Replit/prod): the actually-deployed bundle/shells on thesyndicate.money, any Replit-side env-injected copy, and real SERP/social-card renderings (truncation behavior asserted from platform norms, not observed live).

**Lens 14 (functional-bugs).** Static behavior trace of every public interactive surface (money path, auth/session stack, member dashboard, wallet door, referral share, feeds, chrome, liquidity rail, operator writes, gates). REPO↔PROD PARITY: HEAD a947c82 is docs-only on top of a8a43c6 = the prod-sealed bundle index-DSnsVSxD.js (SHA-256-verified per seal record), deploy backlog empty. COULD NOT CHECK (needs Replit/prod or a live wallet): actual RainbowKit modal sequencing (the SIWE step after connect, and which chainId RainbowKit passes on a wrong-network wallet — finding #5 verified against the library source but not a live wallet), prod env vars (SYNDICATE_AUTH_ENABLED, SYNDICATE_BACKBONE_ENABLED, VITE_WALLETCONNECT_PROJECT_ID, VITE_AVALANCHE_RPC_URL overrides), public-RPC rate-limit behavior of the ~22-chunk /activity window scan, ProposeSourcePromotion.tsx and admin sections/panels traced only structurally (build-gated out of prod), api-server non-auth route handlers (other lens).

**Lens 15 (nobody-asked).** Api-server auth + operator + backbone zones in full, lib/db, studio session state machine, serving configs, all manifests, CI/hooks/test-framework presence, state-machine/race review (nonce/session/suspend sound; invite benign race; client access-state flagged). NOT checkable from this tree (needs Replit/prod): actual HTTP response headers the static layer serves (the anti-framing handoff), the live Node version and whether SYNDICATE_AUTH_ENABLED/SYNDICATE_BACKBONE_ENABLED/CORS_ALLOWED_ORIGINS/SYNDICATE_CANONICAL_HOST are set as the code assumes, real Postgres provider idle-connection behavior, public-RPC rate-limit headroom at current member traffic, and whether Replit auto-restarts a crashed process. Docs-drift spot-checked only where it intersected findings; a full docs/ canon-vs-code reconcile was out of budget for this lens.
