# FOOTER PAGES — HARMONY + TRUTH AUDIT (2026-07-30)

Ordered by the founder (« ce n'est pas harmonisé !!!! et check the truth »), run before any fix: 10 agents,
25 footer-linked pages, both dimensions per page. **78 findings: 6 blocking · 37 important · 35 minor.**
Every finding carries measured evidence (quoted line + the API/chain figure that refutes or the system rule it breaks).
This file is the WORK LEDGER for the harmonization arc — tick findings as slices close, never re-audit from scratch.

> **✅ CLOSED 2026-07-30 (the same evening, commits `2aa7a60`·`c162410`·`2c18004`·`9d5848e`·`ed2a6a7`+the review batch):**
> ALL SIX BLOCKING — the /map+/status era denial (its CLASS entered guard-era-drift) · /referral's
> recognition denial (both copies) · /archive's Entry-1 fossil (dynamic register count) · Terms §8 and
> Privacy wallet-addresses (founder-approved v2 wording + version badges bumped V2/V3) · Google Fonts
> (self-hosted, CSP tightened — the privacy inventory is true again). PLUS the /archive museum promises
> (important) now pinned via featureStatus `archiveGallery`. **72 findings remain open: 36 important ·
> 35 minor + this ledger's own tick discipline.** Every figure above recounted at close.

> **✅ CLOSED 2026-07-30 — HARMONIZATION ARC, SLICE A (DONE-IS-DONE pins):** guard-feature-truth's
> FUTURE_MARK widened to the object-literal form (RED run: 4 unpinned files, exactly the twin-search
> inventory) · 7 future keys registered (standingModel · supportIntake · signalChamber · seatRecord ·
> trustCapital · knowledgeOsGuided · linkRegistry; the acknowledgement node cites the EXISTING
> eventDerivedNotifications rail, never a twin key) · prose pins on faq-content/Support/supportIntake/
> docs-content · /chronicle's unreachable FUTURE teaser branch DELETED · protocolOsMap's
> "nothing is wired" acknowledgement fossil corrected. Guard PASS 660 checks; full chain EXIT 0.
> Closes: /faq four-claims (important) · /recognition standing (important) · /support unpinned
> (minor) · /chronicle dead branch (minor). **68 remain open: 34 important · 33 minor + the tick
> discipline** (recounted: 36−2 · 35−2).

> **✅ CLOSED 2026-07-30 — HARMONIZATION ARC, SLICE B (truth/copy, non-legal):**
> IMPORTANT ×11 — /activity hero clause (Chronicle lane scoped) · /contracts 3 treasury denials +
> founder-wallet twin (the notes now POINT at the live layer) · /contracts LP-pair "no reserve is
> read" · /faq Support CTA (→ the live Guide; stale next-slice comment deleted) · /map completeness
> lead (honest scope; ARCHIVE GROUP BOUND — the coverage guard's opposite pin struck, dated) · /map
> "addresses stay server-side" · /map unpinned future claim (sentence deleted with the binding) ·
> /toolkit stale-complete registry (+ask-activation +wallet-approvals) · /status CANON_AS_OF
> 2026-07-19→30 (guard floor RATCHETED, red-proven: "1 FAILURE (210 passed)" before the bump) ·
> /whitepaper §9-§10 self-updating claim (softened + PINNED to standingModel/seasonBounty — the
> "guarded" sentence is now true) · /tokenomics "watch it vest" (commitment, not chain mechanism).
> MINOR ×9 — /docs "eight topics" (+ the guide-content TWIN the audit had not named) · /faq "twelve
> rungs" · /learning category upgrade (the honest triple) · /learning dead-era header comment ·
> Trader Joe→LFJ (audit named 3 sites; the twin search found 5 MORE: faq×3 · Tokenomics:108 ·
> MemberWalletPanel:277; URLs repointed to lfj.gg — measured: old URL 303-redirects byte-identical,
> /add answers 200) · /proof "read live" · /status "member-identifying data" · /toolkit LIVE_ACTION
> badge → READ_ONLY_PROOF · /toolkit permanent-link note. The /fire-ledger BANNER clause is also
> rewritten (chip classifies, chain names) but its finding stays OPEN until the row grammar shows
> the sender (slice E). Studio guards EXIT 0 · api 21/21 · typecheck 0.
> **48 remain open: 23 important · 24 minor + the tick discipline** (recounted: 34−11 · 33−9).

> **✅ CLOSED 2026-07-30 — HARMONIZATION ARC, SLICE C (the deferred sub-12px floor sweep, now paid):**
> 37 sub-floor occurrences converted across 9 files — mono metadata → `.type-caption` (12px mono),
> prose/hints/labels → `text-xs`, the ProtocolReality note line → `text-sm` (multi-sentence reading
> copy), two eyebrow-shaped labels → `.type-eyebrow` (JoinProtocol raw-units summary ·
> ProtocolAssetsCard VERIFY_SLOGAN, whose exemption ceiling ratcheted 2→1 exactly as its own entry
> ordered). RED proof: the two-way ratchet failed the build on the stale allowlist counts ("CLEARED
> — DELETE its ALLOWLIST entry" ×9) before the 9 entries were deleted. Guard's own PASS line after:
> **debt 216 → 179 occurrences · public 164 → 127 · allowlisted files 52 → 43.** Learning's step
> details also rose 12→14px (the same-page two-sizes finding). Typecheck 0 · full chain EXIT 0.
> Closes IMPORTANT ×4: /join 5 sites · /liquidity 5 runs · /map+/status shared reality-table
> sizes · /status 8 sites. Closes MINOR ×5: /map FigureRow 10px · /contracts sub-floor family ·
> /docs numerals · /learning step chip + two-sizes · /wallet 11px.
> **39 remain open: 19 important · 19 minor + the tick discipline** (recounted: 23−4 · 24−5).

> **✅ CLOSED 2026-07-30 — HARMONIZATION ARC, SLICE D (heading/type-role harmonization):**
> IMPORTANT ×5 — /archive's second h2 joins type-h2 · /learning's text-lg heading → type-h2 ·
> /liquidity's two h2 answers → one (type-h2) + its two h3 treatments aligned (text-base
> font-medium) · /map gains its section scale (Membership engine + Live reality reads, both
> type-h2 — zero before) · /referral's off-system trio (font-light tagline → type-body ·
> mono "Powered by" prose → type-eyebrow · drifting card h3 → type-h3).
> MINOR ×5 — /referral's duplicate "How it works" (the h2 renamed "The program, live") · /terms
> colophon → type-body + measure · /contracts role lines drop the mono voice (prose, not data) ·
> /chronicle's hand-typed verify span → type-eyebrow · /activity's date-group heading closed AS
> DOCUMENTED: the 14px choice already lives registered in guard-type-scale's EYEBROW_EXEMPT with
> its measured reason (the 10px-grouping defect) — one home, no second copy to fix.
> Typecheck 0 · full chain EXIT 0. **29 remain open: 14 important · 14 minor + the tick
> discipline** (recounted: 19−5 · 19−5).


## BLOCKING (6)

### [truth] /archive — "entry one: the duplicate seat" is refuted by the live /chronicle page it links to
- **Where:** C:\Users\kemal\OneDrive\Documents\GitHub\syndicate-os\artifacts\studio\src\pages\Archive.tsx:50
- **Evidence:** Archive.tsx:49-51 renders: "founder-promoted entries are published, each anchored into the public record — entry one: the duplicate seat." — with a "Read the Chronicle" button directly beneath it. But ChronicleTeaser.tsx:61-64 numbers entries CHRONOLOGICALLY by dateUtc (comment at :51-54: "'Entry 1' means the FIRST thing that happened"), and since the eleven origin-history entries were promoted 2026-07-26 (chronicleRegister.ts:141, register size measured 15), the computed live numbering is: Entry 1 = "The getter that was not there" (2026-06-06); "The duplicate seat" = Entry 12 (measured by sorting the register file's ids/dates with the page's own byDateThenId rule). A stranger clicks the card's own button and sees the duplicate seat labeled Entry 12 — the sentence is a fossil of the 4-entry era (the card also still reads as day-one copy: "The solemn record is open").
- **Fix:** Rewrite the Chronicle card to today's register: e.g. "fifteen founder-promoted entries, newest first — from the first recorded error (June 2026) to the ladder decision" — no ordinal claim, or use the register length dynamically (CHRONICLE_REGISTER.length) so the count can never fossilize again.

### [truth] /map and /status — Dead-era claim on two footer pages: "No wallet, transaction, or referral surface is enabled"
- **Where:** artifacts/studio/src/components/ProtocolReality.tsx:34
- **Evidence:** groupBlurb.sale (line 34): "Live state from the deployed membership-sale engines. V3 is the active engine — its public figures are shown as exact raw base units. No wallet, transaction, or referral surface is enabled." This renders in the Sale-engine group on BOTH /map (MAP_GROUPS includes "sale", ProtocolMap.tsx:24) and /status (default groups, ProtocolReality.tsx:185). Refuted today by the site itself and the chain: /join checkout is LIVE since 2026-07-13 (featureStatus.ts checkoutTicket live 2026-07-17; /api/source-status buyReadiness = LIVE_ACTION, "the join transaction path is LIVE"), referral surfaces are LIVE (referralTabs live 2026-07-19), and prod GET /api/protocol/reality (2026-07-30) serves financial.referral.registryLive=true, financial.referral.paidToReferrersTotal="750000", sale.MEMBERSHIP_SALE_V3.receiptCount="12". Sentence last shipped in 38623f1 (2026-07-25) — already false that day. This is the exact AUD-P0 fossil class the 2026-07-16 sweep killed on /proof.
- **Fix:** Replace with the honest phrasing the registry already uses (source-status sale note): "The join transaction itself is signed from the visitor's own wallet on /join — this panel reads only and sends nothing."

### [truth] /privacy — Privacy 'Wallet addresses' claims the wallet↔member-number mapping 'stays server-side, by design' — it is public on the chain and on two of our own APIs
- **Where:** artifacts/studio/src/pages/Privacy.tsx:75
- **Evidence:** Privacy.tsx:75-76: "we never publish which wallet holds which member number — that mapping stays server-side, by design." Same refutation as Terms §8: /api/backbone/feed pairs memberNumber 14 with 0xea88…5881 in one public JSON object; /api/season pairs seat 1 with 0x2445…c721; and MembershipPurchasedV3 emits memberNumber next to the buyer address in every purchase, so the mapping was never server-side-only. The drafts (2026-07-16/19) predate the season board and the 2026-07-25 address-model ruling.
- **Fix:** Version 3 of the policy: state that the seat number and wallet address are both public chain data (short-form + Snowtrace, the pride surface); the mapping that stays server-side and unpublished is identity↔wallet (name/alias/email — of which we hold none).

### [truth] /privacy — Third-parties inventory omits Google Fonts — the browser sends every visitor's IP to Google on every page load
- **Where:** artifacts/studio/src/pages/Privacy.tsx:158
- **Evidence:** Privacy.tsx:156-165 lists exactly "Two kinds of outside services" (WalletConnect + public Avalanche RPC), and 'The short version' (line 49-50) says "Nothing else leaves except what a connection itself requires." Measured on the rig: the only external hosts contacted on a fresh /privacy load, before any wallet action, were fonts.googleapis.com and fonts.gstatic.com (6 font resources). Prod serves the same: index.html:35-37 embeds the fonts.googleapis.com/css2 stylesheet link, and curl of https://thesyndicate.money/privacy shows the link plus a CSP explicitly allowing both Google hosts. Embedded Google Fonts transmitting visitor IPs is the exact pattern German courts held a GDPR violation (LG München I, 3 O 17493/20).
- **Fix:** Grade-AAA fix: self-host Instrument Serif / Work Sans / IBM Plex Mono (e.g. @fontsource) so the claim becomes true — this also removes the render-blocking third-party request. Interim: list Google Fonts as a third party in the next policy version.

### [truth] /referral — DONE-IS-DONE: live referral recognition presented as 'a future, gated concept' — twice, one instance under 'What's live today'
- **Where:** artifacts/studio/src/config/sourceAttributionTerminology.ts:85
- **Evidence:** Anon /referral renders (SourceAttribution.tsx lines 117-125 under the h2 'What's live today', and again in Boundaries via line 96): "any recognition tied to referrals remains a future, gated concept." Recognition tied to referrals is LIVE: api-server/src/data/seasonConfig.ts lines 54-61 credits 'introduction-converted' → axis "connector", 500 XP, and lines 123-131 define the live Connector ladder quests (connector-active…summit + first-introduction) on metric 'introductions-converted'; featureStatus.ts has seasonRanking live since 2026-07-23 and seasonQuests live since 2026-07-24 ('the fed ladder + first-act quests, auto-credit'). Prod GET /api/season (2026-07-30) returns wallets with axes.connector credited (600 and 1100 XP) on the public board — a stranger can refute the sentence by opening /season.
- **Fix:** Rewrite currentState[2] and boundaries[4] in sourceAttributionTerminology.ts: the future-gated thing is the S3 season bounty MONEY (cite seasonBounty), while connector recognition (season XP + ladder titles) is live today; flip/retire sourceRewardConceptLifecycle accordingly and let guard-feature-truth pin the residual future claim to the seasonBounty key.

### [truth] /terms — Terms §8 claims the member-number↔wallet link is never published — our own public APIs and the chain publish it
- **Where:** artifacts/studio/src/pages/Terms.tsx:151
- **Evidence:** Terms.tsx:150-151: "we never publish a link between a member number and a wallet." Refuted three ways today: (1) GET https://thesyndicate.money/api/backbone/feed returns purchase items pairing both: {"memberNumber":14,"memberAddress":"0xea887557c78ec992913d3c31caa7aefe0cc35881"}; (2) GET https://thesyndicate.money/api/season standings pair {"display":"#1","seat":1,"wallet":"0x244531c571966f90f4849e03a507543d90f9c721"}; (3) the on-chain MembershipPurchasedV3 event itself carries memberNumber as its first data param beside the indexed buyer address (activityFeed.ts:32, transcribed from the pinned canon shape) — /activity renders "Member #N · 0x… entered the public registry." The chain publishes this link in every purchase receipt; no server-side design can un-publish it.
- **Fix:** New dated Terms version (the page's own §11 change law): align §8 with the settled 2026-07-25 address model — the number↔wallet pairing is public chain data, celebrated on /activity and /season; the red line we never hold or publish is name/alias/email. Same correction wave as the /privacy twin.


## IMPORTANT (37)

### [truth] /activity — Hero lead over-claims: 'every line below is an on-chain event … with its own verify link' — the Chronicle lane is not
- **Where:** C:\Users\kemal\OneDrive\Documents\GitHub\syndicate-os\artifacts\studio\src\pages\Activity.tsx:21
- **Evidence:** Activity.tsx:21 lead. But LiveActivityFeed.tsx:529-541 builds chronicle-entry lines from the committed register with the code's own admission 'a promotion is a founder COMMIT, not a chain event — no anchor exists and none is invented'; they render with an internal 'Record' link (readHref, LiveActivityFeed.tsx:929-935), no chain verify link, and a synthetic txHash. A visitor selecting the Chronicle facet refutes the lead on sight. (Milestones/deployments checked: real tx anchors — e.g. milestone seats-10 carries tx 0x58f4d5…9c46 in the prod feed — so the chronicle lane is the only counterexample.)
- **Fix:** Soften the universal: e.g. 'every chain line below is an on-chain event with its own verify link; Chronicle promotions link into the permanent record.' One clause keeps the lead irrefutable.

### [truth] /archive — Three prose museum/gallery future promises are unpinned and cite no registry key
- **Where:** C:\Users\kemal\OneDrive\Documents\GitHub\syndicate-os\artifacts\studio\src\pages\Archive.tsx:17
- **Evidence:** Archive.tsx carries three public future promises: lead :17 "The full museum surface is still being built."; :74-75 "What's still building is the museum surface itself — the full gallery is its own slice."; :81-82 "the gallery this page becomes is on its way." PROSE_CLAIM_SITES in guard-feature-truth.ts (:80-98) pins only LiveActivityFeed.tsx and FireLedger.tsx, and featureStatus.ts has no museum/gallery key (grep of the FUTURE section: none matches). Same defect class as the /activity note the guard's own header names as the hole it shipped with — a future promise in English, on a public footer page, standing outside the mechanism that exists to stop a claim outliving its slice.
- **Fix:** Register an `archiveGallery` future key in featureStatus.ts and pin Archive.tsx in PROSE_CLAIM_SITES with one of these fragments; the day the gallery ships, the guard forces all three sentences clean in the same commit.

### [harmony] /archive — Two section headings on one page answer the same question two ways; the second collapses into card-title size
- **Where:** C:\Users\kemal\OneDrive\Documents\GitHub\syndicate-os\artifacts\studio\src\pages\Archive.tsx:71
- **Evidence:** Archive.tsx:24 heads its first section with `type-h2` (serif, fluid), but :71 heads the second with `<h2 className="text-base font-medium text-foreground mb-1.5">` ("What's here today, what's still building") — 16px sans, the EXACT treatment of the page's own card h3 titles (:30 `text-base font-medium`). A page-level h2 rendered at the same size and weight as the h3s inside its cards erases hierarchy; the sibling /contracts page uses type-h2 for both of its section heads (ContractMemory.tsx:47, :70).
- **Fix:** Promote the :71 heading to `type-h2 text-foreground` (or at minimum type-h3) so the page has one heading system.

### [truth] /contracts — Treasury memory cards deny balances/addresses the same page shows live
- **Where:** C:\Users\kemal\OneDrive\Documents\GitHub\syndicate-os\artifacts\studio\src\config\contractMemory.ts:178
- **Evidence:** contractMemory.ts:178 'The treasury wallet role in canon. No address or balance is shown.' (same class at :187 'Structure only — no address, balance, or price.' and :196 'Wallet role funding protocol operations. No address or balance is shown.'). But ContractMemory.tsx:27 renders <ProtocolAssetsCard /> at the top of the SAME page, which prints the vault's live balances (prod GET /api/protocol/reality 2026-07-30: financial.vault.usdcBalance=7475000, avaxBalance=2734485733297386639, wethBalance=26551703798238159, btcbBalance=77818) and Operations USDC, each with a 'Verify on-chain' link to that very wallet's explorer address page (ProtocolAssetsCard.tsx:328-330). The denial is refuted two scrolls up on one screen, and it re-surfaces the address-hiding-as-virtue posture the 2026-07-24/25 address-model rulings killed.
- **Fix:** Rewrite the three treasury notes to point at the live layer instead of denying it, e.g. 'Its live balance and explorer link are shown in Protocol assets above.' Keep the memory cards structural without claiming the page hides what it shows.

### [truth] /contracts — Liquidity Pair card: 'No live price, reserve, or quote is read in this foundation' — the page reads the reserves live
- **Where:** C:\Users\kemal\OneDrive\Documents\GitHub\syndicate-os\artifacts\studio\src\config\contractMemory.ts:214
- **Evidence:** contractMemory.ts:214 note vs ProtocolAssetsCard.tsx:92-93 (financial.lp.reserveSyn / financial.lp.reserveUsdc read live) and :243 rendering 'SYN/USDC pool — {poolSyn} SYN + {poolUsdc} USDC' plus the protocol's LP share % — on the same /contracts page, above this card. Both reality ids are served in prod today. 'in this foundation' is also dead-era vocabulary (the read-only foundation era) surviving in public copy.
- **Fix:** Replace the note with the current truth: the pair's reserves and the protocol's own LP share ARE read live in Protocol assets above; the memory card carries only role/structure. Drop 'this foundation'.

### [truth] /faq — "Reach out through Support" routes to a channel that cannot receive anything
- **Where:** artifacts/studio/src/pages/Faq.tsx:136
- **Evidence:** Faq.tsx:135-136: "or reach out through <Link href=\"/support\">Support</Link>". The destination's own config (config/supportIntake.ts:35) says: "Support isn't open yet — nothing here sends or stores anything." — every channel is lifecycle PREVIEW (supportIntake.ts:41-62). So the FAQ instructs an action the linked surface immediately denies. Meanwhile the floating help assistant IS live on every page (PublicLayout.tsx:539 mounts <SyndicateGuide />), and the code comment Faq.tsx:128 still calls it "the floating support assistant is the next slice" — a fossil: the Guide shipped (components/guide/SyndicateGuide.tsx:15 "the global floating help assistant (Support slice)").
- **Fix:** Reword the CTA to what works today (the chain, the Guide, the live surfaces) or soften to "see where Support will live"; delete the stale next-slice comment.

### [truth] /faq — Four future-claims in FAQ copy stand outside the DONE-IS-DONE mechanism (no featureStatus key, not guard-pinned)
- **Where:** artifacts/studio/src/content/faq-content.ts:62
- **Evidence:** faq-content.ts carries user-visible future claims: :62 Signal Chamber "A planned future module…"; :225 Seat Record "A planned optional identity record… not yet deployed"; :199 institutional trust capital "Future recognition… Future records may reflect it"; :114 "visibility in future identity and verification modules as they ship". None has a key in config/featureStatus.ts (keys checked: no signalChamber/seatRecord/trustCapital entries), and guard-feature-truth.ts's PINNED_CLAIM_SITES/PROSE_CLAIM_SITES contain no entry for content/faq-content.ts (grep: no matches). All four are ACCURATE today (prod contracts group has no identity/governance/vesting contract), but they are exactly the fossil class the registry exists to kill: when any of these ships, no guard turns the surviving "planned" copy RED.
- **Fix:** Register keys (or reuse aliasLayer where it applies) and pin content/faq-content.ts in guard-feature-truth in the same slice.

### [harmony] /faq — WORK-FIRST: the finder tool opens below two full card blocks
- **Where:** artifacts/studio/src/pages/Faq.tsx:77
- **Evidence:** The page order (Faq.tsx:77-121) is: TransparencyPosture (4 manifesto cards, TransparencyPosture.tsx:13-34) → live-stat Card (2 stats + MembersProvenance + a routing paragraph, lines 81-110) → only then the Topics rail + FaqAccordion (the search box and questions the visitor came for). WORK-FIRST law §1-§2 (CLAUDE.md, permanent 2026-07-18): "The page opens on THE WORK… reference material… lives in a COLLAPSED expander at the BOTTOM — one click away, never in the way." On mobile width the search input is roughly two screens down. (The posture block is a deliberate suite pattern — Whitepaper and Docs carry it too — so any change should be suite-wide, but on FAQ specifically it stacks with a second pre-work card.)
- **Fix:** Open on the search + accordion; move the manifesto and the live-stat card below the corpus or into the rail.

### [harmony] /fire-ledger — The Proof of Burn rows still ship the bare verify string the ProofAnchor atom was extracted to kill
- **Where:** C:\Users\kemal\OneDrive\Documents\GitHub\syndicate-os\artifacts\studio\src\pages\FireLedger.tsx:158
- **Evidence:** FireLedger.tsx:153-162: <a className="inline-flex items-center gap-1 font-mono text-xs uppercase tracking-wider text-proof…">verify</a> — no min-height, ~16px tall against the 44px touch floor. ProofAnchor.tsx:7-10 (the house atom, 2026-07-26) names this exact surface: 'the first-visit review then found the SAME defect still standing on /fire-ledger … do not copy those classes a second time — extract them once.' Grep proves ProofAnchor is imported only by MilestonesPanel.tsx — /fire-ledger never adopted it. guard-touch-target.ts:157 still allowlists the page ('a text-[10px] explorer anchor, 15px') and that note is itself stale (the code now says text-xs).
- **Fix:** Replace the inline anchor with <ProofAnchor compact href={`${explorerBase}/tx/${b.transactionHash}`} /> and delete the guard-touch-target allowlist entry in the same slice (RED first: the entry should fail once the page is clean).

### [harmony] /fire-ledger — One page, two row grammars for the same burn line
- **Where:** C:\Users\kemal\OneDrive\Documents\GitHub\syndicate-os\artifacts\studio\src\pages\FireLedger.tsx:143
- **Evidence:** ProofOfBurnRecord's own rows (FireLedger.tsx:136-166): sentence at text-sm (14px), amount inline in the prose, meta at font-mono text-xs, tiny verify link. But when the served record is unavailable the SAME page renders burns through LiveActivityFeed onlyKinds=["burn"] (FireLedger.tsx:104), whose approved row grammar (mockup §②③, LiveActivityFeed.tsx:888/914) puts the sentence on type-body, the figure in its own right-aligned gold tabular-nums column, and a 44px proof anchor. The founder-approved feed geometry exists in the bundle and this page uses it only in its failure mode.
- **Fix:** Render the numbered record through the approved row grammar (either reuse renderLine's grid or restyle the record rows to type-body + amount column + ProofAnchor), keeping the #N pill and sender chip.

### [harmony] /fire-ledger — Banner engraves address-hiding ('never an address') against the settled address model and against /activity
- **Where:** C:\Users\kemal\OneDrive\Documents\GitHub\syndicate-os\artifacts\studio\src\pages\FireLedger.tsx:123
- **Evidence:** FireLedger.tsx:122-124 rendered banner: 'the sender is named as Founder or Community, never an address.' The settled ADDRESS MODEL (founder + legal research 2026-07-25) says an address is PUBLIC, shown short-form with an explorer link on every row, never hidden — and /activity's burn sentences already do exactly that for non-founder, non-organ senders (backboneFeedClient.ts:1076: `From ${line.actorShort}, retired to the burn address…`). Today all 9 prod burns are senderLabel=Founder so nothing is visibly masked yet, but the first community burn will render address-hidden here and address-shown on /activity — the same event, two policies.
- **Fix:** Align the record with the model: show the sender short-form + explorer link beside the Founder/Community chip, and rewrite the banner clause (the chip classifies, the address verifies).

### [harmony] /join — Five sub-floor type sites (10-11px) on the public money surface, including the chain-data verify links
- **Where:** C:\Users\kemal\OneDrive\Documents\GitHub\syndicate-os\artifacts\studio\src\pages\JoinProtocol.tsx:303
- **Evidence:** Line 303 AddressProof renders every money-path destination link at `font-mono text-[10px]` — the verify affordance a stranger must read beside each USDC amount; line 570 `<summary>` "Exact raw base units — verify" and line 573 the raw base-unit block are also `text-[10px]`; lines 368 and 451 render the referrer/split percentage labels at `text-[11px]`. All five sit under the ADR-001 12px readability floor (--text-caption: 0.75rem "READABILITY FLOOR ... nothing readable under 12px", index.css:234). They are known allowlisted debt in guard-type-scale.ts: `"pages/JoinProtocol.tsx": "5 · PUBLIC join page (the conversion surface) — deferred to the general sweep"` (line 191) — counted, but /join is the page where the debt bites a paying stranger.
- **Fix:** Pay the JoinProtocol allowlist entry: AddressProof and the raw-units block to .type-caption (12px mono), the two pct labels to text-xs, the summary label to .type-eyebrow; lower the guard ceiling from 5 to 0 in the same commit.

### [harmony] /learning — Section heading hand-set at text-lg sans instead of the family's .type-h2
- **Where:** C:\Users\kemal\OneDrive\Documents\GitHub\syndicate-os\artifacts\studio\src\pages\Learning.tsx:40
- **Evidence:** Learning.tsx:40: `<h2 className="text-lg font-medium text-foreground mb-1.5">` for "The Knowledge OS map" — a one-off 18px sans heading. The family convention for a section heading on these surfaces is the system class: Archive.tsx:24 `<h2 className="type-h2 text-foreground mb-5">` and ContractMemory.tsx:47 + :70 both `type-h2` (serif, fluid --text-h2 1.45-2.15rem, index.css:462). The type classes exist precisely so "chrome stops drifting into many one-off font sizes" (index.css:410-411).
- **Fix:** Replace with `type-h2 text-foreground` (matching Archive/ContractMemory rhythm), keeping the mb spacing.

### [harmony] /liquidity — WORK-FIRST inversion: three explainer cards sit above the action rail and the live pool figures
- **Where:** artifacts/studio/src/pages/Liquidity.tsx:139
- **Evidence:** Render order is WHY_LP_CARDS grid (lines 139-147) → <ActionRail/> (149) → <LpStatus/> (150); the lead (line 136) even announces the inversion: "Before any chart or reserve number — understand why a liquidity pool exists at all." The engraved WORK-FIRST law: "The page opens on THE WORK — the actions, decisions, and figures its user came for… The user NEVER scrolls past reference material to reach a button." Here the user scrolls past three reference cards to reach Trade/Add Liquidity/View Pool and the live reserves.
- **Fix:** Open on ActionRail + LpStatus; move the three 'Why LP matters' cards below (or into a collapsed expander at the bottom) and rewrite the lead accordingly.

### [harmony] /liquidity — Two h2 treatments on one page: 16px sans 'The pool, read live' vs serif type-h2 'Become an LP'
- **Where:** artifacts/studio/src/pages/Liquidity.tsx:108
- **Evidence:** Line 108: <h2 className="text-base font-medium text-foreground">The pool, read live</h2> (16px Work Sans) vs line 154: <h2 className="type-h2 text-foreground mb-2">Become an LP</h2> (fluid serif). Same rank, two answers to one question — against the type-role system (index.css: .type-h2 = serif headings). The h3s split the same way: line 143 'text-sm font-semibold' vs line 161 'text-base font-medium'.
- **Fix:** Give 'The pool, read live' the same section treatment as 'Become an LP' (type-h2, or demote both consistently); align the two h3 treatments to one class.

### [harmony] /liquidity — Five runs of readable copy at 11px, under the 12px floor — including the protective no-entitlement line
- **Where:** artifacts/studio/src/pages/Liquidity.tsx:91
- **Evidence:** text-[11px] at lines 74 (rail hints), 83 (Verify Pair hint), 91 (LP_NO_ENTITLEMENT_LINE — the full protective sentence 'Nothing is live or promised to liquidity providers…'), 114 (LpStatus row labels), 122 (the methodology note). ADR-001 floor: --text-caption 0.75rem, 'nothing readable under 12px'. Known allowlisted debt: guard-type-scale.ts line 185 pins "pages/Liquidity.tsx": "5 · PUBLIC liquidity page — deferred to the general sweep" — the founder asked for harmonization of exactly these footer pages, so the deferral is now due.
- **Fix:** Convert the five runs to text-xs / .type-caption (12px); ratchet the guard-type-scale debt entry from 5 to 0 in the same commit.

### [truth] /map — "Every signal the protocol serves publicly, in one place" — the page renders 5 of 7 served groups
- **Where:** artifacts/studio/src/pages/ProtocolMap.tsx:158
- **Evidence:** Lead (line 158) claims completeness, but MAP_GROUPS (line 24) = [chain, contracts, tokens, sale, source] while prod GET /api/protocol/reality (measured 2026-07-30) serves 7 groups: archive AND financial are missing from the page. The financial group alone carries ~37 public signals (vault/ops USDC, BTC.b/WETH.e/AVAX balances, LP reserves, burned SYN, Chainlink prices, financial.members.memberCount="14", referral totals, 7 distribution-wallet balances). Only the archive omission is declared (line 171); the financial omission — added to the API 2026-07-25 (d1b6e75) — is silent: the completeness claim rotted when the payload grew (heartbeat-completeness / Invariant-vs-State pattern).
- **Fix:** Either render the financial (and archive) groups or scope the lead honestly ("the protocol's identity, contract, token, sale and source signals"); never a universal the API refutes.

### [truth] /map — "Contract addresses stay server-side" contradicts the sibling registry note and the settled address model
- **Where:** artifacts/studio/src/pages/ProtocolMap.tsx:172
- **Evidence:** Line 172: "Contract addresses stay server-side; this page never connects a wallet and never sends a transaction." The /status registry served by the same API says the opposite is policy: token note — "token and contract addresses … leave the server only as explorer verify-links"; and the settled address law (2026-07-25) makes addresses PUBLIC (short-form + Snowtrace on every row; "an address is NEVER hidden or masked-as-security"). The studio ships explorer links via EXPLORER_BASE_URL/useAddressExplorerUrl in 8 files (season board, home, ProtocolAssetsCard, chronicle). The sentence presents address-hiding as a virtue — the reflex the founder ordered killed on sight.
- **Fix:** State the honest facts without the hiding frame: "This page renders no addresses; every fact is verifiable through its explorer link. It never connects a wallet and never sends a transaction."

### [truth] /map — Unpinned future claim: "that wiring is a future founder-approved slice"
- **Where:** artifacts/studio/src/pages/ProtocolMap.tsx:171
- **Evidence:** Line 171: "Archive artifacts are deliberately not bound on this page yet — that wiring is a future founder-approved slice." featureStatus.ts has NO key for archive-on-map wiring, and guard-feature-truth.ts has no ProtocolMap pin (grep 'ProtocolMap|future founder-approved' → no matches) — a user-visible future claim standing entirely outside the DONE-IS-DONE mechanism ("an unregistered new claim = RED"), the same class fixed 2026-07-26 for the /activity methodology note. Meanwhile the archive group ALREADY renders on the sibling /status (ProtocolRealityPanel default groups include "archive", ProtocolReality.tsx:185), with live values (archive.artifact.1.minted="11") — the wiring exists one footer link away.
- **Fix:** Bind the archive group on /map (it is one array entry) and delete the sentence, or register a featureStatus key and pin the claim in guard-feature-truth.

### [harmony] /map — No section scale on /map: its h2 renders at text-base, identical to the h3s under it
- **Where:** artifacts/studio/src/pages/ProtocolMap.tsx:123
- **Evidence:** <h2 className="text-base font-medium text-foreground">Membership engine — public figures</h2> (line 123) is the page's only h2 and renders at exactly the size of the group <h3 className="text-base font-medium"> titles below (ProtocolReality.tsx:204) — heading levels 2 and 3 are visually indistinguishable, and the page contains zero type-h2 while siblings mark sections with the serif scale (/proof lines 94 and 118, /status lines 163, 180, 291 all use type-h2).
- **Fix:** Put section headings on type-h2 ("Membership engine", "Live reality reads") so /map carries the same section rhythm as its siblings.

### [harmony] /map and /status — Two footer pages render near-identical reality tables with disagreeing self-descriptions
- **Where:** artifacts/studio/src/pages/ProtocolMap.tsx:24
- **Evidence:** /status renders ProtocolRealityPanel groups [chain, contracts, sale, tokens, archive] (ProtocolReality.tsx:185 default); /map renders [chain, contracts, tokens, sale, source] (line 24) plus the V3 band — 4 of 5 tables are the same content behind two footer links. Their claims disagree: /map's lead says "Every signal … in one place" (line 158) while its own footnote says "The authoritative wiring ledger for every module lives on the status hub" (lines 176-180). Neither page is the one authority — the twin-search disease (CHECK ①) at page scale.
- **Fix:** Give each page ONE job: /status = posture ledger + holder index + surface map (drop its full reality tables or collapse them), /map = the complete live reality read (all 7 groups).

### [harmony] /map and /status — Shared reality table sets multi-sentence reading copy at 11px and metadata at 10px
- **Where:** artifacts/studio/src/components/ProtocolReality.tsx:104
- **Evidence:** item.note — full-sentence prose (the sale note is two sentences) — renders at text-[11px] (line 104); failureReason at text-[10px] (62), sourceRef at text-[10px] (92), sourceType at text-[10px] (98), confidence at text-[11px] (101): all under the ADR-001 12px floor on two PUBLIC pages. The debt is allowlisted, not fixed: guard-type-scale.ts:174 "components/ProtocolReality.tsx: 8 · PUBLIC reality band — deferred to the general sweep", and guard-contrast-aa.ts:352 itself names "ProtocolReality.tsx:104 item note, 11px reading copy".
- **Fix:** Notes to text-sm; sourceRef/sourceType/failureReason to type-caption (12px token) — retiring 8 counted debts from the allowlist.

### [truth] /privacy — 'Two small preferences' in local storage — a fresh load writes seven keys before any wallet is connected
- **Where:** artifacts/studio/src/pages/Privacy.tsx:94
- **Evidence:** Privacy.tsx:94-97: "The site stores two small preferences in your own browser: your light/dark theme choice, and a flag noting the guide greeting was already shown." Measured localStorage on a fresh rig load with no wallet ever connected: ["@appkit/connection_status","rk-version","base-acc-sdk.store","syndicate_guide_prompted","@appkit/active_caip_network_id","@appkit/active_namespace","wagmi.store"] — the RainbowKit/wagmi/AppKit/Coinbase-SDK stack persists connection state client-side beyond the two named preferences (wagmiConfig.ts getDefaultConfig).
- **Fix:** Next version: keep the two-preferences sentence for our own writes and add one honest sentence that the wallet-connection libraries also keep their working state in the browser (never sent to us) — or scope wallet-stack storage explicitly under the Third parties section.

### [truth] /privacy — 'What our database holds' omits the activation-request record class (live since 2026-07-22)
- **Where:** artifacts/studio/src/pages/Privacy.tsx:113
- **Evidence:** The section (Privacy.tsx:112-140) inventories: wallet↔number pairing, on-chain mirror, operator registry, notifications+read state, referral channel counters. The database also holds activation_request rows (lib/db/src/schema/operatorWriteZone.ts:199-222): member_wallet (marked SERVER-ONLY), status WAITING/HOLD/DECLINED/CLOSED, asked_at, decided_at, decline_reason — member-keyed rows created when a member asks for referral activation (activationIntake live 2026-07-22 per featureStatus.ts:44), three days after this policy's version date. The page's opening promise is "says plainly what little exists".
- **Fix:** Add the record class to the inventory in the next policy version (a wallet-keyed request row with its decision and the sentence the member reads — nothing else).

### [truth] /recognition — The standing-figure FUTURE claim stands entirely outside the DONE-IS-DONE mechanism
- **Where:** C:\Users\kemal\OneDrive\Documents\GitHub\syndicate-os\artifacts\studio\src\pages\Recognition.tsx:39
- **Evidence:** Recognition.tsx:39 sets `lifecycle: "FUTURE"` (object-literal form) for "Standing over time … is not computed yet", and the lead (:48) repeats it in prose ("What is still to come is the long-term standing figure"). guard-feature-truth.ts:149 scans only the JSX-attribute form (`FUTURE_MARK = /lifecycle="FUTURE"/g`), Recognition.tsx appears in neither PINNED_CLAIM_SITES (:31-64) nor PROSE_CLAIM_SITES (:80-98), and featureStatus.ts contains NO key for the long-term standing model (the FUTURE section holds avatarUpload…chronicleCandidatePipeline; nothing covers standing). The claim is true today (truthStatus.ts:54-55: "Still honestly not-live: recognition (the standing model)"), but the law says an unregistered public future claim = RED — this is the exact class §⑥ was built for after the /activity methodology note (guard-feature-truth.ts:66-73), and this one is invisible to the guard via data indirection.
- **Fix:** Register a `standingModel` future key in featureStatus.ts, pin Recognition.tsx in PROSE_CLAIM_SITES (fragment from the lead) and extend FUTURE_MARK to also match the object-literal `lifecycle: "FUTURE"` form — RED-proven by reverting.

### [harmony] /referral — Off-system type on the anon program page: font-light tagline, prose set in mono, and a drifting card heading
- **Where:** artifacts/studio/src/pages/SourceAttribution.tsx:27
- **Evidence:** Line 27: <p className="text-lg text-foreground/90 font-light measure mb-12"> — font-light exists nowhere in the type roles (.type-body/.type-h3 own body/subhead weights). Line 35: <p className="font-mono text-xs text-primary mb-4">{referralProgram.poweredBy}</p> — 'Powered by Source Attribution' is prose/branding set in mono against the 'mono = data only' law (and hand-approximates an eyebrow without .type-eyebrow). Line 99: h3 'What it is — and is not' at text-base font-medium while the page's other section h3s (lines 50, 62, 73, 85) use type-h3.
- **Fix:** Tagline → type-body (or type-h3) at normal weight; poweredBy → .type-eyebrow; line 99 h3 → type-h3 or an explicit card-title convention shared with its siblings.

### [harmony] /source — WORK-FIRST violation: the page opens on a pitch card, the validate tool sits below the fold
- **Where:** artifacts/studio/src/pages/SourceLinkBuilder.tsx:140
- **Evidence:** DOM order: the 'A referral is an on-chain fact' Card (lines 140-168) precedes panel-link-builder (line 171). Measured on the rig: input-source-id top y=1031px at a 961x415 viewport and y=1365px at an 812px-high viewport - below the fold at both. The page's own copy names the split: 'How the referral program works explains it in full - this page is the validation tool' (lines 160-165). WORK-FIRST law section 1 + the 2026-07-16 referral ruling 'link first, pitch last'.
- **Fix:** Move the validate/build Card first; demote the explainer to a short line or a collapsed section below the tool (it duplicates /referral content the page already links).

### [truth] /status — "As of 2026-07-19" printed under a registry whose content describes 2026-07-25/27 reality
- **Where:** artifacts/api-server/src/data/sourceStatus.ts:97
- **Evidence:** CANON_AS_OF = "2026-07-19T00:00:00.000Z" (line 97) renders on /status as "As of 2026-07-19" (SystemStatus.tsx:205). But the same file's notes now claim capabilities that went live later: recognition — "the season board ranks contribution in public" (live 2026-07-23); treasury — "Treasury MOVEMENTS are now published per transaction on the activity record … including native AVAX" (the 2026-07-27 lane). git log: the file was edited by d1b6e75 (2026-07-25) and 44b24cd (2026-07-27) without bumping CANON_AS_OF. The honesty hub dates its own ledger 8+ days older than the facts inside it.
- **Fix:** Bump CANON_AS_OF in the same commit as any note edit (source-status-truth:guard could pin asOf >= last content change).

### [harmony] /status — The only footer page off the PublicPage shell — no eyebrow, no hero band, icon glued to the h1
- **Where:** artifacts/studio/src/pages/SystemStatus.tsx:150
- **Evidence:** Root is a raw <div className="w-full min-w-0 px-4 sm:px-6 lg:px-8 py-8"> with <Activity className="h-8 w-8" /> beside the type-h1 (lines 150-155), and a lead paragraph with neither type-body nor .measure (line 155-157). Every sibling footer page opens on the PublicPage hero — type-eyebrow label, bordered gradient band, type-body lead with .measure (/proof ProofDashboard.tsx:81, /map ProtocolMap.tsx:155, /contracts ContractMemory.tsx:24, /docs, /learning, /support). /status is the one footer door with a visibly different head — the loudest "not harmonized" symptom on this set.
- **Fix:** Wrap in PublicPage variant="app" (eyebrow e.g. "Public proof", title "System Status", lead = the honesty-hub sentence); drop the decorative icon.

### [harmony] /status — 8 sub-floor font sizes on the public status page (10-11px chips, provenance, table metadata)
- **Where:** artifacts/studio/src/pages/SystemStatus.tsx:81
- **Evidence:** font-mono text-[10px] on the VERIFIED/mode chips (lines 81, 84), era labels (94), the whole provenance strip (107 — chain, freeze block, build, snapshot hash), table sourceRef (255), Surface line (259), publicClass (264), and text-[11px] on Confidence (265) — all under the 12px floor. Declared deferred debt in guard-type-scale.ts:184: "pages/SystemStatus.tsx: 8 · PUBLIC status page — deferred to the general sweep (1 paid by the 2026-07-30 eyebrow conversion)".
- **Fix:** Chips and provenance to type-caption / type-eyebrow (the 2026-07-30 eyebrow-conversion pattern), ratcheting the allowlist toward zero.

### [harmony] /support — Support body copy runs 14px text-sm, one step below the reading system its sibling footer pages were corrected to
- **Where:** artifacts/studio/src/pages/Support.tsx:20
- **Evidence:** Measured on the rig at 961px: intro note, channel descriptions and triage descriptions all render 14px (Support.tsx:20, :31, :46 — all text-sm), while /terms and /privacy body measures 17.41px .type-body after commit 24131e7 fixed exactly this founder-named defect ("Terms/Risk/Privacy are one step SMALLER than every other reading surface"). guard-type-scale §⑦ pins only the 4 named pages, so /support was left behind; the page also has only one .measure column (measureCount=1, the hero lead) — its prose runs unmeasured.
- **Fix:** type-body + .measure on the page's prose (note, channel descriptions, triage list), and add /support to guard-type-scale §⑦'s pinned reading pages so the class cannot regress.

### [harmony] /support — WORK-FIRST violation: a help-seeker gets a dead preview and zero live channel — the page contains no links at all
- **Where:** artifacts/studio/src/pages/Support.tsx:16
- **Evidence:** Measured on the rig: the /support body contains zero anchors ("anchors":[]). Every element is a PREVIEW badge, future-tense copy ("What you'll be able to raise", "How requests would be triaged") or the note "Support isn't open yet — nothing here sends or stores anything." Meanwhile Terms §13 and Privacy Contact both state "Today's public channels are X (@TheSyndicateOne) and the official Telegram" — live channels that exist (brand.ts:63-79) but are unreachable from the one page whose work is getting help.
- **Fix:** Open the page on the work: a 'Reach us today' block linking X and the two official Telegram channels (from brand.ts, one source), with the preview/triage material below it — WORK-FIRST §1/§2.

### [harmony] /tokenomics — Tokenomics is the non-harmonized sibling: no rail, no posture block, fixed max-w-5xl page cap
- **Where:** artifacts/studio/src/pages/Tokenomics.tsx:76
- **Evidence:** Tokenomics.tsx:76 wraps everything in `<Prose className="max-w-5xl">` — a fixed ~1024px cap on the whole content region — with NO SectionIndex and NO TransparencyPosture, despite having 7 anchored sections. Its three living-chassis siblings all share the other pattern: Whitepaper.tsx:90-95 (`<TransparencyPosture />` + `lg:grid lg:grid-cols-[240px_minmax(0,1fr)]` + `<SectionIndex>` + `<Prose className="min-w-0">`), Faq.tsx:78+112-119 (same posture + 240px rail grid), Docs.tsx:42+75-76 (same). Prose.tsx:18-21 states its own law: "The container FILLS its region (never a lonely 68ch column with dead space — founder 2026-07-25)... cards, figures, tables and live-figure panels span the full region"; PublicPage.tsx:12-13: "Never a fixed page cap". The max-w-5xl cap re-creates dead width on wide screens and the missing rail breaks the suite's navigation rhythm.
- **Fix:** Give /tokenomics the sibling chassis: TransparencyPosture (or a deliberate, documented omission), the 240px SectionIndex rail over its 7 existing section ids, and drop max-w-5xl for min-w-0 inside the rail grid.

### [truth] /tokenomics — "Watch it vest on-chain" — but no vesting contract exists; the cliff/schedule is policy, not chain
- **Where:** artifacts/studio/src/pages/Tokenomics.tsx:177
- **Evidence:** Tokenomics.tsx:177-179 renders: "{founder?.note}" + "The wallet is public and its balance is read live above — you can watch it vest on-chain." The note (config/tokenomics.ts:32) is "12-month cliff, then 36-month monthly vesting. Public wallet, no hidden unlocks." Measured against prod: GET https://thesyndicate.money/api/protocol/reality contracts group lists exactly 8 contracts (SYN_TOKEN, USDC, 3× MEMBERSHIP_SALE, SOURCE_REGISTRY_V1, ARCHIVE_1155, LP_PAIR) — no vesting contract. The founder wallet is a plain wallet whose balance has already moved: FOUNDER balance = 119,968,728 SYN = 120,000,000 (12% design) − 31,272, exactly the live burn figure (financial.burn.synBalance = 31,272 SYN). A stranger can verify the balance but can NOT verify a cliff or a monthly schedule — nothing on-chain enforces either. The sentence invites on-chain verification the chain cannot give, under a hardcoded VERIFIED pill (line 166).
- **Fix:** Say what the chain proves: "the wallet is public — watch every movement on-chain"; state the cliff/vesting explicitly as a published commitment (policy), not as an observable mechanism, or deploy/point to a real vesting primitive.

### [truth] /toolkit — 'Every member action, in one place' is stale-complete: member acts shipped after the registry are absent
- **Where:** artifacts/studio/src/pages/MemberToolkit.tsx:18
- **Evidence:** Lead: 'Every member action, in one place - real acts only.' The registry (config/memberActions.ts) holds 5 actions and has no entry for the Ask-for-activation door (featureStatus.ts: activationIntake live since 2026-07-22 at /referral) nor the approval revoke (/wallet, whose header states 'this page hosts real signed member acts (revoke...)', founder GO 2026-07-17). A member can refute the completeness claim from the site itself.
- **Fix:** Add route entries for the activation ask (/referral) and the wallet approvals door (/wallet) to memberActions.ts, or soften the lead's completeness claim.

### [truth] /wallet — Approvals claim covers 'the protocol's known contracts' but exactly one spender is read (Sale V3)
- **Where:** artifacts/studio/src/wallet/MemberWalletPanel.tsx:231
- **Evidence:** MemberWallet.tsx line 27 lead: 'your own approvals toward the protocol's known contracts'; panel heading 'Approvals - known spenders'. The implementation reads ONE allowance: USDC -> Membership Sale V3 (lines 96-99, row at line 231), per its own comment 'Sale V3 today - the list grows only with known protocol spenders'. Prod /api/protocol/reality (read 2026-07-30) lists MEMBERSHIP_SALE (V1) and MEMBERSHIP_SALE_V2 code present as canon contracts - a standing USDC approval left on a retired sale (e.g. an abandoned checkout step-1) would never surface while the V3 row reads 'no standing approval - clean'. Scope: the structural gap is what is measured; whether any nonzero V1/V2 allowance exists today was not queried.
- **Fix:** Read the USDC allowance toward every canon sale contract (V1, V2b, V3) and render a row per nonzero spender; or scope the copy to the active sale.

### [truth] /whitepaper — §10 claims the module list 'flips from future to active by itself here' — but the list is hand-typed static prose
- **Where:** artifacts/studio/src/pages/Whitepaper.tsx:225
- **Evidence:** Lines 224-229: "Each carries a public status (active or future), read from the protocol's registry, so a new module flips from future to active by itself here — nothing is rewritten. Already active this way: the referral program, the live activity record, and the seasons recognition board. Known future modules: later eras · marketplace · the season reward pot." Nothing in Whitepaper.tsx reads any registry for §9/§10 — both lists are string literals; when the season pot goes live this sentence needs a hand edit, the exact fossil-generating shape DONE-IS-DONE exists to stop. §9 (line 217) makes the same static promise: 'each rendering live from the status registry once it activates'. The named lists are TRUE today (matches featureStatus), but the self-updating claim is refuted by the page's own code.
- **Fix:** Either render the active/future module names from featureStatus.ts (making the sentence true), or soften the copy to claim live status only where the page actually reads it.


## MINOR (35)

### [harmony] /activity — Date-group heading hand-types an eyebrow twin instead of the ONE section-label class
- **Where:** C:\Users\kemal\OneDrive\Documents\GitHub\syndicate-os\artifacts\studio\src\components\activity\LiveActivityFeed.tsx:1267
- **Evidence:** LiveActivityFeed.tsx:1267: `font-mono text-sm font-semibold uppercase tracking-[0.14em]` — a hand-rolled section label answering the question .type-eyebrow (index.css:474, --text-caption/0.14em) already answers, at a second size (14px vs 12px). The class's own header calls itself 'ONE decision, imported' after 25 hand-typed twins were killed 2026-07-27; this one survives because it sits above the floor.
- **Fix:** Either use .type-eyebrow, or if 14px is a deliberate feed-hierarchy choice, register the variant once (a class or documented exception) so the decision lives in one place.

### [harmony] /chronicle — Hand-typed eyebrow-shaped label escapes the ONE-section-label-class law under the guard's tracking bar
- **Where:** C:\Users\kemal\OneDrive\Documents\GitHub\syndicate-os\artifacts\studio\src\pages\ChronicleTeaser.tsx:105
- **Evidence:** Line 105: `<span className="font-mono uppercase tracking-wider text-proof">Verify · </span>` — mono + uppercase + wide-ish tracking, the eyebrow behaviour hand-typed instead of `.type-eyebrow`. guard-type-scale §⑥'s widened detector only fires at tracking ≥ 0.1em (`tracking-widest`/arbitrary; guard-type-scale.ts:279-285); `tracking-wider` is 0.05em, so nothing pins this instance — exactly the un-guarded remainder the 2026-07-30 widening comment defers ("the named narrower steps ... folding them is the type-scale slice's work"). Size is legal (inherits text-xs, 12px); the defect is the class duplication, repeated on all 15 entries.
- **Fix:** Replace the span's classes with `type-eyebrow text-proof` (tracking/case/weight then come from the one class).

### [truth] /chronicle — Dead teaser branch still declares the live Chronicle FUTURE (unreachable today, a DONE-IS-DONE fossil-in-waiting)
- **Where:** C:\Users\kemal\OneDrive\Documents\GitHub\syndicate-os\artifacts\studio\src\pages\ChronicleTeaser.tsx:21
- **Evidence:** Lines 16-37: the TeaserSpec carries `lifecycle: "FUTURE"` (line 21) and "The first entry awaits the founder's promotion" (line 29), rendered only `if (CHRONICLE_REGISTER.length === 0)` — the register carries 15 founder-promoted entries (chronicleRegister.ts), so the branch cannot render and no visitor sees the falsehood. But the Chronicle is LIVE and public (featureStatus.ts:119 calls it "LIVE and public"); if the branch ever rendered it would be the exact Settings-Notifications fossil class, and its previewRows also name only the first 4 entries of a 15-entry record.
- **Fix:** Delete the empty-register teaser branch and the spec (the register is append-only via founder commits; an empty state is unreachable by construction), or re-true the spec if the fallback must stay.

### [harmony] /contracts — Sub-floor reading copy on /contracts (11px methodology paragraph; 10-11px across the reality tables)
- **Where:** C:\Users\kemal\OneDrive\Documents\GitHub\syndicate-os\artifacts\studio\src\components\ProtocolAssetsCard.tsx:286
- **Evidence:** ProtocolAssetsCard.tsx:286-291 — a four-sentence valuation-methodology paragraph at text-[11px], plus :269/:325/:327 (slogan, ≈USD, per-row meta at 11px), under the 12px ADR-001 floor. Same disease in the Live-reality tables the page hosts: ProtocolReality.tsx:62 failure reasons at text-[10px], :92/:98 source refs at 10px, :101/:104 per-row notes at 11px. Both files are REGISTERED debt in guard-type-scale.ts:174/176 ('deferred to the general sweep'), so this is a ratchet reminder, not an unguarded leak — but it is live public reading copy below the floor today.
- **Fix:** When the general sweep reaches these files: promote reading copy to text-xs/text-sm tokens and burn the two allowlist entries.

### [harmony] /contracts — Mono face on prose role lines (mono = data only)
- **Where:** C:\Users\kemal\OneDrive\Documents\GitHub\syndicate-os\artifacts\studio\src\pages\ContractMemory.tsx:56
- **Evidence:** ContractMemory.tsx:56: <p className="text-xs font-mono …">{e.role}</p> renders prose phrases ('Settlement stablecoin', 'Superseded membership sale', 'Fixed proceeds split') in the data voice. House law: mono = data only; these are descriptions, not values.
- **Fix:** Drop font-mono (text-xs muted sans), or convert the role into a real label via .type-eyebrow if it is meant as a card eyebrow.

### [harmony] /docs — Journey step numerals at text-[10px] — under the 12px readability floor
- **Where:** artifacts/studio/src/pages/Docs.tsx:58
- **Evidence:** Docs.tsx:58: `className="grid h-5 w-5 shrink-0 place-items-center rounded-full border border-border font-mono text-[10px] text-primary"` — 10px mono numerals on a public page; ADR-001's floor is 12px (index.css:234 "READABILITY FLOOR… nothing readable under 12px"). This is the exact named CLAUDE.md failure class ("a text-[10px] copied from an existing component instead of the token, under the 12px floor"). It is declared debt in guard-type-scale.ts:186 ("pages/Docs.tsx": "1 · PUBLIC docs…"), so the build stays green — but it renders sub-floor today.
- **Fix:** Use the type-caption/12px token (the circle is h-5 w-5 = 20px; a 12px numeral fits).

### [harmony] /docs — Closing-CTA links lack the focus-visible ring the FAQ's identical links carry
- **Where:** artifacts/studio/src/pages/Docs.tsx:131
- **Evidence:** Docs.tsx:131-135 ("Where to next" Learn/Status/Take-a-seat links): `className="text-primary underline decoration-primary/40 underline-offset-2 hover:decoration-primary"` — no focus-visible classes. Faq.tsx:27-28 solved the same problem with a shared linkCls that includes `focus-visible:ring-2 focus-visible:ring-ring…` precisely because these Cards sit outside the <Prose> subtree. Declared debt in guard-focus-visible.ts:156 ("pages/Docs.tsx": "3 · 3 docs <Link>s outside the <Prose> subtree"). Keyboard users get no ring on /docs but do on /faq — sibling disharmony on the same element class.
- **Fix:** Import/share the FAQ's linkCls constant (one decision, one place — CLAUDE.md check ①).

### [harmony] /docs — Docs hero drops the LivingSignature badge its siblings carry — and its own header comment claims it
- **Where:** artifacts/studio/src/pages/Docs.tsx:34
- **Evidence:** Docs.tsx:35-40 passes no `badge` prop to PublicPage, while Tokenomics.tsx:74, Faq.tsx:73 and Whitepaper.tsx:87 all pass `badge={<LivingSignature />}`. The file's own header comment (Docs.tsx:13-14) claims the page is "Composed from the living chassis (PublicPage + LivingSignature + TransparencyPosture + SectionIndex)" — LivingSignature is not even imported. Either the badge belongs on the hero (harmony) or the comment is false (drift).
- **Fix:** Add the LivingSignature badge, or correct the comment with a dated reason for the omission.

### [truth] /docs — "Eight topics" — a typed count against the file's own NUMBER-FREE doctrine
- **Where:** artifacts/studio/src/content/docs-content.ts:71
- **Evidence:** docs-content.ts:71 (the /faq card): "Honest answers across the eight topics…" — accurate today (FAQ_CATEGORIES has exactly 8 categories: basics, syn, membership, vault, liquidity, ranks, archive, risk), but the same file's doctrine header (line 12) states "NUMBER-FREE. Purposes carry zero figures". Adding or merging a category silently falsifies this card; no guard counts it. Also note the /support card (line 174, "nothing is stored until intake is wired") is a future claim with no featureStatus key and no guard-feature-truth pin — accurate today (supportIntake.ts:35 confirms) but in the same unpinned class as the FAQ items.
- **Fix:** "across every topic" — and pin the /support card's claim to a registered key when the intake slice is scheduled.

### [truth] /faq — "Twelve rungs" is a typed figure on a page titled "never a number we typed"
- **Where:** artifacts/studio/src/content/faq-content.ts:195
- **Evidence:** faq-content.ts:195: "The capital axis names a seat's cumulative purchase footprint across twelve rungs, from Citizen to Monolith." Verified accurate today: CAPITAL_AXIS_LADDER (api-server/src/backbone/capitalAxisReadmodel.ts:59-72) has exactly 12 entries, Citizen→Monolith; the Connector ladder claim "Emerging to Summit" also matches (connectorLadder.ts:24-32). But the page lead (Faq.tsx:72) promises answers hold "no figures of its own" and the corpus doctrine (faq-content.ts:8) says "Answers carry ZERO numerals" — a spelled-out count dodges the letter and breaks the spirit: if a rung is ever added, this sentence rots silently with no guard.
- **Fix:** Drop the count ("across its rungs, from Citizen to Monolith") or derive the wording from the ladder module.

### [harmony] /join — Footer wayfinding word drift: 'Join' lands on eyebrow 'Membership'; 'Membership' lands on eyebrow 'Member Home'
- **Where:** C:\Users\kemal\OneDrive\Documents\GitHub\syndicate-os\artifacts\studio\src\pages\JoinProtocol.tsx:710
- **Evidence:** Footer group "Membership" (navigation.ts:72) links module `join` labeled "Join" (modules.ts:199) → JoinProtocol renders `eyebrow="Membership"` (JoinProtocol.tsx:710), while the sibling link labeled "Membership" (modules.ts:180, also the header primary label, navigation.ts:29) → /member renders eyebrow "Member Home" (syndicateFacts.ts:196). Three names rotate across the same two doors: the page wearing the "Membership" eyebrow is not the page the "Membership" links open.
- **Fix:** Align the /join eyebrow with its own footer/header identity (e.g. eyebrow "Join") or relabel the nav pair once in modules.ts — one word per door, imported everywhere (Four-Checks §④).

### [truth] /learning — "Every figure on this site is a live chain read or a line of the indexed public record" upgrades a category the site itself contradicts
- **Where:** C:\Users\kemal\OneDrive\Documents\GitHub\syndicate-os\artifacts\studio\src\config\learningModules.ts:59
- **Evidence:** learningModules.ts:59 (rendered as a topic on /learning): "Every figure on this site is a live chain read or a line of the indexed public record — each with its verify path." and :29: "every figure on this site is read from the chain". But the site's own /contracts surface prints figures it explicitly labels otherwise: contractMemory.ts:169 "Membership proceeds follow a fixed on-chain split — 70% protocol vault, 20% liquidity, 10% operations. Recorded canon, not a live read". The split is real and bytecode-enforced (so no stranger can refute the FIGURE), but the sentence upgrades recorded-canon figures to "live chain read" — the exact never-upgrade-a-fact's-category rule (verification protocol §②).
- **Fix:** Soften to the honest triple: "a live chain read, a line of the indexed public record, or recorded canon the chain enforces — each labelled".

### [harmony] /learning — Step-number chip at text-[11px] sits under the 12px readability floor (declared debt)
- **Where:** C:\Users\kemal\OneDrive\Documents\GitHub\syndicate-os\artifacts\studio\src\pages\Learning.tsx:51
- **Evidence:** Learning.tsx:51: `font-mono text-[11px] text-primary` on the Knowledge-OS step number — under the ADR-001 floor (index.css:234 "READABILITY FLOOR … nothing readable under 12px"). It is counted debt: guard-type-scale.ts:194 allowlists `"pages/Learning.tsx": "1 · PUBLIC learning page — deferred to the general sweep"` — reported so the sweep does not forget a PUBLIC page carries it.
- **Fix:** Swap to `type-caption` / text-xs (12px) and clear the allowlist entry in the same commit (ratchet down).

### [harmony] /learning — Same-page card prose at two sizes: step details read at 12px while module topics read at 14px
- **Where:** C:\Users\kemal\OneDrive\Documents\GitHub\syndicate-os\artifacts\studio\src\pages\Learning.tsx:61
- **Evidence:** Learning.tsx:61: step detail prose is `text-xs` (12px, the caption floor) while the same page's module-card reading copy is `text-sm` (:29) — and every sibling card body on /recognition (:57) and /archive (:35) is text-sm. Multi-sentence reading copy on the education page — the surface whose whole job is reading — rendered at the floor size the system reserves for captions.
- **Fix:** Raise the step `detail` paragraphs to text-sm to match the page's own module cards.

### [truth] /learning — Dead-era header comment contradicts the live intro two paragraphs below it
- **Where:** C:\Users\kemal\OneDrive\Documents\GitHub\syndicate-os\artifacts\studio\src\config\learningModules.ts:5
- **Evidence:** learningModules.ts:5-6 header: "It describes systems that are mostly not wired yet, so the page links to /status for the authoritative wiring ledger." — while :19 in the same file ships "the protocol is live: seats sell on-chain today" (verified live: prod /api/protocol/reality reads sale.MEMBERSHIP_SALE_V3.paused=false, concluded=false, receiptCount=12, memberCount=14). Comment-only (never rendered), but it is the registry-fossil class the featureStatus header warns about: a wrong line in a file every session reads first propagates.
- **Fix:** Strike the sentence, dated, in the next slice that touches the file.

### [truth] /liquidity — Stale DEX brand: copy says 'Trader Joe' while traderjoexyz.com now redirects to lfj.gg
- **Where:** artifacts/studio/src/pages/Liquidity.tsx:31
- **Evidence:** Measured this session: the tradeUrl 301-redirects to https://lfj.gg/avalanche/pool/v1/…?utm_campaign=redirect (curl -L, 2026-07-30); the /add URL resolves 200 at lfj.gg. Copy still names the old brand: line 31 hint 'Swap on Trader Joe', lines 157-158 'the pair on Trader Joe', and Whitepaper.tsx line 192 'the market price from the live Trader Joe pool'. Links WORK (the 403s on dexscreener/lfj bare GETs are bot filtering, documented in liquidityPool.ts) — but a visitor clicking lands on a site that nowhere says 'Trader Joe'. Scope: curl-level check only; in-browser render not verified.
- **Fix:** Say 'LFJ (formerly Trader Joe)' once, then 'LFJ'; point the URLs at lfj.gg directly to drop the redirect hop.

### [harmony] /liquidity — The rail's primary actions are styled as 12px mono eyebrow labels, not as the Button atom
- **Where:** artifacts/studio/src/pages/Liquidity.tsx:66
- **Evidence:** Line 66: the action label ('Trade SYN' — the page's primary CTA) renders in <span className="type-eyebrow…"> — the section-label class (12px mono uppercase, per index.css: 'the section label above a heading, a card title or a data group'), inside a hand-rolled <a> tile rather than the Button atom the rest of the site uses for primary actions.
- **Fix:** Keep the tile layout if approved, but promote the label to type-label/Button typography so the page's one primary action does not read at caption scale.

### [harmony] /liquidity — Twin fact: the LP pair address is hardcoded client-side while the same fact is served by verify-links
- **Where:** artifacts/studio/src/config/liquidityPool.ts:25
- **Evidence:** liquidityPool.ts line 25: LP_PAIR_ADDRESS = "0xe12491b79c9cfc6a07db8cd7fc8b3da0bb019389" (for the DexScreener URL) — the identical address is served by GET /api/protocol/verify-links lpPair (measured 2026-07-30: https://snowtrace.io/address/0xe12491…9389). They match today; the file's own header (lines 16-17) states the principle 'never hardcoded client-side — resolved at render' for the Verify tile while line 25 keeps the second copy. Check-① pattern: one fact, two homes — a future pair migration updates one and not the other.
- **Fix:** Derive the DexScreener URL from the server verify-links payload (or a shared canon import) so the pair address lives in one place.

### [harmony] /map — FigureRow prints wire ids, derivation tags and raw strings at 10px mono — and as primary public UI text
- **Where:** artifacts/studio/src/pages/ProtocolMap.tsx:67
- **Evidence:** 4 allowlisted sub-floor sizes (guard-type-scale.ts:193 "4 · PUBLIC protocol map"): wireId at text-[10px] (line 67), failureReason (74), "derived · raw ÷ 10^6" (82), "exact raw · 60000000000..." (87). The wire ids themselves ("sale.MEMBERSHIP_SALE_V3.totalGrossUsdc") are internal field paths rendered to visitors — the human-words law keeps that vocabulary in code.
- **Fix:** type-caption for the metadata lines; consider dropping the wire id from the public row (the label already names the figure).

### [harmony] /map — Public copy speaks infrastructure jargon: "Public proof organism", "server-side spine", "vendored canon"
- **Where:** artifacts/studio/src/pages/ProtocolMap.tsx:156
- **Evidence:** Eyebrow "Public proof organism" (line 156); lead "…read by a server-side spine and reconciled against vendored canon" (line 158). Compare /proof's lead, which says the same thing in human words ("Every figure on this site is a live chain read or a line of the indexed public record"). The 2026-07-17 truth sweep killed developer-speak site-wide; "vendored canon" is repo vocabulary a visitor cannot parse.
- **Fix:** Eyebrow "Public proof"; lead in plain English ("read live by our own server and checked against the pinned contract records").

### [truth] /privacy — 'No member directory' overclaims now that /season publicly lists every earning member's wallet, ranked with seat numbers
- **Where:** artifacts/studio/src/pages/Privacy.tsx:43
- **Evidence:** Privacy.tsx:42-44 ("The short version"): "no member directory." Since 2026-07-23 (featureStatus.ts:49 seasonRanking live) GET https://thesyndicate.money/api/season serves per-member rows: {"display":"#1","seat":1,"wallet":"0x244531c571966f90f4849e03a507543d90f9c721","explorerUrl":"https://snowtrace.io/…"} — a public, ranked member listing. Under the settled address model this board is canon and an address is not identity, so the intended meaning (no identity directory) is sound — but the flat sentence is contestable on a precision page.
- **Fix:** Next version wording: "no identity directory — no names, no emails; member wallets and seat numbers are public chain data and appear on the public boards."

### [truth] /proof — "Every figure is read live from a contract or the indexed event record" — sibling proof figures are static snapshots
- **Where:** artifacts/studio/src/pages/ProofDashboard.tsx:65
- **Evidence:** Step 01 (line 65) claims every figure is read live. One click away, /status states its holder-index figures come from "a static, hash-pinned snapshot" (SystemStatus.tsx:36-38) — GET /api/holder-index provenance.builtAt = 2026-07-16, mode READ_ONLY_HOLDER_INDEX_AGGREGATES — and the source-status registry is generatedBy "static-canon". The counts DO derive from the indexed record and are currently in sync (snapshot memberTotal 14 = live financial.members.memberCount "14", measured 2026-07-30), but "read live" is the loose word: they are built offline and served static.
- **Fix:** "Every figure is read live from a contract or served from the verified indexed record — never typed in by hand."

### [harmony] /referral — Duplicate heading: 'How it works' appears as an h2 and again as an h3 twenty lines later
- **Where:** artifacts/studio/src/pages/SourceAttribution.tsx:50
- **Evidence:** Line 32: <h2 className="type-h2…">How it works</h2> then line 50: <h3 className="type-h3…">How it works</h3> — identical text at two ranks in the same section, separated only by the status card.
- **Fix:** Rename the h2 to the program-level frame (e.g. 'The program, live') or drop the h3 and let the six steps sit under the h2.

### [harmony] /source — Built share link uses window.location.origin while every other share surface pins the canonical origin (twin)
- **Where:** artifacts/studio/src/pages/SourceLinkBuilder.tsx:77
- **Evidence:** Line 77: const link = `${window.location.origin}/join?source=${sourceId}`. Every sibling share site hardcodes the canonical: ReferralLinkHero.tsx:44, ReferralLinkPanel.tsx:97, ReferralToolsPanel.tsx:342, ReceiptTicket.tsx:87, MemberQuickActions.tsx:106 all build 'https://thesyndicate.money/join?source='; CANONICAL_ORIGIN exists at seo-route-registry.ts:122. Off-prod (rig/preview) /source hands out a localhost share link - two answers to one question (Check 1/4 class).
- **Fix:** Import one canonical join-link builder shared with the referral surfaces.

### [truth] /status — "This payload itself carries no member data" — the payload carries the live member tally block
- **Where:** artifacts/studio/src/pages/SystemStatus.tsx:169
- **Evidence:** Intro (line 168-170): "This payload itself carries no member data and no wallet addresses." Measured against prod GET /api/protocol/reality (2026-07-30): zero 0x address tokens in the payload (the addresses half is TRUE), but the payload contains financial.members.memberCount="14", financial.members.distinctWallets="13", financial.members.seatOverlap="1" — aggregate member figures a reader finds by opening the very API the sentence describes.
- **Fix:** "…carries no member-identifying data and no wallet addresses" — the exact category the registry's own membership note uses.

### [harmony] /status — WORK-FIRST: a ~90-word meta paragraph stands between the title and the first table
- **Where:** artifacts/studio/src/pages/SystemStatus.tsx:164
- **Evidence:** Lines 164-173: the "Protocol reality" section opens with a 6-line explanation (what the payload is, what it is not, where joining happens, how failures render) before any data; the mono MetaStrip follows, then the first table. WORK-FIRST §2: reference material lives collapsed or below — the reader came for the live/pending ledger, and every sibling section intro on this page is 1-2 lines.
- **Fix:** Cut to one sentence ("Live reads of public chain facts — anything unverifiable says so, with the reason") and move the caveats into a collapsed note at the bottom.

### [truth] /support — The page's future claims stand outside the DONE-IS-DONE registry — no support key exists, so no guard can catch the fossil
- **Where:** artifacts/studio/src/pages/Support.tsx:23
- **Evidence:** featureStatus.ts contains no support/supportIntake key (grep: zero matches), and guard-feature-truth.ts has no Support entry (grep "support": no matches), yet the page carries user-visible future claims: "What you'll be able to raise" (Support.tsx:23), "once support opens" (supportIntake.ts:34), "isn't open yet" (supportIntake.ts:35, :54, :60) and four PREVIEW lifecycle badges. The claims are TRUE today (no support channel is live), but this is the exact unpinned-claim class the 2026-07-26 /activity registration closed (featureStatus.ts:104-121).
- **Fix:** Register a supportIntake future key in featureStatus.ts and pin the Support claim sites in guard-feature-truth, so the day support goes live turns every surviving 'isn't open yet' RED.

### [truth] /terms — 'The official Telegram' (singular) on both legal contact sections — the brand maintains two distinct official Telegram channels
- **Where:** artifacts/studio/src/pages/Terms.tsx:194
- **Evidence:** Terms.tsx:193-195 and Privacy.tsx:180-182 both say "X (@TheSyndicateOne) and the official Telegram." brand.ts:68-79 deliberately keeps TWO channels with distinct labels — "Telegram — Announcements" (t.me/TheSyndicateOfficial) and "Telegram — Community" (t.me/TheSyndicateMoney) — the comment at brand.ts:40-41 says the labels are deliberately distinct. A legal contact line pointing at an unnamed singular channel is ambiguous and impersonation-friendly.
- **Fix:** Name the exact channel(s) in the next version of both texts, sourced from brand.ts (one source, imported wording).

### [harmony] /terms — Closing colophon drops off-system to 14px unmeasured while all body copy above runs type-body + measure
- **Where:** artifacts/studio/src/pages/Terms.tsx:200
- **Evidence:** Terms.tsx:200: <p className="text-sm text-muted-foreground border-t border-border/40 pt-4"> ("Published by The Syndicate…") — measured 14px, no .measure, directly under 13 sections whose bodies measured 17.41px inside 68ch columns.
- **Fix:** type-body (or type-label if meant as a colophon) + .measure, matching the page's own reading system.

### [truth] /tokenomics — All 7 section pills hardcode VERIFIED — never derived from the read
- **Where:** artifacts/studio/src/pages/Tokenomics.tsx:77
- **Evidence:** Every ProseSection passes a literal: `eyebrow={<Eyebrow n={1} status="VERIFIED" />}` (lines 77, 92, 103, 139, 157, 166, 183). The status is a static string, not derived from the live read: when the RPC fails, the page shows VERIFIED headers directly above "PENDING · live read" pills (Pending(), lines 22-28) — a self-contradiction. Same pattern on Whitepaper.tsx:96-250 (12× "VERIFIED"), and the Eyebrow helper itself is a re-derived local twin of Whitepaper's (Tokenomics.tsx:38-47 vs Whitepaper.tsx:56-65 — the Whitepaper copy also handles FUTURE tone; CLAUDE.md check ④ IMPORT, NEVER RE-DERIVE). Prod reads verified working today (all figure ids resolve), so nothing false is currently rendered.
- **Fix:** One shared Eyebrow atom whose status derives from the section's own read state (VERIFIED when data resolved, CHECKING/UNAVAILABLE otherwise).

### [truth] /toolkit — LIVE_ACTION badge ('Live - signed from your wallet') on a page hosting no wallet-signed act
- **Where:** artifacts/studio/src/pages/MemberToolkit.tsx:19
- **Evidence:** Badge renders 'Live - signed from your wallet' (truthStatus.ts line 148). The page's actions are copy-link, copy-proof-link, two route links and a public engine read - none is a signature. truthStatus.ts lines 115-118: 'EXACTLY ONE lifecycle - LIVE_ACTION ... the /join checkout'. /wallet's header carries a founder GO for its revoke (MemberWallet.tsx lines 6-10); MemberToolkit.tsx carries no such sanction.
- **Fix:** Use READ_ONLY_PROOF (or document a founder GO in the file header if the badge is intended).

### [truth] /toolkit — 'Derived from your wallet - the same link forever' states an invariant the resolver does not guarantee
- **Where:** artifacts/studio/src/config/memberActions.ts:35
- **Evidence:** Note: 'Your permanent link, derived from your wallet - the same link forever.' The handler (MemberQuickActions.tsx lines 98-106) copies payingSourceId, which per sourceIdentity.ts lines 20-24 returns 'the auth readback's resolved sourceIdHex when present (canonical or founder-signed alike)' - a founder-signed source is not wallet-derived, and a paying-source change changes the copied link.
- **Fix:** Say 'the link that pays you' rather than 'derived from your wallet - the same link forever'.

### [harmony] /wallet — Revoke confirmation renders the tx hash short-form with no Snowtrace link
- **Where:** artifacts/studio/src/wallet/MemberWalletPanel.tsx:267
- **Evidence:** Line 264-268: 'Revoked - your own signed transaction: {lastTx.slice(0, 10)}...{lastTx.slice(-6)}' - plain text, no explorer anchor. The settled address model (2026-07-25) requires short-form + an explorer (Snowtrace) link on every chain-data row. ExternalLink is imported at line 20 and never used in the file - a fossil of the missing link.
- **Fix:** Wrap the short hash in an explorer tx link via the central EXPLORER_BASE_URL; the unused ExternalLink import is the icon for it.

### [harmony] /wallet — Two-sentence readable copy at text-[11px], under the 12px readability floor
- **Where:** artifacts/studio/src/wallet/MemberWalletPanel.tsx:278
- **Evidence:** Line 278: className="text-[11px] text-muted-foreground" on the pool-pointer prose ('Why the pool exists, its live reserves...'). index.css line 234: 'READABILITY FLOOR (ADR-001 2026-07-16): nothing readable under 12px'. Registered as deferred debt in guard-type-scale.ts line 201 ('a single 11px occurrence; deferred to the sweep') - known, but live copy on a member page.
- **Fix:** text-xs (12px) - a one-token change already counted in the sweep debt.

### [harmony] /wallet — Doc-vs-registry conflict: account surfaces declared 'not an SEO page' yet both are INDEX + sitemap
- **Where:** artifacts/studio/src/components/member/MemberAppPage.tsx:23
- **Evidence:** MemberAppPage.tsx lines 23-26: '"account" - a PRIVATE account/dashboard surface (/wallet, /toolkit): not an SEO page, so it resolves to the WORK layout'. seo-route-registry.ts registers /wallet (lines 304-313) and /toolkit (lines 376-385) as indexStatus INDEX, sitemap: true. Two authorities answer one question differently; the account resolve default (checking -> work layout, no h1) means a prerender snapshot taken before session settle carries no on-page title text for indexed pages.
- **Fix:** Pick one truth: either NOINDEX the two account doors, or make the account prerender resolve to the public hero like content doors.

