# MEMBER HOME — TOTAL AUDIT (2026-07-16)

> Ultracode fleet: 9 lenses (design inventory · origin harvest · human readability · data truth · flows · frontend quality · a11y · doctrine/legal · session ledger) + adversarial verification. 35 agents · 170 raw findings · 24 adversarially CONFIRMED · 2 verifiers lost to quota (findings kept as UNVERIFIED-LIKELY). Founder order: 'check ALL, more than what I saw'.

## A. CONFIRMED (verified against the working tree)

### A0. [defect-now · data-truth] SYN balance renders up to 18 raw decimals — violates the READABILITY FLOOR's human-format rule
- **Where:** artifacts/studio/src/lib/rawUnits.ts:35 · lens: design-inventory
- **Detail:** Finding accurate as stated; one addition: the same 18-decimal formatRawUnits path also feeds MemberWalletPanel.tsx (line 94 formats, line 159 renders completely untruncated), so the /member wallet panel is the worse of the two surfaces — the KPI tile at least CSS-clips (with ellipsis, which is still arbitrary clipping, not formatting). JoinCheckout.tsx:449 and JoinProtocol.tsx use the same formatter for SYN quote amounts (usually round, lower severity). Proposed fix direction (display-precision policy with raw string retained for verifiers) is consistent with the rawUnits doctrine header comment.
- **Verifier:** CONFIRMED. formatRawUnits (artifacts/studio/src/lib/rawUnits.ts:35-43) only strips trailing zeros (.replace(/0+$/, "")); ownReads.ts:35 calls formatRawUnits(raw.toString(), 18) for the own-SYN balance. Two live render surfaces: MemberKpiRow.tsx:99 ("Your SYN" KPI tile — its CSS `truncate` class clips with ellipsis at arbitrary width, which is not human formatting) and MemberWalletPanel.tsx:94→159, which renders the full string with NO truncation whatsoever. The founder flag is real: docs/SESSION_STATE.md line 15 records "18-decimal SYN balance must format human" from a prod screenshot (S7-e follow-up). Violates the READABILITY FLOOR's explicit human-format clause.

### A1. [data-slice · capital-axis] Genesis/V1 seats' capital footprint shows '—' while the V1 purchase data exists on-chain
- **Where:** artifacts/api-server/src/routes/capitalStanding.ts:83 · lens: design-inventory
- **Detail:** DEFECT LIVE TODAY per the LIVE-PRODUCTION RULE, founder-flagged at the S7-b seal (docs/SESSION_STATE.md:17-19: genesis footprints "show '—' while THE REAL OLD ON-CHAIN DATA EXISTS (V1 purchases × frozen roster) — must be derived, never dashed"). The capital-standing route (artifacts/api-server/src/routes/capitalStanding.ts:83) and its builder (artifacts/api-server/src/backbone/capitalAxisReadmodel.ts:194-199) exclude V1 rows because purchase events carry no seat ordinal, so seats #1-#8 (incl. the founder's seat #1) get rung=null. The "Your footprint" KPI tile (artifacts/studio/src/wallet/MemberKpiRow.tsx:48) then renders a literal "—". CORRECTION to the original detail: CapitalAxisCard does NOT render nothing — for an unwalked seat it renders a fallback paragraph (CapitalAxisCard.tsx:128-131: "Your early-era footprint predates the walked record — honestly not derivable, never guessed") plus ladder and shield line, with no amount and no rung pill; that copy is itself a truth bug of the era-drift class, since the footprint IS derivable. Derivation inputs already exist in the tree: V1 Purchased events are indexed with buyer address (RawSaleEventInput.memberAddress, activityHeartbeatReadmodel.ts:154) and gross USDC (usdcGrossRaw, :138 — "V1 usdcAmount"), and the frozen genesis roster (artifacts/api-server/src/auth/memberRoster.ts, memberContinuityRecord rows under PART_B_FREEZE_ROOT / V1_MEMBER_ROOT) maps entryWallet→seat ordinal for #1-#8. The wallet→seat→cumulative join is a pure derivation over already-indexed data; no such slice exists yet (no roster reference anywhere in artifacts/api-server/src/backbone/).
- **Verifier:** CONFIRMED on every load-bearing point. (1) The exclusion is exact: capitalStanding.ts:83 carries the quoted note, and capitalAxisReadmodel.ts:194-199 skips all memberNumber===null purchases (V1 rows) from the per-seat walk, so genesis seats #1-#8 return rung=null/cumulativeUsdcRaw=null. (2) The dash is real: MemberKpiRow.tsx:48 renders "—" for the footprint KPI when the standing is unwalked. (3) Derivability is real, not speculative: the V1 sale ABI (sale-abi.ts:38-40) emits buyer (indexed) + usdcAmount; the heartbeat raw events ALREADY index both (memberAddress at activityHeartbeatReadmodel.ts:154, usdcGrossRaw at :138 explicitly "V1 usdcAmount"); the frozen genesis roster (memberRoster.ts, memberContinuityRecord under PART_B_FREEZE_ROOT, triple-matched on-chain vs V1_MEMBER_ROOT) maps entryWallet→seats #1-#8 — the wallet→seat→cumulative join is derivable from already-indexed data, squarely the LIVE-PRODUCTION RULE violation class. (4) Founder-flagged verbatim at docs/SESSION_STATE.md:17-19 ("must be derived, never dashed"). (5) No fix landed: zero roster/PART_B_FREEZE_ROOT references anywhere in artifacts/api-server/src/backbone/.

### A2. [data-slice · referral] The founder's own referral standing shows '—' while BUILDER_SOURCE payout history exists on-chain
- **Where:** artifacts/api-server/src/auth/router.ts:543 · lens: design-inventory
- **Detail:** CONFIRMED, with one location refinement: the canonical-only derivation lives in the spine helper artifacts/api-server/src/lib/protocol/sourceStandingRead.ts (lines 69-71), which GET /api/auth/source-standing (artifacts/api-server/src/auth/router.ts line 566) delegates to. readOwnSourceStanding derives exactly one id — keccak256(encodePacked("SYN.SOURCE.V1", boundAccount)) — reads registry existence/active for it, and resolves counters via sourceKeyOf(sourceId) into the introduction read-model; no payoutWallet/sourceWallet fallback exists anywhere on the standing path. Verified concretely: canonical id for the founder's wallet 0x2445…9C721 computes to 0x70e402e1…30fe, NOT the ACTIVE BUILDER_SOURCE id 0x8338e9ff…1cf620 (non-canonical, per SPEC_REFERRAL §① / SESSION_STATE, payoutWallet = the founder's wallet). sourceKeyOf(0x8338e9ff…) = src_36101e67d77b151bd9b04938, which IS present in INTRODUCTION_SNAPSHOT (introductionSnapshot.ts lines 40-54: 2 attributed purchases, 2 durable introductions, commissionPaidRaw "500000" = $0.50 paid on-chain), while the founder's canonical key src_8fbeae352bb535591e9f9085 is absent — so the founder's own session can never reach his real paid history; it renders as no standing. Founder-flagged verbatim at the S7-b/c/d seal in SESSION_STATE.md ("the founder's own referral standing show '—' while THE REAL OLD ON-CHAIN DATA EXISTS … BUILDER_SOURCE payout = the founder's wallet — must be derived, never dashed"). Violates the LIVE-PRODUCTION RULE (already-indexed figure shown as a dash). The proposed fix (also match indexed sources whose payoutWallet/sourceWallet equals the session account, own-row) is ADR-003-clean, but note the served snapshot is address-free by construction, so the wallet→source mapping must be added at index/build time or via a server-side live read, not from the snapshot rows as they exist today.
- **Verifier:** Verified by computation, not just reading: recomputed the canonical keccak256 derivation and the sha256 sourceKeyOf with node. BUILDER_SOURCE (0x8338e9ff…) hashes to snapshot row src_36101e67d77b151bd9b04938 carrying $0.50 of paid commissions; the founder's canonical derivation (0x70e402e1…) hashes to src_8fbeae352bb535591e9f9085, absent from the snapshot. The defect exists exactly as described; only the file attribution needed refining (derivation in sourceStandingRead.ts, not router.ts itself).

### A3. [defect-now · session] Header 'Member sign-in' does not land the member on /member after signing
- **Where:** artifacts/studio/src/wallet/MemberHeaderAffordance.tsx:173 · lens: design-inventory
- **Detail:** Confirmed at the named location. MemberHeaderAffordance.tsx signed-out branch (line ~178) is onClick={() => openConnectModal()}; SIWE completion lives in rainbowAuthAdapter.ts verify(), which on success only calls announceSessionChanged() — no navigation. Every SESSION_CHANGED_EVENT listener in the studio (RainbowKitRoot, header, KPI row, etc.) re-resolves state in place; grep finds zero navigate/setLocation("/member") in the wallet layer. The reSign path is likewise in-place by explicit comment. Minor nuance: after sign-in the button re-resolves into the seat-pill dropdown containing an "Open Member Home" link, so a manual two-click path exists — but the sign-in never LANDS the member on /member, which is the founder's exact open requirement recorded at the S7-b/c/d seal (docs/SESSION_STATE.md lines 14-17: "header sign-in must LAND on /member", in the S7-e follow-up block). Fix point: navigate to /member after successful verify from the header entry (or add an explicit completion affordance), e.g. in the header's session-change handling when the pre-sign state was signedOut.
- **Verifier:** Verified in current tree: no post-sign navigation exists anywhere (adapter verify only announces SESSION_CHANGED_EVENT; all listeners re-resolve in place). Founder flag is documented verbatim in docs/SESSION_STATE.md S7-e follow-up ("header sign-in must LAND on /member"), making this a recorded live defect, not an invented expectation.

### A4. [defect-now · human-first-copy] The 'memberNumberOf' verify-teaching line reads as machinery jargon on the identity band
- **Where:** artifacts/studio/src/wallet/MemberYourSeat.tsx:244 · lens: design-inventory
- **Detail:** Finding stands as written (file and defect both correct). One addition for the fixer: the same jargon class recurs at artifacts/studio/src/config/memberActions.ts:67 ("Call the engine's own memberNumberOf with your address — the same read behind every figure here.") — fixing only MemberYourSeat.tsx leaves a sibling occurrence on the member actions surface.
- **Verifier:** CONFIRMED, could not refute. MemberYourSeat.tsx:243-248 renders the seated-member teaching line with <code>memberNumberOf</code> as the sentence's operative instruction ("verify it yourself: call memberNumberOf with your address on the engine above") — the symbol is explicitly listed as internal jargon in the HUMAN-FIRST LAW, and the law's only carve-out (after the human words, in parentheses, for verifiers) is not met: there are no human words stating what the check does. Guard-pin claim verified: guard-access-state.ts:711-713 requires the string in that file, but it is inclusion-only, so the human-first rewrite ("ask the contract who holds seat #N — verifiers: memberNumberOf") passes the guard without touching it. Founder-flag claim verified: docs/SESSION_STATE.md:16 lists "the memberNumberOf teaching line reads as jargon" in the open founder follow-up from the S7-b seal — still unfixed in the working tree.

### A5. [defect-now · raw-number] Raw 18-decimal SYN balances rendered as KPI values (founder-flagged class)
- **Where:** artifacts/studio/src/wallet/ownReads.ts:35 · lens: human-readability
- **Detail:** formatRawUnits(raw, 18) (artifacts/studio/src/lib/rawUnits.ts:35-43) only trims TRAILING zeros and applies no display precision, so any non-round SYN figure renders with its full fractional tail. Severity differs by surface: (1) The two live BALANCE surfaces — member dashboard KPI tile "Your SYN" (MemberKpiRow.tsx:99 via ownReads.ts:35) and the wallet page balance card (MemberWalletPanel.tsx:94, rendered at :158) — display the wallet's raw ERC-20 balanceOf, which includes open-market SYN/USDC pool buys and transfers (the public pool is live); those amounts are essentially never round, so up to 18 fractional digits render as the headline value. The KPI tile's CSS `truncate` clips overflow with an ellipsis (arbitrarily hiding digits — not human formatting); the wallet panel value has no clipping at all. (2) The checkout success line (JoinCheckout.tsx:449) is milder than claimed: the live rig quote shows synPerUsdc=100 (an integer rate), so sale synOut = grossUsdc × rate is round for round-dollar purchases and carries at most 6 fractional digits (the join amount input accepts up to 6 fractional USDC digits, e.g. "99.123456" → "9,912.3456 SYN") — still uncapped, but never 18 digits. Violates the Readability Floor's explicitly named class ("a token balance showing 18 decimals") and the Human-First Law. Fix at the display seam: cap headline precision (2-4 decimals, human-rounded) and keep the exact raw figure for verifiers as a tooltip/secondary line (the existing KpiTile `detail`/title pattern fits), never as the headline value.
- **Verifier:** Confirmed with one mechanism correction. All three render sites exist exactly as cited (ownReads.ts:35 → MemberKpiRow.tsx:99; MemberWalletPanel.tsx:94/158; JoinCheckout.tsx:449) and formatRawUnits (rawUnits.ts:40) only strips trailing zeros — no display precision cap exists anywhere on the seam. However, the claim that synOut "is virtually never round" is refuted by a live rig probe: /api/join/quote returns synPerUsdcRaw=100 (integer rate), so synOut = grossUsdc × 100 × 10^12 — a round-dollar purchase renders round ("12,000 SYN") and the worst purchase-derived case is 6 fractional digits (join input accepts up to 6 fractional USDC digits). The genuinely unbounded 18-digit exposure is the two BALANCE surfaces, which read the wallet's raw ERC-20 balanceOf: pool-market buys/transfers (the SYN/USDC public pool is live per verify-links lpPair) produce arbitrary 18-decimal amounts that will render in full. The KPI tile's CSS `truncate` clips with an ellipsis (hides digits, not human formatting); the wallet panel value has no clipping. Violates the Readability Floor's named class and Human-First formatting.

### A6. [defect-now · raw-number] 6-decimal USDC siblings of the raw-balance class
- **Where:** artifacts/studio/src/wallet/MemberWalletPanel.tsx:189 · lens: human-readability
- **Detail:** Confirmed at every cited site: formatRawUnits (artifacts/studio/src/lib/rawUnits.ts:35-43) trims trailing zeros but never rounds, so decimals=6 renders up to 6 fractional digits of USDC. Sites: KPI tile "Your USDC" (MemberKpiRow.tsx:105 via ownReads.ts:62), wallet balance card (MemberWalletPanel.tsx:95 format / :165 render), approvals line `approved: N USDC` (MemberWalletPanel.tsx:189), checkout insufficient-balance line (JoinCheckout.tsx:484, usdcDecimals=data.decimals.usdc=6 via JoinProtocol.tsx:510/524). Nuances: (a) approvals and gross amounts are whole-dollar era prices, so the trim usually shows them clean — the defect surfaces on the user-wallet BALANCE sites, where swap/bridge dust makes "149.999999 USDC" a realistic KPI render; (b) the strongest evidence of violation is in-repo: usdFromRaw (ownReads.ts:116-121) already renders 2-decimal money ("$150.00") on the footprint tile of the SAME KPI row, so 6-decimal and 2-decimal money can sit side by side, and the row's own comment cites the Binance/Coinbase standard (2-decimal stablecoin display); (c) additional lower-severity siblings not in the finding: JoinCheckout.tsx:338 (grossDisplay) and JoinProtocol.tsx:331/394/414/489 use the same 6-decimal formatter on quote-derived whole-dollar amounts. rawUnits.ts's own design note "the raw string is always shown alongside it" is not honored at these sites, so no verify-path companion exists today.
- **Verifier:** REAL. All four cited locations exist verbatim in the working tree and the formatter provably emits up to 6 fractional digits for USDC. Violation is against the READABILITY FLOOR's "formatted human" requirement (the 6-decimal sibling of the named 18-decimal class) plus HUMAN-FIRST: a dollar-stablecoin balance with 6 fractional digits is machine precision, not money display — and the repo's own usdFromRaw (2-decimal, "$0.50") on the same KPI row proves the intended money standard, making the inconsistency visible within a single row. Severity is moderate: whole-dollar approval/gross amounts render clean due to trailing-zero trim; the real-world offender is the wallet balance with dust.

### A7. [defect-now · jargon] 'call memberNumberOf with your address on the engine above' — machinery-first verify line (founder-flagged)
- **Where:** artifacts/studio/src/wallet/MemberYourSeat.tsx:244 · lens: human-readability
- **Detail:** The identity band's verify teaching (artifacts/studio/src/wallet/MemberYourSeat.tsx:243-248) is a builder instruction a zero-crypto newcomer cannot parse or act on: "Read live, never assigned by hand — verify it yourself: call memberNumberOf with your address on the engine above." The actionable clause is pure machinery (memberNumberOf is explicitly in the Human-First jargon list), placed in the main sentence rather than after human words in parentheses. Aggravating facts confirmed in the tree: (a) "the engine above" refers to the VerifyOnChain chip, but no on-screen element is ever labeled "engine" (the chip reads "Verify on-chain"), and (b) the chip's cluster only renders when status.receipt !== null while the teaching line renders for every seated member — so with no receipt, "the engine above" points at nothing on the page. The guard pin (guard-access-state.ts:711-713) only requires the substring "memberNumberOf" in the file, so a Human-First rewrite ("Anyone can check this seat on the public explorer — no one has to take our word (the contract function memberNumberOf answers with your seat number)") passes the guard unchanged. Same machinery-first pattern also user-facing at artifacts/studio/src/config/memberActions.ts:67, rendered by MemberQuickActions.tsx:81.
- **Verifier:** Confirmed against the working tree. The quoted line exists verbatim at MemberYourSeat.tsx:244-247 and violates the Human-First Law as given (memberNumberOf is named in the law's jargon list; here it is the load-bearing instruction, not a trailing parenthetical for verifiers). Refutations tested and rejected: the guard pin only checks for the substring, not the phrasing; VERIFY-FIRST requires a verify path but Human-First governs its wording; and the referenced "engine above" is unlabeled on screen and conditionally absent (receipt === null), making the line even less actionable than the finding claims.

### A8. [defect-now · jargon] Sibling: quick-action note leads with memberNumberOf on the visitor conversion surface
- **Where:** artifacts/studio/src/config/memberActions.ts:67 · lens: human-readability
- **Detail:** The 'Verify my seat on chain' action note in artifacts/studio/src/config/memberActions.ts:67 opens with the contract call ('Call the engine's own memberNumberOf with your address — the same read behind every figure here.'). It is rendered unconditionally — including in the locked state visitors see — by MemberQuickActions.tsx:81, mounted on the visitor conversion surface (MemberAccess.tsx:305) and on MemberToolkit.tsx:22. Rewrite human-first with the function name in parentheses after.
- **Verifier:** Confirmed exactly as described. memberActions.ts:67 note leads with the raw contract function name ('Call the engine's own memberNumberOf with your address — …'), violating HUMAN-FIRST LAW's explicit rule that memberNumberOf-class jargon appears only AFTER human words, in parentheses. Rendered unconditionally (even locked) via MemberQuickActions.tsx:81 on MemberAccess.tsx:305 — whose own code comment calls it 'the visitor conversion surface' — and also on MemberToolkit.tsx:22. All four sibling notes in the registry are human-first; this is the sole outlier. Fix pattern matches the finding: human words first ('Ask the engine for your seat number…'), function name in parentheses for verifiers.

### A9. [defect-now · raw-diagnostics] Raw server diagnostics ('RPC unreachable… fail closed') rendered verbatim to members
- **Where:** artifacts/studio/src/components/referral/MemberReferralDashboard.tsx:96 · lens: human-readability
- **Detail:** Confirmed verbatim-render pipe, current tree: server diagnostics originate in artifacts/api-server/src/lib/protocol/sourceStandingRead.ts:78-80 ("chain identity unverified; live read skipped (fail closed)", "RPC unreachable; live read skipped (fail closed)") and :97 ("source existence read failed (reported as unavailable)"); GET /api/auth/source-standing serves failureReason verbatim (artifacts/api-server/src/auth/router.ts:575) with state "S4" for any active session even when the chain probe failed (router.ts:570); the client passes it raw (artifacts/studio/src/wallet/walletSession.ts:419); MemberReferralDashboard.tsx:94-101 renders readback?.failureReason as the standing card's body sentence for a signed-in member whenever standing is null. No interception exists: the runtime payload discipline gate is disabled (payloadDiscipline.ts:32, DISCIPLINE_ENFORCED=false) and its forbidden lists cover financial/casino framing, not "fail closed"/"RPC". Scope refinement: the common signed-in no-source path receives the server's human-voiced string ("no on-chain referral source exists for this wallet yet — a new source is a founder-signed on-chain act"), so the raw diagnostics surface only on RPC/chain-identity/existence-read failure paths — still a real HUMAN-FIRST LAW violation ("fail-closed" is a named banned machinery term; "RPC" is machinery jargon, rendered as the sentence, not parenthetically after human words). The finding's secondary claim about "indexed" in the client fallback is weak and should be dropped: "indexed" is established approved user-facing vocabulary elsewhere in the same component ("indexed · as of block", "indexed introductions and live registry reads").
- **Verifier:** REAL. The three server diagnostic failureReason strings (containing "fail closed", "RPC") flow unmodified from sourceStandingRead.ts through the auth router and walletSession into MemberReferralDashboard.tsx:96, where a signed-in member sees them as the card body on any RPC/chain/read failure — a direct HUMAN-FIRST LAW violation. The payload discipline gate cannot catch it (disabled, and never covered these terms). Fix belongs client-side (human translation with the diagnostic kept parenthetically for verifiers) or server-side human-voiced strings. Only the "indexed"-vocabulary side-claim is refuted (approved vocabulary elsewhere in the same component).

### A10. [defect-now · jargon] 'Fail-closed' spoken to the buyer in the checkout error state
- **Where:** artifacts/studio/src/wallet/JoinCheckout.tsx:327 · lens: human-readability
- **Detail:** The money flow's unavailable state says 'Live verification failed: {reason}. Fail-closed — nothing can be signed until the live reads succeed.' 'Fail-closed' and 'live reads' are internal doctrine words; the buyer needs 'For your safety, nothing can be signed until we can read your live wallet state. Reload to retry.' The interpolated reasons themselves ('the engine's payment token could not be read') are acceptable.
- **Verifier:** Confirmed verbatim at artifacts/studio/src/wallet/JoinCheckout.tsx:326-329 (the unavailable-phase copy inside the public checkout, which the file header itself calls the only write surface of the public app). The HUMAN-FIRST LAW explicitly names "fail-closed" in its internal-jargon list; here it leads the buyer-facing sentence as bare doctrine ("Fail-closed — ...") rather than appearing after human words in parentheses, and "the live reads" is a second machinery term in the same sentence. The finding's concession is accurate: the interpolated phase.reason strings (set at lines 227 and 235: "the engine's payment token could not be read", "your live allowance or balance could not be read") are human-readable. Counterpoints checked: repo-wide, "fail-closed" is almost entirely code-comment vocabulary; the few other user-facing occurrences (faq-content.ts:54, JoinHistoricalGate.tsx:128, operator-only AdminShell.tsx:153) do not sanction jargon-first copy in the money flow, and sibling checkout states in the same file (lines 252-254, 269-271, 287-289) speak in plain human words, making this branch the outlier. Suggested fix direction matches the finding: e.g. "Live verification failed: {reason}. For your safety, nothing can be signed until your live wallet state can be read. Reload to retry." (optionally "(fail-closed)" in parentheses after the human words for verifiers).

### A11. [defect-now · jargon] 'the walked record' / 'the living canon' — internal derivation vocabulary as the early-era explanation
- **Where:** artifacts/studio/src/wallet/CapitalAxisCard.tsx:129 · lens: human-readability
- **Detail:** A pre-V3 member with no derivable footprint reads (CapitalAxisCard.tsx:128-131): "Your early-era footprint predates the walked record — honestly not derivable, never guessed. The ladder below is the living canon." "Walked record" is the capital-axis readmodel's own internal procedure name (capitalAxisReadmodel.ts calls its derivation "the footprint walk" / "walked seats"), and "living canon" is repo-doctrine vocabulary appearing nowhere else user-facing — a zero-crypto newcomer cannot parse either on first read. Sibling (weaker): MemberKpiRow.tsx:116 tooltip "Early-era footprints are honestly not derivable." — states the negative in semi-technical terms without the human explanation. Human rewrite direction: "Your earliest purchases were made before the protocol started keeping this per-seat record, so your footprint here starts from that point — we never guess backwards."
- **Verifier:** CONFIRMED. Text exact at artifacts/studio/src/wallet/CapitalAxisCard.tsx:128-131 (rendered in the !walked branch — recognized seat with no derivable footprint) and artifacts/studio/src/wallet/MemberKpiRow.tsx:116. "Walk" is provably the readmodel's internal derivation vocabulary: capitalAxisReadmodel.ts:23 ("gross USDC walks the SAME gapless purchase lane"), :122 ("every walked seat"), :202 ("the footprint walk refuses to guess"), :235 ("the walk's end state"). "Living canon" appears nowhere else in the repo — one-off internal doctrine jargon. HUMAN-FIRST LAW violation: machinery vocabulary as the primary user-facing sentence, no human words first, no parenthetical structure.

### A12. [defect-now · jargon] Settings Alias row speaks in internal spec language ('own-row, lineage-tracked… the IDENTITY-ALIAS slice')
- **Where:** artifacts/studio/src/wallet/MemberSettings.tsx:99 · lens: human-readability
- **Detail:** Confirmed at artifacts/studio/src/wallet/MemberSettings.tsx:99 — the Alias row body reads: "A human-readable name above your wallet — opt-in, invisible by default, own-row, lineage-tracked. Arrives with the IDENTITY-ALIAS slice." The component is live user-facing copy: MemberAccess.tsx lazy-loads MemberSettings (lines 55-56) and renders it for signed-in members (lines 212-215). "Own-row" (ADR-003 internal access-scope term), "lineage-tracked", and the internal slice codename "IDENTITY-ALIAS" appear inline in the sentence, not after the human words in parentheses — a direct HUMAN-FIRST LAW violation. The proposed human rewrite ("A display name shown above your wallet address — optional, off by default, and only ever yours to set. Coming later.") preserves the row's truth (opt-in, invisible by default, FUTURE lifecycle).
- **Verifier:** Verified against the current working tree. Line 99 matches the evidence verbatim; the component is wired into the member page (MemberAccess.tsx), so the text is user-facing. Three internal-jargon terms (own-row, lineage-tracked, IDENTITY-ALIAS slice) sit inline without the parenthetical-after-human-words form the HUMAN-FIRST LAW requires. Finding is REAL as described; no location correction needed.

### A13. [defect-now · jargon] 'Reserved on the existing posture system' on the member dashboard
- **Where:** artifacts/studio/src/pages/MemberAccess.tsx:183 · lens: human-readability
- **Detail:** Confirmed exactly as described. MemberAccess.tsx line 183 (within the <p> at 182-184) reads "Reserved on the existing posture system — nothing simulated." as the subtitle of "Coming to your seat" on the SIGNED-IN member dashboard (the visitor composition renders the reserved slots without this subtitle, so the fix is dashboard-side plus the config notes). "Posture system" is verifiably internal architecture vocabulary — it names the truthStatus/PostureBadge/registryPosture/source-status machinery throughout the codebase and appears user-visibly only on verifier/operator surfaces (SystemStatus, admin panels), never as a defined human-facing term. Siblings confirmed and both render user-facing: memberDoors.ts:79 "the event record is live; this member surface arrives on it" (rendered via slot.note on both dashboard and visitor page; "member surface" is dev vocabulary) and memberDoors.ts:44 "complete history served" (rendered in the MemberShell sidebar via DoorRow's note; "served" is machinery flavor, the lightest of the three).
- **Verifier:** REAL. All three strings exist verbatim in the current working tree at the cited locations and all render user-facing. The primary string violates the HUMAN-FIRST LAW: "posture system" is internal machinery jargon leading a member-facing sentence (not after human words in parentheses), on the member dashboard — a first-read consumer surface. Refutation attempts failed: "posture" is nowhere a human-facing brand term; locations are exact; text unchanged in working tree.

### A14. [defect-now · readability-floor] Readability floor: 9-11px user-facing text live across member surfaces
- **Where:** artifacts/studio/src/wallet/MemberHeaderAffordance.tsx:404 · lens: human-readability
- **Detail:** ADR-001's floor (amendment 2026-07-16, docs/adr/ADR-001 lines 110-132: nothing user-facing under 12px; reading copy >=14px; arbitrary text-[9/10/11px] forbidden) is violated in the current tree, and everything is live (WALLET_SESSION_PREVIEW_ENABLED=true in config/walletSessionGate.ts:21; MemberHeaderAffordance renders sitewide via PublicLayout.tsx:26,33). Verified exactly: MemberHeaderAffordance.tsx:404 'Verify my seat on-chain' at text-[9px]; :369 'Verified on chain' pill, :382 chapter chip, :415 seat-vs-session explainer PARAGRAPH (reading copy) at text-[11px]; JoinCheckout.tsx:457 receipt-tx link and :504/:525 'Step 1'/'Step 2' at text-[10px] (the live join flow); MemberWalletPanel.tsx:230 pool explainer at text-[11px]; JoinHistoricalGate.tsx:125 at text-[10px]. Systemic root confirmed at artifacts/studio/src/components/status-pill/StatusPill.tsx:28-29 (full path corrected): sm='text-[10px] sm:text-xs', xs='text-[9px] sm:text-[10px]'. Two refinements: (1) the ADR amendment itself already names 'header affordance, checkout, admin console, syn-label/syn-caption, MembersProvenance' as noted remaining sweep work, so those instances are acknowledged debt rather than unnoticed regressions — still floor violations in the shipped product; (2) the StatusPill instances are NOT covered by that note and contradict the ADR's own 'Appliqué: toute la composition /member' claim: LifecycleBadge (default size sm) renders 10px below the 640px breakpoint on MemberShell.tsx:39, MemberSettings.tsx:54, MemberAccess.tsx:196/331, and MemberReferralDashboard.tsx:216 'Source active' uses size xs = 9-10px at EVERY width — on the very /member composition the ADR declares swept. The dedicated components/member/* files contain zero arbitrary sub-12px classes, so the one-atom StatusPill fix claim is accurate for the member composition; the wallet/ module instances need the already-noted sweep.
- **Verifier:** Adversarially verified against the working tree: every cited line matches verbatim (grep + file reads). The only inaccuracies were cosmetic (StatusPill's full path is components/status-pill/StatusPill.tsx) and framing (part of the violation set is already acknowledged in ADR-001's own RESTE clause as a noted future sweep + no-sub-12px guard). The StatusPill-on-mobile portion is an unacknowledged contradiction of the ADR's 'applied to the whole /member composition' claim, which makes the finding real beyond the documented debt.

### A15. [defect-now · jargon] 'Reading your own row…' — machinery term as the entire wait state
- **Where:** artifacts/studio/src/components/member/MemberQuickActions.tsx:120 · lens: human-readability
- **Detail:** Finding stands as written, with one scope refinement: MemberQuickActions is VISITOR-ONLY in the deployed composition (MemberAccess.tsx renders it only in the signed-out door branch), so the wait state surfaces in transient windows — the just-signed-in race where the component's own useOwnState resolves S4 before the page-level session hook switches to the dashboard (copy-referral-link unlocked while wagmi address is still undefined), or a seated standing response lacking receipt.explorerUrl (share-proof falls through persistently). Transient or not, it is a designed, reachable user-facing string ("honest wait, never a dead button" per the inline comment) and the only naked, unexplained "own row" in user-facing copy. Fix exactly as proposed: change only MemberQuickActions.tsx:120 to "Reading your record…" (the repo's own human word per the door lead and modules.ts); leave all explained occurrences and the guard-pinned dist probe untouched.
- **Verifier:** Confirmed. The exact string exists at artifacts/studio/src/components/member/MemberQuickActions.tsx:120 as the fallback wait state for unlocked actions whose own-row material has not resolved. It is reachable (post-sign-in race before the MemberAccess composition flips; share-proof unlocked with a null explorerUrl) and is the ONLY user-facing occurrence of "own row" with no explanatory frame — every other occurrence (syndicateFacts.ts:284 door lead, modules.ts:173, seo-route-registry.ts:146, MemberWalletPanel.tsx:142, MemberReferralDashboard.tsx:282) pairs the phrase with "your own record" / "there is no directory of members" and self-explains. A naked "row" is machinery shorthand a zero-crypto newcomer cannot parse — a HUMAN-FIRST LAW violation. The suggested fix "Reading your record…" matches the repo's own established human vocabulary ("this page reads your own record" in the door lead). The guard-pinned dist probe "ever your own row" (syndicateFacts.ts:278 comment) lives in a different file and is untouched by the fix.

### A16. [defect-now · jargon] 'indexed' jargon-first strings + unformatted raw block number in the standing label
- **Where:** artifacts/studio/src/components/referral/MemberReferralDashboard.tsx:111 · lens: human-readability
- **Detail:** Locations confirmed exactly as cited: MemberAccess.tsx:162-163 and :313-314 ("Your introductions, your indexed standing, and the Connector ladder."); MemberReferralDashboard.tsx:110-112 ("indexed · as of block {s.asOfBlock}" — asOfBlock is a plain number rendered without grouping, inconsistent with MemberPulse.tsx:75's toLocaleString); MemberKpiRow.tsx:126 ("from the indexed introduction record"). One correction of weight: the MemberKpiRow tooltip is the weakest sibling — its human words lead ("Members you brought in, ...") with the machinery term after, roughly the pattern the HUMAN-FIRST LAW permits, so it is compliant-adjacent rather than a clear violation. Siblings (1) and (2) stand fully. Additional sibling not in the original finding: ProposeSourcePromotion.tsx:237-238 also renders INTRODUCTION_INDEX_SNAPSHOT.asOfBlock ungrouped (founder-facing surface). Fix pattern per the finding stands: human words first, e.g. "as of the latest recorded block (block 65,123,456)" with toLocaleString grouping.
- **Verifier:** Verified in the current tree: all three cited strings exist verbatim at the stated lines. The strongest defects are (a) MemberReferralDashboard.tsx:111 renders asOfBlock (typed number, walletSession.ts:348) with no thousands grouping while MemberPulse.tsx:75 formats blockNumber with toLocaleString("en-US") — a direct READABILITY-FLOOR ("raw numbers formatted human") violation plus intra-page inconsistency — and (b) the jargon-first "indexed standing" intro on both MemberAccess states and the machinery-first "indexed ·" chip prefix, violating the HUMAN-FIRST LAW.

### A17. [defect-now · english-quality] 'Active at 3 durable · title' — cryptic ladder fragment fails first-read English
- **Where:** artifacts/studio/src/components/referral/MemberReferralDashboard.tsx:126 · lens: human-readability
- **Detail:** Confirmed at artifacts/studio/src/components/referral/MemberReferralDashboard.tsx:125-129. The raisesRate=false branch is reachable and is in fact the DEFAULT view: connectorLadder.ts defines exactly one title-only rung — Active (threshold 3, raisesRate:false) — and every member with 0-2 durable introductions sits on Emerging with next=Active, so the header renders literally "Active at 3 durable · title" for every new member. " · title" is undecoded internal shorthand (title-only promotion, same rate); the explanation paragraph below defines "durable introduction" but never explains "· title", so the fragment fails first-read English (HUMAN-FIRST LAW). Font size (text-xs, 12px meta) passes the readability floor — the defect is purely the copy. The summit line (line 131) is only reachable at the top rung and is passable, as the finding says.
- **Verifier:** Verified against the working tree: the quoted JSX exists at lines 125-129, the title-only rung (Active, raisesRate:false) exists in connectorLadder.ts:26, and it is the next rung for the default 0-durable member state — so the cryptic fragment "Active at 3 durable · title" is what most members see first. No on-page copy decodes "· title". Finding stands as described.

### A18. [data-slice · genesis-footprints] Genesis footprints (#1–#8): V1 purchases are excluded from the capital walk though the wallet→seat join is fully derivable server-side today
- **Where:** artifacts/api-server/src/backbone/capitalAxisReadmodel.ts:197 · lens: data-truth
- **Detail:** Finding confirmed as written, with minor precision fixes: (1) the exclusion is at capitalAxisReadmodel.ts:197-199 (the comment is 195-196; finding said 196-199 — approximately right); (2) full paths: artifacts/api-server/src/lib/protocol/historicalFreezeWallets.ts (wallet→seat map, lines 40-52) and artifacts/api-server/src/data/partBExpectations.ts (distribution at line 45); (3) supporting evidence beyond the finding's citations: freezeGate.ts:54 independently confirms V2A numbered rows are {3,4,5} and V2B {6,7,8} with sentinel-0 excluded, proving seats #1-#2 have NO numbered purchase rows anywhere — zero walked footprint today; memberRoster.ts:66-67 confirms the DB-side duplicate of the mapping (member_continuity_record PART_B_FREEZE_ROOT rows queried by lower(entryWallet)); MemberYourSeat.tsx:138 gates the rung chip on rung !== null and MemberKpiRow.tsx:48 renders the dash via `value ?? "—"`; (4) the V2B sentinel-0 join claim is sound: sentinel rows are firstSeat=false repeats by then-existing members (all 8 wallets are in the freeze set) and Purchased rows already carry buyer→memberAddress (backboneDb.ts:575-577, 632-635); (5) runtime note: the local rig's backbone is disabled (no DB) so capital-standing answers "model has not built yet" for every seat locally — the seat-1-vs-seat-6 differential is a code-path certainty (capitalStanding.ts:79-84), not locally reproducible. Everything else in the original detail — the derivation design, the #7/#11 per-era resolution, the three copy sites that die, the OWN-ACCOUNT/CANON_VISIBILITY doctrine defense, and the single founder gate on retroactive historical RISE lines (builder flag to keep rises V2B/V3-only while standingBySeat gains the joined seats) — is accurate as stated.
- **Verifier:** Verified against the working tree: the exclusion, the already-indexed join inputs, the wallet→seat freeze map, all three false-copy sites, and the seat #1–#2 zero-footprint consequence all exist exactly as described. The figure is derivable from already-indexed server data, so the "—"/null rendering violates the LIVE-PRODUCTION RULE and the "honestly not derivable" copy is a truth bug (era-drift class). The doctrine analysis (own-account display legality, public wallet→seat pairing in the client bundle, the one founder gate on retroactive feed rises) is accurate.

### A19. [data-slice · founder-referral-standing] Founder's referral standing: source-standing only derives the canonical keccak sourceId, so his ACTIVE BUILDER_SOURCE (different id, his payoutWallet) reads as 'no source exists' — a truth bug on his own dashboard
- **Where:** artifacts/api-server/src/lib/protocol/sourceStandingRead.ts:100 · lens: data-truth
- **Detail:** As stated in the finding, with one figure corrected: the BUILDER_SOURCE row src_36101e67d77b151bd9b04938 in introductionSnapshot.ts:40-54 carries commissionPaidRaw "500000" = $0.50 (USDC 6 decimals; two $0.25 payments on two $5 attributed purchases), not $5.00. Everything else verifies exactly: readOwnSourceStanding derives only the canonical keccak256("SYN.SOURCE.V1", boundAccount) (sourceStandingRead.ts:69-71) — computed as 0x70e402e1…30fe for the founder's wallet 0x244531C5…9C721 (docs-truth his own private wallet, historical member #1 — OPEN_QUEUE.md:320-325) — which does not exist on-chain, so the endpoint returns standing:null with "no on-chain referral source exists for this wallet yet" (:100-104) while his ACTIVE BUILDER_SOURCE 0x8338e9ff…1cf620 (sourceWallet=payoutWallet=his wallet, 5% LIFETIME, re-activated block 89642946) holds the indexed row 2 introduced / 2 durable / $0.50 paid. Client consequences confirmed: Introductions KPI "—" (MemberKpiRow.tsx:89,119-128), IntroductionStanding asserting no source exists to the signed-in founder (MemberReferralDashboard.tsx:78-104), ShareCard suppressed (:294-300), MyReferralLinkCard advertising the canonical (nonexistent, non-paying) link with "commission activates when your source is founder-signed" (:173-255) instead of his real ACTIVE link. The proposed payoutWallet-fallback derivation inputs exist: SOURCE_LIFECYCLE persists SourceCreated (payoutWallet arg 7) + SourceWalletUpdated + SourcePayoutWalletUpdated (protocolEventScan.ts:59-67); the sourceDecoders.ts:5-7 pin ("sourceConfig view is NEVER read or surfaced by served code") is respected by a DB-fold approach and would need a founder-gated amendment only for a live payoutWallet confirmation. The doctrine notes (own-row purity, the sourceId-echo decision for the link card) stand as written.
- **Verifier:** Verified against the working tree, with the two decisive facts independently recomputed: (1) keccak256(encodePacked("SYN.SOURCE.V1", 0x244531C571966f90f4849e03a507543d90f9C721)) = 0x70e402e1…30fe, which is NOT the BUILDER_SOURCE id 0x8338e9ff…1cf620, so sourceStandingRead.ts:69-71 + :100-104 returns standing:null / "no on-chain referral source exists for this wallet yet" for the founder's session; (2) sourceKeyOf(0x8338e9ff…1cf620) = src_36101e67d77b151bd9b04938 — exactly the indexed snapshot row (2 introduced / 2 durable). OPEN_QUEUE.md:320-325 confirms in docs-truth that 0x2445…9C721 is the founder's own private wallet and the BUILDER source payout destination. All three claimed client consequences confirmed in MemberKpiRow.tsx (Introductions "—"), MemberReferralDashboard.tsx IntroductionStanding (serves the false failureReason to a signed-in member; ShareCard gated on standing), and MyReferralLinkCard (advertises the canonical nonexistent link via deriveSourceId while the real ACTIVE 5% link is the BUILDER id). The fix inputs exist as claimed (protocolEventScan.ts:59-67 SOURCE_LIFECYCLE persists SourceCreated payoutWallet + SourceWalletUpdated/SourcePayoutWalletUpdated; sourceDecoders.ts:5-7 carries the sourceConfig never-served pin). Violates LIVE-PRODUCTION RULE, the era-drift truth-bug class, and defeats the OWN-ACCOUNT DISPLAY RULE on the founder's own row. One numeric correction: commission paid is $0.50, not $5.00.

### A20. [data-slice · own-purchase-history] Own purchase history: the member sees ONE receipt (the entry tx) while every own purchase — repeat buys included — is already indexed with amount, block and tx hash
- **Where:** artifacts/api-server/src/auth/memberRoster.ts:119 · lens: data-truth
- **Detail:** All substantive claims verified; two precision corrections. (a) buyer/recipient and the gross USDC amount are NOT dedicated columns on sale_event_raw — they live inside the SERVER-ONLY decodedJson jsonb (lib/db/src/schema/indexer.ts:96-99, whose doc-comment forbids serializing that column into any public payload). The proposed own-row fallback (lower(recipient)=boundAccount AND firstSeat=true) must therefore extract from decodedJson (or reuse the backbone's existing decode/extraction path, e.g. the saleEventSemantics/activityHeartbeat lane that already yields memberNumber+usdcGrossRaw per row) and emit only {isoDay, humanized amount, txHash, explorerUrl} — never the recipient field itself, consistent with the column's discipline and the 40-hex leak gate. (b) Evidence line refs: the receipt select is memberRoster.ts:119-128 (null-check 130-137); the V3 record shaping is scripts/member-continuity-readmodel.ts:810-828 (entryTransaction: r.transactionHash at line 822), not 816-822. Additional supporting fact found during verification: CapitalAxisCard.tsx:93-96 ships the sentence "the sum of your own on-chain purchases, public and recomputable by anyone" with no link or addend list — so the shipped cumulative figure itself lacks the verify path VERIFY-FIRST requires, strengthening the sum-without-addends inconsistency argument.
- **Verifier:** CONFIRMED on the current tree. (1) memberRoster.ts:105-146 serves exactly one receipt (entryTransaction from member_continuity_record, limit 1); router.ts:477-493 forwards it; MemberYourSeat/MemberHeaderAffordance are the only receipt surfaces; no own-purchase list exists anywhere in the studio. Repeat purchases are a first-class invited flow (memberActions.ts:48-55 "Expand your footprint"; JoinCheckout even prints "Repeat purchase recorded" ephemerally) yet their receipts, fully indexed in sale_event_raw, never render — while CapitalAxisCard displays their SUM (capital-standing cumulativeUsdcRaw, folded from the same rows in capitalAxisReadmodel.ts:204-206) with the words "recomputable by anyone" and no verify path: a VERIFY-FIRST breach on a shipped figure plus the LIVE-PRODUCTION RULE ("derivable from already-indexed data MUST render live"). (2) member_continuity_record's only writer is the founder-gated manual script (package.json member-continuity:write → member-continuity-build.ts; V3 rows shaped in scripts/member-continuity-readmodel.ts:810-828); router.ts:425-436 recognizes post-rebuild V3 seats LIVE ("snapshot rebuild pending"), so such members get receipt:null for an unbounded window while their tx sits in the continuously scanned sale_event_raw — the UI silently degrades share-proof to the generic contract link. OWN-ACCOUNT DISPLAY RULE blesses the fix; ADR-003 own-row posture and the 40-hex leak gate (64-hex tx hashes pass) are compatible as the finding states.

### A21. [data-slice · per-introduction-receipts] Per-introduction receipts card says PENDING_ADAPTER — but the rows are fully indexed today; what is missing is a founder-gated own-row serving decision, not data
- **Where:** artifacts/studio/src/components/referral/MemberReferralDashboard.tsx:314 · lens: data-truth
- **Detail:** MemberReferralDashboard.tsx:314-324 renders a dashed 'Per-introduction receipts' card (LifecycleBadge PENDING_ADAPTER): 'the per-receipt history arrives with row-level serving.' The per-row data is fully indexed today — CORRECTED location: the sale_event_raw table (lib/db/src/schema/indexer.ts:81-118) persists every MembershipPurchasedV3 row with blockNumber, logIndex, transactionHash (NOT NULL, line 92) and SERVER-ONLY decodedJson {sourceId, recipient, acquisitionCost}; AttributedPurchaseRow (introductionReadmodel.ts:53-64) carries sourceId/recipient/acquisitionCostRaw/blockNumber/logIndex but NOT transactionHash (introductionRefresh.ts loadAttributedRows deliberately whitelists it away), so a row-serving slice selects the tx hash from saleEventRaw directly. blockDateByNumber (BuildInputs, introductionReadmodel.ts:118-120, fed from the Protocol Time cache) and the per-recipient durable flag (live SYN balanceOf each backbone cycle, introductionRefresh.ts:258-265) complete the row: date, commission amount, durable flag, receipt tx link are ALL derivable today. Only aggregation is served: readOwnSourceStanding (sourceStandingRead.ts:130-146) returns counts/sums/ladder facts; no per-row endpoint exists. Under the LIVE-PRODUCTION RULE this is a buildable-now gap honestly labeled (not era-drift). DOCTRINE CONSTRAINT, two tiers: (a) a tx-hash-FREE row history (date + commission + durable flag per introduction) crosses NO existing pin — it exposes nothing beyond what the served aggregates already reveal — and is buildable with zero doctrine amendment; (b) the receipt TX LINK crosses the introduction model's privacy pin (introductionReadmodel.ts:22-27: no tx hash — and note the tx exposes the INTRODUCED member's wallet, a third party's, not the viewer's own, so ADR-003 is genuinely engaged) and therefore needs an explicit founder-gated amendment: rows live in a NEW own-row payload in the auth zone (session-bound introducer, his own sourceKey only — the member-standing own-receipt precedent, auth/router.ts:477-493), never in the public snapshot; recipient wallets themselves never emitted.
- **Verifier:** CONFIRMED with one location correction. The PENDING_ADAPTER card is exactly at MemberReferralDashboard.tsx:314-324 with the quoted copy. Per-row data is fully indexed today and only aggregates are served (readOwnSourceStanding returns counts/sums/ladder facts only; no row endpoint exists anywhere). The correction: transactionHash is NOT on AttributedPurchaseRow (introductionReadmodel.ts:53-64 has no such field, and introductionRefresh.ts loadAttributedRows never selects it — the decodedJson whitelist is exactly {sourceId, recipient, acquisitionCost}); it is persisted one layer down on the sale_event_raw table itself (lib/db/src/schema/indexer.ts:92, NOT NULL, part of the chain/tx/log unique index). The substance stands: date (Protocol Time cache), commission (acquisitionCostRaw), durable flag (live SYN read per cycle), and receipt tx (saleEventRaw.transactionHash) are all derivable today, so under the LIVE-PRODUCTION RULE the PENDING card is a buildable-now gap, honestly labeled (not era-drift). The doctrine framing is accurate: the introduction model's privacy pin (introductionReadmodel.ts:22-27, no tx hash) blocks the tx-link column pending a founder-gated amendment, and the own-receipt precedent (auth/router.ts:477-493 serves the member's OWN purchase tx + explorer URL) supports an own-row payload in the auth zone. Refutation attempt failed: a tx-hash-free row version crosses no pin at all, so the gap cannot be excused as genuinely-not-servable.

### A22. [defect-now · era-drift-copy] Header bell tooltip claims the event backbone has not arrived — it is live and powering the pulse on the same page (era-drift truth bug)
- **Where:** artifacts/studio/src/wallet/MemberHeaderAffordance.tsx:284 · lens: data-truth
- **Detail:** MemberHeaderAffordance.tsx:284 title reads 'Notifications — coming soon (arrives with the event backbone).' The event backbone IS live in prod (the served feed renders in MemberPulse on the same dashboard; H2 closed with the complete heartbeat sealed). The parenthetical asserts a dependency is missing when it exists — the exact era-drift class the repo names a truth bug ('text claiming something is not live when it IS live'). MemberSettings.tsx:116-123 already rewrote the identical claim for the identical reason ('the event record is live — member notifications arrive on it'), so the header tooltip is a stale twin the S7-b truth sweep missed. One-line copy fix: e.g. 'Notifications — coming soon. The event record is live; the member notification surface arrives on it.' The trophy tooltip beside it (line 292, 'Seasons & recognition — coming soon') is honest and needs no change.
- **Verifier:** Verified in the working tree: MemberHeaderAffordance.tsx:284 tooltip reads "Notifications — coming soon (arrives with the event backbone)." while the event backbone is live — MemberPulse.tsx (mounted on the same dashboard, MemberAccess.tsx:157) renders the served backbone feed via backboneFeedClient, and MemberSettings.tsx:116-123 already corrected the identical claim in the S7-b sweep with the comment "the event record itself is LIVE... 'not live yet' claimed a missing adapter that exists". This is the era-drift class the binding laws name a truth bug (text claiming something is not live when it IS live). The header tooltip is the stale twin the sweep missed; the trophy tooltip at line 292 is honest and needs no change. The stale framing also appears in the adjacent code comment at lines 276-279 ("They light up with the event backbone... never before") — worth aligning in the same one-line fix, though only the title attribute is user-facing.

### A23. [defect-now · auth-flow] Header sign-in never lands on /member — the founder-ordered redirect does not exist at either success point
- **Where:** artifacts/studio/src/wallet/rainbowAuthAdapter.ts:66 · lens: navigation-flows
- **Detail:** Confirmed as written, with one provenance enrichment: the missing redirect is not an undiscovered oversight but a RECORDED OPEN founder order. SESSION_STATE.md (the authoritative resume point, lines 14-20, S7-b/c/d seal 2026-07-16) opens the founder follow-up "S7-e + data-truth" from the prod screenshot and lists verbatim: "header sign-in must LAND on /member". The in-place resolution was the ORIGINAL Q11-v2 design (docs/direction/OPEN_QUEUE.md Q11 and docs/DESIGN_ROADMAP.md line 41 both record "resolves standing IN PLACE via SESSION_CHANGED_EVENT" as the built pattern) — the founder's 2026-07-16 order supersedes that design, and no code implementing it exists in the working tree. Both success points verified: (1) artifacts/studio/src/wallet/rainbowAuthAdapter.ts:60-68 — verify() is exactly `if (res.ok) announceSessionChanged(); return res.ok;`, no navigation; (2) MemberHeaderAffordance.tsx:143-160 — reSign() calls signInWithWallet(provider, address) at line 154, and walletSession.signInWithWallet (walletSession.ts:92-133) only announces SESSION_CHANGED_EVENT and returns the wired state — no navigation. Grep for setLocation/navigate/window.location across src/wallet: zero matches. Every SESSION_CHANGED_EVENT listener in the app (RainbowKitRoot, HeroSeatCta, header, KPI row, ownReads, etc.) only re-reads state in place — none navigates. The failure-path asymmetry is exact: MemberHeaderAffordance.tsx:193-205 (signError branch) renders a Link to /member, so the only sign-in path that reaches /member today is the one where sign-in FAILED. The fix-shape caution is also correct: rainbowAuthAdapter is mounted globally via RainbowKitAuthenticationProvider in RainbowKitRoot.tsx:75 (wraps the whole app), so the same verify() serves every openConnectModal caller including mid-flow surfaces (/join, /source, /wallet) — an unconditional redirect inside verify() would break those signed flows; the redirect must be pathname-gated or initiator-flagged, and reSign() can redirect unconditionally.
- **Verifier:** REAL. Refutation attempted and failed on all fronts. (a) Code: rainbowAuthAdapter.verify() (rainbowAuthAdapter.ts:60-68) and the reSign() success path (MemberHeaderAffordance.tsx:154 → walletSession.ts:92-133) both resolve in place; zero navigation primitives exist anywhere in src/wallet, and no SESSION_CHANGED_EVENT listener navigates (RainbowKitRoot only invalidates a query). (b) The founder order exists verbatim: SESSION_STATE.md line 16 — "header sign-in must LAND on /member" — recorded 2026-07-16 as the open S7-e follow-up at the S7-b/c/d seal. (c) Laws: violates the founder-order/state-file record and HUMAN-FIRST (a zero-crypto newcomer who signs in from /faq sees only a header pill change and never learns a dashboard exists; the sign-in FAILURE path at MemberHeaderAffordance.tsx:194 lands on /member while success does not). One nuance added in correctedDetail: in-place resolution was the deliberate Q11-v2 design (OPEN_QUEUE Q11, DESIGN_ROADMAP line 41), now superseded by the 2026-07-16 founder order — so the gap is tracked-but-unbuilt, which does not negate the finding. The finding's fix-shape caveat (no unconditional redirect inside the globally-mounted adapter) is validated by RainbowKitRoot.tsx:75.

## B. UNVERIFIED-LIKELY (verifier lost to quota — re-verify at build)

- Member's own Archive artifact holdings: balanceOf(wallet, id) is a live public read shown NOWHERE — the one admitted gap, and it is derivable today with zero server change
- Introductions KPI renders '—' for a member the server has DEFINITIVELY answered has zero introductions (no source exists)

## C. DESIGN GAPS (designed in our research, absent or degraded today)

### C0. Toolkit renders 5 actions vs the designed FULL action registry (origin had 23 actions)
- **Where:** artifacts/studio/src/config/memberActions.ts · lens: design-inventory
- PARTIAL / built-but-thin vs the design: MEMBER_HOME_PLAN §7 ('Toolkit — les 23 actions, verrous visibles') and §11 point 7 ('the FULL action registry rendered as the conversion engine — every member action visible') both describe the origin's 23-action registry (harvest source: origin lib/actions.ts + canAccessAction/actionLockReason). The built MEMBER_ACTIONS registry has 5 actions (copy link, share proof, expand footprint, liquidity, verify seat). The registry comment says 'every action is REAL today (no future filler)' — a legitimate truth-first narrowing, but the SYSTEM-FIRST LAW asks that the full 23-action inventory be put on the founder's screen once so he can pick scope from the whole picture; that inventory pass is not recorded anywhere in the repo.

### C1. The signed-in dashboard has no expand-footprint / Join path — the conversion action is visitor-only
- **Where:** artifacts/studio/src/wallet/CapitalAxisCard.tsx · lens: design-inventory
- DESIGN GAP: the plan's 14-door map includes 'Take your seat /join' inside the member shell, and the own-account rule's whole point is actionable guidance (next rung + progress). But: the quick-actions grid (which carries 'Expand your footprint' → /join) is deliberately VISITOR-ONLY on /member; /join is not a member door; and CapitalAxisCard shows 'Next: Resident at $10' with a progress bar but no route to act on it. A seated member on his own dashboard has no direct path to add to his footprint except leaving via the Toolkit door. One line (a quiet 'Expand your footprint → /join' link on the capital card, recognition-voiced) closes it without violating anti-scarcity (own-account surface, Sephora pattern).

### C2. The `via=` channel layer (SPEC §④, slice R3) — never built despite 'zero dependency, buildable now'
- **Where:** ? · lens: design-inventory
- MISSING entirely: no member share surface appends &via=, no server click/channel capture exists (zero grep hits for via= across artifacts). SPEC_REFERRAL §④ designs it fully (channel ≠ source; self-service; server-logged; attribution provable by pairing click→on-chain receipt) and §⑪ R3 says it 'can be built NOW, in parallel, zero transactions'. It is a named slice (R3) but appears in no active order/queue — a genuinely dropped designed element the member-facing ShareMenu would feed ('12 clicks from Twitter, 1 converted').

### C3. No member-facing 'request my source' flow — yet the MVP proof metric counts source requests
- **Where:** artifacts/studio/src/components/referral/MemberReferralDashboard.tsx · lens: design-inventory
- DESIGN GAP: the link card honestly says 'commission activates when your source is founder-signed', but there is no surfaced way for a member to REQUEST that signature. SPEC §⑩ names the server-side request queue ('file de demandes — jusqu'à l'ÉMETTEUR') as unbuilt work, and the MVP proof metric explicitly measures 'Non-founder members requesting their source ≥2/≥5' during the 30-day window — a metric with no mechanism. Even a minimal path (a request button that lands in the operator console queue, or documented contact) is needed before the proving window opens. The emitter contract (R7) is deliberately ~6 months out (CONNECTOR_LADDER §5), which makes the interim queue matter more, not less.

### C4. Anti-phishing 'official channels' block in Settings — designed, not built
- **Where:** artifacts/studio/src/wallet/MemberSettings.tsx · lens: design-inventory
- MISSING (small): MEMBER_HOME_PLAN étape 7 lists Settings content including 'canaux officiels anti-phishing' (the authoritative list of official links so members can't be phished by look-alikes) and an approval warning (the latter lives correctly on the wallet approvals panel). No official-channels row exists in Settings or elsewhere member-facing; /brand-facts (which would carry official links publicly) is also still unbuilt (Phase 2.10). Cheap, high-safety-value row.

### C5. Door mini-stat badges (entity Dashboard 'a door with a number pulls a click') — LACKS, live-readable today, NO named tracking slice
- **Where:** artifacts/studio/src/config/memberDoors.ts · lens: origin-harvest
- Origin harvest recorded in SEASONS_ENGINE §11: entity-chain's grouped feature cards each carry a mini stat badge ('8 active', 'Live data') — applied to us as 'the Referral door shows the member's live introduction count; season door shows days remaining'. Today's memberDoors.ts renders static notes only, no numbers. The introduction count is ALREADY served own-row by R5 (it's a KPI tile), so the referral-door badge is derivable today; the season badge legitimately waits for Phase 5. No queued slice or inventory line names this chrome — it lives only in the §11 direction text, so it risks being skipped when Phase 5 lands.

### C6. navi-portfolio read-only cross-chain portfolio view — LACKS, in the harvest map but absent from the tracking inventory
- **Where:** docs/direction/LIVING_ORGANISM_MASTER_PLAN.md · lens: origin-harvest
- LIVING_ORGANISM §9 records navi-portfolio-agent's 'read-only cross-chain portfolio view ONLY' as harvest-worthy (the autonomous-advisor vision explicitly dropped). Today's wallet panel reads SYN+USDC on Avalanche only — fine for now, but the capability appears in no SESSION_STATE Phase inventory, no OPEN_QUEUE item, and no reserved slot; it exists only in the §9 harvest map. Mirror-Rule risk: a recorded harvest with no tracking line is how completeness leaks. Low priority; a one-line inventory entry would close the hole.

### C7. KPI provenance and the 'durable' definition live only in hover tooltips — invisible on mobile
- **Where:** artifacts/studio/src/wallet/MemberKpiRow.tsx · lens: human-readability
- Every KPI tile's 'detail' (including the definition 'durable means their wallet still holds SYN' and the footprint shield note) is a title attribute (MemberKpiRow.tsx:40 `<Card title={detail}>`). Title tooltips do not exist on touch devices, so mobile members see '2 durable · 3 total' and a bare '—' with no explanation anywhere — the en-dash-without-explanation class on the fluid-surface-first product. The verify/provenance layer needs a visible affordance (tap/expand or a caption line), not hover-only.

### C8. ProtocolSnapshot renders bare '—' with no explanation at all on failed reads
- **Where:** artifacts/studio/src/components/member/ProtocolSnapshot.tsx · lens: human-readability
- FactRow shows '—' when a chain read fails (correct fail-closed doctrine) but unlike the KPI tiles there is not even a tooltip; a member sees 'SYN burned —' with nothing saying 'temporarily unreadable — nothing is assumed'. One shared honest-dash affordance (visible hint or footnote '— = live read temporarily unavailable') would serve both this card and the KPI row.

### C9. Machinery-dense provenance line on the dashboard: '8 historical · 6 live V3 · snapshot N as of …'
- **Where:** artifacts/studio/src/components/living/MembersProvenance.tsx · lens: human-readability
- The compact MembersProvenance variant embedded in ProtocolSnapshot speaks in engine-version vocabulary: 'historical', 'live V3', 'snapshot … (live runs ahead)'. The dual-authority SHOW-BOTH rule is founder canon, but 'V3' is an internal contract version name reaching users with no human words first. A human lead ('counted two ways: N founding seats + N seats issued by the live engine') can carry the same honesty with V3 in the verifier parentheses.

### C10. Raw wallet/RPC error text passed through to the buyer (fallthrough branch)
- **Where:** artifacts/studio/src/wallet/JoinCheckout.tsx · lens: human-readability
- explainError's fallthrough interpolates the first line of the raw viem/wallet error — up to 180 chars that may contain hex data, addresses, or 'execution reverted' internals — directly into checkout copy: 'The transaction did not go through: {raw}'. Honest, but the raw fragment should be framed as the verifier detail after a human sentence, not spliced mid-sentence. Sibling: MemberWalletPanel.tsx:53 (same pattern, 160 chars).

### C11. Post-purchase receipt is a dead end — 'Seat #N is yours' offers no door to Member Home
- **Where:** artifacts/studio/src/wallet/JoinCheckout.tsx · lens: navigation-flows
- JoinCheckout's receipt state (JoinCheckout.tsx:436-468) — the single strongest moment in member life, the seat just written on-chain — shows the seat line, the SYN amount, and the explorer tx link, then stops. No 'Open Member Home' button, no next step. The brand-new member is stranded on /join; their only route to the dashboard is discovering the header pill dropdown. Every Binance/Coinbase-class flow lands a completed purchase on the portfolio. Given the founder's demand that member life lands on /member, the receipt card needs a primary 'Open Member Home' CTA (and this also naturally satisfies the redirect demand for the door-band sign-in that happens mid-join). Same applies to the repeat-purchase branch ('your seat stays #N') — that member came from 'Expand your footprint' and should be offered the way back to the dashboard where their footprint/rung just moved.

### C12. Session expiry silently demotes /member to the marketing door — no 'session expired' state exists
- **Where:** artifacts/studio/src/pages/MemberAccess.tsx · lens: navigation-flows
- Sessions are absolute-TTL 60 minutes (authConfig.ts:34 SESSION_ABSOLUTE_TTL_MS = 60*60*1000) and in-memory (wiped on every deploy/restart). When the session dies while a member sits on /member, the next SESSION_CHANGED/refocus read flips useOwnSession to false and the whole dashboard is replaced by the visitor door ('Your seat lives here' + ConnectButton) with zero explanation — the member's KPIs, referral standing and settings vanish as if they were never signed in. The header affordance handles this state well (the 'Sign in' re-sign button with the honest tooltip, MemberHeaderAffordance.tsx:207-219) but the page-level UX contradicts it: the page says 'visitor', the header says 'your session expired'. A Coinbase-class product shows 'Your session expired — sign back in' with a one-click re-auth IN PLACE and keeps the page context. Minimal fix: MemberAccess distinguishes 'never signed in' from 'was signed in this pageview, now S1' and renders an expired-session banner with the same reSign action the header uses, instead of the cold marketing door.

### C13. 'Expand your footprint': no duplication — the member dashboard has NO expand action at all; the action shows only to people without a seat
- **Where:** artifacts/studio/src/wallet/CapitalAxisCard.tsx · lens: navigation-flows
- Answer to the duplication question: the header CTA (HeroSeatCta via SeatCtaSlot, PublicLayout.tsx:302-307) is the ONLY expand entry a seated member ever sees, because MemberQuickActions — which carries the 'Expand your footprint' action card — renders exclusively in the VISITOR composition (MemberAccess.tsx:304-306; the member dashboard deliberately drops the grid). Two consequences. (1) Inversion: the quick action whose copy addresses seat-holders ('A further purchase adds SYN to the same seat — never a second seat', memberActions.ts:49-53, lock:'none' so it renders unlocked) is shown ONLY to signed-out visitors and no-seat sessions — the wrong verb for someone whose action is 'Take your seat'. (2) Gap: CapitalAxisCard shows 'Next: {rung} at $X' with a live progress bar (CapitalAxisCard.tsx:97-119) but offers no way to act on it — a progress meter with no button is a dead end by every product-design standard (Sephora/Marriott, the card's own cited pattern, always pairs progress with the action). Recommended: put the 'Expand your footprint' → /join button on CapitalAxisCard (own-account surface, legal per the OWN-ACCOUNT DISPLAY RULE), and either gate the visitor-facing quick action behind lock:'seat' or reword it for visitors.

### C14. Em-dash conflates three different truths in the KPI tiles: loading, read-failure, and honest non-derivability — and the footprint tooltip gives the WRONG explanation on a server error
- **Where:** artifacts/studio/src/wallet/MemberKpiRow.tsx · lens: frontend-quality
- KpiTile (MemberKpiRow.tsx:48) renders '—' for value===null, but null means three distinct things: (a) the read is still in flight (every tile shows '—' on first paint), (b) the read FAILED (500/429/RPC down), (c) the figure is genuinely not derivable (early-era footprint, V1 rows). The repo's own precedent solves this: MemberPulse.tsx distinguishes undefined ('Checking…') from null (honest unavailable-note) — the KPI row, ProtocolSnapshot's FactRow (ProtocolSnapshot.tsx:19 value ?? '—') and MemberWalletPanel (:159/:165 reads.syn ?? '—') should adopt the same three-state shape (loading skeleton/pulse | honest absence text | value). Sharpest sub-case: the footprint tile's tooltip (MemberKpiRow.tsx:116) says 'Early-era footprints are honestly not derivable' — but useOwnFootprint returns null equally when /api/backbone/capital-standing 500s or member-standing fails, so on a server error the UI EXPLAINS the dash with a false reason. That is the era-drift truth-bug class (a claim that doesn't match protocol reality). The server route already separates the cases (capitalStanding.ts route serves rung:null + an honest note for unwalked seats vs a 500 for failure; the client fetchCapitalStanding maps them to CapitalStanding-with-null-rung vs null) — the tile just needs to key its wording off which one it got. Per the LIVE-PRODUCTION RULE, a readable figure shown as a dash is a bug — loading must look like loading.

### C15. CapitalAxisCard silently vanishes on a capital-standing 500 — no retry, no honest note, ladder and shield line gone
- **Where:** artifacts/studio/src/wallet/CapitalAxisCard.tsx · lens: frontend-quality
- CapitalAxisCard.tsx:62 `if (seat === null || standing === null) return null` — correct for no-seat (the band owns that state), but standing===null ALSO covers a transport/500 failure of /api/backbone/capital-standing for a member who has a genuine footprint: the whole card (footprint figure, 12-rung ladder, the always-render shield line) disappears with no explanation and no retry, and its comment ('nothing to say; the band owns the no-seat state') doesn't cover the error case. The honest-gap path (rung null, early-era copy at :128-131) is well handled — only the transport-error path is swallowed. Minimum fix: when seat!==null and standing===null, render the card frame with the honest 'could not be read right now' line and a retry (the MemberYourSeat retry pattern), keeping the ladder from config if desired. Also note the double-fetch chain here (seat effect at :30-46 then standing effect at :48-58) means the card pops in only after two sequential round-trips, and a SESSION_CHANGED that leaves the seat value identical never re-reads the standing (dep [seat] unchanged) — a footprint rise mid-session won't refresh until reload; acceptable today but worth folding into the shared-read hook of finding #3.

### C16. Stale-response race in every SESSION_CHANGED re-read hook — overlapping fetches can resolve out of order and strand the wrong state
- **Where:** artifacts/studio/src/wallet/ownReads.ts · lens: frontend-quality
- The pattern repeated in MemberYourSeat.tsx:62-75, MemberHeaderAffordance.tsx:114-127, MemberSettings.tsx:66-79, MemberKpiRow.useOwnFootprint:57-79, ownReads.useOwnSourceStanding:98-111, MemberReferralDashboard.useOwnSourceStanding:39-62, MemberQuickActions.useOwnState:30-56 and WalletSessionBoot.tsx:18-34 guards only UNMOUNT (`let active = true` flipped in cleanup) — it does not cancel the PREVIOUS in-flight read when the event fires again. Two reads triggered close together (sign-out immediately followed by re-sign-in; the sign-in flow itself fires SESSION_CHANGED from both signInWithWallet and the RainbowKit adapter path) race: the older response can resolve last and overwrite the newer truth — e.g. the pre-login S1 readback landing after the post-login S4 readback leaves the header/band showing signed-out with no further event to correct it. useOwnFootprint has the longer two-hop chain (member-standing → capital-standing), widening the window. Fix: a per-read token (`const seq = ++readSeq; ... if (seq !== readSeq) return`) or AbortController per read() invocation — or simply adopt the React Query design from finding #3, which serializes this by construction. Related same-family cleanups: setTimeout(…,1500) leaks in MemberYourSeat.copyProof:80, MemberQuickActions:69, MemberReferralDashboard.copyLink:206 (benign under React 18, still unclean); MemberWalletPanel.refresh (:84-102) has no stale-guard either — an old-address refresh resolving after an account switch can print the previous wallet's balances.

### C17. ShareMenu popover lacks dismissal and menu semantics
- **Where:** artifacts/studio/src/components/referral/ShareMenu.tsx · lens: a11y-visual
- The hand-rolled share popover (used twice on the member referral card: the link card and ShareCard) has aria-expanded on the trigger but no aria-haspopup/aria-controls, no Escape-to-close, no outside-click dismissal, and no focus management — a keyboard user who opens it and tabs past the last item leaves it hanging open over content; a screen-reader user gets no announcement that a menu opened. The repo already ships Radix DropdownMenu (used in MemberHeaderAffordance) which handles all of this — swapping the hand-rolled popover for it closes the gap with zero new patterns.

### C18. No skip-to-content link sitewide
- **Where:** artifacts/studio/src/components/layout/PublicLayout.tsx · lens: a11y-visual
- PublicLayout renders a dense sticky header (wordmark, up to ~10 nav links, dropdown, socials, chips, theme toggle, member menu, CTA) before <main>, with no skip link and no id/tabindex on main — keyboard and switch users must traverse the entire header on every page, including the member dashboard. A visually-hidden 'Skip to content' anchor as the first focusable element + id on main is the standard fix; the header's focus-visible ring styling already exists to style it consistently.

### C19. Header/nav sub-12px cluster — highest-priority slice of the recorded sweep (seen on every page)
- **Where:** artifacts/studio/src/components/layout/PublicLayout.tsx · lens: a11y-visual
- Within the recorded general sweep, the header cluster deserves to lead because it renders on EVERY page: the wordmark descriptor is text-[8px] (sm: 9px) — the smallest user-facing text on the site, in the masthead (PublicLayout.tsx:172); primary nav links are text-[11px] and only reach 12px at the 2xl breakpoint (:221, :243); the chain pill and live chip are 9px (:103, :119); the chapter badge is 9px (:180); the More-dropdown per-item path hint is 10px (:273). These are labels/meta (floor: >=12px) and the nav links are the site's primary interactive text. No focus-visible or aria gaps here (rings and aria-labels are present) — purely the size class.

### C20. "Learn & Earn earns XP" — the crypto industry's paid-in-tokens brand phrase on a public surface
- **Where:** artifacts/studio/src/config/memberDoors.ts · lens: doctrine-legal
- MEMBER_HOME_RESERVED_SLOTS renders on BOTH /member states, including the public prerendered visitor view. The Quests slot note reads 'Daily and weekly recognition quests — Learn & Earn earns XP.' Bare 'earn' escapes guard-forbidden-copy (only the phrase 'earn rewards' is banned), but 'Learn & Earn' is verbatim the name of programs that pay crypto (Coinbase Earn et al.); on a token-membership site the default reading is 'complete lessons, get paid tokens'. The object here is XP (recognition points) and the adjacent Season slot carries 'never a cash figure', but the Quests slot itself carries no such negation. Positive 'earn' outside a negation on a public surface is the doctrine's named forbidden class — the XP object mitigates but does not neutralize the branded phrase. Fix: reword to e.g. 'Daily and weekly recognition quests — learning grants XP. Recognition only, never a payment.' (Optionally extend the guard: ban 'Learn & Earn' / 'learn and earn' as phrases.)

## D. POLISH

- **D0.** Seat-proof sharebility is copy-link only — no QR / share menu for the receipt (M2 'every link that matters') — PARTIAL: MVP piece 2 wants QR + share surfaces for every link that matters, naming seat proof explicitly. The receipt has 'Share my proof' (clipboard copy) on the band, header menu, and quick actions — but no QR and no ShareMenu, while the referral link has both. Cheap alignment: reuse QrCodeBlock/ShareMenu for the receipt explorer URL.
- **D1.** Designed 'USDC routed by this member' figure — superseded by the footprint tile, with a residual nuance — PARTIAL/superseded: §11 slot 2 designed 'USDC routed (Coming soon — receipt adapter)'. The built KPI row instead ships 'Your footprint' (cumulative gross purchases — effectively the routed figure for V3 seats) and 'Your USDC' (wallet balance, an addition beyond the design). Residual nuance: gross ≠ net-routed (net = gross minus referrer payment); if the member-facing voice ever claims 'routed', th
- **D2.** Header member pill — BUILT per §11 (sigil + seat + era → identity menu) with one small design residue — BUILT: the pill (sigil · Seat #N · Genesis badge) opens the designed identity menu (Member Home · Settings · copy address · share proof · verify own receipt · disconnect), server-readback-only, honest three states. Residue: the reserved bell/trophy icons render desktop-only (seated && !mobile) — the mobile header loses the two reserved slots the wireframe treats as the return-visit hook; and the m
- **D3.** Notification center (Supa bell + list + broadcast) — LACKS, tracked; header bell tooltip carries stale-dependency copy — Origin harvest: Supa's fully in-app notification system (bell, list page, AdminBroadcast console, typed generators) — recorded whole in SEASONS_ENGINE §11 with our three channels. Not built; bell reserved in the header, Settings row honest. One era-drift-class polish: the reserved bell's tooltip still says 'coming soon (arrives with the event backbone)' while the event backbone has been LIVE since
- **D4.** Duplicate 'Your referral' heading stutter — MemberAccess renders an h2 'Your referral' + intro sentence, then MemberReferralDashboard immediately renders its own h3 'Your referral' + lifecycle badge (line 269) — the member reads the same heading twice in a row on both the visitor and dashboard states. One of the two should own the heading.
- **D5.** Era badge inconsistency: the band labels V3-era members 'Member', the header deliberately doesn't — MemberYourSeat.eraLabel maps V3_EMITTED → 'Member' and renders it as a gold meta item right after the 'Member #N' heading (redundant: 'Member #14 · … · Member'), while MemberHeaderAffordance.eraLabel (line 97-99) deliberately returns null for V3 with the comment 'V3-era members are simply members, so their pill stays clean'. One truth, two renders — the band should adopt the header's rule (Genesis
- **D6.** 'Retry read' and 'Reading your live on-chain state (claim gate · allowance · balance)…' — builder phrasing — Two light siblings: the standing-unreadable retry button says 'Retry read' (MemberYourSeat.tsx:220) where 'Try again' is the human word; the checkout wait state parenthesizes internal step names 'claim gate · allowance · balance' (JoinCheckout.tsx:299) — the parenthetical pattern is sanctioned but 'claim gate' is an internal name a buyer never met ('checking your wallet's history, approval, and ba
- **D7.** Settings Notifications row: 'The event record is live — member notifications arrive on it' — 'The event record' and 'arrive on it' are system-view phrasing; a member wants 'Notifications are coming — they will be built on the protocol's live event history; the bell in the header already marks the spot.' Same voice issue, milder, in the reserved header tooltips ('arrives with the event backbone', MemberHeaderAffordance.tsx:284) — 'event backbone' is internal architecture naming.
- **D8.** Sign-out lands in place with no confirmation, and 'Disconnect · end wallet session' does not actually disconnect the wallet — Both sign-out points (MemberHeaderAffordance.tsx:488-494 handleDisconnect, MemberSettings.tsx:139 settings-disconnect) call logoutSession() only: the server session dies, SESSION_CHANGED fires, and the user stays wherever they are with no 'signed out' cue — on /member the dashboard silently morphs into the marketing door. Acceptable as a landing (founder gave no sign-out order) but two polish prob
- **D9.** Signed-in member gets a flash of the visitor door on every /member load — useOwnSession initializes signedIn=false and only flips after the async /api/auth/session fetch resolves (MemberAccess.tsx:66-90), so every navigation to /member paints the full visitor door ('Your seat lives here' + ConnectButton + how-a-seat-works) first, then swaps to the dashboard. On the rig this is ~50ms; in prod with cold network it is a visible logged-out flash — the classic FOUC-of-auth t
- **D10.** No one-click way back to the dashboard from public pages — Member Home is always two clicks deep — The member doors sidebar exists only on /member itself; on every other page (/wallet, /toolkit, /activity, /chronicle…) the sole route back is the header pill → dropdown → 'Open Member Home' (two clicks, and on mobile it is hamburger → sheet → pill → menu). The header's prime CTA slot next to the pill is 'Expand your footprint' → /join — a purchase action, not a home. Exchanges make the logged-in 
- **D11.** Door active-state can never light for hash doors, on desktop or mobile — MemberShell computes active as door.href === location, but wouter's location is the bare pathname ('/member') while two doors carry hashes ('/member#settings', '/member#referral-dashboard') — strict equality never matches, so those doors never show the active treatment; conversely the 'Member Home' door stays active while the member is deep in the settings section. Cosmetic, but it degrades the do
- **D12.** Two drifting copies of useOwnSourceStanding and of the usd() cents formatter — the exact drift ownReads.ts was created to prevent — ownReads.ts's header states 'one read implementation, never two drifting copies', yet MemberReferralDashboard.tsx:37-64 carries its own useOwnSourceStanding (dynamic-import variant of ownReads.ts:96) and :66-71 its own usd() — byte-for-byte the same math as ownReads.usdFromRaw:116. Both hooks mount together on the member dashboard, which is why source-standing is fetched twice (counted in finding 
- **D13.** Progressbar aria is present and correct on both member bars; focus-visible relies on UA defaults on member links — POSITIVE checks the task asked for: (1) both progress bars carry role='progressbar' + aria-label + aria-valuemin/max/now (CapitalAxisCard.tsx:105-113 'Your footprint toward the next rung'; MemberReferralDashboard.tsx:134-141 'Progress to the next Connector rung') — SRs will announce the percent correctly from the 0..1 range; minor polish: add aria-valuetext for a friendlier announcement. (2) Icon-
- **D14.** Identity-band rung pill tooltip omits the financial-benefit negation of the shield line — Shield coverage audit: CapitalAxisCard carries the full SHIELD_LINE (line 23-24, rendered at 156-158) and the KPI footprint tile tooltip carries the negation ('recognition only, never a financial benefit', MemberKpiRow.tsx:116). But the capital-rung pill in the identity band — the FIRST place a member meets his rung — tooltips only 'Your footprint on the capital axis — recognition on the public re
- **D15.** ShareCard puts a commission dollar figure on a deliberately shareable artifact without its boundary line — The share card face renders '$X in verified referral commissions' (subline) with only the tagline 'Proof, not claims — every figure is on-chain and verifiable.' The referral module's boundaryLine ('…not token yield, not passive income, not equity, and not a guarantee of token appreciation') renders once, at the very bottom of MemberReferralDashboard, two cards below. On the dashboard the adjacency
- **D16.** CapitalAxisCard footprint figure has no verify path; KPI provenance is hover-tooltip-only (invisible on touch) — VERIFY-FIRST: every figure carries a verify path. The cumulative footprint on CapitalAxisCard says 'public and recomputable by anyone' but offers no link or anchor (no /activity pointer, no engine link) — prose is a claim, not a path. The four KPI tiles carry their provenance solely in HTML title attributes, which never fire on touch devices — a mobile member (the FLUID SURFACE audience) sees four
- **D17.** Stale JSDoc in referralProgram.ts still claims the R5 indexer 'is not wired yet' — The memberCards doc comment reads 'the introduction read-model / indexer is SPEC slice R5 and is not wired yet, so per-member histories honestly say Not live yet' — but R5 is live (MemberReferralDashboard.tsx header: 'the R5 introduction indexer is live'; the card notes themselves were updated to 'served from the R5 introduction read-model'). Not user-facing, but it is a false truth claim sitting 
- **D18.** Doctrine/verify texts in the header member menu render below the readability floor — Legal-relevant copy in MemberHeaderAffordance sits under the 12px floor: the member's own 'Verify my seat on-chain' receipt link is text-[9px] (line 404) and the seat-vs-session doctrine note ('Your seat is your membership. Signing in… only proves you control this address.') is text-[11px] (line 415); the 'Verified on chain' pill and chapter chip are text-[11px] (369, 382). MemberWalletPanel's poo
- **D19.** ADR-001 readability amendment's RESTE clause overstates what remains (syn-label/syn-caption/MembersProvenance already raised) — The 2026-07-16 READABILITY FLOOR amendment's remaining-scope clause (ADR-001 lines 129-132) lists 'syn-label/syn-caption 9-10px, MembersProvenance compact' as still to sweep, and point 4 (line 123) says '--text-caption 11px → à réévaluer au sweep'. Verified in code: the S7-c token-level raise DID land — index.css:230-232 has syn-eyebrow/syn-label/syn-caption all at 12px, index.css:122 has --text-c

## E. THE LEDGER (already-tracked / open items — the complete book)

- **E0.** Sub-12px text survives in the member header dropdown (text-[9px] and text-[11px]) — PARTIALLY cured vs the READABILITY FLOOR (ADR-001, 2026-07-16). The /member chunks were measured to zero sub-12px, but the member-facing header dropdown still carries text-[9px] on 'Verify my seat on-chain' and text-[11px] on the Verified-on-chain pill, the chapter chip, and the session explainer; M
- **E1.** Season card slot (SEASONS_ENGINE §11 slot 3) — reserved, not built — PARTIAL by design: the reframed SeasonHighlightBubble (season name = era binding, countdown, own XP, own rank, next quest chip, recognition-only voice) is not built; Member Home reserves it visibly as a FUTURE-badged card ('The current season — your XP, your rank, the next quest'), exactly as §11 pr
- **E2.** Quests strip slot (§11 slot 4) — reserved, not built — PARTIAL by design: the 2–3 daily/weekly quest chips with XP + axis tags (Learn & Earn rides here) are Phase-5; the slot is visibly reserved with a FUTURE badge and honest note. Tracked: SEASONS_ENGINE §11 slot 4, MASTER_BUILD Phase 5 gamification slice.
- **E3.** 'While you were away' return recap (§11 slot 5) — reserved, not built, though its data dependency is now LIVE — MISSING as a surface; the slot card is honest ('the event record is live; this member surface arrives on it' — the S7 truth sweep fixed the stale dependency claim). Note the design's original blocker (the event indexer) is DONE (M4/H2 complete heartbeat sealed in prod), so this is now buildable-on-e
- **E4.** Notification center (three channels, bell, list page, broadcast console) — not built; bell + trophy reserved in header — MISSING: the harvested in-app notification system (targeted 1-to-1 typed generators incl. 'promotion due/signed', broadcast console with preview/expiry/history, protocol notifications with verify links) from SEASONS_ENGINE §11 'Notification center' does not exist. The header reserves the bell + seas
- **E5.** Alias (opt-in display name, default = seat number) — not built — MISSING: the §11/SPEC §③ alias layer (opt-in name above the hex, default anonymous 'Member #N', sourceId untouched, alias-accepting /join?source=name links) does not exist. Settings carries the honest FUTURE row. Tracked: M8 IDENTITY-ALIAS thin in the MVP 30-day map (founder-curated writes through t
- **E6.** Avatar: sigil LIVE; uploaded image and NFT avatar — not built — PARTIAL per the founder's three-source decision (2026-07-13: ① sigil ② upload ③ NFT-verified, in that order). ① is live everywhere (deterministic MemberSigil). ② waits on the object-storage infra decision (Replit App Storage candidate) + the admin 'hide avatar' moderation control; ③ (ownerOf-verifie
- **E7.** Referrer Kit v1 — the GENERATED living card image (M2) is not built; the DOM ShareCard is real but lesser — PARTIAL: MVP piece 7 / M2 specifies one click → a client-GENERATED image (canvas) carrying seat · chapter · verified/durable introductions · standing, plus link + QR — the shareable artifact that travels into social feeds. What's built is an on-page DOM ShareCard with real indexed figures (introduct
- **E8.** Collectible/vanity layer (M3): seniority line and seat-number majesty not built; chapter identity IS built — PARTIAL: chapter identity renders from chapters.ts (band meta line + header chip with the honest 'earlier is never better' tooltip). Missing from M3's THIN-V1: the seniority line ('seat #N of M — joined before X% of the story', live reads), the seat-number-in-majesty treatment on receipt/member surf
- **E9.** Co-witness timeline ('what history the seat has SEEN') — not built — MISSING: MVP piece 3's co-witness element (the seat's lived history alongside protocol events). Explicitly listed as M3's 'deliberately waits' (needs M4 depth — which is now live, so it unblocks whenever M3 opens). Tracked in the MVP map.
- **E10.** My Economy / cockpit narrative arc (Identity→Place→Ownership→Momentum→Action→Memory→Proof) — not built as designed — PARTIAL: the S7-b dashboard delivers Identity (band), Ownership (KPI + capital card), Action (referral module + doors), Memory (ChronicleLatest), Proof (verify paths everywhere) — but the LIVING_ORGANISM §3 'citizen's ledger' (your receipts, USDC placed as contribution depth, routing impact, archive
- **E11.** 'My Activity' own-row lens (My | Protocol toggle on the pulse) — not built; explicitly next in the acted order — MISSING: the member's own event lens over the indexed record. MemberPulse ships protocol-only lines and its comment reserves the toggle ('gains My | Protocol with A1'). Tracked: A1 in the founder's acted order (M-INT-1 → A1 → B1 → A2 → M-INT-2), SESSION_STATE 2026-07-16.
- **E12.** 'My Chronicle' (own milestones + register presence) — not built; named A2 in the acted order — MISSING: the member's own milestones/register-presence view. ChronicleLatest shows the register's newest entry (built, promotion-date-sorted). Tracked: A2 in the acted order.
- **E13.** Per-receipt histories (introductions, receipts, commissions as rows) — honest PENDING_ADAPTER card; named M10 — PARTIAL: counts are indexed and live (introduced, durable, commission paid, escrow — all rendered); the row-level histories with per-row verify links are not served. The dashboard says so honestly (dashed card, PENDING_ADAPTER, 'arrives with row-level serving'). Also covers the member's own repeat-p
- **E14.** Member's own multi-axis recognition standing (the 11 axes) — not built — MISSING: the plan's 'Recognition — ton standing sur les 11 axes' (indexer-unblocked list) and D5 verifiable reputation. /recognition exists as a public doctrine page (door live), and the Connector axis IS served (ladder standing in the referral module) + the Capital axis (rung) — 2 of the axes live 
- **E15.** Learn & Earn (quiz → XP/recognition loop over /learning) — not built; Phase-5 by design — MISSING: SEASONS_ENGINE §7.5's module+quiz+pass→recognition engine on top of the existing /learning page (which exists, Learning.tsx). Explicitly Phase 5, same recognition engine as seasons. Tracked: §7.5 + the Quests reserved slot copy already promises 'Learn & Earn earns XP'.
- **E16.** The Guide (deterministic support assistant → capped LLM escalation, floating robot) — not built; /support is an honest teaser — MISSING: LIVING_ORGANISM Track B (B1 deterministic Guide → B2 security spine → B3 LLM → B4 user-level awareness → B5 floating robot UI). /support renders a NOT_ACTIVE-badged teaser describing future channels/triage — honest, no fake '1' badge. Tracked: Track B is ordered after the FAQ (A1) in the ma
- **E17.** Gamified feedback/bug-report module (Supa Feedback harvest → Verifier/Builder/Historian XP) — not built — MISSING: SEASONS_ENGINE §11's founder-remembered feedback module (5 categories with visible rewards, screenshot drop, operator-validated XP crediting). Ships as an admin-managed module Phase 3+ with the Guide; XP wiring Phase 5. Tracked in SEASONS_ENGINE §11 with the anti-spam timing rule already re
- **E18.** Gift-a-seat (recipient field on checkout, recipient-side guards, buyer ≠ recipient receipt) — not built; named M9 — MISSING: M9's recipient field + the recipient-side guard move (the C4 trap) + 'their wallet, permanently' copy. Matters to the member surface because adopted gift seats (#13, #14, #2–#8 destined gifts) need their 'bought as gift' label path (M8+M9). Tracked: M9 in the MVP map, founder-timing.
- **E19.** Session durability across deploys (Q14) — sessions still in-memory; header re-sign is the mitigation — PARTIAL: the designed companion to checkout (DB-backed/persistent sessions so a member never re-signs after a deploy) is not built; MemberHeaderAffordance ships the honest re-sign flow for lapsed sessions (built, prod-proven path). Tracked: Q14 named in the 2026-07-12 handoff as an infra decision. T
- **E20.** Wallet door with APPROVALS PANEL — BUILT to spec (the plan's 'most important of all') — BUILT and conforming: /wallet ships own SYN+USDC balances (fail-closed), the allowances read toward the known spender (Sale V3), APPROVE ≠ PAYMENT in plain words, simulate-first revoke via approve(0) signed by the member (never a server write), honest revert translation, and the /liquidity pointer w
- **E21.** Referral link card (§11 slot 2b) — BUILT exactly to the founder decision — BUILT and conforming: permanent derived link (keccak sourceId, SPEC §③), the TWO honest states read live from the registry (ACTIVE → 'commission paid inside the buyer's own transaction — live'; not signed → 'permanent… activates when founder-signed'), copy/QR/ShareMenu, never a dead or lying button,
- **E22.** Connector ladder standing + promotion-due transparency — BUILT to CONNECTOR_LADDER_POLICY — BUILT and conforming: the 7 founder-final rungs (5/5/6/7/8/10/12%, Partner as separate class at the 30% cap) mirror the policy exactly in config; the member module renders durable-introduction counts (as-of block labeled), the never-empty progress bar (§6 pinned UI law), 'rate never decreases, never
- **E23.** Identity band, KPI row, capital card, doors, settings, two-shells, door band — BUILT per the approved S7/S7-b wireframes — BUILT and conforming, verified against the specs: 'Your Seat' band (sigil · Member #N h1 · Seat Held/No Seat states · rung pill title-only · era/chapter meta · receipt chip + share + verify · honest unreadable+retry · noSeat conversion card not buried); 4 live KPI tiles (fail-closed '—'); CapitalAxi
- **E24.** Fire Ledger, Activity, Chronicle, Archive, Protocol graph doors — open and truthful (the teaser era closed) — BUILT: every door in the member shell opens a real page today ('Coming soon' group emptied at the S7 truth sweep); Fire Ledger carries the live burn total + numbered record (heartbeat burns #1–#8 served); the plan's door cuts (Economy→/tokenomics, Registry→/contracts, Protocol graph→/map, My Syndica
- **E25.** Internal explorer + address labeling + paid identity services (D1/D2) — not built; post-MVP tracks — MISSING by phase: the MiniExplorer harvest, known-addresses labeling extension, and the paid verified-label service are LIVING_ORGANISM Track D / Phase 5–6 items (§9 records the Explorer as the Phase 5–6 item). Member-facing only indirectly (labels would humanize member surfaces). Tracked in LIVING_
- **E26.** Entry-tiers / join checkout with live quote — BUILT (C1–C5 arc); featured amounts + referral display conform — BUILT: /join ships featured amounts (JOIN_AMOUNTS_USDC) + custom compose + the engine's own exact quote (raw strings, fail-closed), the referrer-payment line derived from the QUOTE with the payoutWallet consistency assertion (SPEC §⑧'s two bugs guarded), APPROVE≠PAYMENT flow, seat from the emitted e
- **E27.** Member-side operator/console touchpoints correctly absent; M-INT operator ledger pending as ordered — CONFORMING + tracked: no operator door/action exists in any member-facing config (the locked≠hidden vs removed-entirely split is implemented as designed). The operator-side member intelligence (M-INT-1 per-seat dossiers/RFM, M-INT-2 §D users-registry/audit) is the founder's acted-order step ① and un
- **E28.** Seasons=Era engine, XP, badges, leaderboard, recognition catalog, admin lifecycle — entire Phase-5 engine unbuilt (by design), prerequisites now partially met — MISSING by phase, worth one status line for the founder: the whole SEASONS_ENGINE (season=era binding, XP axes, quest engine, badge tiers, celebration modals, rank snapshots, recognition catalog, the reframed admin lifecycle) remains capture-only. Its stated prerequisites: content suite (partial), a
- **E29.** Wireframe's 'Chapter' identity — BUILT as the canonical constant everywhere it was specified — BUILT: chapterForSeat (chapters.ts canon) renders on the band meta line, the header menu chip (with the doctrine tooltip 'recognition, not a rank; earlier is never better'), and the hero overview chapter config — the deferred 'clean import path' item from the 2026-07-11 handoff got resolved.
- **E30.** Quick-stats strip (entity-chain Dashboard) — HAS BETTER — Origin harvest: entity-chain Dashboard.tsx quick-stats strip at the top of the dashboard (SEASONS_ENGINE §11). Today's dashboard has it better: MemberKpiRow renders 4 LIVE own-row tiles (Your SYN, Your USDC, Your capital footprint '$cum + rung', Introductions), each Label→live Value→provenance toolt
- **E31.** Wallet panel + approvals (entity Wallet.tsx/EnhancedWallet + MEMBER_HOME_PLAN) — HAS BETTER — Origin harvest: entity Wallet.tsx/EnhancedWallet chrome plus the superseded MEMBER_HOME_PLAN's 'most important of all' approvals panel. Built at /wallet (pages/MemberWallet.tsx → MemberWalletPanel): live own SYN+USDC, allowances toward known spenders (Sale V3), APPROVE≠PAYMENT in plain words, REVOKE
- **E32.** ShareCard / ShareMenu / QrCodeBlock (origin TheSyndicate) — HAS BETTER — Origin harvest: TheSyndicate ShareCard/ShareMenu/QrCodeBlock (LIVING_ORGANISM §9). Built in components/referral/: the 'verified introducer' card renders the member's OWN indexed on-chain figures (introduced count, verified commission total) + the real derived permanent link + QR + share menu — real 
- **E33.** My-referral-link card (SEASONS_ENGINE §11 slot 2b, founder decision) — HAS IT — Recorded: every member gets their PERMANENT derived /join?source= link with two honest states (ACTIVE vs awaiting founder signature). Built inside MemberReferralDashboard ('Your referral link' block + ShareCard), with 3 distinct honest standing states after the S7-b founder-screenshot fix.
- **E34.** Header member pill + reserved bell/trophy slots (§11 Header harvest) — HAS IT — Recorded: member pill = sigil + seat # + era badge → identity menu; two reserved header slots (notification bell + season trophy) docking the Supa SeasonHighlightBubble at Phase 5. Built: MemberHeaderAffordance renders the sigil pill, Genesis era badge, chapter chip (recognition-only), own-receipt '
- **E35.** Season card / SeasonHighlightBubble (Supa) — LACKS, tracked Phase 5 + slot reserved — Origin harvest: Supa SeasonHighlightBubble (name + status + countdown + participants + progress + CTA, reframed to recognition-only). Not built — correctly: Phase-5 recognition engine depends on the season=era binding with the NEW sale/era contract. Tracked: MEMBER_HOME_RESERVED_SLOTS 'Season' (FUTU
- **E36.** Quests strip + multi-axis XP (Supa questService/questHelpers) — LACKS, tracked Phase 5 + slot reserved — Origin harvest: quest engine with daily/weekly reset, baseXp+bonusXp, per-axis XP mapping (our Capital/Builder/Connector/Verifier/Historian axes). Not built (Phase 5). Tracked: MEMBER_HOME_RESERVED_SLOTS 'Quests' (FUTURE) + Phase-5 inventory 'Recognition engine — XP + quests + badges + season leader
- **E37.** Badges + tiers + BadgeCelebrationModal (Supa badgeService) — LACKS, tracked Phase 5 — Origin harvest: badge engine (conditionType/tier novice→grandmaster) + the celebration-modal 'moment of recognition' + notification. Not built. Tracked: SESSION_STATE Phase-5 recognition-engine inventory + Recognition catalog line (badge/feature/access/cosmetic/collectible/physical; token/boost/cash
- **E38.** Own-row leaderboard rank + rank-tier visuals + season archive (Supa Seasons.tsx) — LACKS, tracked Phase 5 + Honour Roll — Origin harvest: 'your XP + your rank' own-row beside the leaderboard, top-3 crown/medal ring visuals, past-seasons archive accordion; origin ReputationLeaderboardPreview likewise recorded. Not built. Tracked twice: Phase-5 recognition engine AND 'Public leaderboard — HONOUR ROLL, not a directory' (P
- **E39.** While-you-were-away (origin use-unread-protocol-events + visitor-memory) — LACKS, tracked; dependency now LIVE — Origin harvest: the return-visit engine ('Since you were away'), already coded in origin TheSyndicate (use-unread-protocol-events.ts, visitor-memory.ts, last-visit.functions.ts — named in OPEN_QUEUE's PIPELINE-CHRONICLE entry as feeding this reserved slot). Not built; slot reserved FUTURE on both da
- **E40.** Internal explorer (entity Explorer/BlockExplorer + origin MiniExplorer) — LACKS, tracked Phase 5–6 — Origin harvest: entity Explorer.tsx/BlockExplorer.tsx + origin MiniExplorer + known-addresses.ts labeling seed. Not built; no member-facing explorer exists (verify links point at the public Snowtrace explorer — honest interim). Tracked: SESSION_STATE Phase 5–6 'Internal explorer (harvest MiniExplore
- **E41.** MyEconomy micro-ledger + cockpit narrative arc (origin MemberCockpit) — PARTIAL, remainder tracked C3/A1/A2 — Origin harvest: MyEconomy (your receipts, USDC placed as contribution depth, routing impact, archive memories) + the cockpit arc Identity→Place→Ownership→Momentum→Action→Memory→Proof. Today's dashboard covers the spine: identity band, footprint cumulative + full 12-rung ladder + next-rung progress (
- **E42.** CockpitIntroducedBy — 'who introduced YOU' own-row line — LACKS; tracked only inside C3, data already indexed — Origin harvest: CockpitIntroducedBy (LIVING_ORGANISM §9) — the member's own dashboard names the source that introduced them (recognition framing, own-row, ADR-003-clean). Not built: no member surface renders inbound introduction (only outbound counts). The R5 introduction index + the member's own en
- **E43.** Avatar sources ① sigil ② upload ③ NFT (Supa AvatarUploader) — sigil LIVE, rest tracked with closed storage decision — Founder decision 2026-07-13: all three avatar sources ship in order. ① Deterministic sigil is LIVE (MemberSigil in header pill, menu, settings). ② Uploaded image: the storage decision is CLOSED (Replit App Storage, OPEN_QUEUE 2026-07-14) and 'the build rides the avatar slice of the Member Home arc'.
- **E44.** Alias (opt-in display name over the seat) — LACKS, tracked as the queued IDENTITY-ALIAS slice — Origin harvest: opt-in alias, default = seat number (honour-roll canon); sold ENS-style aliases separately lawyer-gated. Not built; the Settings Alias row honestly says it 'arrives with the IDENTITY-ALIAS slice'. Tracked: OPEN_QUEUE '⏳ QUEUED SLICE — IDENTITY-ALIAS (founder-approved 2026-07-13; open
- **E45.** Gamified feedback module (Supa Feedback.tsx + FeedbackAdmin) — LACKS, tracked Phase 3+/5 — Origin harvest (founder-remembered, re-found 2026-07-13): 5-category feedback with up-front XP reward, screenshot drag-drop, admin triage console; doctrine-fit as recognition-for-effort with the anti-spam rule (XP credited only on operator validation). Not built as the gamified module — today's /sup
- **E46.** Learn & Earn quiz→XP loop (Supa Learn engine) — base /learning LIVE, loop tracked Phase 5 — Origin harvest: Supa's learn-module + quiz + pass→reward engine (zero of Supa's DeFi lesson content). Today: /learning exists with our honest lessons (learningModules.ts) — the passive base the loop builds on. The quiz + recognition loop is tracked: SESSION_STATE Phase-5 'Learn & Earn = earn XP … ⬜ 
- **E47.** OG shareable card / Referrer Kit (MVP brief piece 7) — LACKS, tracked twice — Recorded: shareable OG cards (viral, consent-gated identity — LIVING_ORGANISM §5/D4) and the Referrer Kit/OG card (MVP_FINAL_MASTER_BRIEF piece 7). The on-page ShareCard is real, but the link-preview OG image / downloadable kit is not built. Tracked: SESSION_STATE Phase 5–6 'Shareable cards / OG … ⬜
- **E48.** Archive per-wallet holdings (cockpit Ownership: Portfolio + Collector) — LACKS, honestly stated on-page, tracked via C3 — Origin harvest: the cockpit's Ownership stage (your 1155 collectibles wall). /archive is open with live minted counts, and the page itself states the per-wallet holdings view is the remaining gap (memberDoors S7 truth-sweep comment). Tracked only through the C3 cockpit-arc inventory line — like Cock
- **E49.** Teaser-page pattern + doors discipline (§11 slot 2c/6) — HAS IT, now largely outgrown — Recorded: doors never open onto empty shells, locked-visible on the one posture system. Today the pattern has been outgrown in the best way — the S7 truth sweep emptied the 'Coming soon' door group entirely (Activity, Chronicle, Archive, Fire Ledger all opened as REAL pages); only the three reserved
- **E50.** Guard-pinned 'own row' door phrases — surveyed, no action without guard change — For completeness of the own-row sweep: memberHome.door.lead 'Only ever your own row: there is no directory of members' (syndicateFacts.ts:284) is the guard-pinned dist probe per the file's own comment, and it self-explains with the directory clause; MemberWalletPanel.tsx:142 'you only ever see your 
- **E51.** 'My activity' lens: the member's own feed lines are client-derivable today from the served feed (own seat ordinal); the surface is honestly reserved but the data is live — MemberPulse.tsx:6-7 reserves the 'My | Protocol' lens for A1, and the 'While you were away' reserved slot (memberDoors.ts:79) correctly states 'the event record is live; this member surface arrives on it' — the copy is honest, so no truth bug. Flagging it because the derivation needs NO new server w
- **E52.** Door-and-link probe: all 20 member-life routes serve 200 with real route entries — no dead routes, no teaser dead-ends — Probed on the rig (curl http_code) and cross-checked against App.tsx route table so SPA-fallback 200s could not mask a missing route: /member /recognition /wallet /toolkit /activity /fire-ledger /chronicle /archive /map /liquidity (all 11 'Open today' + Growth doors), /join (expand action), /status 
- **E53.** Bundle: the four wallet member components are correctly lazy; the non-wallet member components ride the shared public bundle by app-wide design — including a QR library — Verified: MemberYourSeat, MemberKpiRow, CapitalAxisCard, MemberSettings are each behind lazy() + Suspense in MemberAccess.tsx:39-57 (guard rule 15 pattern), so the wallet-module code splits correctly. MemberAccess itself is STATICALLY imported (App.tsx:10) — but so is every public page; that is the 
- **E54.** General sub-12px sweep map: 225 occurrences on 219 lines across ~55 files — Full sitewide grep (text-[8|9|10|11]px under artifacts/studio/src): 8px x6 (PublicLayout wordmark + SeatFlowDiagram x5), 9px x28, 10px x117, 11px x74. By surface: HOME/HERO system is the densest (HeroLedger 21, SeatFlowDiagram 11 incl. all six 8px-class uses, ProtocolOverviewPanel 9, HeroStatusChips
- **E55.** Per-seat cumulative-spend endpoint is public and unauthenticated — settled in canon, recorded here — GET /api/backbone/capital-standing?seat=N returns ANY seat's cumulative gross USDC and rung to any caller; the own-account restriction (amounts only on the member's OWN dashboard) is a client-side rendering convention, not a server gate. The route header explicitly settles this ('PUBLIC chain data m
- **E56.** Checks that passed clean (coverage record) — ① Forbidden vocabulary: zero positive hits across all 18 audited files — every yield/passive-income/downline/profit occurrence is inside the negation-built disclaimers (constitutionalLine, boundaryLine, notCharityLine, rankingDoctrine); the only 'earn' is the Quests slot reported separately. ② Expec
- **E57.** S7-e follow-up (open): 18-decimal SYN balance must format human — Founder follow-up opened at the S7-b/c/d prod seal (SESSION_STATE lines 14-16, from his prod screenshot). Code today: ownReads.ts useOwnSynBalance (line 35) renders formatRawUnits(raw, 18) into the 'Your SYN' KPI tile — trailing zeros are trimmed but any non-round balance renders up to 18 fractional
- **E58.** S7-e follow-up (open): the memberNumberOf verify teaching line reads as jargon — Recorded at the S7-b/c/d seal (SESSION_STATE line 16). Still rendered verbatim on the member identity band: 'Read live, never assigned by hand — verify it yourself: call memberNumberOf with your address on the engine above.' (MemberYourSeat.tsx:243-248). HUMAN-FIRST LAW requires the machinery word t
- **E59.** S7-e follow-up (open): header sign-in must LAND on /member — Recorded at the S7-b/c/d seal (SESSION_STATE lines 16-17). Verified: the signed-out header button calls openConnectModal() and the menu re-resolves IN PLACE via SESSION_CHANGED_EVENT (MemberHeaderAffordance.tsx:172-186) — after connect+SIWE the member stays on whatever page they were on; no navigati
- **E60.** S7-e/data-truth follow-up (open): genesis footprints and the founder's own referral standing render '—' while the real old on-chain data exists — Recorded at the S7-b/c/d seal (SESSION_STATE lines 17-19): genesis seats' capital footprints and the founder's own referral standing show dashes although V1 purchases × the frozen roster and the BUILDER_SOURCE payout (= the founder's wallet) are on-chain/indexed — a readable figure shown as a dash i
- **E61.** S7-e follow-up (open): full design-research inventory audit (all repo specs vs the built page) — Recorded at the S7-b/c/d seal (SESSION_STATE line 20): 'plus the full design-research inventory audit (all repo specs vs the built page)' — a System-First-Law style confrontation of every member-home/design spec in the repo against what S7/S7-b actually shipped. Not started; no commit addresses it.
- **E62.** Readability-floor general sweep + no-sub-12px guard still open (measured today: 219 sub-12px sites in 50 files) — Recorded in the S7-c seal and ADR-001 amendment ('RESTE: tranche sweep générale… + un guard no-sub-12px-text'; SESSION_STATE lines 48-49 '~121 arbitrary micro-classes on other pages'). Verified today: 219 occurrences of text-[9px]/text-[10px]/text-[11px] across 50 files outside the /member compositi
- **E63.** The acted order (founder work order 2026-07-16): M-INT-1 → A1 → B1 → A2 → M-INT-2 — all five slices open, M-INT-1 is NEXT — Engraved at the S7 seal and re-stated at the S7-b/c/d seal (SESSION_STATE lines 58-60, 125-137): ① M-INT-1 the member ledger (operator-side per-seat dossiers + RFM/at-risk segments + internal rankings from ALREADY-indexed data — capital walk + R5 + roster + heartbeat lanes; founder-gated S11+, zero 
- **E64.** S8 performance still queued — the two cheap wins verified still real on disk — Queued behind the acted order (SESSION_STATE lines 60, 135, 186). Verified today: artifacts/studio/public/brand/syn-mark-gold.png = 284,591 bytes and avalanche-avax-token.png = 195,169 bytes — exactly the '284KB + 195KB cheap wins' recorded since 2026-07-14 (throne webp already fine at 68KB). W1 §6 
- **E65.** S5 the soul document — still blocked on the founder's paste — Open since 2026-07-14 (SESSION_STATE lines 60, 135-136, 186-187): awaits the founder pasting the doctrine paper; import as TIER-1 with the SUPERSEDED-TERMS banner per the recorded spec. Blocked-on-founder; no code work possible until the paste.
- **E66.** Publicly-promised FUTURE member surfaces awaiting their unlocking arcs (notifications, trophy/seasons, alias, language, reset profile, per-receipt rows) — All honest and labeled today, all still open: header bell + seasons trophy reserved icons ('coming soon — arrives with the event backbone / seasons engine', MemberHeaderAffordance.tsx:280-298); the member Notifications entry ('badge FUTURE — the record is live, the surface isn't', S7-b seal line 42)
- **E67.** NOTED FOR LATER (2026-07-15 seal): the era-facts table on /tokenomics — founder interest logged, not opened — Recorded in the session seal (SESSION_STATE lines 176-177) and at the H2-⑫ gate (line 390-391): a marketing-clean era-facts table for /tokenomics ('marketing, doctrinally clean — founder interest logged') as a possible future slice. Nothing built; no commit mentions it since. Anti-scarcity doctrine 
- **E68.** Alias layer M2/M3 — will replace the referrer address on seat lines; IDENTITY-ALIAS slice queued — Two linked open records: (1) H2-P-a seal (SESSION_STATE line 157-158, 319): 'alias layer M2/M3 will replace the address on this line' — the named-referrer feed line ('brought by 0x3f2…0a91') upgrades to the alias when the layer lands. (2) OPEN_QUEUE queued slice IDENTITY-ALIAS (founder-approved 2026
- **E69.** Avatar upload: storage DECISION closed (Replit App Storage) — the BUILD slice still pending — The decision half closed 2026-07-14 (OPEN_QUEUE lines 47-49: 'Avatar storage = Replit App Storage. Decision recorded; the build rides the avatar slice of the Member Home arc; sigil default stays LIVE; NFT source stays chain-verified ownerOf + badge + verify tooltip'). The build half remains open: Me
- **E70.** Referral snapshot refresh: cadence decided (weekly + before any promotion signature) — each run stays founder-gated until M0; Q18 stale holder-index snapshot remains record-only — Cadence half CLOSED 2026-07-14 (OPEN_QUEUE lines 53-55: WEEKLY + always re-run before signing any promotion; 'the stale-snapshot refresh itself remains a founder-gated run. Full automation of the refresh lands with M0'). Still open: the automation (M0) and Q18 (OPEN_QUEUE line 139, 🔴 record-only): 
- **E71.** Q3 + Q4 — the 2.5a/2.5b knowledge arc still parked (knowledge-registry + guard, then the /knowledge page) — OPEN_QUEUE lines 124-125, both 🟡: Q3 = purity-leaf knowledge-registry.ts + BLOCKING guard-knowledge-map.ts (no page); Q4 = the /knowledge page + PagePurpose atom, after Q3. Parked at its posted gate since 2026-07-13 ('2.5a stays PARKED at its posted gate (Q3/Q4 held; Q5/Q6 unanswered)', line 210). 
- **E72.** Q5 + Q6 + Q9 — three founder confirms for the 2.5 arc, asked and never answered — OPEN_QUEUE lines 126-129: Q5 🔴 the /knowledge vs /knowledge-map route name + title (asked at the 2.5 gate, never answered); Q6 🔴 permanence-as-declared vs derived (status-as-derived ratified, permanence never confirmed — founder confirms at Q3); Q9 🟡 the SERVER-ONLY HOMES exact wording (Option A,
- **E73.** Q8 — MASTER_BUILD_SPEC checkbox drift, verified live and now larger than when recorded — OPEN_QUEUE line 128 (🟡🔵, reconcile the stale checkboxes). Verified today in MASTER_BUILD_SPEC.md: Phase-1 line 39 '- [ ] Color sprawl → 0 → flip the color guard to BLOCKING' is UNTICKED while DESIGN_ROADMAP declares 'Suivi couleur — ✅ FERMÉ' (0 raw colors, no-raw-color BLOCKING); Phase-3 lines 62-
- **E74.** Q10 — protocolOsMap 'knowledge-os' node repoint waits on Q4 — OPEN_QUEUE line 130 (🟡🔵, after Q4). Verified: protocolOsMap.ts:249-257 still carries the knowledge-os node as lifecycle FUTURE with reality 'A first Knowledge OS map is present on the public /learning page today; deeper guided tooling is a future concept.' Correct today (since /knowledge does not 
- **E75.** Q13 — the 2.11 join-flow '5 steps must become 2' + ordering: a founder call, likely superseded by the live checkout but never closed — OPEN_QUEUE line 135 (🔴 founder decides ordering). The C2/C5 checkout shipped and is live in prod since 2026-07-12/13, which likely satisfies or supersedes the item — but per the HARD RULE nothing closes until the founder closes it explicitly. Needs a founder confirm to move to CLOSED (same situatio
- **E76.** Q14 — Reserved-VM / session durability still open, and its symptom is coded around today — OPEN_QUEUE line 136 (🔴 founder/Replit, Phase-3) + MASTER_BUILD_SPEC Phase-3 line 60. Still real: server sessions are in-memory and wiped on every restart/deploy — MemberHeaderAffordance.tsx:129-142 exists specifically to re-sign members stranded by that wipe ('Server sessions are in-memory and are 
- **E77.** Q16 — hero KPI grid (ProtocolOverviewPanel) migration to StatCard/StatusPill still un-migrated — OPEN_QUEUE line 138 (🟡, fold awareness into ⓪, migrate later) + MASTER_BUILD_SPEC Phase-1 line 36. Verified today: ProtocolOverviewPanel.tsx contains zero references to StatCard or StatusPill. (Color sprawl itself is closed at 0 with a blocking guard, so the migration's remaining value is atom cons
- **E78.** Q20 — the /join stale-gate record: superseded-looking since C5 went live, but closing is a founder confirm never taken — OPEN_QUEUE line 140 (🟡 record-only, rides checkout) and the 2026-07-13 note (lines 210-212): 'Q20 looks superseded by the live C5 checkout — closing it stays a FOUNDER confirm, not taken here.' The checkout is sealed in prod; the queue row remains open awaiting the founder's explicit closure per th
- **E79.** Q15 — standing obligation: DESIGN_ROADMAP boxes ticked in the same commit as each slice (honored this session, never closes) — OPEN_QUEUE line 137 (🟡 STANDING, every slice). Verified honored on 2026-07-16: the S7, S7-b, S7-c, S7-d entries are ticked [x] in DESIGN_ROADMAP with the RESTE notes, and the color-sprawl section stands closed. Remains on the ledger because it is a permanent per-slice obligation, not a completable 
- **E80.** Queued slice: PIPELINE-CHRONICLE (founder GO at its M-map moment) — overlap with B1 to reconcile at its gate — OPEN_QUEUE lines 70-92: the organism detects Chronicle-grade events, writes candidates in the §8 voice, proposes with persistent reminder; founder switch for auto-promoting protocol-pure classes; off-chain events first-class; harvest sources named (origin protocol-event-intelligence.ts, chronicle-ad
- **E81.** Queued slice: REFERRAL-SHOWCASE — the conversion register applied to the referral surfaces — OPEN_QUEUE lines 94-103 (founder-decided 2026-07-14, EXECUTE ONLY ON FOUNDER GO): apply CANON_PROTOCOL_LANGUAGE §7's four flagship lines to /referral, the /join referral card, and the future Referrer Kit / OG card; bold claim + verify link; legal verbatim never moves. Rationale recorded: the instant
- **E82.** Queued slice: GIFT-A-SEAT — recipient-parameter checkout (post-hand-selling unless the founder signals earlier) — OPEN_QUEUE lines 291-308 (founder-approved 2026-07-13, EXECUTE ONLY ON FOUNDER GO): optional gift-recipient field on checkout using the deployed buy(gross, recipient, …) parameter; ALL recipient-side guards move to the RECIPIENT (the C4 trap, flagged three times); honest copy; referral applies norma
- **E83.** Queued slice: SEO-301 — /source-attribution permanent redirect to /referral (or wait for the domain transfer) — OPEN_QUEUE lines 327-339 (founder-approved for queuing 2026-07-13, EXECUTE ONLY ON FOUNDER GO): server-side 301 /source-attribution → /referral with query strings preserved intact (?source= links must keep paying); /referral keeps self-canonical. Infra reality: if the Replit static layer cannot expr
- **E84.** M-map remainder beyond the acted order: M2 Referrer Kit · M3 seat majesty · M7 Economy · the seven-signatures Chronicle story — From the M1-arc plateau (SESSION_STATE lines 840-842) and the MVP master brief (OPEN_QUEUE lines 283-289, canon — the founder picks from the map): M2 sharebility/Referrer Kit, M3 seat majesty (where the CONVERSION register + REFERRAL-SHOWCASE stage), M7 Economy/GDP page, PIPELINE-CHRONICLE, and the 
- **E85.** Deferred horizon (tracked, not in-flight): www→apex 301, HSTS/preload, i18n, pro-firm contract work, Phase 3-6 holding area — OPEN_QUEUE lines 142-146 + 235-239 + 64-68: ⏸ www→apex 301 rides the domain transfer ~Sept 2026; ⏸ HSTS/preload → Phase 6; ⏸ i18n founder-deferred (when opened: harvest Supa-Exchange's 12-locale mechanism, EN+FR first, legal stays English, copy guards extended PER LOCALE before any language); ⏸ PRO-
