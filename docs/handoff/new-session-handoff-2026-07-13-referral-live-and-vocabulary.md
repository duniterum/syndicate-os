# Handoff — 2026-07-13 · Referral LIVE end-to-end + the vocabulary laws

**Read after `docs/SESSION_STATE.md` (its top blocks carry the full detail).**

## WHAT IS LIVE IN PRODUCTION (all founder-verified)
- **The Syndicate sells seats**: C5 flipped; first real purchase = seat #13, tx `0x353bf2c0…c42178`
  ($5 via the referral link → $0.25 paid to the referrer IN the buyer tx → net split 70/20/10 exact
  → 500 SYN). The living readback recomputed 13/12 BY ITSELF.
- **Referral program publicly ACTIVE** (LIVE_ACTION lifecycle; guard-safe-source re-locked "honestly
  ACTIVE"); referral-first naming everywhere; member-aware seat line; multichain-USDC message.
- **Pending Replit publish (pushed, all gates green locally):** `6ac9977` (deep jargon sweep) +
  `24ca95d` (/referral canonical URL + flow vocabulary). After publish VERIFY: /referral serves the
  program page (tab title "Referral Program"), /source-attribution still serves 200 (alias,
  noindex,follow), footer/buttons point to /referral, /join fail-closed line says "A referrer
  payment of X% applies".

## THE VOCABULARY LAWS (canon — do not relearn)
1. **Referral = user word** on every public surface; **Source = protocol word** (proof/registry/
   operator + the "Powered by Source Attribution" credit).
2. **Money-flow vocabulary is FLOW-BASED** (advisor re-engraved, SESSION_STATE ③): buyer line =
   "Paid to referrer"; "commission" = REFERRER-surface business word only; `acquisitionCost` =
   bytecode word, banned publicly, RESERVED for future real marketing costs. Formula: Gross →
   referrer payment (if eligible) → net protocol contribution → 70/20/10. Doctrinal sentence
   verbatim in /referral boundaries. Values from quote(), never registry commissionBps.
3. **SEO-metadata law**: banned financial words NEVER in titles/descriptions, not even negated
   (truncated snippets) — commented in seo-route-registry; negations live in page bodies.

## NEXT (founder signal; OPEN_QUEUE carries the queue)
- **R2** — founder signs the first member `createSource` (deterministic `SYN.SOURCE.V1` id;
  PREREQ R1: conditions doc hashed → metadataHash, BLOCKING for LIFETIME). Unlocks the
  auto-derived member link card (NOTE: the existing BUILDER_SOURCE predates the derivation —
  verified, no variant matches).
- **R5** — the introduction indexer → unlocks per-member histories (the memberCards honestly say
  PENDING_ADAPTER until then).
- Group B (guard machinery) = Phase 6 per CANON_LOI_ANTIBLOCAGE · Group C1 DISCIPLINE_ENFORCED =
  LATER, do not re-raise · Chronicle candidate awaits founder promotion.

## STANDING VERIFICATION PROTOCOL (this session's habits)
Local gates before any push: typecheck · guards · seo:check · seo:rewrites:check · **surface:audit**
(the one once missed — Replit gate caught it) · build; chain claims verified against the DEPLOYED
contract (read-only scripts); live-domain check after every publish. pnpm is absent on the founder
Windows machine — use npm + PORT=5173 env for builds; Replit remains build/deploy truth.

## FINAL LIVE STATE (measured on thesyndicate.money after the last publish, e1048ff)
- **/referral**: robots `index, follow` + canonical `https://thesyndicate.money/referral` ✓
- **/source-attribution**: robots `noindex, follow`, no canonical tag (the tooling emits canonicals
  for INDEX routes only — Google drops the alias and follows to /referral; links never break) ✓
- **BOTH URLS EXIST AT ONCE — BY DESIGN** (canonical + alias). Do not "fix" this.
- ⚠️ Replit's cycle report said /referral "naît en posture PENDING/noindex" — a MISREAD of the
  prerender line (the 3 noindex shells are /recognition, /archive, /source-attribution). /referral
  is INDEX + in the sitemap, measured live above. Nothing to do.
