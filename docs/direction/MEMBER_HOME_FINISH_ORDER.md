# MEMBER HOME — FINISH ORDER (the execution plan)

**Founder-approved 2026-07-18. "C'est bon comme ça."** The sequence to FINISH the Member
Home arc **before** other arcs. Companion to `BACKLOG.html` + `OPEN_QUEUE.md` (the authority);
this is the ORDER, not a new truth. Reconstructed from the current `MemberAccess.tsx` reality,
the `MEMBER_HOME_TOTAL_AUDIT_2026-07-16.md`, the canon, and the 173-item backlog by a
5-agent sweep (critic verdict: SOUND, one item dropped as already-sealed).

## Definition of "done" for Member Home
The cockpit arc **Identity → Place → Ownership → Momentum → Memory → Proof** complete, with
**zero truth defects** on the member's own page and **reachability confirmed**. Concretely:
genesis (#1–#8) footprints render real derived values (never "—"); the founder's ACTIVE
BUILDER_SOURCE resolves everywhere (never "no source"); every own purchase shows (not one);
three-state "—" honesty (loading / read-failure / honest-non-derivable are distinct);
My Activity (A1) live; own Archive holdings shown; "who introduced you" + seat majesty;
My Chronicle; verify-first on every figure; auth lands on /member + no dead-end;
human-first copy; readability floor (no sub-12px); WORK-FIRST composition; ADR-003 own-row.

## The order — 21 steps, 4 phases

### 🔴 Phase A — live truth defects (fix first: cheapest, most damaging, on the founder's own page)
> **⚠️ RECONCILED 2026-07-18 (5-agent map `wf_aa4aa0ab` + Claude-Code direct verify): steps 1 & 4
> were ALREADY SEALED before this doc was written — the doc carried the pre-D-TRUTH audit A1
> forward (documentation drift). Verified reality below. Remaining REAL work = steps 2 + 5 (+ a
> founder chain-fact for 3).**
1. ✅ **DONE (access-model slice 1).** ~~Remove the duplicate "Your referral" heading~~ — the
   component now owns its single h2 (`MemberReferralDashboard.tsx:308`, "so it can never be
   duplicated by whoever mounts it"); no h2+h3 stutter remains.
2. 🔴 **REAL — Member gamification copy — SHOW the money + keep XP ≠ season-cagnotte clear.** Fix the
   slot copy "Recognition only, never a cash figure" (`memberDoors.ts:114`, the "Season" slot — it
   implies hiding, a Visibility-Law defect). Hold three truths at once (see "hard-won doctrine" below).
   Founder OKs final wording inline. Quests slot (`:115`) left ALONE — no "never a payment" pile-on.
3. ✅ **CONFIRMED WORKING (founder's /member screenshot 2026-07-18).** The D2 payoutWallet fallback
   (`sourceStandingRead.ts:127-167`; index keyed on `payoutWallet` ONLY, `introductionRefresh.ts:311-318`)
   was already sealed at `235205c`. The founder signs in with his payout wallet (`0x2445…C721`), so
   his source resolves: "Introductions 2 durable · 2 total" + footprint "$70.00 · Patron" render live.
   The doc was stale. No multi-wallet broadening needed.
4. ✅ **DONE + SEALED (slice D-TRUTH `235205c`, 2026-07-16, Replit 6/6).** Real standing for all 8
   genesis seats is live (seat 1 → Patron $70; seats 2–5/7/8 → Citizen $5). The "—" was the
   pre-D-TRUTH state. `GENESIS_JOIN_EMITS_RISES=false` (`capitalAxisReadmodel.ts:82`) = the founder's
   settled no-retroactive-lines call. Nothing to build; the doc was stale.
5. 🔨 **REAL (pure build) — Shared read + true three-state "—" + CapitalAxisCard retry/no-vanish +
   race guard.** 3 zones fan out duplicate reads today; the card vanishes on failure with no retry,
   and one ambiguous "—" conflates loading / read-failure / honest-null. Autonomous to preview.

### 🟡 Phase B — the core (what makes the page "finished")
6. **Auth/nav flow** — land on /member after sign-in · "Open Member Home" CTA on the receipt ·
   in-place session-expiry re-sign. **Confirm live auth posture with Replit** (reachability = risk #1).
7. **Live introduction-count badge** on the Referral door + snapshot-refresh automation (M0).
8. **A1 — My Activity** — own-row receipt binder + "My | Protocol" pulse lens + Receipts door/tile
   go live. **SYSTEM-FIRST inventory on screen + wireframe.** (Engraved-next; feeds other zones.)
9. **M10 — per-receipt / per-introduction row histories** (verify-linked, own-row) — wireframe, rides A1.
10. **Verify-first affordances** — visible verify links on the footprint sum + KPI tiles (not hover-only).
11. **Ownership — own Archive ERC-1155 holdings** on the page — wireframe.
12. **Place — "who introduced you" + seat majesty (M3) + co-witness timeline** — SYSTEM-FIRST + wireframe.
13. **B1 — THE FIRSTS engine** (Chronicle-candidates queue; feeds A2) — pure build.
14. **A2 — My Chronicle** — own milestones + register presence — wireframe.

### 🟢 Phase C — reserved slots (dependency order)
15. **Avatar** (upload + NFT-verified + admin-hide; sigil default stays) — wireframe.
16. **M8 — opt-in Identity-Alias** over the seat + alias-resolver backend.
17. **M2 — Referrer Kit v1** (living share card + QR) — wireframe.
18. **Guide — member-aware deterministic corpus + first-minute onboarding greet** (zero-LLM).
19. **"Request my source" flow + interim server-side queue** (the MVP-proof-metric mechanism).

### ⚪ Phase D — polish (end of arc)
20. **HUMAN-FIRST jargon sweep** across /member.
21. **Readability floor** — no sub-12px + money-precision cap + block grouping + a no-sub-12px guard.

## Deferred UNTIL AFTER Member Home (do not confuse with finishing it)
Season card / Quests strip (Phase-5 seasons engine) · multi-axis recognition (engine) ·
**"Since you were away"** (RETIRED — folded into the sealed notification center) ·
**Notifications v2** (autogen + prefs; shell is sealed — that presence finishes MH) ·
**M7 /economy** (separate page) · **M9 Gift-a-Seat** (checkout feature) · **swap ramp** ·
register/census · toolkit 23-action registry · **Q16 hero-KPI grid + PublicHome TruthLabel**
(verified: these are PUBLIC-home, NOT Member Home — the member KPI tiles were migrated in S7-b).

## Binding laws for this arc
- **WORK-FIRST** — every new zone opens on the work; diagnostics collapse to the bottom or aren't shown.
- **VISUAL CHANGE LAW** — layout/composition steps need a founder wireframe THEN a preview gate
  (desktop+mobile, both themes, every img naturalWidth>0). Wireframe steps: **8, 9, 11, 12, 14, 17.**
  All other visible steps still hit the preview gate. Defect/copy/data steps are preview-only.
- **SYSTEM-FIRST** — before step 8 (A1) and step 12 (Place), put the COMPLETE inventory on the
  founder's screen; scope is picked from the whole picture.
- **VISIBILITY LAW (TIER-0)** — WE SHOW THE MONEY (see below).
- **HEARTBEAT COMPLETENESS** — A1's My|Protocol lens keeps /activity + pulse complete; any future
  contract/wallet joins scan targets + read-model + §8 + /activity in the SAME slice.
- **SEO RIDES THE SLICE** — steps that move a page's reality update its meta/OG/JSON-LD/source-status
  in the SAME commit.

Pure-build (no founder gate, autonomous to the preview): 1, 5, 7, 10, 13, 18, 21.
Founder-gated (decision/wireframe/content): 2, 3, 4, 6, 8, 9, 11, 12, 14, 15, 16, 17, 19, 20.

## Hard-won doctrine (settled 2026-07-18 — DO NOT re-litigate; I erred toward hiding three times)
- **Learn & Earn = XP = recognition, never cash** (XP→USDC conversion is the ONE banned mechanism).
  SETTLED_RULES §2. Not a defect — do not add a "never a payment" negation.
- **The season cagnotte = REAL USDC** — the COMPANY's money, merit/effort-based, discretionary per
  season, USDC-not-SYN, never touches 70/20/10, no random draw, lawyer-gated = a legal marketing
  gift. SETTLED_RULES §2+§8; SEASONS §8. The build (Merkle/params) = `gam-payout`, Phase-5, deferred.
- **WE SHOW THE MONEY** — `CANON_VISIBILITY_LAW.md` (TIER-0): "on a chain, hiding does not exist";
  the pot, payouts, treasury are SHOWN and verifiable, never hidden. **"We're a company, not a
  charity" (founder).** The only forbidden thing: a member directory / wallet↔person link /
  exposing a non-consenting member — NEVER amounts. → the "never a cash figure" copy is a DEFECT (step 2).

## Provenance
Sweep run `wf_391ebe7f-bbb` (5 agents). Current-reality map from `MemberAccess.tsx` + its zone
components; target from the total audit + recognition handoff + LIVING_ORGANISM_MASTER_PLAN cockpit
arc + the S7 seals. OPEN_QUEUE.md + SESSION_STATE.md remain the authority.
