# MVP-FINAL MASTER BRIEF + THE 30-DAY MAP (founder-decided, 2026-07-14)

**Status: FOUNDER-DECIDED SCOPE, COMPLETE AND FINAL.** Self-contained — no session needs the
story retold. Companion queue entries live in `OPEN_QUEUE.md`; slices open ONLY on founder GO,
one at a time, with the normal gates (founder GO before every commit, live verification after
every deploy, honest notes on every deviation).

---

## 1. THE CHALLENGE

The founder's sequence: ① finish the MVP as fast as quality allows → ② THEN a 30-day proving
window starts — strangers arriving, sharing, buying — to prove demand. The MVP must be the
machine that does not break under momentum, because momentum comes once.

## 2. THE PROOF METRIC (founder-agreed 2026-07-14 — no interpretation war on day 29)

30-day window, ALL CHANNELS ALLOWED (paid ads, Zealy quests, campaigns; targeting
crypto-natives in emerging markets is the strategy). ONE exclusion: the founder's own wallets
(self-buys prove nothing — arithmetic, not virtue).

| Metric | FLOOR (continue) | TARGET |
|---|---|---|
| Seats bought by strangers | 15 | 40 |
| …of which via shared links | ≥5 | ≥15 |
| Second-generation referrals (stranger referred by a stranger) | ≥1 | ≥3 |
| Seats still held at day 30 | ≥80% | ≥90% |
| Non-founder members requesting their source | ≥2 | ≥5 |

**The queen proof is second generation — the protocol working without the founder.**
Below floor ≠ dead: the MESSAGE needs work, not the machine.

## 3. WHERE PRODUCTION STANDS (verified, sealed — as of 2026-07-14)

Checkout live and proven with real money (seats #13, #14) · referral publicly active, terms
published + hash-committed on-chain (`0xc848…6e48`) · founder source ACTIVE (`SYN.SOURCE.V1`,
`0x804e80f1…ae974`) · introduction indexer live, own-row, fail-closed · Connector ladder canon
(`CONNECTOR_LADDER_POLICY.md`, TIER-0) with the promotion loop armed (simple + transparency, no
waiting compensation) · security headers, 1 instance, CSP clean · honest readback "N seats /
M distinct wallets" self-updating. **No new smart contracts ~6 months** (deployed registry
covers everything; PRO-FIRM HORIZON carries promotion contract, issuer, Router V4, audits).

## 4. THE SEVEN PIECES (machine + memory + story — they ship together)

1. **HERO REDESIGN** — the project speaks for itself: zero marketing copy, zero promises;
   live proof IS the hero. Harvest the origin's design language (TheSyndicate = the richest
   design source), truth-first rebuild.
2. **FULL SHAREBILITY** — QR + share surfaces for every link that matters (referral link,
   seat proof, the OG card). Share-surface guards exist — reuse.
3. **THE COLLECTIBLE / VANITY LAYER** — "I was here before everyone." Seat number in majesty,
   chapter identity (Genesis #1–#333), seniority permanence, co-witness (what history the seat
   has SEEN), Archive artifacts as memory. Doctrine root: origin SPRINT A.9 "WHAT MAKES A SEAT
   MATTER" + `SEAT_RECORD_ARCHITECTURE_DECISION.md`. Ethical FOMO is HISTORICAL only ("miss my
   place in the story") — never financial.
4. **ACTIVITY** — the public heartbeat. "The protocol learned to speak through verified
   receipts" — every rendered line receipt-backed, never a claim.
5. **CHRONICLE** — the institutional story, founder-promoted turning points only (promotion is
   a HUMAN act). First true chapters already lived: the owned seat-duplicate, the first
   real-money seat, the first convention source, the ladder decision.
6. **ECONOMY** — living Protocol Economy / My Economy surfaces, shapes harvested from the
   origin, rebuilt truth-first on the verified spine. Volume is not revenue; no member claim
   on treasury; declared allocation model for non-membership streams.
7. **THE REFERRER KIT** — better than web2 banners: a GENERATED OG CARD carrying on-chain data
   (seat, chapter, verified introductions, standing) — provable, shareable in seconds, zero
   writing. One click → living card + link + QR. The card can only show what the chain proves
   (seed: `shareCardSample` — "Proof, not claims"). **Pieces 2+3+7 interlock: vanity becomes
   the acquisition tool.**

## 5. THE VOICE

**"THE CHAIN SPEAKS — and it speaks of PERSONS."** Receipts become sentences; IDENTITY-ALIAS
(queued) puts a human-readable name above the EVM hex — opt-in, default invisible, own-row,
lineage-tracked. The site is never a de-pseudonymization tool; no forced wallet↔identity
directory, ever.

## 6. STANDING LAWS (unchanged, enforced — the short list)

Truth-first / LIVE-PRODUCTION · fail-closed, never stale · verify links on every figure ·
banned public vocabulary (invest/raised/profit/return/yield/APY/dividend/passive income/
guaranteed/moon/pump/package/tier…) · money-flow vocabulary (buyer-side "Paid to referrer",
never "acquisition cost"; Gross → referrer payment if eligible → net contribution → 70/20/10) ·
seat binary, recognition multi-axis, capital ONE axis never the throne · locked ≠ hidden ·
zero twin pages · one badge system · own-row · opt-in visibility · anti-blocking law ·
APPROVE ≠ PAYMENT · seat number ONLY from the emitted event.

## 7. FACTS CORRECTIONS ENGRAVED (founder ADDENDUM 2026-07-14 — FINAL; supersedes every
## earlier gift note, including this section's first version and a0db477)

- **The founder's OWN seat is HISTORICAL MEMBER #1 (Genesis)** — his 9-year private wallet
  `0x2445…9C721` (duniter.eth; never a third party). *"The first is me."*
- **ALL V3 test seats are GIFTS awaiting adoption: seat #13 (`0x0Dd8…c4D20`) AND seat #14
  (`0xEA88…35881`)** — none of them is the founder's own seat. During the tests, "c'est moi"
  meant "the TESTER is me, not a stranger" (relevant for the PROOF METRIC: those buys never
  count) — not "the seat is mine". Label: founder-test everywhere the honesty label applies,
  until adoption; then "bought as gift" renders from receipt truth (buyer ≠ eventual identity).
- **Historical seats #2–#8 are likewise destined gifts** — one of them already holds the known
  double seat #7+#11 (the future Chronicle chapter: *"the first duplicate was a gift"*).
- **Recipient identities stay OFF-repo and OFF-surface** until each person opts in via
  IDENTITY-ALIAS — only the facts above are recorded, no names, ever.
- DISCIPLINE_ENFORCED re-arm stays LATER (founder) · deployment-secrets cleanup stays Phase 6.

---

## 8. THE 30-DAY MAP (delivered 2026-07-14; the founder picks slice 1)

Sizes: S ≈ one slice · M ≈ 2–3 slices · L ≈ 3–5 slices. Every entry: THIN-V1 done +
deliberately-waits + unblocks. **CRITICAL PATH = M4 (event backbone automation)** — it feeds
Activity, per-receipt rows, Economy flows, and Chronicle auto-candidates. **HIGHEST-LEVERAGE
EARLY WIN = M1+M2+M3 (hero + card + collectible — the founder's momentum face).**

| # | Slice arc | Size | THIN-V1 = DONE (ships) | Deliberately waits | Unblocks |
|---|---|---|---|---|---|
| **M0** | **Refresh automation + member link card** | S–M | The introduction read-model refreshes ITSELF in-process on the server (boot-build + interval rebuild, in-memory, committed snapshot = boot fallback; asOfBlock always honest). The member dashboard auto-derives the OWN referral link card (deriveSourceId + live sourceExists). | Cron/DB-driven indexing (that's M4); admin cadence controls. | The first thing that breaks under traffic, unbroken; sharing gets its link. |
| **M1** | **Hero redesign** | M–L | Home hero rebuilt on the origin's design language: live proof as hero (seats live, burn live, routing live, verify links), zero marketing copy. Prerender/SEO intact. | Deep interior-page redesign; animations beyond the design system. | The stranger's first 5 seconds; every story surface hangs off it. |
| **M2** | **Sharebility + Referrer Kit v1** (pieces 2+7) | M | One click on the dashboard → the member's LIVING CARD: client-generated image (canvas, own-row data: seat · chapter · verified/durable introductions · standing) + link + QR; share menus wherever a link matters. Card shows ONLY chain-proven figures. | Server-rendered per-seat OG images for social unfurls (v2 — real infra); any figure the chain can't prove. | The acquisition loop: vanity becomes the tool. |
| **M3** | **Collectible / vanity thin** (piece 3) | M | Seat number in majesty on receipt + member surfaces; chapter identity rendered from `chapters.ts`; seniority line ("seat #N of M — joined before X% of the story", live reads); historical-FOMO copy per SPRINT A.9, never financial. | Co-witness event timeline (needs M4); Archive-artifact binding. | The card's substance (feeds M2 v2); the story of mattering. |
| **M4** | **EVENT BACKBONE automation** ⟵ CRITICAL PATH | M–L | The existing indexer machinery (cursors + sale_event_raw + Protocol Time + heartbeat read-model) runs UNATTENDED: scheduled incremental scans, fail-closed cursor discipline, served address-safe activity feed endpoint. | New event kinds beyond purchases/registry lifecycle; notifications. | M5 Activity · M10 per-receipt rows · M7 flows · M6 auto-candidates. |
| **M5** | **Activity surface** (piece 4) | M | /activity public heartbeat: receipt-backed lines only ("a seat was written · block N · verify"), recency-truthful, address-safe aggregate voice. Home teaser repointed from "coming" to live. | Filters, pagination depth, member-personal feeds. | The site breathes in public; proof of life for strangers. |
| **M6** | **Chronicle thin** (piece 5) | S–M | /chronicle with the FIRST TRUE CHAPTERS hand-written and founder-promoted (the owned seat-duplicate · the first real-money seat · the first convention source · the ladder decision) — 4–6 entries, receipt/tx-linked, solemn register. Promotion stays a human act. | The candidate pipeline from the backbone; auto-drafts. | The institutional memory; "he PROVES it when it isn't clean." |
| **M7** | **Economy thin** (piece 6) | M | /economy Protocol Economy Observatory composed from the EXISTING reality spine (supply, burn, routing, treasury, LP — all live, all verify-linked), evidence-labeled, not-a-yield-dashboard; declared allocation model for non-membership streams as copy. | My Economy cockpit (Member Home arc); flow charts needing M4 depth. | The "is this real?" answer for the diligent stranger. |
| **M8** | **IDENTITY-ALIAS thin** (the voice) | M | Alias above the hex: opt-in, default invisible, own-row; founder-curated writes through the EXISTING operator write zone (no new write surface); the founder-wallet label (`duniter.eth` wallet) as the first entry; "bought as gift" renders from receipt truth on adopted seats. | Self-service alias writes; lineage records (the succession design); ENS resolution. | The chain speaks of PERSONS — Activity/Chronicle lines gain names. |
| **M9** | **GIFT-A-SEAT** | M | The recipient field on checkout; ALL recipient-side guards moved to the RECIPIENT (knownMember, historical gate, era caps — the C4 trap); "their wallet, permanently" copy; referral applies; receipt records buyer ≠ recipient. | Gift messaging/wrapping UX. | Future gifts clean; #14-style adoptions get their label path. |
| **M10** | **Per-receipt row histories** | M | Member dashboard histories (introductions, receipts, commissions) as rows from the backbone read-models, own-row, verify-linked per row. | Export; operator analytics. | Closes the last PENDING_ADAPTER cards. |
| — | **Riders** | XS | SEO-301 (needs Replit's 301-support answer) rides any deploy; 2.5a stays parked at its gate (Q5/Q6) unless the founder pulls it in. | — | — |

**Suggested order:** M0 → M1 → M2 → M3 → M4 → M5 → M6 → M7 → M8 → (M9, M10 as the window
allows — M9 is founder-timing, M10 needs M4). M6 can jump earlier at the founder's word (it
is story, not machinery). The founder reads, picks slice 1, and the arc runs gate by gate.
