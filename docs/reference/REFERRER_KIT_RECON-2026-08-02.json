{
  "summary": "Two senior recon agents inventory the COMPLETE Referrer Kit system (dossier/mockups/origin ambitions vs built today) for the SYSTEM-FIRST gate",
  "agentCount": 2,
  "logs": [],
  "result": {
    "system": {
      "summary": "The Referrer Kit is NOT a greenfield: the K-arc (K1 arsenal + K2 invitee side + K3.a/b/c admin axis) sealed in prod 2026-07-20→22 already delivers most of master-brief piece 7 — 13 chain-proven artifacts live at /referral/tools, the per-source /join unfurl, and the full ask→sign→bell admin loop, all under 12 live featureStatus keys. What the founder's work-order item 3 (engraved #8, M2+M3) still contains: M3 Collectible/vanity (seat majesty, seniority line, co-witness, historical FOMO — the card's SUBSTANCE, entirely unbuilt), the M2-v2 per-seat living link previews (most pages still unfurl one static 1280×720 image), the /join referral card (the REFERRAL-SHOWCASE remainder), K4 no-seat-promoter reach (founder decision reserved), and P the public press kit. Every existing referral mockup is approved AND realized; M3 and each new surface needs a fresh founder-approved wireframe per the Visual Change Law.",
      "inventory": [
        "THE KIT'S SPINE — one wallet, one permanent link (sourceId = keccak256 of the wallet), the link hero exactly once on /referral, &via= as the only channel layer — settled law (docs/reference/REFERRAL_PAGE_DESIGN.md, guard-pinned; docs/reference/REFERRER_KIT_SYSTEM_INVENTORY.md header)",
        "M2 DEFINITION — one click → the member's LIVING CARD (seat · chapter · verified/durable introductions · standing) + link + QR, chain-proven figures only; v2 = server-rendered per-seat OG unfurls (docs/direction/MVP_FINAL_MASTER_BRIEF.md §4 piece 7 + §8 row M2; BACKLOG.html M2 row)",
        "M3 DEFINITION — seat number in majesty on receipt + member surfaces · chapter identity from chapters.ts · seniority line ('seat #N of M — joined before X% of the story', live reads) · historical-FOMO copy per origin SPRINT A.9, never financial; deliberately waits: co-witness timeline (backbone now done — unblocked) + Archive-artifact binding (MVP_FINAL_MASTER_BRIEF.md §8 row M3 + §4 piece 3; BACKLOG.html M3 row: 'the momentum face where the CONVERSION register stages')",
        "THE INTERLOCK — pieces 2+3+7: vanity becomes the acquisition tool; M3 supplies the card's substance, feeds M2 v2; both interlock with the referral showcase (MVP_FINAL_MASTER_BRIEF.md §4; BACKLOG.html #8)",
        "THE ARSENAL (K1, live) — 13 artifacts in ONE KIT_ARTIFACTS table: standing card og 1200×630 / square / story 1080×1920 · record card ('N members introduced', mounts only on a real record, never money) · banners 300×250/336×280/300×600/728×90/320×100 (canon set; 468×60 retired; 970×250+600×200 noted extend-later) · A4 poster · business card · QR print 1000×1000 PNG+SVG (&via=print) · QR video 900×900 (&via=youtube) (artifacts/studio/src/components/referral/referrerKit.tsx + ReferralToolsPanel.tsx; OPEN_QUEUE K1/K1.2/K1.3/K1.4 records)",
        "THE TOOLS TAB (K1, live) — gallery + ArtifactActions under every artifact (Download · Copy my link · Share) + LIVING MOMENTS v1 from the member's own rows + creator kits pre-tagged &via=youtube/blog/whatsapp/print + the 4 flagship §7 lines + the collapsed promote guide carrying the SYN-holding truth + legal seal (/referral/tools; featureStatus referrerKit live 2026-07-20)",
        "THE FLAGSHIP CONVERSION REGISTER — the four §7 lines verbatim ('You don't wait to get paid…' · 'Nothing to claim…' · 'can never break a sale — can never be lost' · 'the referral program where the payout is part of the purchase'), each bold claim + verify link (docs/direction/CANON_PROTOCOL_LANGUAGE.md §7; live in the /referral Overview hero + Tools)",
        "THE INVITEE SIDE (K2, live) — per-source server-painted /join?source= unfurl (satori painter, status-gated to ACTIVE sources, 10-min TTL — a mutable fact is never an immortal card) + the honest 'Introduced by 0x… · never changes your price' strip (artifacts/api-server/src/joincard/ + routes/joinCard.ts; studio serve.mjs 3c; featureStatus joinInviteeCard)",
        "THE RECEIPT SHARE MACHINERY (live) — the ticket + binder + dual share (copy-first, six intents, native sheet carries the PNG), the 1200×630 receipt share card with the member's OWN introduction QR, the commission register share door on /receipt/{tx}, painted 4-face preview cards with rotation in the link (?f=1..4) (RECEIPT_AND_SHARE_SYSTEM_INVENTORY.md §1-2; RBIND2_HERO_SHARE_DIRECTION.md; featureStatus receiptPublicPage/paintedPreviewCards/commissionRegister)",
        "THE ADMIN AXIS (K3.a/b/c, live) — member Ask-for-activation intake with live eligibility card · the review queue with server preflight + verdicts + bells · the stacked create+activate signing session with the quote-refusal CODE gate · pause/revoke doors · per-source Performance table (now Universe 3: ALL chain-created sources via stored (tx,logIndex) receipts, 2026-08-02) + screen-exact CSV (featureStatus activationIntake/sourceReviewQueue/sourcePerformance; proven end-to-end on Seat #3's real activation 2026-07-22)",
        "CHANNEL ANALYTICS (live) — 6 preset chips + custom &via tags, aggregate clicks/verified-joins per tag, anonymous beacons, no shorteners/localStorage/per-channel sourceIds ever (ReferralLinkPanel.tsx + api channel/router.ts; composer verdicts canon)",
        "THE CONNECTOR LADDER — Emerging 5% (0) → Summit 12% (300), rate never decreases, progress bar never empty, no leaderboards of other members (docs/direction/CONNECTOR_LADDER_POLICY.md; /referral/ladder live)",
        "THE FOUNDER'S ENRICHMENT ORDERS (2026-07-20, engraved) — visual-first (the referrer sees THE BANNER at real size, never text) · think 300 referred (scalable/modular) · a dedicated Tools sub-tab with printables + audience kits · LIVING MOMENTS: Activity/Chronicle/indexer referral events become dated provable shareable moments · money/effort/recognition axes on the surface · nothing blocks (OPEN_QUEUE 2026-07-20 entry; memory referrer-surfaces-visual-first-scale-living)",
        "TYPOGRAPHY FLOORS LAW — per-format floors from the VIEWING CONTEXT, carried in code (KIT_ARTIFACTS[].typeFloor) and enforced by the 5-probe harness (fit · square-box · type-floor · overlap · QR-centering) (docs/reference/ARTIFACT_TYPOGRAPHY_FLOORS.md; memory artifact-verification-fit-and-pixel-probes)",
        "M3 ORIGIN QUARRY — SPRINT A.9 'WHAT MAKES A SEAT MATTER' + SEAT_RECORD_ARCHITECTURE_DECISION.md (doctrine roots, origin repo) · the 640×360 member identity card (Member # as 64px gold hero) · the labs generator (member card + 5 protocol proof cards) · the Archive's five visual families (artifact card · seal · pixel secret · certificate · legacy) with the deterministic artwork generator · badge taxonomy/rarity tiers/celebration modal — zero share mechanics there (RECEIPT_AND_SHARE_SYSTEM_INVENTORY.md §3; REFERRER_KIT_SYSTEM_INVENTORY.md §A)",
        "SHARE/KIT ORIGIN QUARRY — the Product OS Studio trio (Proof & Share Center: 9 proof-card families + the share loop · the full Press Kit page: 8 sections incl. approved/banned language · the referral dashboard ambition ceiling) + the brand asset generator (gen-interlock.mjs regenerating 48 files from one script) + the OG machine ('a pasted URL renders an empty grey card' = the single biggest leak) (REFERRER_KIT_SYSTEM_INVENTORY.md §B-C; RECEIPT_AND_SHARE_SYSTEM_INVENTORY.md §3)",
        "THE BENCHMARK SYNTHESIS (engraved, durable) — GMX on-chain provability + Binance/OKX one-tap QR poster + Coinbase calm share panel + Galxe/Layer3 share-the-proof grammar; mainstream: Airbnb pipeline + Stripe issued-badge + Morning Brew ladder (minus the banned nudges); full adopt/reject ledgers with the anti-MLM filter (REFERRER_KIT_SYSTEM_INVENTORY.md benchmark sections)",
        "SETTLED LAWS BINDING EVERY SLICE — artifact copy FIXED and verifiable-facts-only · no invitee discount ever (no fixed seat price exists — first purchase decides) · no name/face landing (short-form address only) · milestone nudges ('1 away') banned · anti-MLM rejects (leaderboards, downlines, recruit-N-more, cash-flex) · no gain-promise glyphs · fail-closed display (rate from the quote, address from payoutWallet) · the two business red lines only (inventory header + REFERRAL_PAGE_DESIGN.md; memories seat-price-first-purchase-decides, mechanism-decides-not-symbol)",
        "NOTIFICATION LAYER RESERVES — Section B categories (referral success · commission paid with amount · recognition/rung rise) designed but auto-generation is a FUTURE key (eventDerivedNotifications): today every send is a founder act; seat-only addressing cannot reach a no-seat promoter (docs/reference/LIVING_NOTIFICATION_LAYER.md Part 2; featureStatus.ts:145; inventory §4)",
        "THE REMAINING WORK-ORDER PIECES — K4 reach & lifecycle (no-seat promoter, wallet-addressed sends: founder decision reserved at its gate) · P the public press kit page · the /join referral card (REFERRAL-SHOWCASE remainder, OPEN_QUEUE line 2101) · M2-v2 living previews (scope E: per-page/per-seat unfurls; site still ships one static 1280×720 image) · admin arbitrary-wallet standing lookup (critic #21 remainder) · kit-asset governance/terms-doc versioning (critic #22) · localized copy packs FR/EN undecided (critic #5) · embeddable web badge/email-signature banner (critic #3) · crypto-native rails (Farcaster frames…) (critic #6) · the AFFILIATE/BD_NETWORK class kit end-to-end (critic #9)",
        "VANITY-ADJACENT FUTURE KEYS (registered, never re-plan as new) — seatRecord (optional future ERC-721 identity candidate) · archiveGallery · seasonOwnRow · aliasLayer (M8 the voice) · standingModel (featureStatus.ts future block)"
      ],
      "built": [
        "/referral 5-tab dashboard + the ONE link hero + 4 standing figures + eligibility/Ask-for-activation door — LIVE since 2026-07-19/22 (artifacts/studio/src/components/referral/MemberReferralDashboard.tsx, ReferralLinkHero.tsx, ActivationDoor.tsx; keys referralTabs/introductionRows/activationIntake)",
        "THE ARSENAL /referral/tools — LIVE 2026-07-20, hardened through K1.2/K1.3/K1.4: 13 artifacts, real interlock emblem (public/syn-mark-gold.svg), typography floors in code, 5-probe harness green at worst case, ArtifactActions everywhere, living moments v1, creator kits, promote guide (referrerKit.tsx 13 ids verified: og/square/story/record/b300/b336/b600/b728/b320/poster/bizcard/qrprint/qrvideo; key referrerKit)",
        "K2 invitee side — LIVE (prod 0134cc6 + 7c174cc): per-source painted /join?source= unfurl with the ACTIVE-status gate + TTL cache law (join-card:guard 29 pins), the introduced-by strip on /join (api-server/src/joincard/, routes/joinCard.ts; JoinProtocol.tsx IntroductionStatus; key joinInviteeCard)",
        "K3 admin axis whole — LIVE (prod 89057bb/f9f3495/c08bbc8): intake + review queue + founder ask-alert bell + stacked signing session + quote-refusal code gate + pause/revoke doors + Performance tab + CSV; proven end-to-end on Seat #3 2026-07-22 (operator/activationQueueService.ts, sourcePerformanceService; keys sourceReviewQueue/sourcePerformance/consoleReferralKpis)",
        "Commission register + share door — LIVE (prod e2911be): /referral/commissions 7-zone commission documents, each sharing its permanent /receipt/{tx} page with the 4-face rotation in the link (ReferralCommissionsPanel.tsx; key commissionRegister)",
        "Receipt system — LIVE: /receipt/{txHash} public page, painted 4-face 1200×630 preview cards (api receiptcard/), the binder + ticket dual share per the approved receipts-hero-share-mockup.html, the 1200×630 receipt share card with the member's own introduction QR (keys receiptPublicPage/paintedPreviewCards)",
        "Channel composer + &via analytics — LIVE (ReferralLinkPanel.tsx + api channel/router.ts; key channelAnalytics)",
        "The chapter machinery M3 depends on — HARDENED 2026-08-02: ONE server chapter table (api lib/protocol/chapters.ts, all five chapters, mirror-pinned to the studio's frozen canon), chapter chips already render on tickets, painted faces, the public hero CH badge, the admin ledger Chapter column (SESSION_STATE (a5)/(a6))",
        "The autonomy layer under every kit figure — LIVE (prod 7a916a3, 2026-08-02): the continuity spine runs every backbone cycle, 'Referral sources' counts chain SourceCreated acts (5), the source roster IS the chain, zero hand-fed figures remain",
        "Every referral mockup on disk is approved AND realized: referral-page/referral-tabs (5-tab page), referral-tools-mockup.html (K1 approved 'C'EST APPROUVÉ! GO AND GO-LIVE'), receipts-hero-share-mockup.html (R-BIND-2), receipt-preview-cards-mockup.html (R-CARDS), join-invitee-mockup.html (K2), k3-admin-axis-mockup.html (K3) — no approved-but-unbuilt mockup exists in this system",
        "Adjacent, awaiting deploy (batchable): the A1 My Activity Mine|Protocol lens — built + rig-verified 2026-08-02, client-only (work-order item 2)"
      ],
      "gaps": [
        "M3 COLLECTIBLE/VANITY — entirely unbuilt: the seat-in-majesty treatment on receipt + member surfaces, the seniority line ('seat #N of M — joined before X% of the story', live reads), historical-FOMO copy per SPRINT A.9 (never financial), co-witness event timeline (its M4 dependency is DONE — now unblocked), Archive-artifact binding (deliberately-waits). No wireframe exists; the Visual Change Law requires one first (MVP_FINAL_MASTER_BRIEF §8 M3; BACKLOG M3 'En file')",
        "THE M2×M3 INTERLOCK — the standing card family does not yet carry M3's substance (seniority/story lines); M2's definition says M3 'supplies the card's substance and feeds M2 v2' — a card redesign slice once M3's reads exist (wireframe required)",
        "M2 v2 — LIVING LINK PREVIEWS (scope E, the origin's crown jewel): every page except /receipt/{tx} and /join?source= still unfurls the ONE static 1280×720 image (format also off the 1200×630 house standard); per-page then per-seat server-painted previews remain unbuilt — the painter infrastructure (satori, joincard/receiptcard patterns) already exists to extend (RECEIPT_AND_SHARE_SYSTEM_INVENTORY §2/§5-E)",
        "THE /join REFERRAL CARD — the REFERRAL-SHOWCASE remainder (OPEN_QUEUE line 2101): the §7 conversion register applied to /join so the BUYER learns the referral program's unique claim; K2 built the invitee attribution side, not this buyer-facing showcase card. /join today carries only the introduction-status strip",
        "K4 — REACH & LIFECYCLE: no mechanism exists to reach a no-seat promoter (notification addressing is seat-only); the founder decision (wallet-addressed sends) is explicitly reserved at its gate (OPEN_QUEUE work order; inventory §4 + critic #23)",
        "P — THE PRESS KIT PAGE (public): unbuilt; the origin's press.tsx (8 sections: descriptions, brand system, official channels, approved/banned language, media usage) is the quarry; every word to the founder on screen at its slice (OPEN_QUEUE work order)",
        "ADMIN REMAINDER — the arbitrary-wallet standing lookup (critic #21's open third); kit-asset governance (where admin-approved assets live, versioning/retirement) + terms-doc versioning when the terms text changes (metadataHash re-hash + updateSourceTerms process) — critic #22, uninventoried",
        "CRITIC GAPS STILL OPEN — localized copy packs (kit language FR/EN undecided, #5) · embeddable web badge/email-signature banner beyond the harvested referrer-badge.png (#3) · video/motion assets beyond QrVideo (#4) · crypto-native distribution rails: Farcaster frames/mini-apps, wallet share sheets (#6) · the AFFILIATE/BD_NETWORK non-member class kit end-to-end: agreement, terms doc, admin flow (#9)",
        "LIVING MOMENTS v2 — v1 ships from the member's existing own rows only; the founder's order ④ (Activity/Chronicle/indexer referral events as dated provable shareable moments) awaits the events→moments rail, which interlocks the eventDerivedNotifications future key (nothing auto-generates today)"
      ],
      "founderDecisions": [
        "Scope pick (SYSTEM-FIRST): from this full picture, which piece opens the arc — M3 vanity substance, the M2×M3 card interlock, the /join referral card, M2-v2 living previews, K4 reach, or P press kit?",
        "M3 wireframe verdict: WHERE does the seat-majesty layer live and how does it look — the ticket, /member's Your Seat strip, the standing card, and/or a dedicated public seat surface? (Visual Change Law: no code before your approved wireframe)",
        "M3 copy: the exact seniority line and the historical-FOMO sentences ('joined before X% of the story') — full text on screen for your verdict before they render anywhere",
        "Co-witness v1 scope: should the first M3 slice include 'what history this seat has SEEN' (the backbone feed makes it buildable now), or does it wait for a later slice?",
        "K4 (reserved to you at its gate since 2026-07-20): may the protocol reach a no-seat promoter by wallet-addressed sends, or does reach stay seat-only?",
        "M2-v2 living previews: green-light the server painter extension so every page (and later every seat) unfurls its own painted card instead of the one static July-10 image?",
        "The kit's language: does the arsenal stay English-only, or do FR (or FR/EN) copy packs join it? (the one critic gap with no recorded ruling)",
        "Press kit slice P: when you open it, every word (descriptions, official channels, approved/banned language) comes to you on screen — do you want it inside this arc or after?",
        "Extend-later banner formats (970×250 · 600×200, noted in the banner canon): add now or leave noted?",
        "Admin: do you want the arbitrary-wallet standing lookup (the one K3 intelligence piece left open), respecting admin-sees-public-never?"
      ],
      "recommendedSlices": [
        "SLICE 1 — M3 READS (buildable WITHOUT founder input, server+client data only, nothing visual moves): serve the seniority facts own-row and publicly (seat #N of M, chapter from the ONE chapters.ts table, joined-before-% live, first-block anchor) + the co-witness read (events after the seat's block, from the existing backbone feed) — the substance every later card imports; guard-pinned, no wireframe needed because no surface changes",
        "SLICE 2 — M3 THE VANITY SURFACE (founder wireframe FIRST): seat-in-majesty + seniority line + historical-FOMO copy on the ticket and member surfaces, copy on screen for verdict, preview gate desktop+mobile before deploy — the 'momentum face' where the conversion register stages",
        "SLICE 3 — THE M2×M3 INTERLOCK (founder wireframe for the card redesign): the standing card family (og/square/story + record card) gains the M3 substance lines; the 5-probe harness + typography floors already enforce the mechanics — vanity becomes the acquisition tool, the engraved interlock closed",
        "SLICE 4 — THE /join REFERRAL CARD (small; wireframe for the card's placement, copy is already engraved verbatim §7): the buyer-facing showcase card on /join — closes the REFERRAL-SHOWCASE remainder that must never dissolve into M2/M3",
        "SLICE 5 — M2 v2 LIVING PREVIEWS (server arc, largely buildable without founder input once green-lit — the painter, guard, and serve-time substitution patterns are all proven; the card DESIGNS get a mockup): per-page painted unfurls first (kills the 1280×720 static + the format mismatch), then per-seat cards riding Slice 1's reads",
        "SLICE 6 — K4 REACH (founder decision at the gate first) + the admin arbitrary-wallet lookup if wanted",
        "SLICE 7 — P THE PRESS KIT PAGE (public; origin press.tsx quarry; every word on screen)",
        "BACKGROUND (no decision blocks them, ride any slice): the embeddable web badge snippet · extend-later banner formats if ruled in · living-moments v2 groundwork noted against the eventDerivedNotifications future key (never re-planned as new)"
      ],
      "notCovered": "This recon read records and code, not pixels or the chain: it did not render /referral/tools or re-export the 13 artifacts (their probes' green is taken from the sealed gate records, not re-run today), did not re-verify prod behavior online (prod = 3a3efb8 per SESSION_STATE; no live requests made), did not read MembershipSaleV3.sol or the registry contracts directly (the no-SYN-at-pay-time truth is quoted from the engraved K1 verify-first record), and did not open the origin repos themselves (the quarry is cited from the durable 6-lens inventory, per the never-re-search ledger discipline). Third-party benchmark figures remain directional as the inventory itself flags. The BACKLOG M2 row's 'the generic member Referrer Kit is the open half' phrasing predates the K1 seal — featureStatus (referrerKit live 2026-07-20) is the truth; treat that row's remaining substance as M3 + M2-v2, not a rebuild of the arsenal."
    },
    "builtState": {
      "built": [
        "referrerKit.tsx (artifacts/studio/src/components/referral/referrerKit.tsx) — 13 fixed-ink artifacts in KIT_ARTIFACTS: CardOg 1200x630, CardSquare 1080x1080, CardStory 1080x1920, RecordCard (mounts only on a real record), Banner300/336/600/728/320, PosterA4, BizCard, QrPrint 1000x1000 (PNG + vector SVG w/ quiet zone), QrVideo 900x900; real interlock emblem inlined, per-artifact typeFloor declared. LIVE in prod at /referral/tools since 2026-07-20 (K1 arc sealed d170131; featureStatus.referrerKit=live).",
        "ReferralToolsPanel.tsx (same dir) — the Tools tab (tab 6): 7 sections (card, banners real-size, offline pack, living moments from own rows, creator kits pre-tagged &via=youtube/blog/whatsapp/print, the 4 flagship lines with copy buttons, collapsed promote guide + legal seal). EVERY artifact carries its own action row: Download PNG, Copy my link, Share… (native sheet carrying the PNG file, honest fallbacks for no-file-share engines). Gate: needs connected + signed wallet; link = payingSourceId(readback.sourceIdHex, address) -> buildJoinLink. LIVE.",
        "MemberReferralDashboard.tsx — 6 deep-linkable tabs (/referral, /introductions, /commissions, /ladder, /link 'Channels', /tools); four figures ALWAYS above tabs (Introductions, Durable, Commission paid, Escrow — with own-wallet verify links); ReferralLinkHero mounted exactly once above tabs (full link + Copy + QR + ShareMenu + live 'Source active' pill); ActivationDoor under it. All LIVE (referralTabs 2026-07-19).",
        "The dual-share machinery (R-BIND-2, LIVE): ReceiptTicket.tsx dual surface — Copy link first, six intents in order X->WhatsApp->Telegram->LinkedIn->Facebook->Email via lib/shareTargets.ts (the ONE intents module), 'Share with other apps' full-width feature-detected (the only channel carrying the PNG); hero rail cap-5 in ReceiptsBinderPanel.tsx (/receipts); ReceiptShareCard 1200x630 <300KB with QR of the member's OWN introduction link.",
        "The painted-card share loop (LIVE): /receipt/{txHash} public page (PublicReceiptPanel + serve.mjs per-URL og substitution) + api receiptcard/ painter (satori+resvg, 4 faces: THE SEAT / WHERE YOUR MONEY WENT / THE STORY / THE PROOF; GET /api/receipt-card/{hash}.png?f=1..4, fail-closed 302); the rotation lives in the LINK (?f advances per share act) via ReferralCommissionsPanel ShareRow. receiptPublicPage + paintedPreviewCards + commissionRegister all live 2026-07-20.",
        "The invitee side K2 (LIVE, sealed 0134cc6 + hardening 7c174cc): api src/joincard/ per-SOURCE painted unfurl (GET /api/join-card/{sourceId}.png — ACTIVE sources only, 10-min TTL on a mutable fact, fail-closed generic) + serve.mjs shape-valid /join?source= self-referential og substitution + the introduced-by strip on /join ('Introduced by 0x… - it never changes your price'). featureStatus.joinInviteeCard=live.",
        "Attribution rail (LIVE): lib/joinLink.ts is the ONE join-link builder (canonical origin from the SEO registry — guard-duplicate-facts pins it); /join reads ?source= format-checked, validates read-only (GET /api/source/validate), JoinCheckout passes sourceId into quote+buy() only when the quote confirms sourceProvided && sourceValid; commission paid inside the buyer's own tx; rate from the QUOTE, address from sourceConfig.payoutWallet; &via beacons: POST /api/channel/click + /conversion (tx-verified, aggregate daily counters, no identity); NO localStorage capture, NO per-visitor tags, NO per-channel sourceId (rejected canon, enforced).",
        "Channels composer + analytics (LIVE): ReferralLinkPanel.tsx — 6 preset chips + custom tag (normalized, max 24), shows ONLY the tagged variant, never shortened; per-tag clicks/verified-joins table via GET /api/auth/channel-standing.",
        "Admin axis K3 (all LIVE 2026-07-22): ActivationDoor intake + live eligibility card (activationIntake), SourceReviewQueue at head of /admin/sources with fail-closed preflight + Approve->prefilled ProposeSourceCreate + code-gated paused-refusal probe + pause/revoke doors (sourceReviewQueue), per-source Performance table + screen-exact CSV (sourcePerformance) — extended 2026-08-02 to ALL created sources with owner wallets from cached chain receipts (5 created = 5 known); consoleReferralKpis live.",
        "The served data a kit surface can ride today: GET /api/auth/source-standing -> SourceStandingReadback {sourceOrigin canonical|founder-signed, sourceIdHex, standing: attributedPurchases, introducedMembers, durableIntroductions, commissionPaidRaw, escrowOwedRaw, asOfBlock, currentBps, entitledBps, entitledTitle, promotionDue, crossedAt*}; GET /api/auth/introduction-rows (per-row date/tx/durable/commission); member-standing (seat -> chapterForSeat); ladderProgress(durable) rung titles; channel breakdown; activation state; join quote carries broughtByShort.",
        "referrer-badge.png exists in artifacts/studio/public (the harvested interlock listing badge the blog-embed snippet points at) — served 200 in prod since K1.",
        "Vanity substance already live elsewhere (DONE-IS-DONE — never re-plan): /registry public per-seat register (#N, address link, chapter, rung, joined — theRegister 2026-07-30), /season recognition board (seasonRanking), /member identity band MemberYourSeat (sigil, seat, chapter/era, capital rung, receipt chip + share + verify), painted faceSeat card.",
        "ShareCard.tsx ('verified introducer' card) — built with REAL own-row figures, but UNMOUNTED: no page imports it (grep-proven; only referrerKit/ReceiptTicket reference the name in comments).",
        "Guards binding these surfaces: studio 30-guard chain (guard-feature-truth pins every future-claim to featureStatus; guard-forbidden-copy banned vocabulary; guard-safe-source disclaimer pins; guard-access-state rule 15 dynamic wallet imports; guard-receipt-ticket share/link pins incl. exactly ONE join?source= construction site; no-raw-color with tagged fixed-ink allowlist) + api 21-guard chain (join-card:guard 29 pins incl. the active-source status gate + mutable-fact cache law; receipt-card:guard; duplicate-facts:guard). The FIT / SQUARE-BOX / TYPE-FLOOR / PIXEL probes are RIG-session probes (not a committed CI script); the committed half is KIT_ARTIFACTS[].typeFloor + ARTIFACT_TYPOGRAPHY_FLOORS.md."
      ],
      "founderDecisions": [
        "M3 scope pick (SYSTEM-FIRST gate): of the collectible/vanity inventory — seat-number-in-majesty surfaces, the seniority line ('seat #N of M — joined before X% of the story'), historical-FOMO copy (SPRINT A.9 register), co-witness timeline (backbone now live, so unblocked), Archive-artifact binding — which pieces make the M3 slice, and on which surfaces (member card, public seat page, kit artifacts)? Composition change = wireframe first.",
        "Kit v2 substance: once M3 exists, do the kit's cards gain the seniority/chapter-story line (the engraved 'M3 supplies the card's substance / feeds M2 v2')? New card text = full copy on screen + re-run of the artifact probes.",
        "The connected-no-seat promoter picture (your own 2026-07-20 order, still open): a no-seat signed-in wallet already derives its permanent link and sees the arsenal gate — what should its /referral/tools view be (full arsenal now, or a conversion frame until seated)? The chain only pays a seated SYN-holding introducer.",
        "Scope E — site-wide living previews: today every non-receipt page unfurls ONE static 1280x720 image (off the 1200x630 house standard). The painter infra exists. Build per-page (and later per-seat) painted previews now, or hold?",
        "ShareCard.tsx (the unmounted verified-introducer card): retire it, or absorb it (the RecordCard already covers its claim)? One-line verdict; either way is a small housekeeping slice.",
        "Kit-asset governance (critic item 22): at think-300 scale, is the code-fixed artifact set the governance (my recommendation — nothing to version, nothing to retire), or do you want an admin-curated asset center like Notion/Stripe?",
        "Reaching no-seat referrers (critic item 23): notification addressing is seat-only today; a no-seat promoter is unreachable by bell. Accept as-is until the promoter picture is decided, or order the reach mechanism into that same slice?"
      ],
      "gaps": [
        "M3 Collectible/vanity thin — the ONLY unbuilt half of engraved #8 (BACKLOG row 'En file'): seat number in majesty on receipt + member surfaces, chapter identity from chapters.ts (now also the server's one table), the seniority line ('seat #N of M — joined before X% of the story', live reads), historical-only FOMO copy per SPRINT A.9, co-witness event timeline (its M4 dependency is NOW SEALED — the backbone runs unattended), Archive-artifact binding. MASTER_BRIEF: 'the card's substance (feeds M2 v2)'.",
        "Kit v2 enrichment riding M3: no kit artifact today carries seniority/percent-of-story/season substance — the cards say seat, chapter, durable count, rung, record count only.",
        "Scope E (engraved, reserved): site-wide per-PAGE / per-SEAT living link previews — every page except /receipt/{tx} and /join?source= still unfurls the one static 1280x720 July-10 image (format mismatch vs the 1200x630 house standard). The satori painter infra exists and is proven.",
        "The connected-no-seat promoter picture — founder-ordered into the frame 2026-07-20, never decided: the link is tool-open (any signed-in wallet derives it) but the Tools arsenal renders only behind the signed session, and no conversion-facing kit frame exists for the no-seat promoter class (nor the AFFILIATE/BD class kit).",
        "Critic items still open from the dossier: 21-remainder (arbitrary-wallet standing lookup for the admin — per-source performance now covers all created sources, but no free lookup), 22 (kit-asset governance/versioning — no admin-curated asset center; the artifact set is code-fixed), 23 (no mechanism to reach a no-seat referrer: notification addressing is seat-only).",
        "ShareCard.tsx is dead code today — built, real-figures, unmounted; the dossier's own verdict option was 'retired or absorbed'.",
        "The season/XP interlock on referral surfaces: seasonOwnRow is a FUTURE key; no season standing reaches the kit or the referral dashboard.",
        "eventDerivedNotifications (future key): nothing generates a referrer's bell from an indexed chain event (a sealed introduction rings no bell automatically — today only the activation verdict does, server-written).",
        "The artifact geometry probes (FIT/SQUARE-BOX/TYPE-FLOOR/PIXEL) live only as rig-session probes — any future artifact edit must re-run them by hand; no committed script enforces them in the guards chain."
      ],
      "inventory": [
        "The living standing card, 3 share formats (og/square/story) + the record card — MASTER_BRIEF piece 7 / M2; dossier scope C (BUILT: referrerKit.tsx)",
        "The performing banner set 300x250 / 336x280 / 300x600 / 728x90 / 320x100 — ARTIFACT_TYPOGRAPHY_FLOORS banner canon (BUILT)",
        "The offline/print pack: A4 poster, business card, naked QR PNG + vector SVG, &via=print baked in — founder t-shirt order 2026-07-20 (BUILT)",
        "The video QR overlay &via=youtube for creators — founder order (BUILT)",
        "Creator kits pre-tagged per channel (&via=youtube/blog/whatsapp/print) + the blog embed badge referrer-badge.png — Tools mockup (BUILT)",
        "Living moments — the member's own dated events become share acts (introduction sealed -> receipt link; standing -> the card) — Tools mockup section 4 (BUILT v1)",
        "The words: the four verbatim flagship lines (CANON_PROTOCOL_LANGUAGE section 7) + the promote guide with approved/banned language — origin press-kit harvest (BUILT: copy rows + collapsed guide)",
        "THE link hero (one canonical link, exactly once) + Channels composer + &via aggregate analytics — REFERRAL_PAGE_DESIGN canon (BUILT)",
        "Receipt share machinery: ReceiptShareCard PNG, R-BIND-2 dual share, /receipt/{tx} public page, 4 painted faces, ?f rotation-in-the-link — RBIND2 + R-CARDS canon (BUILT)",
        "The invitee receiving end: per-source painted /join?source= unfurl + honest introduced-by strip — K2, the critic's item 7 (BUILT)",
        "The admin axis: intake door, review queue, code-gated preflight, lifecycle doors, per-source performance + CSV — K3.a/b/c (BUILT)",
        "M3 collectible/vanity: seat majesty, chapter identity, seniority line, historical FOMO, co-witness, Archive binding — MASTER_BRIEF piece 3 / BACKLOG #8 second half (NOT BUILT — the open half)",
        "Site-wide living previews per page / per seat — dossier scope E (NOT BUILT)",
        "The connected-no-seat promoter picture + AFFILIATE/BD class kit — founder order 2026-07-20 / critic item 9 (OPEN)",
        "Kit-asset governance (admin resource center, versioning) — critic item 22 (OPEN)",
        "Reach to no-seat referrers (notifications are seat-addressed) — critic item 23 (OPEN)",
        "Origin quarry (never law): Share Center 9 proof-card families, Press Kit page, brand.ts, share-dialog, gen-interlock.mjs asset generator, ShareActions bar, protocol snapshot cards — REFERRER_KIT_SYSTEM_INVENTORY sections A-C (harvested selectively; generator not ported)",
        "Benchmark adopt ledger: one-tap generated poster (Binance/OKX), calm one-click panel (Coinbase), on-chain provable earnings (GMX/dYdX), share-the-proof grammar (Galxe/Layer3) — dossier benchmark (adopted in K1); reject ledger (gain-promise imagery, milestone nudges, MLM, urgency) engraved and enforced by guards"
      ],
      "notCovered": "Read-only recon: no builds, guards, or rig runs executed this session (guard/gate states are quoted from SESSION_STATE/OPEN_QUEUE seals, all dated 2026-07-20..2026-08-02, not re-measured). The two origin repos were not re-walked (the durable dossier REFERRER_KIT_SYSTEM_INVENTORY.md stands per the RESEARCH LEDGER discipline). The .sol pay-time truth for a no-SYN introducer (critic verify-item 25) was not re-read — it must be verified before scoping the no-seat promoter slice. Mockup HTMLs (referral-tools-mockup, join-invitee-mockup) were located but not pixel-audited. Prod runtime was not probed.",
      "recommendedSlices": [
        "0 (rider, no build): the A1 Mine|Protocol lens is committed and AWAITING DEPLOY (batchable) — the kit arc's first deploy carries it; nothing in this arc waits on it.",
        "1 — M3 recon-to-wireframe (the SYSTEM-FIRST gate this arc owes): present the full vanity inventory (this recon) and hand a wireframe for the founder-picked surfaces. NOT buildable without founder input (Visual Change Law: composition = approved wireframe first). The plumbing recon IS done: chapters.ts server+studio, the spine's live member_total (for 'seat #N of M'), the backbone feed (for co-witness) all exist — zero new indexer work expected.",
        "2 — M3 build slice(s) from the approved wireframe: seniority line + chapter/seat majesty + historical-FOMO copy (full text on screen at its gate). Server arithmetic on live reads; fail-closed.",
        "3 — M2-v2 kit enrichment: fold the M3 substance into CardOg/Square/Story/RecordCard (KIT_ARTIFACTS is the one table — a new line joins every affordance automatically); re-run the four rig probes; copy on screen. Buildable without founder input EXCEPT the card wording (copy law).",
        "4 — Scope E living previews, page-level first: paint per-page 1200x630 previews with the proven satori painter + serve.mjs substitution (the receipt/join pattern generalized). Buildable without founder input (infra + engraved posture); per-SEAT previews wait for M3's substance.",
        "5 — Housekeeping (buildable now, no founder input): retire-or-absorb ShareCard.tsx per the founder's one-line verdict; optionally commit the artifact geometry probes as a repeatable script so future artifact edits can't skip them.",
        "6 — The no-seat promoter frame + reach: ONLY after the founder decision and the .sol verify (critic item 25). Interlocks with critic item 23 (notification reach).",
        "Throughout: think-300 law (artifacts are per-member constants — the page does not grow), the two red lines only, and DONE-IS-DONE — nothing above re-opens K1/K2/K3 or any live key."
      ],
      "summary": "Engraved #8 (M2 Sharebility/Referrer Kit + M3 Collectible/vanity) is roughly two-thirds SHIPPED and one-third open. M2 is DONE and live in prod since 2026-07-20: the full arsenal at /referral/tools (13 fixed-ink artifacts — 3 standing-card formats + record card + 5 canon banners + A4 poster + business card + QR print/video packs, each with its own Download/Copy/Share row), the one link hero + Channels &via analytics, the R-BIND-2 dual share on receipts, the 4-face painted /receipt/{tx} loop with rotation-in-the-link, and the K2 invitee side (per-source painted /join?source= unfurl + introduced-by strip). The K3 admin axis (intake, review queue, lifecycle doors, performance+CSV) is also sealed. What remains of #8 is M3 — the collectible/vanity substance (seat majesty, seniority line, historical FOMO, co-witness — its M4 dependency is now satisfied) which then feeds the kit's cards as M2-v2 — plus the reserved Scope E site-wide living previews, the undecided connected-no-seat promoter picture, and three small open critic items (asset governance, no-seat reach, unmounted ShareCard.tsx). The founder's scope pick for M3 (wireframe-first) is the arc's single blocking decision; Scope E and the housekeeping slice are buildable without him."
    }
  },
  "workflowProgress": [
    {
      "type": "workflow_phase",
      "index": 1,
      "title": "Recon"
    },
    {
      "type": "workflow_agent",
      "index": 1,
      "label": "recon:system",
      "phaseIndex": 1,
      "phaseTitle": "Recon",
      "agentId": "a2c7ebdfe504f5129",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1785702605052,
      "queuedAt": 1785702598602,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "The Referrer Kit is NOT a greenfield: the K-arc (K1 arsenal…",
      "promptPreview": "SENIOR SYSTEM-INVENTORY RECON (grade-AAA). Repo: C:\\Users\\kemal\\OneDrive\\Documents\\GitHub\\syndicate-os. READ-ONLY (no edits/commits). Plumbing: corepack pnpm run <script>; api guards via .\\node_modules\\.bin\\tsx.cmd from artifacts\\api-server.\nTHE ARC: the founder's work-order item 3 — the Referrer Kit / sharebility (BACKLOG engraved #8: \"M2 Sharebility/Referrer Kit + M3 Collectible/vanity — the hig…",
      "lastProgressAt": 1785702987687,
      "tokens": 167490,
      "toolCalls": 34,
      "durationMs": 382635,
      "resultPreview": "{\"summary\":\"The Referrer Kit is NOT a greenfield: the K-arc (K1 arsenal + K2 invitee side + K3.a/b/c admin axis) sealed in prod 2026-07-20→22 already delivers most of master-brief piece 7 — 13 chain-proven artifacts live at /referral/tools, the per-source /join unfurl, and the full ask→sign→bell admin loop, all under 12 live featureStatus keys. What the founder's work-order item 3 (engraved #8, M2…"
    },
    {
      "type": "workflow_agent",
      "index": 2,
      "label": "recon:built",
      "phaseIndex": 1,
      "phaseTitle": "Recon",
      "agentId": "ad4f88839d36d3120",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1785702600846,
      "queuedAt": 1785702598603,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Read-only recon: no builds, guards, or rig runs executed th…",
      "promptPreview": "SENIOR BUILT-STATE RECON (grade-AAA). Repo: C:\\Users\\kemal\\OneDrive\\Documents\\GitHub\\syndicate-os. READ-ONLY (no edits/commits). Plumbing: corepack pnpm run <script>; api guards via .\\node_modules\\.bin\\tsx.cmd from artifacts\\api-server.\nTHE ARC: the founder's work-order item 3 — the Referrer Kit / sharebility (BACKLOG engraved #8: \"M2 Sharebility/Referrer Kit + M3 Collectible/vanity — the highest-…",
      "lastProgressAt": 1785702981925,
      "tokens": 188914,
      "toolCalls": 41,
      "durationMs": 381076,
      "resultPreview": "{\"built\":[\"referrerKit.tsx (artifacts/studio/src/components/referral/referrerKit.tsx) — 13 fixed-ink artifacts in KIT_ARTIFACTS: CardOg 1200x630, CardSquare 1080x1080, CardStory 1080x1920, RecordCard (mounts only on a real record), Banner300/336/600/728/320, PosterA4, BizCard, QrPrint 1000x1000 (PNG + vector SVG w/ quiet zone), QrVideo 900x900; real interlock emblem inlined, per-artifact typeFloor…"
    }
  ],
  "totalTokens": 356404,
  "totalToolCalls": 75
}