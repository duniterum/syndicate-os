/**
 * @workspace/os-contracts — public type surface.
 *
 * TYPE-ONLY package. Re-exports the Source Boundary contracts. These types are
 * vocabulary/shape contracts only — they are NOT evidence that any data,
 * source, adapter, or live wiring exists.
 */
export type * from "./source-boundary";
