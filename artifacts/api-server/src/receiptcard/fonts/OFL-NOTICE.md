# Embedded font files — SIL Open Font License 1.1

The receipt-card painter embeds these font binaries (satori requires raw
font data; the site's own faces, so the painted card speaks the site's
voice):

- **IBM Plex Mono** Regular + SemiBold — © IBM Corp., SIL OFL 1.1
  (source: github.com/google/fonts, `ofl/ibmplexmono`)
- **Work Sans** Regular + SemiBold — © Wei Huang, SIL OFL 1.1
  (source: fonts.gstatic.com static instances of the Google Fonts family)

Both licenses permit bundling and server-side rendering. Full license text:
https://openfontlicense.org — the fonts are unmodified.
