{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "packageManager": "pnpm@10.26.1",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "build:release": "node -e \"process.env.PORT='5173';process.env.BASE_PATH='/';require('child_process').execSync('pnpm run build',{stdio:'inherit'})\"",
    "release:gate": "pnpm run typecheck && pnpm --filter @workspace/studio run seo:check && pnpm --filter @workspace/studio run seo:rewrites:check && pnpm --filter @workspace/studio run surface:audit && pnpm --filter @workspace/studio run guards && pnpm run build:release && pnpm --filter @workspace/api-server run guards"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.4",
    "typescript": "~5.9.3"
  }
}
